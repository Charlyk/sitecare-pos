---
phase: 09-period-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n.jsx
  - src/styles.css
autonomous: true
requirements: [HIST-04]
must_haves:
  truths:
    - "Eight new keys exist under BOTH ro and en in src/i18n.jsx: h_range_start, h_range_end, h_range_apply, h_range_cap_message, h_period_in_7, h_period_in_30, h_period_in_range_prefix, h_empty_prefix (UI-SPEC § New i18n Keys)"
    - "The ro and en key sets are symmetric — every key present in one section is present in the other, so no locale can render a raw key name at runtime"
    - "The old period-baked empty-state key is RENAMED to h_empty_prefix, not duplicated alongside it — the string 'Nicio comandă în ultimele 30 de zile.' / 'No orders in the last 30 days.' ceases to exist in both locales (UI-SPEC § New i18n Keys: treat as a rename, not an append)"
    - "h_empty_prefix carries only the base phrase (ro 'Nicio comandă' / en 'No orders') with no trailing period and no period name baked in — the period phrase and the sentence-final period are appended by the component (D-13)"
    - "h_empty_sub is unchanged in both locales — the period rides the main line only, preserving P7 D-13's reservation of the sub-line for Phase 10's filter copy"
    - "h_range_apply is noun-bearing (ro 'Aplică intervalul' / en 'Apply Range') — it names WHAT is applied, not a bare OK or Save (UI-SPEC § Copywriting Contract)"
    - "h_range_cap_message names the 366-day limit explicitly (ro 'Intervalul maxim este de 366 de zile.' / en 'Maximum range is 366 days.') rather than saying only that the range is invalid (D-09)"
    - "No key is declared more than once within either the ro or the en object — the project shipped duplicate-key defects twice in v1.0 and once in Phase 7"
    - "h_period_today, h_period_7, h_period_30, h_period_custom, h_today, h_error_title, check_connection, h_retry are reused unchanged and not redeclared — no new key duplicates an existing string's job (D-12/D-13)"
    - "A spin keyframe and a .spin class exist in src/styles.css, rotating 360 degrees over 800ms linear infinite — the only new animation this phase adds (UI-SPEC § Period Switch Loading Contract)"
    - "The stylesheet change is purely additive — no existing keyframe, class, or declaration in styles.css is modified, so no shipped visual changes as a result of this plan"
  artifacts:
    - "src/i18n.jsx — 8 new keys x 2 locales; the old empty-state key removed from both"
    - "src/styles.css — @keyframes spin + .spin class"
  key_links:
    - "h_empty_prefix is consumed by screen-history.jsx's EmptyBlock (09-04) — renaming here without 09-04's matching change would render an empty main line, which is why 09-04 depends on this plan"
    - "h_period_in_7 / h_period_in_30 / h_period_in_range_prefix are consumed by 09-04's periodPhrase helper; h_period_today is reused lowercased for the Today case rather than adding a ninth key (D-13, UI-SPEC Period-Dependent Copy Contract table)"
    - ".spin is consumed by 09-04's period-switch spinner"
---

<objective>
Add the eight new i18n keys and the one new CSS keyframe Phase 9's UI needs, and rename the
period-baked empty-state string into the composable `h_empty_prefix` the D-13 sentence is built from.

Purpose: isolating the string and stylesheet surface into its own Wave-1 plan lets the screen work
(09-04/09-05) land against keys that already exist, and keeps the duplicate-key risk — a defect this
project has shipped three times — inside a plan whose whole job is to grep before it writes.
Output: `src/i18n.jsx` and `src/styles.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-period-control/09-CONTEXT.md
@.planning/phases/09-period-control/09-UI-SPEC.md
@src/i18n.jsx
@src/styles.css
</context>

## Artifacts this phase produces

See `09-01-PLAN.md` § "Artifacts this phase produces" for the full Phase 9 symbol list. This plan
contributes:

**`src/i18n.jsx`:** `h_range_start` · `h_range_end` · `h_range_apply` · `h_range_cap_message` ·
`h_period_in_7` · `h_period_in_30` · `h_period_in_range_prefix` · `h_empty_prefix`. The old
period-baked empty-state key ceases to exist.

**`src/styles.css`:** `@keyframes spin` · `.spin`

<tasks>

<task type="auto">
  <name>Task 1: New i18n keys + empty-state key rename (ro + en)</name>
  <files>src/i18n.jsx</files>
  <read_first>
    - `src/i18n.jsx` lines 200-245 (the `ro` history block) and lines 432-478 (the `en` mirror). Read both fully before writing — the two blocks are positionally parallel and the additions must preserve that parallelism.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § New i18n Keys — the authoritative key/value table, and § Copywriting Contract for the rationale behind each string.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Period-Dependent Copy Contract — the table showing why Today gets no new key (`h_period_today` lowercased serves the prepositional form) while the other three periods do.
  </read_first>
  <action>
    First, grep `src/i18n.jsx` for each of the eight new key names. All eight must be absent. If any
    is already present, stop and report rather than shadowing it — this project has shipped
    duplicate-key defects three times (`search_placeholder` in v1.0, and once in Phase 7).

    Add these eight keys to the `ro` history block. Place the four `h_range_*` keys adjacent to the
    existing `h_period_*` group and the four prose keys adjacent to the empty-state keys, so related
    strings stay grouped the way they are today:

    | key | ro | en |
    |---|---|---|
    | `h_range_start` | `De la` | `From` |
    | `h_range_end` | `Până la` | `To` |
    | `h_range_apply` | `Aplică intervalul` | `Apply Range` |
    | `h_range_cap_message` | `Intervalul maxim este de 366 de zile.` | `Maximum range is 366 days.` |
    | `h_period_in_7` | `în ultimele 7 zile` | `in the last 7 days` |
    | `h_period_in_30` | `în ultimele 30 de zile` | `in the last 30 days` |
    | `h_period_in_range_prefix` | `în intervalul` | `in the range` |
    | `h_empty_prefix` | `Nicio comandă` | `No orders` |

    Mirror all eight into the `en` block at the positionally parallel location.

    Then RENAME the existing empty-state key in both locales. The entry at `src/i18n.jsx:237` (ro —
    currently the full period-baked sentence about the last 30 days) and its mirror at `:468` (en)
    are REMOVED; `h_empty_prefix` replaces them. Do not leave the old key behind as dead code
    alongside the new one — the entire reason D-13 exists is that the period must now be composed at
    render time rather than baked into the string, and a leftover key with a hardcoded 30-day period
    inside it is a live trap for the next phase.

    `h_empty_sub` is UNCHANGED in both locales. `h_period_today`, `h_period_7`, `h_period_30`,
    `h_period_custom`, `h_today`, `h_error_title`, `check_connection`, and `h_retry` are all reused
    verbatim by later plans — do not touch, do not redeclare.

    Note for the executor: `h_empty_prefix` deliberately carries no trailing period. The
    sentence-final period is appended by the component in 09-04, because the period phrase sits
    between the prefix and the full stop.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "h_range_start" src/i18n.jsx` returns 2 (ro + en). Likewise `h_range_end`, `h_range_apply`, `h_range_cap_message`, `h_period_in_7`, `h_period_in_30`, `h_period_in_range_prefix`, `h_empty_prefix` — each returns exactly 2.
    - `grep -c "h_empty:" src/i18n.jsx` returns 0 — the old key is gone from both locales.
    - `grep -c "h_empty_sub" src/i18n.jsx` returns 2 — the sub-line key is untouched.
    - `grep -c "ultimele 30 de zile" src/i18n.jsx` returns 1 — the only surviving instance is inside `h_period_in_30`; the period-baked empty-state sentence no longer exists.
    - `grep -c "Aplică intervalul" src/i18n.jsx` returns 1 and `grep -c "Apply Range" src/i18n.jsx` returns 1.
    - `grep -c "Intervalul maxim este de 366 de zile." src/i18n.jsx` returns 1 and `grep -c "Maximum range is 366 days." src/i18n.jsx` returns 1.
    - `grep -c "h_period_today" src/i18n.jsx` returns 2 — reused, not redeclared.
    - No key declared more than twice (once per locale): `node -e "const s=require('fs').readFileSync('src/i18n.jsx','utf8'); const keys=[...s.matchAll(/^\s{4}(h_[a-zA-Z0-9_]+):/gm)].map(m=>m[1]); const counts={}; keys.forEach(k=>counts[k]=(counts[k]||0)+1); const bad=Object.entries(counts).filter(([,n])=>n>2); if(bad.length){console.error('DUPLICATE',bad);process.exit(1);} console.log('ok')"` prints `ok` and exits 0.
    - `npx vitest run` exits 0 — no existing test referenced the removed key.
  </acceptance_criteria>
  <done>Eight new keys exist symmetrically in ro and en; the old empty-state key is gone from both; `h_empty_sub` and every reused key are untouched; no key is declared more than twice.</done>
</task>

<task type="auto">
  <name>Task 2: spin keyframe + .spin class</name>
  <files>src/styles.css</files>
  <read_first>
    - `src/styles.css` lines 170-200 — the existing `@keyframes slideDown` block and the `.btn-disabled-offline` class immediately after it, for the file's formatting and comment conventions. Also lines 240-280 (`@keyframes slideIn`, `@keyframes pulseRing`), the two other animation precedents.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Period Switch Loading Contract — the spinner spec (16px `refresh` icon, `var(--sc-muted-foreground)`, 800ms linear infinite) and the note that no existing spin keyframe exists to reuse.
  </read_first>
  <action>
    Add a `spin` keyframe and a `.spin` utility class to `src/styles.css`, placed adjacent to the
    existing animation keyframes so all animations stay grouped:

    - `@keyframes spin { to { transform: rotate(360deg); } }` — a `to`-only keyframe; no `from` is
      needed, since the implicit start is the element's current unrotated transform.
    - `.spin { animation: spin 800ms linear infinite; }` — `linear`, not `ease`: an eased spinner
      visibly stutters at each revolution boundary.

    Add a one-line comment recording that this is the period-switch loading affordance (D-05), and
    that it is deliberately distinct from the greyed-out/inert convention.

    This task is purely additive. Do not modify `.btn-disabled-offline` — 09-05 reuses its existing
    `0.45` opacity value for the disabled Apply button per UI-SPEC, so changing it here would move a
    value two plans depend on. Do not modify any existing keyframe. Do not add any other class: the
    0.6-opacity dimming D-05 specifies is applied inline in `screen-history.jsx` (09-04), matching
    that file's established inline-style convention, not as a new stylesheet class.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/styles.css','utf8'); if(!/@keyframes\s+spin\s*\{/.test(c)) throw new Error('missing spin keyframe'); if(!/\.spin\s*\{[^}]*animation:\s*spin\s+800ms\s+linear\s+infinite/.test(c)) throw new Error('missing or wrong .spin rule'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node check in `<verify>` prints `ok` and exits 0.
    - `grep -c "@keyframes spin" src/styles.css` returns 1.
    - `grep -c "rotate(360deg)" src/styles.css` returns 1.
    - `git diff --numstat src/styles.css` shows 0 lines deleted — the change is additive only.
    - `grep -c "opacity: 0.45" src/styles.css` returns 1 — `.btn-disabled-offline` is unmodified.
    - `npx vitest run` exits 0.
  </acceptance_criteria>
  <done>A `spin` keyframe and `.spin` class exist, `linear` and `infinite` at 800ms; `git diff --numstat` confirms zero deletions in the stylesheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n table → rendered copy | Every string in this file reaches the user verbatim. A missing key renders its own name; an asymmetric key set makes that locale-dependent, so it can pass review in one language and ship broken in the other. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Information Disclosure (of internals to the user) | An asymmetric key set — a key added to `ro` but not `en`, rendering the raw key name to English users | medium | mitigate | Every acceptance criterion for Task 1 asserts a count of exactly 2 per key, which fails on any single-locale addition. The count-based check catches the asymmetry regardless of which locale was missed. |
| T-09-06 | Tampering (silent behavior change) | A duplicate key shadowing an earlier declaration within the same locale object — the defect this project has shipped three times | medium | mitigate | Task 1 mandates a grep-before-write for all eight names, and the acceptance criteria include a programmatic duplicate scan that fails on any `h_*` key appearing more than twice across the file (twice = once per locale). |
| T-09-07 | Denial of Service (of a shipped feature) | Removing the old empty-state key while `screen-history.jsx` still reads it, blanking the empty state's main line | medium | mitigate | The rename is confined to this plan, and 09-04 (which owns the consumer) declares `depends_on: [09-02]` and is placed in a later wave, so the consumer change lands after — never concurrently with — the rename. `npx vitest run` in this plan's verify catches any other consumer that was missed. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan or phase — zero new dependencies per `09-RESEARCH.md` § Package Legitimacy Audit ("Not applicable this phase"). |
</threat_model>

<verification>
- `npx vitest run` — full suite green. This plan removes a key that `screen-history.jsx` still reads at this wave; the suite must still pass because no existing test asserts on that key's text. If a test does fail on it, that is the signal that the consumer change cannot wait for 09-04 — report rather than patching the consumer here.
- The duplicate-key scan in Task 1's acceptance criteria exits 0.
- `git diff --numstat src/styles.css` shows zero deletions.
</verification>

<success_criteria>
- Eight new keys, symmetric across `ro` and `en`, matching the UI-SPEC table's values exactly.
- The old period-baked empty-state key exists in neither locale; `h_empty_sub` is unchanged in both.
- No `h_*` key is declared more than twice across the file.
- `@keyframes spin` and `.spin` exist; nothing else in `styles.css` changed.
</success_criteria>

<output>
Create `.planning/phases/09-period-control/09-02-SUMMARY.md` when done.
</output>
