---
phase: 09-period-control
plan: 05
type: execute
wave: 3
depends_on: [09-04]
files_modified:
  - src/screen-history.jsx
  - src/__tests__/screen-history.test.jsx
autonomous: false
requirements: [HIST-04]
must_haves:
  truths:
    # --- Goal-backward observable truths (SC2) ---
    - "Clicking the Custom pill opens a popover holding a start and an end native date input; picking both and clicking Apply Range refetches the list for exactly that range and the day-grouped list reloads for it (HIST-04, SC2)"
    - "The popover is anchored under the Custom pill only — a position:relative wrapper around that one button, not around the whole segmented-control group — and opens downward at top: calc(100% + 6px) (UI-SPEC Custom Range Popover Contract)"
    - "The popover panel chrome is copied verbatim from shell.jsx:150 — #fff background, 1px solid hsl(120 10% 88%) border, 10px radius, 0 4px 16px rgba(0,0,0,0.12) shadow, z-index 50 — at 280px wide with 16px padding"
    # --- D-02: explicit Apply ---
    - "Nothing fetches until Apply Range is clicked — the popover holds both dates in local state and typing or picking a date issues no request, so correcting a year never triggers throwaway fetches of year-wide ranges (D-02)"
    - "Apply Range is disabled whenever either field is empty, the range is invalid, or the span exceeds the cap — a half-entered range never reaches the API (D-02, D-09)"
    - "The disabled Apply button uses opacity 0.45 with cursor not-allowed, matching the established .btn-disabled-offline value at styles.css:186 rather than a new number (UI-SPEC)"
    - "Clicking outside the popover or pressing Escape closes it without applying — clicking away is how people cancel, and apply-on-close would apply a range staff were abandoning (D-02, UI-SPEC)"
    - "The popover opens with both fields blank, not prefilled with today — prefilling today in both would make an Apply-able but meaningless zero-day range the default (UI-SPEC, resolving 09-CONTEXT's Claude's Discretion item)"
    # --- D-09/D-10/D-11: guardrails ---
    - "The cap message renders at 11px/600 in var(--sc-destructive) and names the 366-day limit via h_range_cap_message — it is a validation hint, not a button and not a confirmation (D-09, UI-SPEC Color/Typography)"
    - "The native inputs constrain what is pickable per D-11: the End field's max is today; the Start field's max is the chosen End (or today when End is empty); the Start field's min is End-minus-366-days once End is chosen; both recompute as the other field changes"
    - "An Apply-time check still runs through validateCustomRange regardless of the native min/max, because date inputs accept typed values that bypass them — the validator is the authoritative gate and the min/max is only an affordance (D-11)"
    - "The 366-day cap comes from history-utils.js's MAX_RANGE_DAYS import — the number is not re-literalled in this file (D-10)"
    - "An invalid range is never silently auto-corrected — the fields keep what staff typed and Apply stays disabled with the reason shown (D-11)"
    # --- D-03/D-04: the pill never advertises a range that isn't live ---
    - "Once a range is applied, the Custom pill's label becomes the formatted range from formatDateRange (e.g. '3 mar. – 17 mar.') replacing the static Interval label, the pill adopts the standard selected styling, and the chevDown icon stays visible to signal the popover reopens (D-03)"
    - "Clicking any preset while a custom range is applied CLEARS it — the pill reverts to h_period_custom and a reopened popover starts blank, with no remembered range (D-04)"
    - "The pill shows dates only when that range is what the list is showing — there is no reachable state in which the pill advertises a range that is not live (D-03 + D-04's combined invariant)"
    - "The applied custom range flows through the same useMemo and the same useHistoryOrders({from,to}) seam as the presets — no second fetch path exists"
    - "The custom range's query key is the resolved from/to ISO strings only, with no 'custom' sentinel folded in, so re-applying the same range in one session hits the existing cache entry with no refetch (RESEARCH Pitfall 4, D-08)"
    # --- Edge probe lift: HIST-04 / adjacency (covered) ---
    - "A custom range whose start equals its end is applicable — it fetches exactly that single local day (from local midnight to the exclusive start of the next day) and the pill renders the same date on both sides of the dash"
    # --- UI-SPEC lift: E1 covered rows ---
    - "E1/overflow: the Custom pill widens with its applied-range content rather than truncating or wrapping — the segmented-control container is not fixed-width; this is D-03's explicitly accepted cost, not an oversight"
    - "E1/long-text: the formatted range stays compact because formatDateRange omits the year when the range sits inside the current year, keeping the widened pill reasonable beside three siblings"
    # --- UI-SPEC lift: E2 covered rows ---
    - "E2/empty: the popover opens with both date fields blank"
    - "E2/populated: the panel renders the two uppercase field labels, the two native inputs, the cap-message slot, and the Apply button per the UI-SPEC panel-layout diagram"
    - "E2/partial: with only one field filled, Apply stays disabled — a half-entered range never reaches the API"
    - "E2/overflow: the 366-day cap is the explicit guardrail against an unbounded range reaching the unpaginated, unvirtualized list"
    # --- UI-SPEC lift: E2 / error (BACKSTOP) ---
    - statement: "A range that fails validation — end before start, or either field empty — must never allow Apply to submit; the guardrail is enforced by the button's disabled state, not by a post-submit error. Attempting Apply with an invalid range fires no fetch"
      verification: backstop
  prohibitions:
    - statement: "The Custom pill must never display a date range that is not the range the list is currently showing. The pill is the answer to 'what am I looking at?' — a pill reading '3 mar. – 17 mar.' above rows from a different period is a false claim about which days' money is on screen. There must be no reachable state, including a preset click, a failed apply, or a dismissed popover, in which the pill advertises a range that is not live."
  artifacts:
    - "src/screen-history.jsx — CustomRangePopover component, Custom pill wiring, D-03 label, D-04 clearing, custom branch in the range memo"
    - "src/__tests__/screen-history.test.jsx — popover open/dismiss, validation-gated Apply, apply-refetches, D-03 label, D-04 clearing assertions"
  key_links:
    - "Apply -> customRangeToQuery -> the same useMemo -> useHistoryOrders({from,to}) — the custom range enters the identical seam the presets use; a second fetch path would break D-08's cache reuse and D-06's settled-label tracking at once"
    - "validateCustomRange gates the Apply button's disabled state AND is re-checked at click time — one function, two enforcement points, because the native min/max cannot catch typed values"
    - "MAX_RANGE_DAYS is imported here for the Start field's min computation and inside validateCustomRange for the authoritative check — one constant, both layers (D-10)"
    - "formatDateRange serves the pill label here and the tile sub-label / empty-state phrase in 09-04's periodLabel/periodPhrase — one formatter, three render sites, no per-site reformatting (D-14)"
---

<objective>
Build the custom-range popover — the one genuinely net-new UI surface in this phase, and the one the
design handoff does not draw at all. Two native date inputs, guardrails at the input and at Apply, an
explicit Apply button, and a pill that shows the applied range only while that range is live.

Purpose: this is SC2, and it is the last of HIST-04. It is also the phase's only real design
invention — `sitecare-orders/project/src/screen-history.jsx:159` gives the Custom pill a chevron and
`setPeriod('custom')` and nothing else. `09-UI-SPEC.md`'s Custom Range Popover Contract is the design
authority here, built from already-shipped primitives.
Output: `src/screen-history.jsx` complete; all four periods live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-period-control/09-CONTEXT.md
@.planning/phases/09-period-control/09-RESEARCH.md
@.planning/phases/09-period-control/09-PATTERNS.md
@.planning/phases/09-period-control/09-UI-SPEC.md
@.planning/phases/09-period-control/09-01-SUMMARY.md
@.planning/phases/09-period-control/09-04-SUMMARY.md
@src/screen-history.jsx
@src/shell.jsx
@src/history-utils.js
</context>

## Artifacts this phase produces

See `09-01-PLAN.md` § "Artifacts this phase produces" for the full Phase 9 symbol list. This plan
contributes, in `src/screen-history.jsx`:

- `CustomRangePopover` — component (module-private)
- `data-testid` values: `history-range-popover`, `history-range-start`, `history-range-end`,
  `history-range-apply`, `history-range-cap`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CustomRangePopover — fields, guardrails, Apply</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/shell.jsx` lines 17-29 — the outside-click dismissal effect (`useRef` + `document.addEventListener('mousedown', ...)` with cleanup in the effect's return). This is the ONLY popover precedent in the codebase and the shape to copy. Note it has no Escape listener; UI-SPEC requires one here.
    - `src/shell.jsx` lines 148-150 — the panel chrome and anchor to copy verbatim. Only the vertical direction flips: the sidebar menu opens upward (`bottom: calc(100% + 6px)`), this one opens downward (`top: calc(100% + 6px)`), because the filter bar sits near the top of the screen.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Custom Range Popover Contract — the whole section, including the panel-layout diagram, the D-11 min/max rules, and the disabled-Apply visual.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Spacing Exceptions row 3 — the `6px` offset is formally grandfathered on consistency grounds; do not round it to 8px.
    - `src/history-utils.js` — `validateCustomRange`, `customRangeToQuery`, `MAX_RANGE_DAYS` as added by 09-01, and their exact reason-string vocabulary.
    - `src/styles.css` line 186 — `.btn-disabled-offline`'s `opacity: 0.45` / `cursor: not-allowed`, the established disabled-button dimming to match.
    - `src/screen-history.jsx` — `FilterBar` as rewritten by 09-04.
  </read_first>
  <action>
    Add a module-private `CustomRangePopover({ t, onApply, onClose })` component to
    `src/screen-history.jsx`.

    State — both dates live in the popover's own local state, per D-02:
    - `const [start, setStart] = useState('')` and `const [end, setEnd] = useState('')`. Both blank on
      open. UI-SPEC resolves 09-CONTEXT's Claude's-Discretion prefill question this way deliberately:
      prefilling today in both fields would make an Apply-able but meaningless zero-day range the
      default state.
    - Nothing in this component fetches. `onApply` is called only from the Apply button's click
      handler. Native date inputs fire change events mid-typing, so a fetch-when-both-set design would
      issue several throwaway fetches of potentially year-wide ranges while someone corrects a year —
      that is exactly what D-02 rejects.

    Dismissal — copy `shell.jsx:20-28`'s effect shape and extend it:
    - A `useRef` on the panel; a `document` `mousedown` listener closing on a click outside it; and a
      parallel `document` `keydown` listener closing on `Escape`. Both listeners are registered in one
      `useEffect` and removed in its cleanup return. `shell.jsx` has no Escape listener — this is the
      one place this component extends the precedent rather than copying it, per UI-SPEC.
    - Dismissal calls `onClose` and never `onApply`. Clicking away is how people cancel; applying on
      close would commit a range staff were abandoning (D-02).
    - `document.addEventListener` is the correct API here and is not a `window.*` module-global
      violation of CLAUDE.md rule 4 — `shell.jsx` establishes the same precedent.

    Panel chrome — copy from `shell.jsx:150` verbatim: `position: 'absolute'`,
    `top: 'calc(100% + 6px)'`, `left: 0`, `background: '#fff'`,
    `border: '1px solid hsl(120 10% 88%)'`, `borderRadius: 10`,
    `boxShadow: '0 4px 16px rgba(0,0,0,0.12)'`, `zIndex: 50`. Add `width: 280`, `padding: 16`. The
    `6px` offset is grandfathered on cross-surface-consistency grounds — a 6px-vs-8px mismatch between
    the app's only two popovers would read as an inconsistency bug the moment both are seen together.
    Give the panel `data-testid="history-range-popover"`.

    Layout, per UI-SPEC's panel diagram:
    - Two field groups side by side with an `8px` gap. Each: an uppercase label at `fontSize: 10.5`,
      `fontWeight: 700`, `letterSpacing: '0.06em'`, `textTransform: 'uppercase'` — reading
      `t('h_range_start')` / `t('h_range_end')` — above an `<input type="date">`. Give the inputs
      `data-testid="history-range-start"` / `"history-range-end"`. The native inputs' own typography
      is browser default and exempt from this app's type system by design (D-01's accepted cost).
    - The cap/validation message slot below, rendered only when a reason is present: `fontSize: 11`,
      `fontWeight: 600`, `color: 'var(--sc-destructive)'`, `marginTop: 8`,
      `data-testid="history-range-cap"`. It renders `t('h_range_cap_message')` for the `'too-long'`
      reason. For the other reasons the field constraints and the disabled button already communicate
      the problem, and D-09 only requires a message that names the limit — do not invent additional
      copy, which would need new i18n keys UI-SPEC does not authorize.
    - The Apply button: `className="btn-primary"`, right-aligned, `marginTop: 16`, label
      `t('h_range_apply')`, `data-testid="history-range-apply"`. This is the only new accent-colored
      element in the phase and the only new primary CTA on this screen.

    Guardrails — two layers, per D-11:
    - Native `min`/`max`, recomputed as the other field changes: the End input's `max` is today's
      `YYYY-MM-DD`; the Start input's `max` is `end || today`; the Start input's `min`, once `end` is
      set, is End-minus-`MAX_RANGE_DAYS`-plus-one days (the earliest start whose inclusive span is
      still within the cap). Import `MAX_RANGE_DAYS` from `history-utils.js` — do not write 366 in
      this file. Future dates are excluded because History is finished-only per `P7 D-01`, so a future
      range is guaranteed empty.
    - `const reason = validateCustomRange(start, end)` on every render. Apply is
      `disabled={reason !== null}`, and when disabled carries `opacity: 0.45` with
      `cursor: 'not-allowed'` — the `.btn-disabled-offline` value, not a new number. The Apply click
      handler re-checks `reason` and returns early if non-null, because native date inputs accept
      typed values that bypass `min`/`max` entirely and the validator is the authoritative gate.
    - Never auto-correct. The fields keep exactly what staff typed and the reason is shown instead —
      silently changing someone's input reads as a bug (D-11).

    On a valid Apply, call `onApply(customRangeToQuery(start, end))` and then `onClose()`.

    Note on platform (RESEARCH Pitfall 3, non-blocking): `<input type="date">` renders no calendar
    affordance on WebKit older than Safari 14.1 / macOS 11 Big Sur, degrading to a plain text field
    with no `min`/`max` UI. `tauri.conf.json` sets no `minimumSystemVersion`. This changes no code —
    the Apply-time validator is the guardrail on every platform regardless — but record it in the
    SUMMARY as a known degraded-affordance case on pre-Big-Sur macOS.
  </action>
  <behavior>
    - Rendering the popover shows both field labels, two date inputs, and the Apply button; both inputs have an empty value.
    - With both fields empty, Apply is disabled and no cap message renders.
    - With only the start field filled, Apply is disabled — a half-entered range is not applicable.
    - With a valid range (start 2026-03-03, end 2026-03-17), Apply is enabled and no cap message renders.
    - Clicking Apply with a valid range calls `onApply` exactly once with a `{ from, to }` deep-equal to `customRangeToQuery('2026-03-03','2026-03-17')`, then calls `onClose`.
    - With end before start, Apply is disabled and `onApply` is never called even if the button is clicked programmatically.
    - With a span over 366 days, Apply is disabled and the cap message renders reading `h_range_cap_message`, in `var(--sc-destructive)`.
    - With a span of exactly 366 days, Apply is enabled and no cap message renders.
    - With start equal to end, Apply is enabled — a single-day range is valid (adjacency edge).
    - The End input's `max` attribute is today's YYYY-MM-DD. After picking an end date, the Start input's `max` is that end date and its `min` is 366 days earlier (inclusive-span arithmetic).
    - A `mousedown` outside the panel calls `onClose` and never `onApply`.
    - An `Escape` keydown calls `onClose` and never `onApply`.
    - A `mousedown` INSIDE the panel does not close it.
    - Unmounting the popover removes both document listeners — asserted by spying on `document.removeEventListener` or by confirming a post-unmount Escape does not call `onClose`.
  </behavior>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0.
    - `grep -c "function CustomRangePopover" src/screen-history.jsx` returns 1.
    - `grep -c "type=\"date\"" src/screen-history.jsx` returns 2.
    - `grep -c "validateCustomRange" src/screen-history.jsx` returns at least 2 — the import and the render-time check.
    - `grep -c "MAX_RANGE_DAYS" src/screen-history.jsx` returns at least 2 — the import and the min computation.
    - `grep -v "^//" src/screen-history.jsx | grep -c "366"` returns 0 — the cap number is not re-literalled in this file.
    - `grep -c "top: 'calc(100% + 6px)'" src/screen-history.jsx` returns 1 — the grandfathered offset, copied from shell.jsx.
    - `grep -c "boxShadow: '0 4px 16px rgba(0,0,0,0.12)'" src/screen-history.jsx` returns 1 — chrome copied verbatim.
    - `grep -c "'keydown'" src/screen-history.jsx` returns 2 — the add and the remove; the Escape listener is cleaned up.
    - `grep -c "opacity: 0.45" src/screen-history.jsx` returns 1 — the disabled Apply matches the established value.
    - `grep -c "className=\"btn-primary\"" src/screen-history.jsx` returns 1 — exactly one new primary CTA.
    - `npx vitest run src/__tests__/screen-history.test.jsx -t "popover"` exits 0 and reports at least 10 passing tests.
  </acceptance_criteria>
  <done>The popover renders per UI-SPEC's diagram from shell.jsx's chrome, gates Apply on the pure validator at both render and click time, constrains both inputs via recomputed min/max, dismisses on outside-click and Escape without applying, and never fetches until Apply.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the popover to the Custom pill — D-03 label, D-04 clearing, custom range fetch</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/screen-history.jsx` — `HistoryScreen`'s `selectedPeriod` state, the range `useMemo`, and the `onSelectPeriod` handler as written by 09-04, including its deliberate early-return on `'custom'` that this task replaces. Also `FilterBar`'s pill `.map()`.
    - `.planning/phases/09-period-control/09-CONTEXT.md` D-03, D-04, and `<specifics>` → "The pill never advertises a range that isn't live" — the invariant D-03 and D-04 lock into together.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Custom Range Popover Contract, the D-03 and D-04 bullets, and § Period Pills Contract for the selected styling the Custom pill adopts.
    - `src/history-utils.js` — `formatDateRange` as added by 09-01.
    - `src/icons.jsx` — the `chevDown` icon, which the design source already places on this pill.
  </read_first>
  <action>
    Extend `selectedPeriod` to `{ id, customRange }` where `customRange` is `{ from, to }` or absent.
    09-04 shaped this state as an object precisely so this is an extension, not a replacement.

    Extend the range `useMemo`: when `selectedPeriod.id === 'custom'`, return
    `selectedPeriod.customRange`; otherwise `getPresetRange(selectedPeriod.id)` as before. The memo
    stays keyed on `selectedPeriod`. The applied custom range therefore flows through the identical
    seam the presets use, into the same `useHistoryOrders({from, to})` call. Do not add a second
    fetch path: a separate call site would break D-08's cache reuse and 09-04's settled-period
    tracking simultaneously, and the query key must remain the resolved `from`/`to` strings only —
    folding a `'custom'` sentinel into it would silently defeat instant-restore for custom ranges
    specifically (RESEARCH Pitfall 4).

    Replace 09-04's `'custom'` early-return in `onSelectPeriod` with popover toggling:
    - Add `const [rangeOpen, setRangeOpen] = useState(false)` (in `FilterBar`, which owns the pill
      markup, or lifted — the executor's call, provided the popover unmounts when closed so its blank
      local state is genuinely fresh on each open, per D-04).
    - Clicking the Custom pill toggles the popover open/closed regardless of whether a range is
      currently applied — reopening is how staff change an applied range.
    - The popover's `onApply({from, to})` sets `selectedPeriod` to `{ id: 'custom', customRange: {from, to} }`
      and closes.

    D-03 — the pill shows the applied range:
    - The Custom pill's label is `formatDateRange(customRange.from, customRange.to, locale)` when
      `selectedPeriod.id === 'custom'` and a `customRange` is present; otherwise `t('h_period_custom')`.
      Use the same `lang === 'ro' ? 'ro-RO' : 'en-GB'` mapping the file already uses. Route this
      through 09-04's `periodLabel` helper rather than doing a second lookup here — D-12's whole point
      is one label source, and the tile sub-label reads the same function for the same period.
    - The pill adopts the standard selected styling from 09-04's state-driven ternary; no special
      case. The `chevDown` icon stays visible beside the label, signaling the popover reopens.
    - The pill widens with its content — the segmented-control container is not fixed-width. This is
      D-03's explicitly accepted cost and the surrounding controls shifting is expected, not a defect
      to work around with truncation.

    D-04 — a preset click clears the custom range:
    - `onSelectPeriod(presetId)` sets `selectedPeriod` to `{ id: presetId }` with no `customRange`
      field. The Custom pill reverts to `t('h_period_custom')` and unselected styling, and because the
      popover unmounts when closed, reopening it starts blank. No remembered range.
    - This is not a convenience tradeoff to soften: D-03 and D-04 lock into one invariant — the pill
      shows dates only when that range is what the list is showing. Remembering a cleared range would
      create exactly the state where the pill advertises a range that is not live. The accepted cost
      is that a misclick on a preset destroys a typed range and both dates must be re-entered.
  </action>
  <behavior>
    - Clicking the Custom pill renders the popover; clicking it again removes the popover from the document.
    - The popover is absent from the document on mount.
    - Applying a valid range calls `useHistoryOrders` on the next render with a `{from, to}` deep-equal to `customRangeToQuery(start, end)`, and closes the popover.
    - After applying 2026-03-03 to 2026-03-17 with lang ro, the Custom pill's text contains '3 mar. – 17 mar.' and no longer reads 'Interval'.
    - After applying, the Custom pill renders with the selected styling (`var(--sc-foreground)` background) and the other three pills are unselected.
    - After applying, the chevDown icon is still rendered within the Custom pill.
    - After applying, the tile sub-label reads the same formatted range once the data settles — one label source, pill and tile agree (D-12).
    - After applying, the empty-state phrase for a zero-order custom range reads 'Nicio comandă în intervalul 3 mar. – 17 mar.' (D-13, exercising 09-04's custom branch for the first time).
    - Applying a single-day range (start equals end) fetches a from/to exactly 24h apart and the pill reads the same date on both sides (adjacency edge).
    - After applying, clicking the '7 zile' pill: the Custom pill reverts to 'Interval', the 7-day pill becomes selected, and the fetched range is `getPresetRange('7')`.
    - After that preset click, reopening the popover shows both fields blank — the applied range was not remembered (D-04).
    - There is no rendered state in which the Custom pill shows a date range while the resolved range passed to the hook is a preset range — the pill's dates and the live range are the same value or the pill shows 'Interval'.
    - Dismissing the popover with Escape after typing a range leaves the pill reading 'Interval' and the fetched range unchanged — a dismissed range was never applied.
  </behavior>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0.
    - `grep -c "customRange" src/screen-history.jsx` returns at least 3 — the memo branch, the apply handler, and the label branch.
    - `grep -c "useHistoryOrders(" src/screen-history.jsx` returns 1 — exactly one fetch call site for all four periods.
    - `grep -c "'custom'" src/screen-history.jsx` — no occurrence appears inside the `queryKey` or is passed to `useHistoryOrders`: `sed -n '/const range = useMemo/,/\], \[selectedPeriod\]/p' src/screen-history.jsx | grep -c "queryKey"` returns 0, and 09-03's hook owns the key.
    - `grep -c "formatDateRange" src/screen-history.jsx` returns at least 1, routed through `periodLabel`.
    - `grep -c "chevDown" src/screen-history.jsx` returns 1 — the icon is rendered on the Custom pill.
    - A test asserts that after applying a range and then clicking a preset pill, the Custom pill's text equals the `h_period_custom` label and contains no digits.
    - A test asserts that after applying a range, clicking a preset, and reopening the popover, both date inputs have an empty value.
    - A test asserts Escape-dismissing a typed-but-unapplied range leaves the hook's call argument unchanged.
    - `npx vitest run` exits 0 — full suite green.
  </acceptance_criteria>
  <done>The Custom pill opens the popover, an applied range fetches through the same seam as the presets and renders on the pill, a preset click clears it, and no reachable state shows a range on the pill that is not the live one.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — live API, native picker chrome, and the 366-day worst case</name>
  <action>Blocking human checkpoint. Present the verification script below, wait for the resume signal, and record the three answers in the SUMMARY.</action>
  <what-built>
    All four period pills are live. The three presets (Today / 7 days / 30 days) retarget the fetch;
    the Custom pill opens a date-range popover with an explicit Apply Range button, a 366-day cap, and
    an applied range rendered on the pill itself. The summary strip and day-grouped list retarget
    together from one fetch. Every automated behavior above is green.

    Three things genuinely cannot be verified without you, and each is a live-API or live-platform
    question a mocked test would only encode an assumption about:

    1. **The native date picker's real chrome on this machine's macOS.** RESEARCH Pitfall 3: WebKit
       renders no calendar affordance for `<input type="date">` before Safari 14.1 / macOS 11 Big Sur,
       and `tauri.conf.json` pins no `minimumSystemVersion`. D-01 accepted "browser-chrome-styled,
       doesn't match our design system" as a cost; it did not accept "a plain text field with no
       calendar at all." The unit tests pass either way.
    2. **The 366-day worst case (D-10), which nobody has measured.** `P7 D-03` renders every row with
       no cap and no virtualization, and `listAdminOrders` has no pagination — so the range is the
       only thing bounding how much data reaches the screen. A 366-day fetch is ~12x the 30-day
       default. This is `09-CONTEXT.md`'s named trigger for revisiting virtualization.
    3. **That the dimmed-loading state reads as loading, not as disabled** (D-05) — a judgment about
       perception on a screen that renders both conventions at once (inert status pills at 0.5, dimmed
       rows at 0.6). No test can answer this.
  </what-built>
  <how-to-verify>
    Run the app against the live API: `npm run tauri dev`, sign in, open History (sidebar, cashier role).

    **A. Preset switching (SC1)**
    1. The screen opens on "30 zile" — selected pill dark, list populated, tiles reading "30 zile".
    2. Click "7 zile". Watch the transition: rows should DIM (stay visible and readable, not vanish),
       a small spinner should appear next to the pill group, and the tile sub-labels should still read
       "30 zile" until the new data lands — then pill, rows, tiles, and sub-label should all agree.
       → Confirm the rows never collapse to skeletons and the scroll position never jumps.
       → Confirm you never see "7 zile" above a number that is still the 30-day figure.
    3. Click "Azi". Confirm the list shows only today's finished orders, and the tiles agree with the
       day header's own count and revenue figure.
    4. Click back to "30 zile". It should be instant with no visible refetch (it is cached).

    **B. The dimming reads as loading, not disabled (D-05, item 3 above)**
    5. During a switch, try clicking a row while it is dimmed. It should still open. If the dimmed
       state feels like "this is broken/disabled" rather than "this is updating", say so — that is a
       real finding, and the remedy is a design change, not a bug fix.

    **C. Custom range (SC2)**
    6. Click "Interval". → **Item 1 above:** does a real OS calendar open when you click a date field,
       or is it a bare text box? Report which. A bare text box on a modern Mac is a defect; on an old
       one it is the known pre-Big-Sur degradation.
    7. Pick a start and an end a couple of weeks apart. Confirm nothing fetches until you click
       "Aplică intervalul".
    8. Click Apply. Confirm the list reloads for exactly that range, and the pill now reads e.g.
       "3 mar. – 17 mar." instead of "Interval".
    9. Try to pick an end date in the future — the OS calendar should grey them out.
    10. Try to set a range longer than a year. Apply should stay greyed and the message
        "Intervalul maxim este de 366 de zile." should appear.
    11. Click "7 zile". The Interval pill should revert to "Interval". Reopen it — both fields blank.

    **D. The 366-day worst case (item 2 above)**
    12. Open Interval, set a full 366-day range ending today, and Apply. **Time it.** Report roughly
        how long until rows appear, whether the spinner kept animating throughout, and whether the app
        felt frozen at any point. If it stalls noticeably, that is D-10's accepted-but-unmeasured risk
        landing, and it is the trigger to schedule virtualization — report the number rather than
        treating it as a pass/fail.

    **E. Scope (nothing else moved)**
    13. Confirm the status filter pills, the search box, and the Export button are all still greyed
        and unclickable.
    14. Switch the language to EN in Settings and repeat step 8 — the pill should read "3 Mar – 17 Mar",
        not a numeric form, and the empty-state sentence (if you can find an empty period) should read
        grammatically.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what you saw — especially the answers to items 1 (calendar or text box?), 2 (how long did 366 days take?), and 3 (did the dimming read as loading?).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed date string → SDK query | A native date input accepts typed values that bypass its own `min`/`max` entirely. This is the phase's only genuinely untrusted user input reaching a network parameter. |
| Apply click → unpaginated fetch | One click can request up to a year of orders against an endpoint with no pagination, into a list with no virtualization. |
| applied range → pill label | The pill is the answer to "what am I looking at?" — it names which days' money is on screen. |
| popover local state → screen state | The popover holds a half-built range that must not escape until Apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-17 | Denial of Service (client-side, self-inflicted) | An oversized range reaching `admin.orders.list` — no pagination, no virtualization, so the range is the only bound on data volume | high | mitigate | Two layers, per D-11: native `min`/`max` recomputed as each field changes (narrows what is *pickable*), and `validateCustomRange`'s `MAX_RANGE_DAYS` check at both render time (gating `disabled`) and click time (early return). The click-time re-check is the one that matters — `min`/`max` is only an affordance and typed values bypass it. `MAX_RANGE_DAYS` is imported, and a grep criterion asserts `366` is not re-literalled here, so the two layers cannot drift apart. Blocking threshold is `high`, so this must close before the phase seals. **Residual, accepted by D-10:** 366 days is ~12x the 30-day default and has never been measured; the human checkpoint above measures it. |
| T-09-18 | Tampering (client-side input) | A malformed typed date reaching `customRangeToQuery` and producing a garbage `from`/`to` | medium | mitigate | `validateCustomRange`'s `'incomplete'` branch (09-01) rejects any value not matching `YYYY-MM-DD` and any value parsing to NaN, and Apply's click handler returns early on any non-null reason — so `customRangeToQuery` is unreachable with an unvalidated pair. Asserted by a test that clicking a disabled/invalid Apply never calls `onApply`. |
| T-09-19 | Spoofing (false assertion about a financial record) | The Custom pill displaying a range that is not the live one — after a preset click, a dismissed popover, or a failed apply | high | mitigate | D-04's clearing is what makes this unreachable, and it is why remembering a range was rejected. The pill's label branch reads `selectedPeriod.customRange`, which is the same value the range memo resolves from — one source, so the pill and the fetched range cannot diverge by construction. Tests assert the pill reverts on a preset click, that a dismissed range never reaches the pill, and that reopening after clearing shows blank fields. This is the plan's `must_haves` prohibition made executable. |
| T-09-20 | Tampering (unintended commit) | A range being applied by dismissal rather than by explicit Apply — committing a range staff were abandoning | medium | mitigate | `onClose` and `onApply` are separate callbacks; the dismissal effect calls only `onClose`. Tests assert that both outside-click and Escape call `onClose` and never `onApply`, and that an Escape-dismissed typed range leaves the hook's call argument unchanged. |
| T-09-21 | Denial of Service (resource leak) | The `document` `mousedown`/`keydown` listeners outliving the popover, accumulating across open/close cycles | low | mitigate | Both listeners are registered in one `useEffect` and removed in its cleanup return, per `shell.jsx`'s precedent. A grep criterion asserts `'keydown'` appears twice (add + remove), and a test asserts a post-unmount Escape does not fire `onClose`. |
| T-09-22 | Information Disclosure | The native date picker rendering no `min`/`max` affordance on pre-Big-Sur WebKit, leaving staff to discover constraints only at Apply | low | accept | The Apply-time validator is the guardrail on every platform, so the security property holds regardless — only the affordance degrades. `tauri.conf.json` pins no `minimumSystemVersion`; the human checkpoint reports the actual behavior on the target machine rather than blocking on it. RESEARCH Pitfall 3, Assumption A2. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan or phase — zero new dependencies per `09-RESEARCH.md` § Package Legitimacy Audit. `<input type="date">` is a browser built-in, not a package; D-01 chose it specifically to avoid adding a date library. |
</threat_model>

<verification>
- `npx vitest run` — full suite green.
- `grep -v "^//" src/screen-history.jsx | grep -c "366"` returns 0.
- `grep -c "useHistoryOrders(" src/screen-history.jsx` returns 1 — one fetch path for all four periods.
- The blocking human checkpoint is approved, with explicit answers to the calendar-chrome, 366-day-timing, and dimming-perception questions.
</verification>

<success_criteria>
- SC2: staff can pick a custom start and end date and the list reloads for exactly that range.
- D-01/D-02: a native-input popover with an explicit Apply; nothing fetches before the click.
- D-03/D-04: the applied range renders on the pill, and a preset click clears it — no reachable state advertises a range that is not live.
- D-09/D-10/D-11: the 366-day cap is enforced at the input and at Apply, from one imported constant; nothing is auto-corrected.
- The custom range flows through the same memo, hook, and query key as the presets — one fetch path.
- HIST-04 is complete: all four periods live.
</success_criteria>

<output>
Create `.planning/phases/09-period-control/09-05-SUMMARY.md` when done. Record the human
checkpoint's three answers verbatim — particularly the 366-day timing, which is the first
measurement of `P7 D-03`'s deferred "measure against real data before adding machinery" and the
input to whether virtualization gets scheduled.
</output>
