---
phase: 14-branch-scoped-cache-re-scoping
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/use-order-detail.js
  - src/use-stats.js
  - src/use-menu.js
  - src/__tests__/use-order-detail.test.js
  - src/__tests__/use-stats.test.js
  - src/__tests__/use-menu.test.js
autonomous: true
requirements: [SCOPE-01]
must_haves:
  truths:
    - "use-order-detail.js queryKey is `['order', branchId, id]`; error path routes through unwrapSdkResult('Failed to get order'). (SC1)"
    - "use-stats.js queryKey is `['stats', branchId]`; error path routes through unwrapSdkResult('Failed to load stats'). (SC1)"
    - "use-menu.js queryKey is `['menu', branchId]`; error path routes through unwrapSdkResult('Failed to list menu'). (SC1)"
    - "In all three hooks branchId = `useAppStore((s) => s.currentBranch?.id) ?? null` and is the first variable segment immediately after the resource name. (D-07)"
    - "Changing currentBranch yields a different key → fresh fetch for each of the three hooks. (SC1)"
  artifacts:
    - src/use-order-detail.js
    - src/use-stats.js
    - src/use-menu.js
    - src/__tests__/use-order-detail.test.js
    - src/__tests__/use-stats.test.js
    - src/__tests__/use-menu.test.js
  key_links:
    - "Each hook imports unwrapSdkResult from src/data.jsx (created by Plan 14-01) — read-only consumption, no data.jsx edit"
  prohibitions:
    - statement: "MUST NOT add `!!branchId` to any hook's enabled. use-order-detail keeps its existing `enabled: !!client && !!id` exactly (id-gating is unrelated to branch-gating)."
      status: enforced
      verification: "grep confirms no `branchId` term appears inside any `enabled:` expression in the three hooks"
    - statement: "MUST NOT leave any of the three hooks on an unscoped key."
      status: enforced
      verification: "phase-gate grep audit returns zero unscoped `['order']`/`['stats']`/`['menu']` queryKey matches in these files"
    - statement: "MUST NOT use `queryClient.resetQueries()` (D-01 uniform mechanism)."
      status: enforced
      verification: "grep -rn 'resetQueries' src/ returns zero matches"
---

<objective>
Expand the proven tracer pattern to three more fetch hooks: `use-order-detail.js`, `use-stats.js`, `use-menu.js`. Each gets the branch-scoped key and routes its error path through the `unwrapSdkResult()` helper created in Plan 14-01. Add a branch-key SC1 test for each.

Purpose: Mechanical, near-identical retrofit of the orders-detail/stats/menu hooks so a branch change produces a fresh fetch instead of a stale hit (SCOPE-01 SC1).
Output: Three retrofitted hooks + three new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-CONTEXT.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch-scope use-order-detail.js and use-stats.js</name>
  <read_first>
    - src/use-orders.js (the PROVEN tracer reference from Plan 14-01 — the exact import + branchId selector + key + unwrapSdkResult shape to mirror)
    - src/use-order-detail.js (current key line `['order', id]`, error line, `enabled: !!client && !!id`)
    - src/use-stats.js (current key `['stats']`, error line)
    - src/data.jsx (confirm unwrapSdkResult signature created in 14-01)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (per-file retrofit for use-order-detail and use-stats)
  </read_first>
  <files>src/use-order-detail.js, src/use-stats.js</files>
  <behavior>
    - useOrderDetail(id) with currentBranch { id: 'branch-a' } produces key ['order', 'branch-a', id].
    - useStats() with currentBranch { id: 'branch-a' } produces key ['stats', 'branch-a'].
    - Both still fetch when currentBranch is null (enabled unchanged).
  </behavior>
  <action>
    In src/use-order-detail.js: import `useAppStore` from './store.js' and `unwrapSdkResult` from './data.jsx'; read `const branchId = useAppStore((s) => s.currentBranch?.id) ?? null;`; change queryKey to `['order', branchId, id]`; change the queryFn's error line so it returns `normalizeOrder(unwrapSdkResult(result, 'Failed to get order').order)`. Keep `enabled: !!client && !!id` EXACTLY as-is (do not add or remove any branch term) and keep `staleTime: 0`.
    In src/use-stats.js: import `useAppStore` from './store.js' and `unwrapSdkResult` from './data.jsx'; read the same branchId selector; change queryKey to `['stats', branchId]`; change the queryFn to `return unwrapSdkResult(result, 'Failed to load stats')`. Keep `enabled: !!client` unchanged — never `!!branchId`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-order-detail.test.js src/__tests__/use-stats.test.js</automated>
  </verify>
  <acceptance_criteria>
    - use-order-detail.js queryKey literal is exactly `['order', branchId, id]`; `enabled: !!client && !!id` unchanged.
    - use-stats.js queryKey literal is exactly `['stats', branchId]`; `enabled: !!client` unchanged.
    - Both files import unwrapSdkResult from './data.jsx' and route their error path through it.
    - No `branchId` term appears inside any `enabled:` expression.
  </acceptance_criteria>
  <done>use-order-detail and use-stats are branch-scoped and error-routed through the shared helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch-scope use-menu.js + SC1 tests for all three hooks</name>
  <read_first>
    - src/use-orders.js (proven tracer reference)
    - src/use-menu.js (current key `['menu']`, error line, `staleTime: 5 * 60 * 1000`)
    - src/__tests__/use-orders.test.js (test template: mock scaffolding, real useAppStore seeding, QueryClient wrapper)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (Test Patterns — new hook test file template; use-menu retrofit)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-VALIDATION.md (test convention: do NOT mock ../store.js; SC1 map)
  </read_first>
  <files>src/use-menu.js, src/__tests__/use-order-detail.test.js, src/__tests__/use-stats.test.js, src/__tests__/use-menu.test.js</files>
  <behavior>
    - useMenu() with currentBranch { id: 'branch-a' } produces key ['menu', 'branch-a'].
    - SC1 (each hook): seeding a currentBranch id makes that id appear as the segment immediately after the resource name in the cache key.
  </behavior>
  <action>
    In src/use-menu.js: import `useAppStore` from './store.js' and `unwrapSdkResult` from './data.jsx'; read the branchId selector; change queryKey to `['menu', branchId]`; change the queryFn to `return unwrapSdkResult(result, 'Failed to list menu')`. Keep `enabled: !!client` and `staleTime: 5 * 60 * 1000` unchanged.
    Create src/__tests__/use-order-detail.test.js, src/__tests__/use-stats.test.js, and src/__tests__/use-menu.test.js, each copying the mock scaffolding from use-orders.test.js (mock @tauri-apps/plugin-store, @charlyk/admin-client, @tauri-apps/plugin-opener, @tauri-apps/api/core, ../auth.jsx; import the REAL useAppStore — do NOT mock ../store.js) with `beforeEach(() => useAppStore.setState({ currentBranch: null }))`. Each test file mocks the relevant SDK method (kitchen.orders.get / admin.dashboard.getToday / kitchen.menu.list) returning `{ data, error: null }`, seeds `useAppStore.setState({ currentBranch: { id: 'branch-a', name: 'A', slug: 'a', isDefault: true, isActive: true } })`, renders the hook, awaits isSuccess, and asserts the QueryClient cache holds a key whose segment after the resource name is 'branch-a'.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-order-detail.test.js src/__tests__/use-stats.test.js src/__tests__/use-menu.test.js</automated>
  </verify>
  <acceptance_criteria>
    - use-menu.js queryKey literal is exactly `['menu', branchId]`; routes error through unwrapSdkResult; enabled/staleTime unchanged.
    - `npx vitest run src/__tests__/use-order-detail.test.js src/__tests__/use-stats.test.js src/__tests__/use-menu.test.js` exits 0.
    - Each new test file seeds a real currentBranch and asserts 'branch-a' is the segment after the resource name in the cache key.
  </acceptance_criteria>
  <done>use-menu is branch-scoped; all three hooks have a passing SC1 branch-key test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | branchId folded into client-side cache keys only; never sent as a request param (server resolves branch server-side, D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | client-persisted branchId as authorization boundary | medium | mitigate | These hooks only READ currentBranch?.id from session-only Zustand state (Phase 13 D-10). No persistence added; store.js not edited. |
| T-14-02 | Information Disclosure | raw SDK error strings via unwrapSdkResult | low | accept | No UI surfacing this phase (Phase 17 owns toasts); helper feeds internal err.code only. |

**Out of scope:** V4 Access Control is server-enforced. No package installs → no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run src/__tests__/use-order-detail.test.js src/__tests__/use-stats.test.js src/__tests__/use-menu.test.js` exits 0.
- Phase-gate grep audit finds zero unscoped `['order']`/`['stats']`/`['menu']` queryKeys in these three files.
- No `branchId` term inside any `enabled:` expression.
</verification>

<success_criteria>
use-order-detail, use-stats, and use-menu are branch-scoped, error-routed through unwrapSdkResult, and each has a passing SC1 branch-key test.
</success_criteria>

<output>
Create `.planning/phases/14-branch-scoped-cache-re-scoping/14-02-SUMMARY.md` when done.
</output>
