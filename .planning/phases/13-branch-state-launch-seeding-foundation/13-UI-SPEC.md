---
phase: 13
slug: branch-state-launch-seeding-foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-21
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Scope Note (read first)

Phase 13 is a **state / data-layer phase**, not a screen-building phase (per `13-CONTEXT.md` §domain).
It delivers a session-only `currentBranch` Zustand field, a `getMe()` seeding call in `auth.jsx`, and a
new `useBranches()` TanStack hook. **No consumer reads `currentBranch` yet.** The branch switcher UI,
switch flow, toasts, and 403 recovery are explicitly deferred to Phase 16/17 and are **out of scope**
for this contract — inventing them here would be scope creep and design debt.

The net new visual surface for this phase is near-zero. Exactly two visual/interaction deltas exist:

1. **The existing `coldStartBusy` blank gate** (`app.jsx:224-227`) is reused, not replaced — D-01/D-02
   allow it to hold slightly longer for one `getMe()` round-trip. **No new blank/spinner/skeleton state
   is introduced.**
2. **The sidebar `displayName`** (`shell.jsx:31`) now renders from a populated `authUser` on cold start
   (D-05/D-06) instead of falling back to the hardcoded literal `'Eduard Albu'`. This is a data/copy
   correctness fix to an existing element, not a new component.

Everything else in this document either documents "no change" explicitly (so the checker and executor
don't misread silence as a gap) or covers the one genuinely new copy decision the fix above requires
(the identity-label fallback, see Copywriting Contract).

**shadcn note:** `components.json` was not found. Per `CLAUDE.md` §Design Fidelity, this project's
design system is locked and lives in `src/colors_and_type.css` / `src/styles.css` — "Do not change
colors, spacing, typography, or component layout without explicit instruction." Introducing shadcn now
would contradict that explicit project instruction, and this phase adds no new component surface that
would need it. The shadcn init gate is therefore intentionally **not** run. `Tool: none` below reflects
this project-level decision, not an oversight.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled CSS custom properties + inline JSX styles (project-locked, see Scope Note) |
| Preset | not applicable |
| Component library | none (bespoke components under `src/*.jsx`) |
| Icon library | custom `Icon` component (`src/icons.jsx`) |
| Font | Outfit (sans, weights 400–900) + Caveat (handwriting accent) — loaded once in `src/colors_and_type.css`, imported via `main.jsx` |

---

## Spacing Scale

**No new spacing is introduced this phase** — there is no new visual surface to size. Documented for
completeness/inheritance only; unchanged by this phase.

| Token | Value | Usage (existing, inherited) |
|-------|-------|-------|
| xs | 4px | Icon gaps, dot indicators |
| sm | 8px | Compact element spacing (e.g. `nav-group-label` gap) |
| md | 16px | Default element spacing (e.g. sidebar padding-x) |
| lg | 24px | Content/section padding (`.content-pad`) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: the existing sidebar/topbar chrome (`sidebar-footer`, `user-chip`, `topbar`) already uses
some off-scale inline values (10px, 14px, 18px) predating this phase — pre-existing, not touched or
introduced by Phase 13, not to be "fixed" here (out of scope; D-06 requires the change stay "minimal
and additive").

---

## Typography

**No new typography is introduced this phase.** The one touched element (sidebar `displayName`) keeps
its existing inline styling verbatim; only the *data* feeding it changes (D-06). Documented for
inheritance/reference only.

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| Body | 16px | 400 | 1.6 | `--sc-type-body` |
| Sidebar identity label (existing, unchanged) | 13px | 600 | normal (inline) | `shell.jsx:171` inline style — reused as-is |
| Sidebar identity sublabel (existing, unchanged) | 11px | 400 | normal (inline) | `shell.jsx:172` inline style, `--sc-muted-foreground` color |
| Heading (h3) | 20px | 700 | 1.3 | `--sc-type-h3` |
| Display | 48–60px (clamp) | 900 | 1.1 | `--sc-type-h1` / `--sc-type-display` |

---

## Color

**No new color usage is introduced this phase.** Documented for inheritance/reference only — the
identity-label fix reuses `var(--sc-foreground)` (default text color, inline style already omits an
explicit color) and the existing muted sublabel color; no new hex/hsl value is added anywhere.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(40 60% 97%)` (`--sc-background`) | App background, main content surface |
| Secondary (30%) | `#fbf6ea → #f7f1e1` gradient (sidebar), `hsl(40 30% 94%)` (`--sc-secondary`) | Sidebar, cards, topbar |
| Accent (10%) | `hsl(120 14% 49%)` (`--sc-primary`, user-selectable via 4 accent swatches) | Active nav item background, primary buttons, focus rings — unchanged by this phase |
| Destructive | `hsl(0 84.2% 60.2%)` (`--sc-destructive`) | Not used by this phase (no destructive action introduced) |

Accent reserved for: active `.nav-item`, `.btn-primary`, focus/ring states — same reservation as before
this phase; not expanded.

---

## Copywriting Contract

This phase introduces **no new CTA, no new empty state, no new error state, and no destructive action**
— see Scope Note. Exactly one new copy decision is required, driven by the D-06 sidebar-name fix:

| Element | Copy |
|---------|------|
| Primary CTA | N/A — no new CTA this phase |
| Empty state heading | N/A — `currentBranch`/`useBranches()` have no UI consumer yet (deferred to Phase 16) |
| Empty state body | N/A — same as above |
| Error state | N/A — no new visible error UI; D-03/D-04 failure handling is deliberately silent (background focus-retry only). The user-visible 403 recovery path is explicitly Phase 17's scope, not this phase's |
| Destructive confirmation | N/A — no destructive action in this phase |
| **Sidebar identity fallback label** *(new, defaulted — not explicit in CONTEXT.md D-06)* | When `authUser` is entirely unresolved at paint time (a non-401 cold-start `getMe()` failure per D-03, or the brief pre-`getMe()` instant on the `signIn()` path), `displayName` must **not** fall back to a hardcoded personal name (removing the pre-existing `'Eduard Albu'` bug is the whole point of D-06). Recommended fallback: a generic, role-neutral literal — **"Staff"** (en) / **"Personal"** (ro) — not a real name. Flag for planner confirmation; not locked by CONTEXT.md. |

Composition rule for the populated case (locked by D-06): `[firstName, lastName].filter(Boolean).join(' ').trim()`,
else `authUser.email`, else the generic fallback above. Initials continue to derive from whatever string
`displayName` resolves to (existing `split(' ').map(w=>w[0])...` logic, unchanged).

---

## UI Considerations

Applicable state considerations resolved: 2 covered, 1 backstop, 1 unresolved, 1 explicitly out-of-scope-dismissed.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| loading | Cold-start app-shell gate (nav) | ✅ covered | Existing `coldStartBusy` blank white screen (`app.jsx:224-227`) is reused unchanged; D-01/D-02 lock that one `getMe()` round-trip may extend it slightly, but no new spinner/skeleton/text is introduced. |
| empty | Sidebar identity label (static-content) | 🧪 backstop | `authUser` can be entirely `null` when `Shell` paints (non-401 cold-start `getMe()` failure per D-03, or the brief pre-resolution instant). `displayName` must render the generic fallback in the Copywriting Contract above, not a hardcoded name. Verify with a held-out render test: `Shell` with `authUser=null` shows the generic fallback, never a specific person's name. |
| long-text | Sidebar identity label (static-content) | ⚠ unresolved | Real `firstName`/`lastName` values (replacing the short `'Eduard Albu'` fallback) may run longer than the 240px sidebar allows. The existing `.user-chip` name `<div>` (`shell.jsx:171`) has no `text-overflow`/`white-space` guard today. Not fixed in this phase — layout is explicitly out of scope (D-06: "keep the change minimal and additive"). Flagged as a planner assumption: accept as-is (matches pre-existing sidebar behavior for other long strings) or add a one-line ellipsis rule if it proves visually broken during execution. |
| error | `getMe()` call on cold start / sign-in (auth flow) | ✅ covered | D-03/D-04 fully define the failure handling and it is intentionally silent by design (no toast, no inline error, no new UI): a non-401 failure leaves the app authenticated with `currentBranch: null`, backstopped only by a focus-triggered retry. No visible error UI is added this phase; Phase 17 owns the user-visible 403 recovery path. |
| empty / loading / error / populated | `currentBranch`, `useBranches()` branch list | dismissed | No consumer reads `currentBranch` or calls `useBranches()`'s data anywhere in the UI this phase (CONTEXT.md §domain, explicit). All state coverage for branch data is out of scope until Phase 16 (switcher UI) and Phase 17 (403 handling) build actual consumers — raising it here would invent UI that doesn't exist. |

<!-- Status vocabulary (locked by probe-core projectTruths):
     ✅ covered   → a plain truth string lifted into must_haves.truths
     🧪 backstop  → a flat scalar { statement, verification: backstop }; at verify time, no explicit
                    evidence → insufficient_spec → human_needed (never a silent pass, #1154)
     ⚠ unresolved → an explicit planner assumption (surfaced, never silently dropped)
     Rows are REPLACED (not appended) on a probe re-run — idempotent. -->

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized this project (see Scope Note) |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
