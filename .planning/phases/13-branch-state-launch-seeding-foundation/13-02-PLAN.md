---
phase: 13-branch-state-launch-seeding-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/use-branches.js
  - src/__tests__/use-branches.test.js
autonomous: true
requirements:
  - BSTATE-02
user_setup: []

must_haves:
  truths:
    # --- ROADMAP SC3 + BSTATE-02 ---
    - "SC3/BSTATE-02: The accessible-branches list loads via client.me.branches.list() through a ['branches']-keyed TanStack Query hook (useBranches)."
    - "BSTATE-02: the list refetches on window focus (refetchOnWindowFocus: true) and is never cached indefinitely — a finite staleTime (30_000 ms, matching the useStats/useOrders precedent), NOT Infinity."
    - "D-09: the query key is ['branches'] — deliberately NOT branch-prefixed this phase (re-keying is Phase 14's job)."
    - "D-09: the sole enabled gate is !!client — useBranches has no dependency on currentBranch resolving."
    # --- Edge probe: 'empty' edge (load-bearing) ---
    - "Edge/BSTATE-02 empty: a single-branch tenant (a one-element accessible list) and an empty list are both valid results — useBranches returns data.length 1 or 0 without special-casing, and single-branch first-paint is not gated (SC5 regression: no !!branchId block)."
  prohibitions:
    - "MUST NOT add a !!branchId or !!currentBranch clause to useBranches's enabled gate — enabled: !!client only; a branchId gate would regress first-paint timing for single-branch tenants (Pitfall 11 / Pitfall 5). This is a standing v1.2 regression check across all 7 existing data hooks too."
    - "MUST NOT use Infinity (or omit) staleTime — the list must not be pinned stale; a finite window is required (D-09)."
    - "MUST NOT build useBranchSwitch() or wire useBranches() into app.jsx this phase — deferred to Phase 16 (D-08); no dead code, no consumer."
    - "MUST NOT use the getMe() throwing-contract style here — client.me.branches.list() DOES follow the {data,error} fields convention (unlike getMe); unwrap with `if (result.error) throw ...` exactly like use-stats.js."
  artifacts:
    - "src/use-branches.js — the useBranches() hook (new file)"
    - "src/__tests__/use-branches.test.js — new test file covering the enabled gate, staleTime, refetchOnWindowFocus, and the {data,error} unwrap"
  key_links:
    - "useBranches → useAuth().client → client.me.branches.list() (the {data,error} fields-style fetch)"
    - "useBranches enabled: !!client → single-branch tenants load with no first-paint delay (the SC5 regression seam)"
  assumptions:
    - "FLAGGED (edge/BSTATE-02 adjacency): inapplicable — the accessible-branches list has no merge/collision/touch semantics to specify this phase (no switching, no de-dup). No task."
    - "FLAGGED (edge/BSTATE-02 ordering): inapplicable — no consumer renders the list this phase (D-08), so output ordering/stability is unspecified and unobservable; the switcher (Phase 16) will define any ordering it needs. No task."
    - "PLANNER (Claude's Discretion, D-09 + RESEARCH A1): staleTime is set to 30_000 ms to match the shipped useStats/useOrders precedent; a one-line follow-up tweak if a different window is ever wanted, no design impact."
    - "UI (dismissed, not this phase): every branch-data UI state — the switcher's empty/loading/error/populated list, zero-one-many counts, long branch names — is deferred to Phase 16/17, which build the actual consumers (UI-SPEC E4)."
---

<objective>
Deliver BSTATE-02: a ['branches']-keyed TanStack Query hook (useBranches) over client.me.branches.list() that refetches on window focus and after any branch-access error, with a finite staleTime — never cached indefinitely. The hook has no UI consumer this phase (D-08); it is the data-layer foundation the Phase 16 switcher will read.

Purpose: Separate the accessible-branches list (a genuine server list resource → TanStack Query) from the single currentBranch fact (→ Zustand, plan 13-01). This hook is architecturally identical to useStats()/useOrders().

Output: One new hook file plus its test.

## SDK Coverage Decision (ai-integration contribution)
Adopted this phase: `client.me.branches.list()` (→ Array<AccessibleBranch>, {data,error} fields style). Deferred: `client.me.branches.switch()` / `useBranchSwitch()` → Phase 16 (D-08) — not built here, no dead code.

## Artifacts this phase produces (this plan)
- `useBranches` hook + the new file `src/use-branches.js`
- New test file `src/__tests__/use-branches.test.js`
- No store changes, no i18n keys, no app.jsx wiring
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-branch-state-launch-seeding-foundation/13-CONTEXT.md
@.planning/phases/13-branch-state-launch-seeding-foundation/13-RESEARCH.md
@.planning/phases/13-branch-state-launch-seeding-foundation/13-PATTERNS.md
@src/use-stats.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useBranches() mirroring use-stats.js, plus its Wave-0 test</name>
  <files>src/use-branches.js, src/__tests__/use-branches.test.js</files>
  <read_first>
    - src/use-stats.js (the full 16-line analog — mirror its shape exactly, swapping the SDK call)
    - src/__tests__/use-orders.test.js (the renderHook + QueryClientProvider + `vi.mock('../auth.jsx')` + mocked-client pattern to mirror for the new test)
    - .planning/phases/13-branch-state-launch-seeding-foundation/13-RESEARCH.md (§Pattern 3 useBranches, §Pitfall 5, Code Examples §3)
    - .planning/phases/13-branch-state-launch-seeding-foundation/13-PATTERNS.md (§src/use-branches.js — the 1:1 mirror with {data,error} unwrap)
    - node_modules/@charlyk/admin-client/dist/index.d.ts (AccessibleBranch line 670; me.branches.list → Array<AccessibleBranch>, ~line 1991)
  </read_first>
  <behavior>
    - Test: useBranches() calls client.me.branches.list() and returns its data (an AccessibleBranch[]) on success.
    - Test: when the SDK returns { error: ... }, the queryFn throws (query enters error state) — the {data,error} unwrap, not the throwing-contract style.
    - Test: with client null, the query does not run (fetchStatus 'idle') — enabled: !!client.
    - Test: the hook's options set refetchOnWindowFocus true and a finite staleTime (assert staleTime is a number and not Infinity — e.g. 30000).
    - Test: a single-element list ([one branch]) and an empty list ([]) both pass through unchanged (edge/empty).
  </behavior>
  <action>
    Create src/use-branches.js as a 1:1 structural mirror of src/use-stats.js: import useQuery from '@tanstack/react-query' and useAuth from './auth.jsx'; export function useBranches() pulling `const { client } = useAuth()` and returning useQuery with queryKey ['branches'], a queryFn that awaits `client.me.branches.list()`, unwraps via `if (result.error) throw new Error(result.error.error ?? 'Failed to load branches')` and returns result.data, `enabled: !!client`, `staleTime: 30_000`, and `refetchOnWindowFocus: true`. Do NOT add any branchId/currentBranch clause to enabled. Do NOT add a switch mutation. Create src/__tests__/use-branches.test.js mirroring use-orders.test.js's harness (hoisted `vi.mock('../auth.jsx')`, mocked plugin-store/admin-client, renderHook with a QueryClientProvider wrapper, retry:false) covering the five behaviors above; assert the staleTime/refetchOnWindowFocus options by reading them off the returned query's configuration or by asserting the hook's option object shape.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-branches.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/use-branches.js exports useBranches with queryKey ['branches'], enabled: !!client (no branchId/currentBranch clause), staleTime finite (30_000, not Infinity), refetchOnWindowFocus: true.
    - src/use-branches.js unwraps client.me.branches.list() with the {data,error} fields style (throws on result.error) — NOT a getMe-style bare try/catch.
    - src/use-branches.js contains no useBranchSwitch and no app.jsx import/wiring.
    - src/__tests__/use-branches.test.js covers success, error-throw, enabled-off, finite-staleTime + focus-refetch, and the single/empty-list edge.
    - `npx vitest run src/__tests__/use-branches.test.js` exits 0.
  </acceptance_criteria>
  <done>useBranches() fetches the ['branches'] list via the {data,error} unwrap, gated on !!client only, with finite staleTime and focus-refetch, and its test proves the enabled gate, finite staleTime, and single/empty-list edge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SiteCare API → app (branches.list response) | Untrusted server list crosses into the TanStack Query cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Information Disclosure (stale-trust) | useBranches cache serving a revoked-access branch list | low | mitigate | Finite staleTime (30_000) + refetchOnWindowFocus true means the list is never pinned indefinitely and re-validates on focus (BSTATE-02 / D-09). Full 403 access-revocation recovery is Phase 17. |
| T-13-04 | Denial of Service (self-inflicted) | branches.list() failure | low | mitigate | The {data,error} unwrap throws into TanStack Query's own error state (retry:false in tests); no unhandled rejection, no consumer to crash (no UI reads it this phase). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit — not applicable); no install task exists. |

No high-severity threats — phase does not block on this register.
</threat_model>

<verification>
- Full suite green: `npx vitest run` exits 0 (new file is additive; no shared-file regression expected).
- useBranches enabled gate is !!client only — grep/inspection confirms no branchId/currentBranch clause (standing Pitfall 11 regression).
- staleTime is finite and refetchOnWindowFocus is true.
</verification>

<success_criteria>
- BSTATE-02 satisfied: accessible-branches list loads via client.me.branches.list(), refetches on focus, finite staleTime, never indefinitely cached.
- SC3 met. No !!branchId gate (SC5 single-branch regression preserved).
- `npx vitest run` exits 0.
</success_criteria>

<output>
Create `.planning/phases/13-branch-state-launch-seeding-foundation/13-02-SUMMARY.md` when done.
</output>
