---
phase: 11-reprint-csv-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: false
requirements: [HIST-12]
user_setup: []

must_haves:
  truths:
    - "@tauri-apps/plugin-dialog and @tauri-apps/plugin-fs are registered across all four Tauri layers (package.json, Cargo.toml, lib.rs, capabilities/default.json) so save() and writeTextFile() resolve at runtime (D-13)"
    - "capabilities/default.json grants dialog:allow-save and fs:allow-write-text-file and NO fs:scope entry — the dialog plugin auto-extends fs scope to the user-picked path for the session (Research A1)"
    - "The plugin-dialog install is gated behind a blocking-human legitimacy check because the package was [SUS]-flagged for a same-day publish (T-11-SC)"
    - "The existing Vitest suite stays green after the install (no runtime import of the new plugins yet)"
  artifacts:
    - package.json
    - src-tauri/Cargo.toml
    - src-tauri/src/lib.rs
    - src-tauri/capabilities/default.json
  key_links:
    - "lib.rs .plugin() chain registers tauri_plugin_dialog::init() and tauri_plugin_fs::init() after tauri_plugin_opener::init() (lib.rs:429)"
    - "capabilities/default.json permissions array gains dialog:allow-save + fs:allow-write-text-file only"
---

<objective>
Install and register the two official Tauri v2 plugins that CSV export depends on — `@tauri-apps/plugin-dialog` (native Save dialog) and `@tauri-apps/plugin-fs` (`writeTextFile`) — across the four-file lockstep (npm, Cargo, `lib.rs` registration, capabilities grant), mirroring the existing `store`/`opener`/`process` plugin pattern exactly (D-13).

Purpose: This is the file-IO foundation for HIST-12. It keeps the Rust side thin (official plugins, no bespoke command) and grants the narrowest capability set that makes `save()`→`writeTextFile()` work. No JS consumer is wired here — plan 11-04 does that.
Output: Two new plugins registered and permitted; app still builds; suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reprint-csv-export/11-RESEARCH.md
@.planning/phases/11-reprint-csv-export/11-PATTERNS.md
</context>

## Artifacts this plan produces

- New npm deps in `package.json`: `@tauri-apps/plugin-dialog` (^2.7.2), `@tauri-apps/plugin-fs` (^2.5.1)
- New Cargo crates in `src-tauri/Cargo.toml`: `tauri-plugin-dialog = "2"`, `tauri-plugin-fs = "2"`
- New registration lines in `src-tauri/src/lib.rs`: `.plugin(tauri_plugin_dialog::init())`, `.plugin(tauri_plugin_fs::init())`
- New capability grants in `src-tauri/capabilities/default.json`: `dialog:allow-save`, `fs:allow-write-text-file`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain legitimacy gate for @tauri-apps/plugin-dialog (T-11-SC)</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/11-reprint-csv-export/11-RESEARCH.md (## Package Legitimacy Audit — the [SUS] verdict and why)
  </read_first>
  <what-built>
    `@tauri-apps/plugin-dialog@^2.7.2` was flagged `[SUS]` by the automated supply-chain gate for a same-day (2026-07-18) publish timestamp — NOT a typosquat/slopsquat pattern. It has ~955K weekly downloads and lives in the official `tauri-apps/plugins-workspace` monorepo alongside `plugin-store`/`plugin-updater`/`plugin-window-state` already trusted in this project. This gate is a 10-second confirmation that the package still resolves to the official monorepo before it is installed. `[SUS]` legitimacy checkpoints are never auto-approvable.
  </what-built>
  <action>
    HALT before any install. Run `npm view @tauri-apps/plugin-dialog repository.url` and confirm it resolves to the official `github.com/tauri-apps/plugins-workspace` monorepo (git+https form is fine). Optionally confirm the `@tauri-apps` org scope and a `latest` dist-tag via `npm view @tauri-apps/plugin-dialog maintainers dist-tags`. If the repo URL is anything other than `tauri-apps/plugins-workspace`, STOP and report — do not proceed to Task 2's install. This checkpoint is never auto-approvable regardless of workflow.auto_advance.
  </action>
  <how-to-verify>
    1. Run: `npm view @tauri-apps/plugin-dialog repository.url`
    2. Confirm it resolves to `github.com/tauri-apps/plugins-workspace` (git+https form is fine).
    3. Optionally run `npm view @tauri-apps/plugin-dialog maintainers dist-tags` and confirm the `@tauri-apps` org scope and a `latest` tag.
    4. If the repo URL is anything other than the official `tauri-apps/plugins-workspace`, STOP and report — do not install.
  </how-to-verify>
  <verify>
    <human-check>npm view @tauri-apps/plugin-dialog repository.url resolves to tauri-apps/plugins-workspace and human has typed "approved"</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the mismatch.</resume-signal>
  <done>The plugin-dialog repository.url is confirmed to be the official tauri-apps/plugins-workspace monorepo and the human has approved proceeding to install.</done>
  <acceptance_criteria>
    - `npm view @tauri-apps/plugin-dialog repository.url` output contains `tauri-apps/plugins-workspace`.
    - Human has typed "approved" before Task 2 runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install + register plugin-dialog and plugin-fs across the four-file lockstep</name>
  <files>package.json, package-lock.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/capabilities/default.json (the 8-line permissions array to extend — analog)
    - src-tauri/src/lib.rs (the `.plugin(...)` chain at lines 426-429 — analog)
    - src-tauri/Cargo.toml (existing `tauri-plugin-store`/`tauri-plugin-process` entries at lines 20-33 — analog)
    - package.json (existing `@tauri-apps/plugin-*` entries at lines 17-21 — analog)
    - .planning/phases/11-reprint-csv-export/11-PATTERNS.md (## package.json / Cargo.toml / lib.rs / capabilities — 4-file lockstep)
  </read_first>
  <action>
    Install both plugins and register them in lockstep, mirroring the existing `store`/`opener`/`process` plugins so no layer is left partial:
    1. npm: run `npm install @tauri-apps/plugin-dialog@^2.7.2 @tauri-apps/plugin-fs@^2.5.1` (source `~/.cargo/env` first if `cargo` is not on PATH, per the project's recorded plugin-install precedent). Confirm both land in `package.json` dependencies alongside the sibling `@tauri-apps/plugin-*` entries.
    2. Cargo: add `tauri-plugin-dialog = "2"` and `tauri-plugin-fs = "2"` to the `[dependencies]` block in `src-tauri/Cargo.toml` (NOT the platform-gated `[target...]` block — dialog/fs are cross-platform, same tier as `store`/`opener`/`process`).
    3. lib.rs: append `.plugin(tauri_plugin_dialog::init())` and `.plugin(tauri_plugin_fs::init())` to the builder `.plugin(...)` chain, immediately after `.plugin(tauri_plugin_opener::init())` at lib.rs:429. Do not touch the platform-gated window-state registration below it.
    4. capabilities: add the two permission strings `dialog:allow-save` and `fs:allow-write-text-file` to the `permissions` array in `src-tauri/capabilities/default.json`. Per Research Pitfall 2, do NOT add any `fs:scope` entry — the dialog plugin session-extends fs write scope to the picked path (Research A1). Grant nothing broader than these two.
    Commit `Cargo.lock` and `package-lock.json` so the build is reproducible (matches the project's committed-lockfile decision).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '@tauri-apps/plugin-dialog' package.json && grep -q '@tauri-apps/plugin-fs' package.json` succeeds.
    - `grep -q 'tauri-plugin-dialog' src-tauri/Cargo.toml && grep -q 'tauri-plugin-fs' src-tauri/Cargo.toml` succeeds.
    - `src-tauri/src/lib.rs` contains `tauri_plugin_dialog::init()` and `tauri_plugin_fs::init()` in the `.plugin(...)` chain.
    - `src-tauri/capabilities/default.json` `permissions` array contains `dialog:allow-save` and `fs:allow-write-text-file` and contains NO `fs:scope` entry.
    - `npx vitest run` passes (full suite green — no runtime import of the new plugins added yet, so this proves no regression).
  </acceptance_criteria>
  <done>Both plugins installed, registered in lib.rs, and permitted in capabilities/default.json with the narrow grant; lockfiles updated; full Vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/crates registry → build | Third-party install-time code enters the supply chain |
| JS → OS filesystem (via dialog+fs plugins) | User-picked path crosses into a native write capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SC | Tampering | `npm install @tauri-apps/plugin-dialog` (same-day-publish [SUS]) | high | mitigate | Task 1 blocking-human checkpoint verifies `repository.url` resolves to the official `tauri-apps/plugins-workspace` monorepo before install; never auto-approvable |
| T-11-CAP | Elevation of Privilege | over-broad `fs:scope` capability grant | high | mitigate | Grant only `dialog:allow-save` + `fs:allow-write-text-file`; rely on the dialog plugin's session-scoped path auto-extension (Research A1) instead of a persistent broad `fs:scope` rule |
</threat_model>

<verification>
- Full Vitest suite green (`npx vitest run`).
- `capabilities/default.json` grants exactly the two narrow permissions and no `fs:scope`.
- Four-file lockstep complete (npm + Cargo + lib.rs + capabilities).
</verification>

<success_criteria>
Both official Tauri v2 plugins are installed and registered end-to-end with the narrowest capability grant; the app builds and the existing test suite is green; the [SUS] plugin passed a blocking-human legitimacy check before install.
</success_criteria>

<output>
Create `.planning/phases/11-reprint-csv-export/11-01-SUMMARY.md` when done.
</output>
