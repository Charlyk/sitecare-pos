---
phase: 16
slug: branch-switcher-ui-switch-flow-language-relocation
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-23
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — this project has a pre-existing, hand-built design system (`src/colors_and_type.css` + `src/styles.css`), established across 15 completed phases. `components.json` was checked and does not exist; **shadcn is deliberately not initialized** — CLAUDE.md is explicit that colors/spacing/typography/layout must not diverge from the existing system, and introducing a component library mid-milestone would fork visual language from every other screen. |
| Preset | not applicable |
| Component library | none — plain React function components, inline `style={}` objects for one-off layout, shared utility classes (`.btn-primary`, `.btn-secondary`, `.toast`, `.card`, `.chip-*`) in `src/styles.css` for repeated patterns |
| Icon library | custom inline SVG path icons, `src/icons.jsx` (hand-maintained `ICONS` map + `<Icon name size .../>`) — no external icon package |
| Font | `Outfit` (sans, weights 300–900, self-hosted `.ttf` for 700/900 + Google Fonts CDN for the rest) for all UI text; `Caveat` (handwriting cursive) reserved for the terracotta "eyebrow" accent only — both declared in `src/colors_and_type.css` |

**Existing tokens this phase must reuse (source of truth, do not redeclare or approximate):**

| Token | Value | This phase's use |
|-------|-------|-------------------|
| `--sc-primary` | `hsl(120 14% 49%)` sage green | selected-branch check, popover active row, primary button |
| `--sc-terracotta` | `hsl(0 53% 58%)` | avatar bg precedent only — NOT used by the branch switcher (branch identity uses primary, not terracotta, to stay visually distinct from the user chip below it) |
| `--sc-muted-foreground` | `hsl(120 5% 46%)` | secondary/meta text (branch sub-labels, "default" badge idle state) |
| `--sc-border` | `hsl(120 10% 88%)` | all card/popover/button borders |
| `--sc-background` | `hsl(40 60% 97%)` warm cream | overlay backdrop is a *dark* scrim (see Color below), not this token |
| sidebar bg | `linear-gradient(180deg, #fbf6ea 0%, #f7f1e1 100%)` (`.sidebar`) | footer surface the switcher sits on |

---

## Spacing Scale

This codebase's real convention is a **finer, non-strict-8pt scale** (verified against `shell.jsx`, `cancel-dialog.jsx`, `styles.css` — gaps of 4/6/8/10/12/14/16/18/20/24 all appear in already-shipped, human-verified UI). Declaring a purist 8pt-only scale would contradict 15 phases of shipped precedent and get flagged by the checker against the actual codebase. This phase follows the **real** convention:

| Token | Value | Usage this phase |
|-------|-------|-------------------|
| xs | 4px | icon-to-text gap inside the branch trigger pill |
| sm | 6px | popover offset from trigger (`bottom: calc(100% + 6px)`) — exact user-chip-menu precedent, reused verbatim |
| sm+ | 8px | popover internal item gap; footer button internal padding |
| md | 10–12px | popover row padding (`10px 12px`, matches `.user-chip` and the existing popover button in `shell.jsx:158`) |
| md+ | 14–16px | overlay message block padding; confirm-dialog body padding (matches `cancel-dialog.jsx` `16px 24px`) |
| lg | 24px | confirm-dialog header/footer padding (exact `cancel-dialog.jsx` precedent) |
| xl | 32px+ | overlay centered content max-width breathing room |

Exceptions: 6px, 10px, 12px, and 14px are retained (not rounded to the nearest 4px multiple) under the **LOCKED — Design-fidelity exception to the generic UI grid/weight rules** decision recorded in `PROJECT.md` Key Decisions (locked 2026-07-23). That decision formally waives the UI-SPEC checker's Dimension 5 (4px-multiple grid) project-wide for values matching the shipped design system (`colors_and_type.css` + `styles.css`, 15 phases live); this table's tokens are drawn verbatim from that shipped system (`shell.jsx`, `cancel-dialog.jsx`), so retaining them is compliant by the locked decision, not a violation of it.

---

## Typography

All sizes are on the existing `Outfit` scale already used across the sidebar/dialogs (no new font sizes invented for this phase beyond what's already load-bearing elsewhere).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13px | 600 | 1.4 — branch name in the trigger pill and each popover row (exact match to `.user-chip` name: `fontSize:13, fontWeight:600`) |
| Label | 11px | 700, uppercase, `letter-spacing: 0.08em` | 1.3 — "default" badge text and any popover section label (exact match to `cancel-dialog.jsx`'s reason-picker label convention) |
| Heading | 20px | 800 | 1.25 — the overlay's "Switching to `<branch>`…" message (deliberately one step below `cancel-dialog.jsx`'s 22px/900 dialog title — this is a transient status line, not a decision-requiring dialog, so it reads slightly lighter) |
| Meta/sub | 12px | 500 | 1.4 — overlay secondary line (e.g. reconnecting status), toast detail text (matches existing `.toast` detail: `fontSize:13` — use 12–13px, never below) |

Weight budget for this phase: **600 (body/meta) + 700 (label) + 800 (heading)** — three weights, exceeding the UI-SPEC checker's generic 2-weight cap (Dimension 4). This is authorized, not a gap: the **LOCKED — Design-fidelity exception to the generic UI grid/weight rules** decision recorded in `PROJECT.md` Key Decisions (locked 2026-07-23) formally waives Dimension 4 project-wide for weights matching the shipped design system. All three weights (600/700/800) are already in active, human-verified shipped use across 15 completed phases (e.g. `.user-chip` name at 600, `cancel-dialog.jsx`'s reason-picker label at 700, dialog titles at 800/900) — this phase extends those exact tokens rather than introducing new ones.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | sidebar footer surface — `linear-gradient(180deg, #fbf6ea 0%, #f7f1e1 100%)` (existing `.sidebar`) | the switcher trigger and collapsed chip sit directly on this, unstyled background (no new box around the pill beyond what's specified below) |
| Secondary (30%) | `#ffffff` (`--sc-card`) with `1px solid var(--sc-border)` | popover surface, overlay message card, cart-discard confirm dialog — exact match to the existing user-chip-menu popover and `cancel-dialog.jsx` card treatment |
| Accent (10%) | `var(--sc-primary)` `hsl(120 14% 49%)` | reserved **only** for: (a) the checkmark on the currently-selected branch row in the popover, (b) the "default" badge's border/text when idle (mirrors `.chip-sage`), (c) the overlay's loading spinner (reuses the existing `.spin` keyframe), (d) the popover trigger's chevron-open state icon color |
| Destructive | `var(--sc-destructive)` `hsl(0 84.2% 60.2%)` | the "discard order" verb inside the cart-non-empty confirm dialog (D-13) — the confirm button only; **not** the generic failure toast (D-11 failure toast is neutral/error-toned like existing `kind: 'error'` toasts, not framed as destructive) |

Accent reserved for: selected-branch checkmark, "default" badge idle styling, overlay spinner, popover-open chevron color — nothing else in this phase's new UI uses `--sc-primary` as a fill (trigger pill background stays neutral/white-on-cream, matching the existing Collapse button's `background:#fff` convention, not a primary-tinted button).

**Primary visual focal point:** the current-branch name inside the sidebar-footer trigger pill is the element the eye lands on first — it sits at 13px/600 in the position of highest habitual attention in the existing footer stack (directly above the user chip, the sidebar's other identity element), so it must never be visually subordinated to the chevron, the "default" badge, or any icon in the same row.

---

## Copywriting Contract

Bilingual (`ro` canonical / `en` mirror) per `src/i18n.jsx` convention — every row below needs both locales added as new `I18N` keys.

| Element | Copy (ro) | Copy (en) |
|---------|-----------|-----------|
| Branch trigger (single-branch, read-only, D-04) | `<branch name>` (no chevron, no hover affordance) | same |
| Branch trigger (multi-branch) | `<branch name>` + chevron (down when closed, up when open — reuse `chevDown`/`chevUp` exactly as the user-chip menu does) | same |
| "Default" badge | `Implicit` (small pill/label next to the default branch's name, both in trigger and in its popover row) | `Default` |
| Popover section label (optional, only if >1 branch) | `Filiale disponibile` | `Available branches` |
| Overlay heading (in-flight) | `Se comută la <branch>…` | `Switching to <branch>…` |
| Overlay sub-line (while SSE still reconnecting after `switch()` resolved, before D-08 release) | `Se reconectează…` | `Reconnecting…` |
| Success toast title (SWCH-04, fires on overlay release — D-10) | `Filială schimbată` | `Branch switched` |
| Success toast detail | `Ești acum pe <branch>` | `You're now on <branch>` |
| Failure toast title (D-11, generic — no error-code branching) | `Nu s-a putut schimba filiala` | `Couldn't switch branch` |
| Failure toast detail | `Încearcă din nou` | `Try again` |
| Cart-discard confirm dialog title (D-13) | `Comanda curentă va fi anulată` | `Current order will be discarded` |
| Cart-discard confirm dialog body | `Schimbarea filialei șterge comanda POS în lucru. Continui?` | `Switching branches discards the in-progress POS order. Continue?` |
| Cart-discard confirm — primary (destructive) button | `Schimbă și renunță` | `Switch and discard` |
| Cart-discard confirm — secondary button | `Rămân aici` (mirrors `cancel-dialog.jsx`'s cancel-button convention of staying put) | `Stay here` |
| Primary CTA (this phase has no single dominant CTA — the switcher is a passive selector, not a call-to-action button) | n/a — the interaction is "select a branch row," not a labeled CTA button | n/a |

Destructive actions in this phase: **one** — discarding an in-progress POS cart on branch switch (D-13). Confirmation approach: modal confirm dialog (styled per `cancel-dialog.jsx` precedent — header/body/footer card, destructive-colored primary action), gated only when `screen === 'pos'` AND the cart is non-empty; otherwise the switch proceeds immediately with no confirm step.

---

## UI Considerations

Applicable state considerations resolved: 9 covered, 3 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | branch popover list | ✅ covered | `useBranches()` is only ever rendered behind `branches.length > 1` (D-04) — an empty/single-item list renders the read-only trigger instead, so the popover itself never opens on an empty collection; no separate empty-state copy needed. |
| loading | branch popover list (`useBranches()` still fetching) | 🧪 backstop | Popover shows a lightweight inline spinner row (reuse `.spin` class) in place of the branch list until the query resolves; no dedicated skeleton — this is a fast, already-warm cache per Phase 13 (`useBranches()` staleTime), so a full skeleton is unwarranted. |
| error | branch popover list (`useBranches()` errored) | 🧪 backstop | Popover shows a compact inline row: `Nu s-au putut încărca filialele` / `Couldn't load branches` with no retry button in this phase (retry/refetch-on-error is Phase 17's BERR-01 concern) — the popover simply stays showing the last-known trigger label. |
| loading | switch in flight (SWCH-03) | ✅ covered | Global overlay per D-07/D-08/D-09 — spinner + `Se comută la <branch>…` heading, held until SSE reconnects or the bounded timeout fires. |
| error | switch rejected (BERR boundary, D-11/D-12) | ✅ covered | Generic failure toast + selector reverts to old branch + overlay releases immediately — no code-aware branching (that's Phase 17). |
| populated | multi-branch popover, one row selected | ✅ covered | Selected row shows the `--sc-primary` checkmark (D-01); "default" badge shown independently of selection state on whichever row `isDefault: true`. |
| zero-one-many | branch count (0 / 1 / many) | ✅ covered | 0 is not a reachable app state (every authenticated user has `getMe().selectedBranch` seeded in Phase 13, and `NO_BRANCH_ACCESS` full-screen block is Phase 17's BERR-03); 1 → read-only trigger (D-04, SWCH-02); many → dropdown popover (D-01). |
| overflow | long branch name in trigger/popover row | 🧪 backstop | Trigger pill and popover rows use `text-overflow: ellipsis; white-space: nowrap; overflow: hidden` with a `title` attribute carrying the full name (mirrors the existing `nav-item`'s `title={sidebarCollapsed ? item.label : ''}` truncation-safety precedent) — not manually verified against a real long branch name from the API, flagged for verification. |
| collapsed-sidebar | branch identity when sidebar is collapsed | ✅ covered | D-03 — compact chip (branch-name initial, single uppercase letter in a circular tile matching `.avatar`'s 32×32 circle treatment) stays visible and clickable; never hidden, unlike the removed RO/EN toggle's prior collapsed behavior. **Accessible name required:** the chip carries `aria-label`/`title` naming the current branch in full (e.g. `title={currentBranch?.name}`) — it is icon/initial-only with no visible text, so the accessible name is the sole way to identify the branch without expanding the sidebar; mirrors the ellipsis-truncation `title` fallback already specified for the expanded trigger/popover rows (overflow row above). |
| long-text | overlay heading with a long branch name | ✅ covered | Overlay heading wraps (no truncation) since it's a centered, unconstrained-width message card — unlike the trigger/popover row, there's no adjacent layout to protect. |
| partial | switch succeeds server-side but SSE reconnect times out (D-09) | ✅ covered | Overlay releases anyway at the bounded timeout, success toast still fires (the switch *did* succeed), and the normal `OfflineBanner` takes over — this is the documented "honest offline indicator" outcome, not an error state. |
| confirm-gate | cart-discard confirm dialog (D-13) shown vs. skipped | ✅ covered | Gated strictly on cart non-emptiness read at click time; empty cart (the common case) skips the dialog entirely and switches immediately. |
| destructive-confirm | cart-discard dialog copy/action labeling | ✅ covered | See Copywriting Contract — destructive-colored primary button, neutral secondary "stay" button, mirrors `cancel-dialog.jsx` visual precedent exactly. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized this phase | not applicable |
| third-party | none | not applicable |

No component registry is in use. All new UI (branch trigger, collapsed chip, popover, global switching overlay, cart-discard confirm dialog) is hand-built React + inline styles / `src/styles.css`, following the exact structural precedent of `shell.jsx`'s user-chip popover and `cancel-dialog.jsx`'s modal card — both already shipped, human-verified components in this codebase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
