---
phase: 12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/normalize-order.test.js
autonomous: true
requirements: [D-05, D-06]
must_haves:
  truths:
    - "normalizeOrder with o.total OMITTED and o.tax present returns total = subtotal + tax + deliveryFee + tip − discount (tax is NOT dropped)"
    - "normalizeOrder percent-discount fallback branch (discountType: 'percent') yields a cRON-scaled RON discount, NOT a 100× inflated value (10% of 96 RON = 9.60, not 960)"
    - "A combined case (fallback total path with tax present AND percent discount present) is internally consistent: the asserted total equals the asserted subtotal/tax/deliveryFee/tip minus the asserted discount"
    - "All assertions are on RON outputs (post-cRON), never on raw cents"
    - "The full vitest suite red count does not increase — only the 3 pre-existing v1.0 failures remain red"
  artifacts:
    - "src/__tests__/normalize-order.test.js — new describe block covering the CR-01/CR-02 fallback-total + percent-discount paths"
  key_links:
    - "normalize-order.test.js → src/data.jsx normalizeOrder (already-fixed fallback total + discount math) — test-only, no source edit"
---

<objective>
Backfill the missing regression tests (D-06) for `normalizeOrder`'s fallback-total and
percent-discount paths — the exact financial paths the milestone audit flagged as CRITICAL and
"completely untested." The underlying code is ALREADY FIXED (commits `7d9810b` tax, `30c89d8`
percent-discount, per D-05); this plan adds coverage ONLY. No `src/data.jsx` edit.

Purpose: lock the CR-01/CR-02 financial fix behind automated regression tests so a future edit to
the fallback path cannot silently reintroduce tax-omission or a 100×-inflated percent discount.
Output: a new describe block in `src/__tests__/normalize-order.test.js`.

Scope note (D-05): this is verify-and-backfill, not re-implement. Do NOT touch `src/data.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-CONTEXT.md
@.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan EDITS one existing file — it creates no new source files.

Net-new symbols:
- One new `describe('normalizeOrder — fallback total + discount (D-06 / CR-01 / CR-02)', ...)` block
  (exact name at executor discretion) in `src/__tests__/normalize-order.test.js`, with ~3 `test()`
  cases: (1) fallback-total-includes-tax, (2) percent-discount-not-100×-inflated,
  (3) combined-and-internally-consistent.

No production symbols are added. `src/data.jsx` is read-only for this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Backfill normalizeOrder fallback-total + percent-discount regression tests (D-06)</name>
  <files>src/__tests__/normalize-order.test.js</files>
  <read_first>
    - src/data.jsx §200-221 — the shipped (fixed) fallback total + discount math; confirm the exact
      formula before writing assertions: discount uses `cRON(o.subtotal) * rawDiscountAmt / 10000`
      for percent, `cRON(rawDiscountAmt)` otherwise; total falls back to
      `+(subtotal + tax + deliveryFee + tip - discount).toFixed(2)` when `o.total` is absent.
    - src/data.jsx — the `cRON` helper (single cents→RON converter, `v / 100`); every monetary field
      passes through it, so all assertions are on RON values.
    - src/__tests__/normalize-order.test.js §1-37 — existing test-module conventions (imports
      `normalizeOrder` from `../data.jsx`; plain `describe`/`test`/`expect`; no mocks) to mirror.
    - .planning/phases/10-filters-search/10-REVIEW.md §45,57,67 — CR-01 (percent-discount 100×) and
      CR-02 (tax-omission) origin definitions and the worked "10% of 96 RON = 9.60" example.
  </read_first>
  <action>
    Add ONE new `describe` block to `src/__tests__/normalize-order.test.js` (append after the
    existing blocks; do not modify existing tests). Cover exactly the D-06 fallback paths, asserting
    on RON (post-`cRON`) outputs:

    1. Fallback total INCLUDES tax: construct an order with `o.total` OMITTED (undefined), and
       `subtotal`, `tax`, `deliveryFee` present in the SDK's cents encoding, no discount. Assert
       `normalizeOrder(o).total` equals the RON sum `subtotal_RON + tax_RON + deliveryFee_RON`
       (i.e. tax is NOT dropped). Pick cents values whose RON sum is exact to 2 decimals.

    2. Percent discount is cRON-scaled, NOT 100× inflated: construct an order with `o.total` omitted,
       `discountType: 'percent'`, `discountAmount` in the SDK's percent encoding, and a `subtotal`
       such that the worked example holds — a 10% discount on a 96 RON (9600 cents) subtotal must
       yield a `discount` of `9.60`, and assert the resulting `total` reflects `9.60` subtracted, NOT
       `960`. Assert the discount value directly (via a field the normalized order exposes, or by
       reconstructing it from the asserted total/subtotal/tax) so the 100× regression is caught head-on.

    3. Combined + internally consistent: one order in the fallback path with BOTH tax present AND a
       percent discount present. Assert each component (subtotal, tax, deliveryFee, tip, discount) in
       RON individually, then assert `result.total` equals `subtotal + tax + deliveryFee + tip −
       discount` computed from those same asserted RON values — closing the exact CR-01/CR-02 gap.

    Use the SDK's actual field names as consumed by `normalizeOrder` (`subtotal`, `tax`,
    `deliveryFee`, `tip`, `discountType`, `discountAmount`, `total`) — read them from `src/data.jsx`,
    do not guess. Do NOT edit `src/data.jsx`. Do NOT weaken any existing test.

    Reference the decision IDs in a leading comment: this backfills D-06 (CR-01 percent-discount /
    CR-02 tax-omission, fixed by `30c89d8` / `7d9810b`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/normalize-order.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/normalize-order.test.js` passes with the new cases green.
    - The new describe block asserts (a) fallback total includes tax, (b) percent discount is not
      100× inflated (9.60 not 960 on the worked example), (c) a combined internally-consistent case.
    - `git diff -- src/data.jsx` is EMPTY (source untouched — verify-only per D-05).
    - Full suite red count unchanged: `npx vitest run` shows the same 3 pre-existing v1.0 failures
      (build-pipeline BILD-04, offline-buttons ×2) and no new reds.
  </acceptance_criteria>
  <done>
    normalize-order.test.js contains D-06 regression coverage for the fallback-total (tax-included)
    and percent-discount (cRON-scaled) paths; all new cases pass; src/data.jsx is untouched; full
    suite adds no new red.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test-only code. No new external input, no API/SDK call, no data-layer or Tauri-capability change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Tampering | `normalizeOrder` fallback financial math (`src/data.jsx`) | medium | mitigate | These regression tests are the mitigation — they guard the financial-display path (fallback total / percent discount) against silent monetary miscalculation. This is a correctness guardrail, not an external-attacker threat. |

Attack surface for this plan is minimal: no new external inputs, no new API/SDK calls, no new Tauri
capability grants, no new CSP entries. No package installs.
</threat_model>

<verification>
- `npx vitest run src/__tests__/normalize-order.test.js` green.
- `npx vitest run` full suite: only the 3 documented pre-existing v1.0 failures remain red.
- `git diff -- src/data.jsx` empty.
</verification>

<success_criteria>
D-06 regression tests exist and pass; the CR-01/CR-02 financial fix is now covered; no source code
changed; no new suite failures.
</success_criteria>

<output>
Create `.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-01-SUMMARY.md` when done
</output>
