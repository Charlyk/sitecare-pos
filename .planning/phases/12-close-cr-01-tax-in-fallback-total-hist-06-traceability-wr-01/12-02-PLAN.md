---
phase: 12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-history-screen-foundation/07-VERIFICATION.md
  - .planning/phases/07-history-screen-foundation/07-04-SUMMARY.md
autonomous: true
requirements: [D-09, HIST-06]
must_haves:
  truths:
    - "07-VERIFICATION.md Requirements Coverage table includes a HIST-06 row (satisfied by 07-04's client-computed summary strip)"
    - "07-VERIFICATION.md's closing 'No orphaned requirements' line no longer claims HIST-06 is 'scoped to later phases (8-10)' — the mis-statement is corrected to reflect HIST-06 belonging to Phase 7"
    - "07-04-SUMMARY.md requirements-completed frontmatter contains HIST-06 alongside HIST-05 and HIST-13"
    - "REQUIREMENTS.md is NOT modified (already correct: HIST-06 → Phase 7 / 07-04, Complete)"
    - "No source code or test files are touched — documentation-only plan"
  artifacts:
    - ".planning/phases/07-history-screen-foundation/07-VERIFICATION.md — HIST-06 row + corrected closing note"
    - ".planning/phases/07-history-screen-foundation/07-04-SUMMARY.md — HIST-06 added to requirements-completed"
  key_links:
    - "07-VERIFICATION.md HIST-06 row ↔ 07-04-SUMMARY requirements-completed ↔ REQUIREMENTS.md HIST-06 traceability — the three verification bookkeeping sources now agree"
---

<objective>
Close the HIST-06 traceability gap (D-09): the client-computed summary strip is functionally
delivered and verified, but the `HIST-06` tag is orphaned from Phase 7's verification bookkeeping.
`REQUIREMENTS.md` is already correct — the gap is in `07-VERIFICATION.md` (omits HIST-06 from its
requirements table; its closing line wrongly states HIST-06 is "scoped to later phases (8-10)") and
in `07-04-SUMMARY.md` (`requirements-completed` omits HIST-06).

Purpose: make the three verification sources (REQUIREMENTS traceability, VERIFICATION table, SUMMARY
frontmatter) agree so the milestone audit re-derivation (12-04) can drop the HIST-06 gap cleanly.
Output: two doc edits. Documentation-only — no code, no tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
This plan EDITS two existing documentation files — it creates no new files and no code symbols.

Changes:
- `07-VERIFICATION.md`: one new table row (HIST-06) + a one-line correction to the closing
  "No orphaned requirements" statement.
- `07-04-SUMMARY.md`: `HIST-06` added to the `requirements-completed` frontmatter list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add HIST-06 to 07-VERIFICATION.md table and correct the mis-scoped closing note (D-09)</name>
  <files>.planning/phases/07-history-screen-foundation/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-history-screen-foundation/07-VERIFICATION.md §91-101 — the Requirements
      Coverage table (rows HIST-01/02/03/05/13) and the closing line 101 that reads "HIST-04 and
      HIST-06 through HIST-12 are correctly scoped to later phases (8-10)".
    - .planning/REQUIREMENTS.md §HIST-06 (lines ~36 and ~104) — the AUTHORITATIVE, already-correct
      traceability: HIST-06 → Phase 7 / 07-04, Complete, "client-computed summary strip (orders,
      revenue, average order value, refunds count)". Use its wording; do NOT edit REQUIREMENTS.md.
    - .planning/v1.1-MILESTONE-AUDIT.md §72-79 — the audit's HIST-06 narrative (summary strip
      delivered + verified across Phase 7 4-tile strip / computeSummary; Phase 9 SC3 retarget;
      Phase 10 D-15 Avg fix) — source for the "satisfied by 07-04" evidence phrasing.
  </read_first>
  <action>
    Two edits to `07-VERIFICATION.md`, both scoped (use Edit, not Write):

    1. Add a `HIST-06` row to the Requirements Coverage table (§93-99), matching the existing column
       shape `| Requirement | Source Plan | Description | Status | Evidence |`. Set: Requirement =
       HIST-06; Source Plan = 07-04; Description = the client-computed summary strip (orders, revenue,
       average order value, refunds count) per REQUIREMENTS.md; Status = ✓ SATISFIED; Evidence =
       `computeSummary(finished)` in 07-04's data-flow trace, 4-tile summary strip present and
       behaviorally verified (reference the Phase 7 verification evidence, not new behavior).

    2. Correct the closing line (§101). It currently reads that "HIST-04 and HIST-06 through HIST-12
       are correctly scoped to later phases (8-10)". Rewrite it so HIST-06 is NO LONGER listed among
       the later-phase requirements — HIST-06 belongs to Phase 7 (satisfied by 07-04). Keep HIST-04
       and HIST-07…HIST-12 as later-phase scope (those are genuinely later). Ensure the sentence's
       claimed Phase 7 requirement set now reads HIST-01, 02, 03, 05, 06, 13.

    Cite D-09 in an inline note or the edited prose so the traceability fix is auditable. Do NOT edit
    REQUIREMENTS.md (already correct). Do NOT touch any source or test file.
  </action>
  <verify>
    <automated>grep -c 'HIST-06' .planning/phases/07-history-screen-foundation/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `07-VERIFICATION.md` now contains a HIST-06 row in the Requirements Coverage table with Status ✓ SATISFIED and Source Plan 07-04.
    - The closing line no longer states HIST-06 is scoped to later phases; it lists HIST-06 among Phase 7's satisfied requirements.
    - `grep -n 'scoped to later phases' 07-VERIFICATION.md` no longer returns a line that includes HIST-06 in the later-phase set.
    - `git diff -- .planning/REQUIREMENTS.md` is EMPTY (authoritative file untouched).
    - No file under `src/` is modified.
  </acceptance_criteria>
  <done>
    07-VERIFICATION.md has a HIST-06 SATISFIED row and its closing note correctly scopes HIST-06 to
    Phase 7; REQUIREMENTS.md unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add HIST-06 to 07-04-SUMMARY.md requirements-completed frontmatter (D-09)</name>
  <files>.planning/phases/07-history-screen-foundation/07-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-history-screen-foundation/07-04-SUMMARY.md §39 — the current
      `requirements-completed: [HIST-05, HIST-13]` line (HIST-06 confirmed absent).
    - .planning/phases/07-history-screen-foundation/07-04-SUMMARY.md §10-13 — the `provides` block
      confirming 07-04 delivered the "Client-computed summary strip (D-15)" — the concrete evidence
      that HIST-06 belongs on this plan's completed list.
  </read_first>
  <action>
    Edit the `requirements-completed` frontmatter field in `07-04-SUMMARY.md` (§39) from
    `[HIST-05, HIST-13]` to include HIST-06 — result `[HIST-05, HIST-06, HIST-13]` (ordering at
    executor discretion; keep it a valid YAML flow list). Scoped Edit only. Reference D-09 in the
    plan's SUMMARY rationale, not inside the frontmatter value. Do NOT change any other frontmatter
    field, body content, or coverage entries in this file. Do NOT touch source or test files.
  </action>
  <verify>
    <automated>grep -q 'HIST-06' .planning/phases/07-history-screen-foundation/07-04-SUMMARY.md && grep -q 'requirements-completed:.*HIST-06\|HIST-06' .planning/phases/07-history-screen-foundation/07-04-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `07-04-SUMMARY.md` `requirements-completed` frontmatter includes HIST-06 (alongside HIST-05 and HIST-13).
    - The YAML frontmatter still parses (flow-list syntax intact).
    - No other field in the file changed; no source/test file touched.
  </acceptance_criteria>
  <done>
    07-04-SUMMARY.md declares HIST-06 as a completed requirement; the three verification bookkeeping
    sources (REQUIREMENTS, VERIFICATION, SUMMARY) now agree on HIST-06 → Phase 7 / 07-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No code, no runtime, no external input, no API/SDK/Tauri/CSP change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-01 | Repudiation | Requirement-traceability bookkeeping (VERIFICATION/SUMMARY) | low | accept | Correcting an orphaned requirement tag IMPROVES auditability. No attacker-facing surface; the only "risk" is a doc edit, verified by grep. Accepted with no further control. |

No new external inputs, API/SDK calls, Tauri capability grants, or CSP entries. No package installs.
</threat_model>

<verification>
- `grep -c 'HIST-06' 07-VERIFICATION.md` ≥ 1 (table row present).
- `grep 'HIST-06' 07-04-SUMMARY.md` matches inside `requirements-completed`.
- `git diff -- .planning/REQUIREMENTS.md` empty.
- `git status` shows only the two Phase 7 doc files changed by this plan.
</verification>

<success_criteria>
HIST-06 traceability gap closed across Phase 7's verification bookkeeping; REQUIREMENTS.md unchanged;
audit re-derivation (12-04) can now drop the HIST-06 gap.
</success_criteria>

<output>
Create `.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-02-SUMMARY.md` when done
</output>
