---
phase: 04-core-screens
plan: 08
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - src/screen-settings.jsx
  - src/i18n.jsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03

must_haves:
  truths:
    - "A 'Display' tab is visible in the SettingsScreen tab bar (after existing tabs)"
    - "Clicking the Display tab shows the display pane: lang toggle, density toggle, accent swatch picker"
    - "Clicking RO button calls setLang('ro'); clicking EN button calls setLang('en')"
    - "Clicking Balanced calls setDensity('balanced'); clicking Dense/Compact calls setDensity('dense')"
    - "Clicking a color swatch calls setAccent with the swatch ID: 'sage', 'indigo', 'terracotta', or 'charcoal'"
    - "The active lang/density/accent button is visually distinct (primary background) from inactive buttons"
    - "SettingsScreen imports useAppStore directly — no prop-drilling from app.jsx"
    - "lang, density, accent values are read from Zustand — changes persist via existing partialize (no new code needed for persistence)"
  artifacts:
    - path: "src/screen-settings.jsx"
      provides: "Display tab + lang toggle + density toggle + accent swatch picker all wired to useAppStore"
      contains: "display"
    - path: "src/i18n.jsx"
      provides: "Bilingual strings for Display tab: display_tab, display_lang_label, display_density_label, display_accent_label"
      contains: "display_tab"
  key_links:
    - from: "src/screen-settings.jsx"
      to: "src/store.js"
      via: "useAppStore reads lang/density/accent and calls setLang/setDensity/setAccent directly"
      pattern: "useAppStore"
    - from: "src/store.js"
      to: "@tauri-apps/plugin-store"
      via: "partialize already includes lang, density, accent — persistence automatic, no new code needed"
      pattern: "partialize"
---

<objective>
Add a Display tab to SettingsScreen (SET-01, SET-02, SET-03) with language toggle, density toggle, and accent color swatch picker. All controls wire directly to Zustand setters — persistence is handled automatically by the existing partialize configuration.

Purpose: Staff can customize the app's language, density, and accent color — preferences persist across restarts via the existing Zustand plugin-store persistence layer.

Output: screen-settings.jsx with Display tab added to tab bar and display pane with three control sections. i18n.jsx with Display tab bilingual strings.
</objective>

<execution_context>
@/Users/eduardalbu/Developer/sitecare-pos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardalbu/Developer/sitecare-pos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eduardalbu/Developer/sitecare-pos/.planning/PROJECT.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/ROADMAP.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/STATE.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/phases/04-core-screens/04-PATTERNS.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/phases/04-core-screens/04-RESEARCH.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/phases/04-core-screens/04-UI-SPEC.md

<interfaces>
<!-- Zustand store setters — VERIFIED from store.js -->
```javascript
// These setters already exist and persist via partialize:
setLang: (v) => set({ lang: v }),          // lang is in partialize
setDensity: (v) => set({ density: v }),    // density is in partialize
setAccent: (v) => set({ accent: v }),      // accent is in partialize
// Access via:
const lang = useAppStore((s) => s.lang);
const setLang = useAppStore((s) => s.setLang);
const density = useAppStore((s) => s.density);
const setDensity = useAppStore((s) => s.setDensity);
const accent = useAppStore((s) => s.accent);
const setAccent = useAppStore((s) => s.setAccent);
```

<!-- Filter toggle bar pattern (from 04-PATTERNS.md line 771-782) -->
```javascript
<div style={{ display: 'inline-flex', background: '#fff', border: '1px solid hsl(120 10% 90%)', borderRadius: 10, padding: 3 }}>
  {items.map(f => (
    <button key={f.id} onClick={() => setActive(f.id)}
      style={{ border: 0, background: active === f.id ? 'var(--sc-primary)' : 'transparent', color: active === f.id ? '#fff' : '#555', padding: '7px 20px', borderRadius: 8, fontWeight: 600, fontSize: 13, cursor: 'pointer', fontFamily: 'inherit' }}>
      {f.label}
    </button>
  ))}
</div>
```

<!-- Accent color definitions (from 04-PATTERNS.md and UI-SPEC) -->
```javascript
const accentSwatches = [
  { id: 'sage',       color: 'hsl(120 14% 49%)', labelRo: 'Sage (verde)',      labelEn: 'Sage (green)' },
  { id: 'indigo',     color: 'hsl(230 50% 55%)', labelRo: 'Indigo (albastru)', labelEn: 'Indigo (blue)' },
  { id: 'terracotta', color: 'hsl(0 53% 52%)',   labelRo: 'Teracotă (roșu)',   labelEn: 'Terracotta (red)' },
  { id: 'charcoal',   color: 'hsl(120 8% 25%)',  labelRo: 'Cărbune (gri)',     labelEn: 'Charcoal (grey)' },
];
```

<!-- Tab structure pattern — read screen-settings.jsx to see existing tab definition shape -->
// Existing tabs follow a pattern like: { id: 'users', label: 'Users', icon: 'users' }
// Add: { id: 'display', label: t('display_tab'), icon: 'grid' }
// sliders icon does NOT exist — use 'grid' as icon (verified in 04-PATTERNS.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Display tab to SettingsScreen with lang/density/accent controls; add Display i18n strings</name>
  <files>
    src/screen-settings.jsx
    src/i18n.jsx
  </files>

  <read_first>
    - src/screen-settings.jsx — read full file. Locate: (1) the TABS array or equivalent tab definition structure (exact object shape: {id, label, icon}); (2) the tab pane content block structure (how `{tab === 'users' && ...}`, `{tab === 'tax' && ...}` etc. are structured); (3) existing useAppStore imports — screen-settings may already import useAppStore for lang, or it may receive lang as a prop; determine which; (4) existing imports section — what's imported; (5) where the tab bar renders (what className or JSX structure); (6) the full screen layout — identify where the Display tab pane content should go (last pane).
    - src/store.js — re-read the partialize function to confirm lang, density, accent ARE in the partialize list (this verifies persistence works without any new code).
    - src/i18n.jsx — read last 20 lines of ro and en sections.
    - src/icons.jsx — confirm 'grid' icon exists in ICON_PATHS (fallback if 'sliders' is not found).
  </read_first>

  <behavior>
    - Display tab is appended LAST in the tab bar (after all existing tabs)
    - Tab uses 'grid' icon (sliders does not exist)
    - Tab label uses t('display_tab') — bilingual
    - Display pane renders three card sections separated by gap: 16px
    - Lang section: pill toggle with 'ro' and 'en' buttons; active button has primary background
    - Density section: pill toggle with 'balanced' and 'dense' buttons; active button has primary background
    - Accent section: 4 circular swatches 40×40px; active swatch has white ring outline; setAccent called on click
    - All setters (setLang, setDensity, setAccent) are called from useAppStore directly — no prop-drilling
    - If screen-settings already receives lang as a prop, the Display tab should still call useAppStore for setLang (but can display current lang from the prop)
    - lang, density, accent VALUES are read from useAppStore (not from props) so they are always current
    - D-16 persistence verification: note in the plan that the executor should manually verify persistence by changing values and restarting the app (covered in Plan 09 human verify step)
  </behavior>

  <action>
**src/screen-settings.jsx — changes:**

1. Add or verify useAppStore import:
```javascript
import { useAppStore } from './store.js';
```

2. Inside `SettingsScreen` function, add store selectors (after existing hooks, using individual selectors to avoid re-render issues):
```javascript
const lang = useAppStore((s) => s.lang);
const setLang = useAppStore((s) => s.setLang);
const density = useAppStore((s) => s.density);
const setDensity = useAppStore((s) => s.setDensity);
const accent = useAppStore((s) => s.accent);
const setAccent = useAppStore((s) => s.setAccent);
```
Note: If SettingsScreen already receives `lang` as a prop, prefer the prop value for i18n translation (useT(lang)) but use the Zustand value for the currently selected toggle state. The prop and store value should be the same — either is fine for reading.

3. Define accent swatches (at component level or as a module constant above the component):
```javascript
const ACCENT_SWATCHES = [
  { id: 'sage',       color: 'hsl(120 14% 49%)' },
  { id: 'indigo',     color: 'hsl(230 50% 55%)' },
  { id: 'terracotta', color: 'hsl(0 53% 52%)' },
  { id: 'charcoal',   color: 'hsl(120 8% 25%)' },
];
```

4. Add Display tab to the TABS array (append last). Read the existing tab array to find exact object shape, then add:
```javascript
{ id: 'display', label: t('display_tab'), icon: 'grid' }
```
If the tabs array is defined before `t` is available, use a static string for label and use `t('display_tab')` inline in the tab render, or restructure as needed to follow the existing pattern exactly.

5. Add the Display tab pane content. Place it after all existing `{tab === '...' && ...}` blocks:
```javascript
{tab === 'display' && (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 16, maxWidth: 560 }}>

    {/* Language section */}
    <div className="card" style={{ padding: 20 }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: 'var(--sc-muted-foreground)', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 12 }}>
        {t('display_lang_label')}
      </div>
      <div style={{ display: 'inline-flex', background: '#fff', border: '1px solid hsl(120 10% 90%)', borderRadius: 10, padding: 3 }}>
        {[
          { id: 'ro', label: 'Română' },
          { id: 'en', label: 'English' },
        ].map(l => (
          <button key={l.id} onClick={() => setLang(l.id)}
            style={{
              border: 0, background: lang === l.id ? 'var(--sc-primary)' : 'transparent',
              color: lang === l.id ? '#fff' : '#555',
              padding: '7px 20px', borderRadius: 8,
              fontWeight: 600, fontSize: 13, cursor: 'pointer', fontFamily: 'inherit',
            }}>
            {l.label}
          </button>
        ))}
      </div>
    </div>

    {/* Density section */}
    <div className="card" style={{ padding: 20 }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: 'var(--sc-muted-foreground)', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 12 }}>
        {t('display_density_label')}
      </div>
      <div style={{ display: 'inline-flex', background: '#fff', border: '1px solid hsl(120 10% 90%)', borderRadius: 10, padding: 3 }}>
        {[
          { id: 'balanced', labelRo: 'Echilibrat', labelEn: 'Balanced' },
          { id: 'dense',    labelRo: 'Compact',    labelEn: 'Dense' },
        ].map(d => (
          <button key={d.id} onClick={() => setDensity(d.id)}
            style={{
              border: 0, background: density === d.id ? 'var(--sc-primary)' : 'transparent',
              color: density === d.id ? '#fff' : '#555',
              padding: '7px 20px', borderRadius: 8,
              fontWeight: 600, fontSize: 13, cursor: 'pointer', fontFamily: 'inherit',
            }}>
            {lang === 'ro' ? d.labelRo : d.labelEn}
          </button>
        ))}
      </div>
    </div>

    {/* Accent color section */}
    <div className="card" style={{ padding: 20 }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: 'var(--sc-muted-foreground)', textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 12 }}>
        {t('display_accent_label')}
      </div>
      <div style={{ display: 'flex', gap: 12 }}>
        {ACCENT_SWATCHES.map(a => (
          <button
            key={a.id}
            onClick={() => setAccent(a.id)}
            title={a.id}
            style={{
              width: 40, height: 40, borderRadius: '50%',
              background: a.color,
              border: 'none',
              outline: accent === a.id ? `3px solid ${a.color}` : '3px solid transparent',
              outlineOffset: 2,
              boxShadow: accent === a.id ? `0 0 0 2px #fff, 0 0 0 4px ${a.color}` : 'none',
              cursor: 'pointer',
              transform: accent === a.id ? 'scale(1.1)' : 'scale(1)',
              transition: 'transform 150ms, box-shadow 150ms',
            }}
          />
        ))}
      </div>
    </div>

  </div>
)}
```

**src/i18n.jsx — add Display tab strings to BOTH ro and en sections:**

In `ro` section:
```javascript
display_tab: 'Afișaj',
display_lang_label: 'Limbă',
display_density_label: 'Densitate',
display_accent_label: 'Culoare accent',
```

In `en` section:
```javascript
display_tab: 'Display',
display_lang_label: 'Language',
display_density_label: 'Density',
display_accent_label: 'Accent color',
```
  </action>

  <verify>
    <automated>cd /Users/eduardalbu/Developer/sitecare-pos && grep -n "display\|Display" src/screen-settings.jsx | head -20 && grep "display_tab\|display_lang" src/i18n.jsx && grep -n "setLang\|setDensity\|setAccent" src/screen-settings.jsx && npx vitest run src/__tests__/screen-settings.test.jsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep "tab === 'display'" src/screen-settings.jsx` returns 1 line (display pane conditional)
    - `grep "'display'" src/screen-settings.jsx` returns >= 2 lines (tab definition + pane condition)
    - `grep "setLang\|setDensity\|setAccent" src/screen-settings.jsx` returns >= 3 lines (one per setter call in onClick handlers)
    - `grep "useAppStore" src/screen-settings.jsx` returns >= 1 line (import or usage)
    - `grep "ACCENT_SWATCHES\|sage.*indigo\|indigo.*terracotta" src/screen-settings.jsx` returns >= 1 line (swatches array present)
    - `grep -c "display_tab" src/i18n.jsx` returns >= 2 (ro + en)
    - `grep -c "display_lang_label" src/i18n.jsx` returns >= 2 (ro + en)
    - `grep -c "display_density_label" src/i18n.jsx` returns >= 2 (ro + en)
    - `grep -c "display_accent_label" src/i18n.jsx` returns >= 2 (ro + en)
    - `npx vitest run src/__tests__/screen-settings.test.jsx --reporter=verbose` exits 0
    - `npx vitest run --reporter=verbose` exits 0 — no regressions
  </acceptance_criteria>

  <done>screen-settings.jsx: Display tab added to tab bar with 'grid' icon, display pane renders lang toggle (calls setLang), density toggle (calls setDensity), accent swatch picker (calls setAccent) — all reading current values from useAppStore directly. i18n.jsx: display_tab, display_lang_label, display_density_label, display_accent_label in both ro and en.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User setting change → Zustand → plugin-store | lang/density/accent values flow from UI click → Zustand setter → partialize → @tauri-apps/plugin-store (OS persistence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | Accent/lang/density values | accept | Values are constrained to hardcoded options in JSX; no free-text input; Zustand setter accepts any string but rendering uses CSS custom properties which safely ignore unknown values |
| T-04-08-02 | Information Disclosure | Settings persisted in plugin-store | accept | lang/density/accent are non-sensitive UI preferences; no PII stored; OS-level storage is appropriate |
| T-04-08-03 | Elevation of Privilege | Settings tab access | accept | Settings screen is role-gated by FOH/BOH switch at shell level; no additional per-tab auth needed for display preferences |
</threat_model>

<verification>
```bash
cd /Users/eduardalbu/Developer/sitecare-pos
# Verify Display tab
grep "tab === 'display'\|display_tab\|setLang\|setDensity\|setAccent" src/screen-settings.jsx
# Verify i18n
grep "display_tab\|display_lang_label\|display_density_label\|display_accent_label" src/i18n.jsx
# Full suite
npx vitest run --reporter=verbose 2>&1 | tail -20
```
Expected: display tab present in pane condition and tab definition, all setters called in onClick, i18n keys in both languages, suite exits 0.
</verification>

<success_criteria>
- screen-settings.jsx: Display tab added to tab list (icon: grid), display pane with lang toggle (setLang), density toggle (setDensity), accent swatches 40×40px (setAccent), all values read from useAppStore directly
- i18n.jsx: display_tab, display_lang_label, display_density_label, display_accent_label in both ro and en
- Persistence: lang/density/accent are in the existing partialize list in store.js — no new code needed; changes automatically persist via plugin-store
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-screens/04-08-SUMMARY.md`
</output>
