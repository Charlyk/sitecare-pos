---
phase: 08-read-only-order-detail-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/i18n.jsx
autonomous: true
requirements: [HIST-10]

must_haves:
  truths:
    - "ROADMAP.md Phase 8 SC1 no longer promises handled-by, and states prep time means the derived actual duration (D-09, D-10)"
    - "ROADMAP.md Phase 8 SC2 names 401/404 — the errors getOrder actually documents — not 401/403 (D-08)"
    - "REQUIREMENTS.md HIST-10 no longer promises handled-by, and the Design Elements Cut table records why (D-09)"
    - "REQUIREMENTS.md records that the AdminOrder summary normalizes to an empty items array, not a null one, so the pre-hydration state is an empty receipt rather than an absent one (planner finding F-01)"
    - "src/i18n.jsx defines h_detail_error_title, h_prep_time, h_canceled_after, h_detail_no_items in both the ro and en objects"
    - "No i18n key is defined twice within the same locale object — `npx vitest run src/__tests__/i18n.test.js` passes"
    - "The four pre-existing keys h_retry, h_back_to_history, check_connection, and elapsed are reused unchanged, not duplicated under new names"
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - src/i18n.jsx
  key_links:
    - "h_prep_time / h_canceled_after are consumed by the duration row in 08-03; h_detail_error_title and h_detail_no_items by the items-card states in 08-04 — all four must exist in both locales before those plans render them"
---

<objective>
Correct the two planning documents that promise a field this phase has cut, and add the four i18n
keys the rest of the phase renders.

Purpose: `08-CONTEXT.md` D-09 cuts "handled-by" because `Order` has no such field — it exists only as
`events[].actor` (`string | null`, undocumented semantics), and deriving it risks attributing an order
to the wrong staff member. ROADMAP.md SC1 and REQUIREMENTS.md HIST-10 both still promise it. SC2 also
asserts a `403` the SDK does not document. Left unamended, the phase cannot pass its own verification
by construction. This plan is Wave 1 so every later plan builds against corrected documents.

Output: amended ROADMAP.md Phase 8 SC1/SC2, amended REQUIREMENTS.md HIST-10 + a new Design Elements
Cut row, and four new i18n keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-read-only-order-detail-view/08-CONTEXT.md
@.planning/phases/08-read-only-order-detail-view/08-UI-SPEC.md
</context>

## Artifacts this phase produces

Symbols and files created across all five Phase 8 plans (listed in every plan so the
source-grounding pass does not flag them as drift):

| Artifact | Kind | Plan | File |
|----------|------|------|------|
| `h_detail_error_title` | i18n key (ro+en) | 08-01 | `src/i18n.jsx` |
| `h_prep_time` | i18n key (ro+en) | 08-01 | `src/i18n.jsx` |
| `h_canceled_after` | i18n key (ro+en) | 08-01 | `src/i18n.jsx` |
| `h_detail_no_items` | i18n key (ro+en) | 08-01 | `src/i18n.jsx` |
| `deriveDuration(order)` | exported pure function | 08-02 | `src/history-utils.js` |
| `historyStatusMeta(status, t)` | newly *exported* (was module-private) | 08-02 | `src/screen-history.jsx` |
| `detailLoading` prop | new prop on `OrderDetailScreen` | 08-04 | `src/screen-detail.jsx` |
| `detailError` prop | new prop on `OrderDetailScreen` | 08-04 | `src/screen-detail.jsx` |
| `onRetryDetail` prop | new prop on `OrderDetailScreen` | 08-04 | `src/screen-detail.jsx` |
| `ItemsSkeletonRows` | module-private component | 08-04 | `src/screen-detail.jsx` |
| `ItemsErrorBlock` | module-private component | 08-04 | `src/screen-detail.jsx` |
| `mergedHistoryOrder` | route-level derived value | 08-05 | `src/app.jsx` |

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP Phase 8 SC1/SC2 and REQUIREMENTS HIST-10 per D-09 and planner finding F-01</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/ROADMAP.md` — the Phase 8 block (`### Phase 8: Read-Only Order Detail View`) you are editing; read the surrounding Phase 7 and Phase 9 blocks so the scoped edit does not disturb them
    - `.planning/REQUIREMENTS.md` — line ~40 (HIST-10) and the `## Design Elements Cut (no API data source)` table at line ~47, so the new row matches the existing column shape exactly
    - `.planning/phases/08-read-only-order-detail-view/08-CONTEXT.md` — the `<roadmap_impact>` section, which is the authoritative wording for this amendment, and D-09/D-10 which justify it
  </read_first>

  <action>
Apply four scoped edits. Use `Edit` (scoped string replacement) on both documents — never `Write`,
which would destroy the other phases and requirements outside the diff window.

**Edit 1 — ROADMAP.md Phase 8, Success Criterion 1.** It currently reads:

  "Opening a historical order hydrates the detail view via `getOrder(id)` and shows items with
  modifiers, subtotal, delivery fee, total, customer phone, delivery address, handled-by, and prep
  time — none of which exist on the `AdminOrder` summary already in hand"

Two corrections, per D-09 and D-10: remove the staff-attribution field from the list entirely, and
make "prep time" explicitly mean the duration derived from `events[]` timestamps rather than the
accept-time estimate. The trailing "none of which exist on the `AdminOrder` summary already in hand"
clause stays true for the remaining fields and should be kept.

**Edit 2 — ROADMAP.md Phase 8, Success Criterion 2.** It currently says the fallback covers a failure
"or returns 401/403". `GetOrderErrors` in `node_modules/@charlyk/admin-client/dist/index.d.ts`
documents **401 and 404 only** — there is no 403. Replace `401/403` with `401/404`. Keep the rest of
SC2 (the loading state, and the `AdminOrder` fields staying visible rather than blanking) verbatim —
that half is accurate and is what 08-04 implements.

**Edit 3 — REQUIREMENTS.md HIST-10** (line ~40). Same removal as Edit 1: drop the staff-attribution
field from the field list. Keep the `readOnly` / D-07 / D-09 traceability parenthetical and the
"hydrated on demand via `getOrder(id)`" clause unchanged.

**Edit 4 — REQUIREMENTS.md `## Design Elements Cut (no API data source)`.** Add one row matching the
table's existing column shape, recording that the handled-by field is cut because `Order` carries no
such field — it exists only as `events[].actor`, typed `string | null` with undocumented semantics,
so deriving it risks misattributing an order to the wrong staff member (D-09). Cite D-09.

**Edit 5 — record planner finding F-01 in REQUIREMENTS.md**, immediately below the HIST-10 line as a
short indented note. `08-RESEARCH.md` and `08-CONTEXT.md` both assert the `AdminOrder` summary reaches
`screen-detail.jsx` with a null items value, and that the screen therefore falls back to the "minimal
totals card". This is **false and was verified false** by executing `normalizeOrder` against an
`AdminOrder`-shaped object: `normalizeOrder` maps `items: (o.items ?? []).map(...)`, which returns an
empty array — never null — and `use-history-orders.js` runs every summary through `normalizeOrder`.
The note should record three consequences, because later plans depend on them:
  1. The ungated Modify control is reachable on the `history-detail` route **in production today** —
     it is a live defect, not one this phase would introduce by hydrating.
  2. The minimal-totals-card fallback at `screen-detail.jsx:191-204` is unreachable on both routes.
  3. The pre-hydration state is an *empty* receipt, not an *absent* one, so loading must be
     distinguished from a genuinely empty order by query state rather than by the items value.
Keep it to a few lines — this is a traceability breadcrumb, not an essay.
  </action>

  <verify>
    <automated>grep -c 'handled-by' .planning/ROADMAP.md .planning/REQUIREMENTS.md; grep -n '401/404' .planning/ROADMAP.md; grep -n 'F-01' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `awk '/^### Phase 8:/,/^### Phase 9:/' .planning/ROADMAP.md | grep -c 'handled-by'` returns `0`
    - `awk '/^### Phase 8:/,/^### Phase 9:/' .planning/ROADMAP.md | grep -c '401/403'` returns `0`
    - `awk '/^### Phase 8:/,/^### Phase 9:/' .planning/ROADMAP.md | grep -c '401/404'` returns `1`
    - The ROADMAP Phase 8 block still contains exactly 4 numbered success criteria, and SC3 and SC4 are byte-identical to their pre-edit text
    - `grep -n 'HIST-10' .planning/REQUIREMENTS.md` still returns the requirement line and its traceability-table row — the requirement is amended, not deleted
    - The HIST-10 requirement line contains neither `handled-by` nor `handled by`
    - The `## Design Elements Cut (no API data source)` table has exactly one more row than before, that row names `events[].actor` and cites `D-09`, and its column count matches the pre-existing rows
    - `.planning/REQUIREMENTS.md` contains a note citing `F-01` that names `normalizeOrder` and states the summary yields an empty items array
    - `git diff --stat .planning/ROADMAP.md` shows no phase other than Phase 8 touched (Phases 7, 9, 10, 11 headings and bodies unchanged)
  </acceptance_criteria>

  <done>ROADMAP Phase 8 SC1/SC2 and REQUIREMENTS HIST-10 no longer promise handled-by or a 403; the Design Elements Cut table explains the omission; finding F-01 is recorded against HIST-10.</done>
</task>

<task type="auto">
  <name>Task 2: Add the four new i18n keys to both the ro and en locale objects</name>
  <files>src/i18n.jsx</files>

  <read_first>
    - `src/i18n.jsx` — the whole file. You need the exact shape of both locale objects and their existing keys. The `ro` object's History block ends around line 241 (`h_back_to_history`), the `en` object's around line 468. Note `h_retry`, `h_back_to_history`, `check_connection`, and `elapsed` already exist in BOTH locales — reuse them, do not redefine them.
    - `src/__tests__/i18n.test.js` — the existing parity/duplicate-key test, so you know what shape it enforces
    - `.planning/phases/08-read-only-order-detail-view/08-UI-SPEC.md` — the `### New i18n keys required` table, which is the authoritative source for every string below
  </read_first>

  <action>
Add exactly four keys to `src/i18n.jsx`, each in **both** the `ro` and `en` objects, with these exact
values from the UI-SPEC copywriting contract:

| Key | ro | en |
|-----|----|----|
| `h_detail_error_title` | `Nu am putut încărca bonul` | `Couldn't load this receipt` |
| `h_prep_time` | `Timp de pregătire` | `Prep time` |
| `h_canceled_after` | `Anulată după` | `Canceled after` |
| `h_detail_no_items` | `Nicio poziție pe această comandă` | `No items on this order` |

Place them adjacent to the existing History keys in each locale — after `h_back_to_history` in `ro`
(~line 241) and after `h_back_to_history` in `en` (~line 468) — so the History block stays contiguous.

**Grep before you add.** This project hit duplicate-key defects twice during v1.0, and a duplicate
literal key in a JS object silently overwrites the earlier one rather than erroring. Confirm none of
the four already exists in either locale before writing. `h_detail_no_items` in particular was added
to the UI-SPEC late, by the post-verification probe, and has not been grep-verified upstream.

Do **not** add keys for status-specific error copy (404 vs 401 vs network). D-08 locks one generic
message for every failure, and `useOrderDetail` discards the HTTP status anyway.

Note the `en` value for `h_detail_error_title` contains an apostrophe — use double quotes for that
string literal, matching how the neighbouring `h_error_title: "Couldn't load history"` is already
written in the `en` object.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/i18n.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "h_detail_error_title" src/i18n.jsx` returns `2` (once per locale)
    - `grep -c "h_prep_time" src/i18n.jsx` returns `2`
    - `grep -c "h_canceled_after" src/i18n.jsx` returns `2`
    - `grep -c "h_detail_no_items" src/i18n.jsx` returns `2`
    - `grep -c "h_retry:" src/i18n.jsx` returns `2` and `grep -c "check_connection:" src/i18n.jsx` returns `2` — unchanged, no duplicates introduced
    - `npx vitest run src/__tests__/i18n.test.js` passes
    - `npx vitest run` — full suite green (no locale-parity regression)
    - `node -e "import('./src/i18n.jsx')"` style check is not required; the vitest run above already imports the module and would throw on a syntax error
  </acceptance_criteria>

  <done>All four keys resolve in both locales via `useT`, the i18n parity test passes, and no pre-existing key was duplicated or shadowed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits two planning documents and a static string table. No untrusted input crosses any boundary; no network, storage, or DOM surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Spoofing / Repudiation | ROADMAP SC1 + REQUIREMENTS HIST-10 promising a "handled-by" field | medium | mitigate | Remove the promise from both documents (Task 1) so no downstream plan is pressured to synthesize staff attribution from `events[].actor` — a `string \| null` field with undocumented semantics whose misreading would attribute an order to the wrong person. The Design Elements Cut row records the reason so a future phase cannot silently reintroduce it. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this phase — `08-RESEARCH.md`'s Package Legitimacy Audit records zero new packages, and every dependency (`@charlyk/admin-client`, `@tanstack/react-query`, `zustand`, React) was vetted in prior phases. No legitimacy checkpoint is required because there is no install to gate. |
</threat_model>

<verification>
- `npx vitest run` — full suite green
- ROADMAP Phase 8 block contains 4 success criteria, none naming handled-by or 403
- REQUIREMENTS HIST-10 amended in place with a Design Elements Cut row and the F-01 note
- Four new keys present in both locales, no duplicates
</verification>

<success_criteria>
Every downstream Phase 8 plan can render `t('h_prep_time')`, `t('h_canceled_after')`,
`t('h_detail_error_title')`, and `t('h_detail_no_items')` in both languages, and can be verified
against a ROADMAP and REQUIREMENTS that describe what the SDK can actually deliver.
</success_criteria>

<output>
Create `.planning/phases/08-read-only-order-detail-view/08-01-SUMMARY.md` when done
</output>
