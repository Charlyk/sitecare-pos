---
phase: 10-filters-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/history-utils.js
  - src/__tests__/history-utils.test.js
  - src/i18n.jsx
autonomous: true
requirements: [HIST-07, HIST-08, HIST-09]
must_haves:
  truths:
    - "foldDiacritics NFD-decomposes then strips the whole Unicode combining-diacritical-marks block, so both modern comma-below (U+0219/U+021B) and legacy cedilla (U+015F/U+0163) ș/ț fold, and ă/â/î fold (HIST-09 encoding; D-11)"
    - "matchesSearch returns true for an empty query and for a whitespace-only query — an empty search matches every order (HIST-09 empty)"
    - "matchesSearch matches on dailyOrderNumber, on the id[0:8] fallback when dailyNumber is null, and on customer.name — no row is unreachable by the text printed on it (HIST-09; D-09)"
    - "matchesStatus('all') matches every order; any named status returns deriveDisplayStatus(order) === statusFilter and never re-implements precedence inline (HIST-07 empty/precision; INVARIANT)"
    - "matchesType('all') matches every order; any named type returns order.type === typeFilter exactly, doing no 'local' mapping of its own (HIST-08 empty/adjacency)"
    - "the three status buckets partition a finished-only list exactly: deriveDisplayStatus never returns null there and All = Completed + Refunded + Canceled (HIST-07 boundary/ordering — refunded takes outright precedence)"
    - "HIST-09 precision: text search does no numeric rounding or tie-breaking — matching is pure substring containment (covered-trivial)"
  prohibitions:
    - "history-utils.js must not import react, data.jsx, or @charlyk/admin-client — the module stays pure and unit-testable without a DOM"
    - "matchesStatus must not carry its own refunded/canceled/completed precedence — a second precedence source would drift from deriveDisplayStatus"
  artifacts:
    - "src/history-utils.js exports foldDiacritics, matchesStatus, matchesType, matchesSearch"
    - "src/__tests__/history-utils.test.js covers foldDiacritics (both ș/ț encodings + ă/â/î), matchesSearch (dailyOrderNumber, id[0:8] fallback, customer.name, empty query), matchesStatus, matchesType"
    - "src/i18n.jsx contains h_empty_filtered_title and h_clear_filters under both ro and en"
  key_links:
    - "matchesStatus → deriveDisplayStatus (single source of status precedence truth)"
    - "matchesSearch → foldDiacritics (query and name normalized on the same axis before comparison)"
---

<objective>
Add the three pure filter predicates and the diacritic-folding helper to the React-free
derivation layer, and add the two net-new i18n keys the filtered empty-state needs — the
foundation every screen-wiring task in later waves builds on.

Purpose: These four functions (`foldDiacritics`, `matchesStatus`, `matchesType`, `matchesSearch`)
are the pure, unit-testable core of HIST-07/08/09. Landing them first, with direct tests,
means the heavy screen-wiring plan (10-03) consumes proven primitives rather than inventing
matching logic inline. The i18n keys land here too because they have no dependencies and 10-03/10-04
consume them.
Output: `src/history-utils.js` (+4 exports), `src/__tests__/history-utils.test.js` (extended),
`src/i18n.jsx` (+2 keys, ro + en).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-filters-search/10-CONTEXT.md
@.planning/phases/10-filters-search/10-RESEARCH.md
@.planning/phases/10-filters-search/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (source-grounding excludes these from drift verification):
- `foldDiacritics(s)` — new export in `src/history-utils.js`
- `matchesStatus(order, statusFilter)` — new export in `src/history-utils.js`
- `matchesType(order, typeFilter)` — new export in `src/history-utils.js`
- `matchesSearch(order, query)` — new export in `src/history-utils.js`
- i18n keys `h_empty_filtered_title`, `h_clear_filters` — new in `src/i18n.jsx` (ro + en)
Reused unchanged (do NOT recreate): `deriveDisplayStatus` (history-utils.js:196), `filterFinishedOrders`
(history-utils.js:187), and the existing type/status/empty i18n keys (`all`, `delivery`, `pickup`,
`dinein`, `h_status_*`, `h_search`, `h_empty`, `h_empty_sub`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add foldDiacritics + matchesSearch to history-utils.js</name>
  <files>src/history-utils.js, src/__tests__/history-utils.test.js</files>
  <read_first>
    - src/history-utils.js:1-8 (module purity header — no react/data.jsx/@charlyk imports) and :187-201 (filterFinishedOrders, deriveDisplayStatus — the export/JSDoc conventions to mirror)
    - src/__tests__/history-utils.test.js (existing pure-unit-test pattern to extend)
    - .planning/phases/10-filters-search/10-RESEARCH.md § Pattern 3 + Pitfall 2 (the ș/ț dual-encoding risk) and § Code Examples
    - .planning/phases/10-filters-search/10-CONTEXT.md D-09, D-11
    - src/screen-history.jsx:158 (orderNumberLabel usage — confirms the id[0:8] fallback the search must mirror)
  </read_first>
  <behavior>
    - foldDiacritics('Rădulescu') === 'Radulescu' (ă folds)
    - foldDiacritics('Gheorghița') === 'Gheorghita' — modern comma-below ț (U+021B)
    - foldDiacritics('Gheorghiţa') === 'Gheorghita' — legacy cedilla ţ (U+0163); both encodings fold because the strip targets the whole combining-marks block
    - foldDiacritics('Șerban') folds the leading Ș (both encodings)
    - matchesSearch(order, '') === true and matchesSearch(order, '   ') === true (empty/whitespace matches all)
    - matchesSearch matches when the query is a substring of String(dailyOrderNumber)
    - matchesSearch matches on order.id.slice(0,8) when order.dailyOrderNumber is null/undefined (Pitfall 4)
    - matchesSearch matches on a diacritic-folded, lowercased customer.name (e.g. query 'radulescu' matches customer.name 'Rădulescu')
    - matchesSearch tolerates a missing customer (order.customer undefined) without throwing
  </behavior>
  <action>
    Add two named `function` exports to src/history-utils.js, following the module's existing
    JSDoc + no-import conventions (mirror deriveDisplayStatus at :196). foldDiacritics(s) returns
    `s.normalize('NFD')` with the full Unicode combining-diacritical-marks block stripped — target
    the whole block (U+0300–U+036F), NOT a hand-picked subset, so both the comma-below and cedilla
    ș/ț encodings fold (per RESEARCH Pitfall 2). matchesSearch(order, query): fold+trim+lowercase
    the query; if empty, return true; build the number label as String of dailyOrderNumber when it
    is a number, else order.id.slice(0,8), lowercased; fold+lowercase (order.customer?.name ?? '');
    return true when the folded query is a substring of the number label OR the folded name. Do NOT
    import react/data.jsx/@charlyk. Extend src/__tests__/history-utils.test.js with a
    describe('foldDiacritics') block covering both ș/ț encodings + ă/â/î, and a describe('matchesSearch')
    block covering the dailyOrderNumber match, the id[0:8] fallback (seed an order with
    dailyOrderNumber:null), the customer.name diacritic-folded match, and the empty-query case.
  </action>
  <acceptance_criteria>
    - `grep -c 'export function foldDiacritics' src/history-utils.js` === 1 and `grep -c 'export function matchesSearch' src/history-utils.js` === 1
    - `grep -c -E "from 'react'|data\.jsx|@charlyk" src/history-utils.js` === 0 (module purity preserved)
    - history-utils.test.js asserts foldDiacritics('Gheorghița') and foldDiacritics('Gheorghiţa') both === 'Gheorghita' (both ș/ț encodings)
    - history-utils.test.js asserts matchesSearch matches the id[0:8] fallback for an order with dailyOrderNumber:null, and returns true for query ''
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/history-utils.test.js</automated>
  </verify>
  <done>
    foldDiacritics and matchesSearch are exported from history-utils.js with no react/data.jsx/@charlyk
    import; the new describe blocks pass, including both ș/ț encodings and the dailyNumber:null id[0:8] case.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add matchesStatus + matchesType predicates</name>
  <files>src/history-utils.js, src/__tests__/history-utils.test.js</files>
  <read_first>
    - src/history-utils.js:196-201 (deriveDisplayStatus — matchesStatus MUST delegate to this, never re-derive precedence)
    - .planning/phases/10-filters-search/10-CONTEXT.md D-02 INVARIANT (partition, refunded precedence) + D-08 (type vocabulary is unified to 'dinein' upstream in normalizeOrder — this predicate does mapping-free equality only)
    - src/__tests__/history-utils.test.js (extend)
  </read_first>
  <behavior>
    - matchesStatus(order, 'all') === true for any order
    - matchesStatus(order, 'refunded') === true only when deriveDisplayStatus(order) === 'refunded' (a canceled+refunded order counts as refunded only)
    - matchesStatus(order, 'completed') === true only for a COMPLETED, non-refunded order
    - matchesType(order, 'all') === true for any order
    - matchesType({type:'dinein'}, 'dinein') === true; matchesType({type:'delivery'}, 'dinein') === false
    - matchesType does no 'local' translation of its own (that is normalizeOrder's job, plan 10-02)
  </behavior>
  <action>
    Add two named `function` exports to src/history-utils.js. matchesStatus(order, statusFilter):
    return true when statusFilter === 'all'; otherwise return deriveDisplayStatus(order) === statusFilter
    — call deriveDisplayStatus, do not reimplement the refunded/canceled/completed precedence.
    matchesType(order, typeFilter): return true when typeFilter === 'all'; otherwise return
    order.type === typeFilter (plain equality; the 'local'→'dinein' mapping already happened at the
    normalizeOrder boundary per D-08, landed in plan 10-02). Extend history-utils.test.js with
    describe('matchesStatus') (all-matches-everything, each named bucket via a deriveDisplayStatus-shaped
    fixture, and the refunded-precedence case for a canceled+refunded order) and describe('matchesType')
    (all-matches-everything, exact-equality per named type, non-match).
  </action>
  <acceptance_criteria>
    - `grep -c 'export function matchesStatus' src/history-utils.js` === 1 and `grep -c 'export function matchesType' src/history-utils.js` === 1
    - the matchesStatus body contains no inline 'CANCELLED'/'COMPLETED'/'refunded' status literal (delegates to deriveDisplayStatus)
    - history-utils.test.js asserts matchesStatus(order,'refunded') is true only for a refunded-precedence order and matchesType({type:'delivery'},'dinein') === false
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/history-utils.test.js</automated>
  </verify>
  <done>
    matchesStatus and matchesType are exported; matchesStatus delegates to deriveDisplayStatus (grep confirms
    no inline 'CANCELLED'/'COMPLETED'/'refunded' literals in the matchesStatus body); all new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add h_empty_filtered_title + h_clear_filters i18n keys (ro + en)</name>
  <files>src/i18n.jsx</files>
  <read_first>
    - src/i18n.jsx (ro history block near h_empty/h_empty_sub, en history block near the mirror) — confirm exact placement
    - .planning/phases/10-filters-search/10-UI-SPEC.md § New i18n Keys + § Copywriting Contract (exact strings)
    - .planning/phases/10-filters-search/10-CONTEXT.md D-13, D-14 and the duplicate-key warning (v1.0 hit it twice)
  </read_first>
  <action>
    First grep src/i18n.jsx for h_empty_filtered_title and h_clear_filters to confirm neither exists
    (both confirmed absent at planning time). Add both keys under BOTH the ro and en dictionaries,
    placed adjacent to the existing h_empty / h_empty_sub pair. Values per the UI-SPEC Copywriting
    Contract: h_empty_filtered_title ro 'Nicio comandă nu se potrivește cu filtrele active.' / en
    'No orders match the active filters.'; h_clear_filters ro 'Șterge filtrele' / en 'Clear filters'.
    Do not redeclare any existing key (all, delivery, pickup, dinein, h_status_*, h_search, h_empty,
    h_empty_sub are reused unchanged, not re-added).
  </action>
  <acceptance_criteria>
    - `grep -c 'h_empty_filtered_title' src/i18n.jsx` === 2 (once ro, once en) and `grep -c 'h_clear_filters' src/i18n.jsx` === 2
    - the ro h_empty_filtered_title value is exactly 'Nicio comandă nu se potrivește cu filtrele active.' and en is 'No orders match the active filters.'
    - no pre-existing i18n key was redeclared (all/delivery/pickup/dinein/h_status_*/h_search/h_empty/h_empty_sub unchanged)
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c -E "h_empty_filtered_title|h_clear_filters" src/i18n.jsx) -eq 4</automated>
  </verify>
  <done>
    Exactly two new keys exist, once each in ro and once each in en (4 total matches); no duplicate-key
    collision; existing keys untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystroke → search predicate | free-text query enters matchesSearch |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Information Disclosure | matchesStatus / matchesType / matchesSearch | low | accept | Predicates only ever NARROW an already-auth-scoped, finished-only list — no predicate can add rows, so a crafted query cannot surface unauthorized data |
| T-10-04 | Tampering (XSS) | search query text | low | accept | The query flows only into String.prototype.includes; it is never used to build HTML/DOM/query strings. React JSX escaping covers the input's own echoed value (wired in 10-03) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/history-utils.test.js` green (predicate + folding coverage)
- `grep -c -E "react|data\.jsx|@charlyk" src/history-utils.js` — imports of those remain absent (purity preserved)
- Exactly 2 new i18n keys present (4 matches across ro + en)
</verification>

<success_criteria>
foldDiacritics, matchesStatus, matchesType, matchesSearch exported and unit-tested (both ș/ț encodings,
id[0:8] fallback, empty query, status precedence via deriveDisplayStatus); history-utils.js still pure;
h_empty_filtered_title and h_clear_filters present in ro + en with no duplicates.
</success_criteria>

<output>
Create `.planning/phases/10-filters-search/10-01-SUMMARY.md` when done
</output>
