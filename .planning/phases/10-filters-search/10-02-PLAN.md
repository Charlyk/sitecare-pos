---
phase: 10-filters-search
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data.jsx
  - src/__tests__/normalize-order.test.js
  - src/__tests__/screen-orders.test.jsx
autonomous: true
requirements: [HIST-08]
must_haves:
  truths:
    - "normalizeOrder maps orderType 'local' to type 'dinein' at the boundary, so History's type filter matches dine-in rows (HIST-08; D-08)"
    - "normalizeOrder passes 'delivery' and 'pickup' through unchanged, and still falls back to 'dinein' for a genuinely absent/unmapped type (adjacency — only 'local' is translated)"
    - "an order whose raw orderType was 'local' now matches the live Orders screen's Dine-in type filter, which previously silently excluded it (F-02)"
    - "live Orders chips, KDS rendering, and the detail view render the dine-in chip byte-identically before and after the fix — typeMeta has no 'local' key and already falls through to map.dinein"
  prohibitions:
    - "the fix must not change how any live order RENDERS — only filter matching (equality against 'dinein') changes; do not touch typeMeta or the row chip"
    - "the mapping must not be applied inside a history-only predicate — that would leave the shipped Orders defect in place and create a second type vocabulary (D-08 rejected alternative)"
  artifacts:
    - "src/data.jsx normalizeOrder translates raw type/orderType 'local' → 'dinein' (the one line at src/data.jsx:222)"
    - "src/__tests__/normalize-order.test.js asserts 'local'→'dinein' and delivery/pickup passthrough (F-02 regression)"
    - "src/__tests__/screen-orders.test.jsx asserts a formerly-'local' order appears under the live Dine-in filter and under All"
  key_links:
    - "normalizeOrder .type output → screen-orders.jsx:187 live type filter (o.type !== typeFilter) → the F-02 defect site"
    - "src/screen-pos.jsx:12 orderTypeMap (outbound dinein→local) — the deliberate concept D-08 inverts inbound; left unmodified"
---

<objective>
Fix the SDK `'local'` → UI `'dinein'` type mapping at the single normalization boundary
(`normalizeOrder`, `src/data.jsx:222`) and prove the shared-path regression surface (Orders, KDS,
detail) is unaffected.

Purpose: HIST-08 requires `orderType: 'local'` to present as Dine-in. Fixing it at the boundary —
inverting `screen-pos.jsx:12`'s existing outbound `orderTypeMap` — makes `'dinein'` the single
app-wide type vocabulary inbound and outbound, repairing HIST-08 AND a live Orders defect (F-02)
in one line. This plan is isolated in Wave 1 (own files) precisely because it touches shared
live-order code and needs its own regression gate before the History screen wiring (10-03) relies
on the corrected type value.
Output: `src/data.jsx` (one line), `src/__tests__/normalize-order.test.js` (extended),
`src/__tests__/screen-orders.test.jsx` (extended).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-filters-search/10-CONTEXT.md
@.planning/phases/10-filters-search/10-RESEARCH.md
@.planning/phases/10-filters-search/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New/changed symbols from THIS plan:
- `normalizeOrder` type-field mapping in `src/data.jsx:222` — changed (adds the `'local'`→`'dinein'` case)
- new test cases in `src/__tests__/normalize-order.test.js` and `src/__tests__/screen-orders.test.jsx`
Reused unchanged (do NOT modify): `src/screen-pos.jsx:12` outbound `orderTypeMap`; `typeMeta`
(screen-orders.jsx:15); the four `normalizeOrder` consumers (use-orders.js, use-history-orders.js,
use-order-detail.js, use-sse.js) — the fix is fully upstream of all of them.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizeOrder boundary fix — 'local' → 'dinein'</name>
  <files>src/data.jsx, src/__tests__/normalize-order.test.js</files>
  <read_first>
    - src/data.jsx:195-250 (normalizeOrder — the exact return object; line 222 is `type: o.type ?? o.orderType ?? 'dinein'`)
    - src/screen-pos.jsx:12 (orderTypeMap = { dinein: 'local', pickup: 'pickup', delivery: 'delivery' } — the outbound map being inverted; proof 'local'↔'dinein' is deliberate)
    - src/__tests__/normalize-order.test.js (existing file to extend)
    - .planning/phases/10-filters-search/10-CONTEXT.md D-08 + .planning/phases/10-filters-search/10-RESEARCH.md § Runtime State Inventory (consumer trace) and § Code Examples (exact diff shape)
  </read_first>
  <behavior>
    - normalizeOrder({ id:'x1', orderType:'local', status:'COMPLETED' }).type === 'dinein'
    - normalizeOrder({ id:'x2', orderType:'delivery' }).type === 'delivery'
    - normalizeOrder({ id:'x3', orderType:'pickup' }).type === 'pickup'
    - normalizeOrder({ id:'x4' }).type === 'dinein' (absent type still falls back to 'dinein')
    - an order that already carries type:'delivery' (not orderType) still yields 'delivery'
  </behavior>
  <action>
    Change the single mapping expression at src/data.jsx:222. Compute the raw value once from
    `o.type ?? o.orderType`; when it equals 'local', emit 'dinein'; otherwise emit that raw value,
    preserving the existing `?? 'dinein'` fallback for a genuinely absent/unmapped type. Only 'local'
    is explicitly translated — 'delivery'/'pickup' and every other value pass through unchanged. Do
    not touch any other line of normalizeOrder, do not touch screen-pos.jsx's outbound orderTypeMap.
    Extend src/__tests__/normalize-order.test.js with the F-02 regression cases: 'local'→'dinein',
    'delivery' and 'pickup' passthrough, and absent-type fallback.
  </action>
  <acceptance_criteria>
    - normalize-order.test.js asserts normalizeOrder({orderType:'local'}).type === 'dinein'
    - normalize-order.test.js asserts normalizeOrder({orderType:'delivery'}).type === 'delivery' and {orderType:'pickup'}.type === 'pickup'
    - only line src/data.jsx:222 (the type mapping) changed inside normalizeOrder; screen-pos.jsx orderTypeMap untouched
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/normalize-order.test.js</automated>
  </verify>
  <done>
    normalizeOrder yields type 'dinein' for orderType 'local' and passes delivery/pickup through unchanged;
    the extended normalize-order.test.js passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F-02 live-path regression — Dine-in filter matches formerly-'local' orders</name>
  <files>src/__tests__/screen-orders.test.jsx</files>
  <read_first>
    - src/screen-orders.jsx:15-22 (typeMeta — no 'local' key, falls through to map.dinein; render is byte-identical) and :175-196 (typeFilters, the `visible` filter, and :187 `if (typeFilter !== 'all' && o.type !== typeFilter) return false;` — F-02's defect site)
    - src/screen-kitchen.jsx:69 (independent `type = order.type ?? order.orderType ?? 'dinein'` — never equality-checks 'local', so unaffected either way)
    - src/__tests__/screen-orders.test.jsx (existing RTL file to extend)
    - .planning/phases/10-filters-search/10-RESEARCH.md § Runtime State Inventory (consumer trace) — the enumerated regression surface
  </read_first>
  <behavior>
    - Seed the Orders list with an order whose normalized type is 'dinein' (i.e. raw orderType 'local'); selecting the Dine-in type filter shows that order (previously it was silently excluded)
    - The same order appears under the 'all' type filter (unaffected either way)
    - Delivery/pickup orders still appear only under their own filter and under All (no regression)
  </behavior>
  <action>
    The filter LOGIC at screen-orders.jsx:187 needs NO change — it already compares o.type against
    the selected filter id; the fix is that o.type now arrives as 'dinein' for local orders. Add an
    RTL regression test to src/__tests__/screen-orders.test.jsx: render OrdersScreen with a fixture
    order normalized to type 'dinein' present, click the Dine-in type pill, assert the order renders;
    assert it also renders under All; assert a delivery order is excluded from the Dine-in filter.
    This is the assertion whose absence let F-02 ship. Do NOT modify screen-orders.jsx production code
    in this plan — if the test fails, the fault is upstream in Task 1's data.jsx change, fix it there.
    Confirm (read-only) that typeMeta and screen-kitchen.jsx render dine-in identically before/after.
  </action>
  <acceptance_criteria>
    - screen-orders.test.jsx renders a dine-in (normalized type 'dinein') order, selects the Dine-in type filter, and asserts the order is visible
    - the same test asserts the dine-in order also shows under the 'all' type filter, and a delivery order is excluded from Dine-in
    - `git diff --name-only` shows no production change to src/screen-orders.jsx or src/screen-kitchen.jsx (test-only + the upstream data.jsx fix from Task 1)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screen-orders.test.jsx</automated>
  </verify>
  <done>
    A dine-in (formerly 'local') order matches the live Orders Dine-in filter and appears under All; delivery/pickup
    unaffected; no production edit to screen-orders.jsx/screen-kitchen.jsx (chip rendering byte-identical).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK response → normalizeOrder | raw AdminOrder/Order crosses into the app's internal vocabulary (list, SSE, detail all pass through here) |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | normalizeOrder type mapping (src/data.jsx:222) — shared live-order path | medium | mitigate | The change is scoped to a single equality translation; regression tests assert live Orders Dine-in matching (F-02) plus delivery/pickup passthrough, and rendering is proven unaffected because typeMeta/KDS never equality-check 'local' (fall through to map.dinein). No persisted store holds the old mapping (Runtime State Inventory: nothing to migrate) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/normalize-order.test.js src/__tests__/screen-orders.test.jsx` green
- `grep -rn "'local'" src/*.jsx src/*.js` — re-run guard: still only src/screen-pos.jsx:12 (outbound map) references the literal; no new inbound comparison introduced (RESEARCH assumption A2)
- No production diff in src/screen-orders.jsx or src/screen-kitchen.jsx
</verification>

<success_criteria>
`orderType: 'local'` normalizes to `type: 'dinein'`; delivery/pickup pass through; the live Orders Dine-in
filter matches formerly-'local' orders (F-02 closed); dine-in chip rendering byte-identical across Orders/KDS/detail.
</success_criteria>

<output>
Create `.planning/phases/10-filters-search/10-02-SUMMARY.md` when done
</output>
