---
milestone: v1.1
milestone_name: Orders History Screen
audited: 2026-07-19T00:00:00Z
corrected: 2026-07-19T00:00:00Z   # Phase 12 Plan 04, Task 3 (D-08) re-derivation
status: passed
scores:
  requirements: 13/13   # all functionally delivered & verified; HIST-06 traceability gap RESOLVED (12-02)
  phases: 5/5
  integration: 12/12    # connections wired; state-continuity limitation RESOLVED (12-03, historySelection)
  flows: 6/6            # all flows complete; return-from-detail continuity RESOLVED (12-03)
gaps: {}   # no functionally-unsatisfied requirement; no hard-broken flow
tech_debt:
  - phase: 10-filters-search
    severity: resolved
    items:
      - "Financial-total bug (RESOLVED): the audit's original 'CR-01' label for the tax-omission bug does not match this project's own source-of-truth review — see the CR-01/CR-02 Numbering Reconciliation note below. Both bugs it covered are fixed: tax-omission fixed by commit 7d9810b (`fix(10): CR-02 include tax in normalizeOrder fallback total`); percent-type-discount 100x inflation fixed by commit 30c89d8 (`fix(10): CR-01 convert percent-type discount from cents to RON`). 12-01 backfilled the missing regression-test coverage (`normalize-order.test.js`) for both fallback-total and percent-discount paths, closing 10-REVIEW.md's 'completely untested' finding. Re-verified this phase (Phase 12 Plan 04 Task 1): `git diff` confirms `src/data.jsx` byte-unchanged since the phase base, `npx vitest run` green (except the 3 pre-existing v1.0 failures)."
  - phase: cross-phase (07/08/09/10)
    severity: resolved
    items:
      - "Return-from-detail state continuity (RESOLVED, commits e0c797a/7c0d726/4b694f7, Phase 12 Plan 03): period/status/type/search selection lifted to a new session-only Zustand `historySelection` slice (mirrors the existing `selectedOrder`/`historyOrder` precedent), preserved across the History→history-detail→Back round-trip (D-01/D-02) and reset to defaults on any other screen exit via `setScreen`'s conditional branch (D-03). Live-verified this phase (Task 2 human checkpoint, approved): all four selections (period + status + type + search) survive Back, filtered correctly on the first frame, and reset to 30d/All/All/empty when navigating away and back."
  - phase: 07-history-screen-foundation
    severity: resolved
    items:
      - "HIST-06 traceability gap (RESOLVED, commits b991d6e/44b4afe, Phase 12 Plan 02): `07-VERIFICATION.md` requirements table now includes HIST-06 and its closing scoping note is corrected; `07-04-SUMMARY.md`'s `requirements-completed` frontmatter now lists HIST-06 alongside HIST-05/HIST-13. `REQUIREMENTS.md` was already correct and needed no change (D-09). Doc-only fix — no behavior change; the summary strip itself was already functionally delivered and verified in Phase 7/9/10."
      - "G-07-1 (RESOLVED, commit 50492d5, `fix(print): print the table number on dine-in receipts`): Rust `dead_code` warning for the `table` field is closed — the field is now read. Re-verified this phase (Phase 12 Plan 04 Task 1): `cargo check --lib` completes with zero warnings."
  - phase: 09-period-control
    severity: resolved
    items:
      - "WR-01 (RESOLVED, commit 033cc39, `fix(09-05): WR-01 widen Custom pill outside-click boundary to include toggle button`): re-clicking the open Custom pill now closes the popover (the outside-click ref now wraps toggle+panel together, not just the panel). 3 regression tests present in `screen-history.test.jsx`. Live-verified this phase (Task 2 human checkpoint, approved): re-click closes the popover; Escape and true outside-click also still close it."
  - phase: milestone-wide (pre-existing, from v1.0)
    severity: info
    items:
      - "3 pre-existing unrelated test failures documented in every phase's deferred-items.md: build-pipeline.test.js (BILD-04 — bundle.createUpdaterArtifacts is 'v1Compatible' not true) and offline-buttons.test.jsx ×2 (U12 — OrdersScreen rendered without a QueryClientProvider wrapper). Both last touched in Phase 6 (v1.0); confirmed unrelated to v1.1 at every phase base commit, and re-confirmed as the ONLY red in Phase 12's own verify pass (492/495 passing). Left deferred, NOT fixed this phase — out of scope per CONTEXT.md."
nyquist:
  compliant_phases: [07, 08, 09, 10, 11]
  partial_phases: []
  not_validated_phases: []   # RESOLVED (D-10, Phase 12 Plan 04 Task 3): /gsd-validate-phase 10 and /gsd-validate-phase 11 promoted both from draft to validated, nyquist_compliant: true — zero gaps found in either phase (both already had complete VERIFICATION.md/UAT.md evidence; no auditor spawn needed)
  missing_phases: []
  overall: compliant
---

# Milestone v1.1 — Orders History Screen — Audit Report

**Audited:** 2026-07-19
**Corrected:** 2026-07-19 (Phase 12 Plan 04, Task 3 — D-08)
**Scope:** Phases 7–12 (6 phases, 13 requirements: HIST-01 … HIST-13, plus Phase 12's tech-debt closeout)
**Verdict:** `passed` — all 13 requirements functionally delivered and verified; the CRITICAL financial-total bug, the cross-phase state-continuity limitation, the HIST-06 traceability gap, the WR-01 popover defect, and the G-07-1 Rust warning are all RESOLVED (re-verified with commit SHAs, regression tests, and a live human checkpoint in Phase 12). Nyquist coverage is now compliant across all 5 v1.1 phases. Only the 3 pre-existing, unrelated v1.0 test failures remain — documented, deferred, out of scope. This is the re-derived verdict; see §6 for the full reasoning.

> **This audit was originally issued as `tech_debt` on 2026-07-19** (see `tech_debt` entries above,
> now marked `resolved`, for the pre-correction findings). Phase 12 (`close-cr-01-tax-in-fallback-
> total-hist-06-traceability-wr-01`) was scoped specifically to close every item this audit flagged.
> This correction (D-08) updates the findings in place rather than issuing a second audit document,
> per CONTEXT.md's decision to verify-and-correct rather than re-run a full milestone audit.

---

## 1. Requirements Coverage (3-source cross-reference)

Sources: REQUIREMENTS.md traceability table (`[x]`) · each plan's SUMMARY `requirements-completed` frontmatter · each phase's VERIFICATION.md requirements table.

| REQ-ID | Phase | Traceability | SUMMARY frontmatter | VERIFICATION table | Final |
|--------|-------|--------------|---------------------|--------------------|-------|
| HIST-01 | 7 | [x] | 07-02, 07-06 | ✓ SATISFIED | **satisfied** |
| HIST-02 | 7 | [x] | 07-03 | ✓ SATISFIED | **satisfied** |
| HIST-03 | 7 | [x] | 07-03 | ✓ SATISFIED | **satisfied** |
| HIST-04 | 9 | [x] | 09-01…09-05 | ✓ SATISFIED | **satisfied** |
| HIST-05 | 7 | [x] | 07-04, 07-05, 07-06 | ✓ SATISFIED | **satisfied** |
| HIST-06 | 7 | [x] | 07-04 (added 12-02) | ✓ SATISFIED (added 12-02) | **satisfied** |
| HIST-07 | 10 | [x] | 10-01, 10-03, 10-04 | ✓ SATISFIED | **satisfied** |
| HIST-08 | 10 | [x] | 10-01, 10-02, 10-03 | ✓ SATISFIED | **satisfied** |
| HIST-09 | 10 | [x] | 10-01, 10-03, 10-04 | ✓ SATISFIED | **satisfied** |
| HIST-10 | 8 | [x] | 08-01, 08-02, 08-05 | ✓ SATISFIED | **satisfied** |
| HIST-11 | 11 | [x] | 11-02, 11-03 | ✓ SATISFIED | **satisfied** |
| HIST-12 | 11 | [x] | 11-01, 11-02, 11-04 | ✓ SATISFIED | **satisfied** |
| HIST-13 | 7 | [x] | 07-04 | ✓ SATISFIED | **satisfied** |

**13/13 requirements functionally satisfied.** No orphaned requirement went unbuilt; no `gaps_found` requirement.

### HIST-06 — the one traceability defect

HIST-06 (client-computed summary strip: orders, revenue, average order value, refunds count) is **functionally delivered and verified**:
- Phase 7 VERIFICATION: 4-tile summary strip present, `computeSummary(finished)` in the Level-4 data-flow trace ("✓ FLOWING"), tiles behave correctly per render state.
- Phase 9 VERIFICATION SC3: summary strip retargets from the same fetched list, no second data source.
- Phase 10 VERIFICATION D-15: Avg tile renders `formatRON(0)` not em-dash — behaviorally tested.

The **"HIST-06" label was orphaned** from the verification bookkeeping: it appeared in no VERIFICATION.md requirements table and no SUMMARY `requirements-completed` field, and Phase 7 VERIFICATION.md:101 even asserted (incorrectly, after the 2026-07-17 reassignment that moved HIST-06 to Phase 7) that it was "scoped to later phases." This was a **documentation defect, not a functional gap** — the behavior was always real and tested; only the requirement tag was never attached.

**RESOLVED (Phase 12 Plan 02, commits `b991d6e`/`44b4afe`):** `07-VERIFICATION.md`'s requirements
table now lists HIST-06 and its closing note is corrected; `07-04-SUMMARY.md`'s
`requirements-completed` frontmatter now includes HIST-06. `REQUIREMENTS.md` needed no change — it
was already correct (D-09).

### CR-01/CR-02 Numbering Reconciliation (D-08)

This audit's original tech-debt list (and Phase 12's own CONTEXT.md, which inherited the numbering)
called the **fallback-total-omits-tax** bug "CR-01." That does **not** match the bug's origin
document, `10-filters-search/10-REVIEW.md`, which defines **CR-01 as the percent-type-discount
100x-inflation bug** (`10-REVIEW.md:45`) and **CR-02 as the tax-omission bug** (`10-REVIEW.md:67`).
The fix commits follow `10-REVIEW.md`'s original numbering, not this audit's:

- `7d9810b` — `fix(10): CR-02 include tax in normalizeOrder fallback total` (the tax-omission bug
  this audit called "CR-01")
- `30c89d8` — `fix(10): CR-01 convert percent-type discount from cents to RON` (the sibling
  100x-discount bug, `10-REVIEW.md`'s actual CR-01)

**Both bugs are fixed.** This correction cites each by commit SHA + description rather than
silently relabeling either bug — a future reader diffing this audit against `10-REVIEW.md` should
see the discrepancy explained here, not conclude one of the two fixes never happened. 12-01 added
the previously-missing regression-test coverage for both paths (`normalize-order.test.js`), closing
`10-REVIEW.md`'s "completely untested" finding for `discountType`/`discountAmount`.

---

## 2. Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 7 — History Screen Foundation | ✓ passed | 4/4 + 13/13 | No gaps. UAT 26/0. HIST-06 traceability gap RESOLVED (Phase 12-02). |
| 8 — Read-Only Order Detail View | ✓ passed | 9/9 | No gaps. UAT 20/0. |
| 9 — Period Control | ✓ passed | 17/17 | WR-01 popover re-click defect RESOLVED (commit `033cc39`; live-reverified Phase 12-04 Task 2). UAT complete. |
| 10 — Filters + Search | ✓ passed | 28/30 | 2 items routed to human verification — both closed in UAT (2/0). Financial-total bugs (CR-01/CR-02 per `10-REVIEW.md` numbering) RESOLVED — see reconciliation note above. |
| 11 — Reprint + CSV Export | ✓ passed | 15/15 | 3 Manual-Only items closed in UAT (3/0). All 3 code-review findings fixed inline (commit d3d20e4). |
| 12 — Close CR-01/HIST-06/WR-01 | ✓ complete | 4/4 plans | Tech-debt closeout: D-06 regression tests backfilled (12-01), HIST-06 traceability fixed (12-02), return-from-detail state continuity lifted to store (12-03), verify-pass + live checkpoint + Nyquist promotion + this audit correction (12-04). |

All 5 v1.1 phases (7-11) carry a `status: passed` VERIFICATION.md and a `status: complete`, 0-issue
UAT.md; Phase 12 (tech-debt closeout, no new HIST requirements) is complete per its own SUMMARY.md
set. The Manual-Only / human-verification items (Phase 10: 1440×900 two-row wrap + debounce feel;
Phase 11: physical thermal reprint, CSV-in-Excel with diacritics, Tauri runtime capability grants;
Phase 12: WR-01 popover re-click + return-from-detail continuity + reset-on-leave) were all resolved
through UAT/live-checkpoint passes.

---

## 3. Cross-Phase Integration

Integration checker verdict: **12 of 12 connections wired, 0 orphaned exports, 0 hard breaks.** Both SDK entry points (`client.admin.orders.list`, `client.kitchen.orders.get`) have exactly one live call site each. The single `useHistoryOrders({from,to})` call site genuinely composes Phase 9 (period) + Phase 10 (filters) — `days` and `summary` both derive from the filtered `visible` set, so day headers and the summary strip cannot disagree with the filtered list by construction. Flow 6 below is now wired via Phase 12-03's `historySelection` store slice.

### E2E flows (6 of 6 complete)

1. ✓ Sidebar → store → router → HistoryScreen (HIST-01)
2. ✓ Row click → `openHistoryOrder` → history-detail → readOnly detail hydrated via `useOrderDetail`, merged over the summary (HIST-02→10)
3. ✓ Single shared `useHistoryOrders` composing period + filters + summary (HIST-03/04/05/06/07/08/09)
4. ✓ Reprint — `onPrint={handlePrint}` wired on history-detail route, printer-configured gate (HIST-11)
5. ✓ CSV export — `buildCsv(visible)` → native `save()`/`writeTextFile()`, capability grants present (HIST-12)
6. ✓ **Back from read-only detail → History with list + period intact** — RESOLVED (Phase 12-03, commits `e0c797a`/`7c0d726`/`4b694f7`). Period/status/type/search selection now lives in a session-only Zustand `historySelection` slice that survives the History→history-detail→Back round-trip (D-01/D-02) and resets to defaults on any other screen exit (D-03). Live-verified in Phase 12-04's Task 2 human checkpoint (approved): all four selections intact on Back, correctly filtered on the first frame, reset to 30d/All/All/empty on navigating away and back.

---

## 4. Tech Debt (ranked) — all RESOLVED except the deferred INFO item

1. **[RESOLVED] Financial-total bugs — fallback `total` omits `tax` + percent-discount 100x inflation** (`src/data.jsx`, Phase 10 review). Fixed by `7d9810b` (tax) and `30c89d8` (percent-discount) — see the CR-01/CR-02 Numbering Reconciliation note in §1 for why this audit's original "CR-01" label doesn't match `10-REVIEW.md`'s own numbering. Regression tests backfilled by Phase 12-01 (`normalize-order.test.js`). Re-verified this phase: `src/data.jsx` byte-unchanged since the Phase 12 base commit, full suite green except the 3 documented pre-existing failures.

2. **[RESOLVED] Return-from-detail state continuity** (cross-phase 07/08/09/10, closed by Phase 12-03). Period + filters/search now survive the History→detail→Back round-trip via the new session-only `historySelection` Zustand slice; reset to defaults on any other screen exit. Live-verified in this phase's Task 2 human checkpoint (approved).

3. **[RESOLVED] HIST-06 traceability gap** (Phase 7, closed by Phase 12-02). HIST-06 now appears in `07-VERIFICATION.md`'s requirements table and `07-04-SUMMARY.md`'s `requirements-completed` frontmatter; the mis-statement about scope is corrected. Doc-only fix, no behavior change.

4. **[RESOLVED] WR-01 — Custom pill popover re-click** (Phase 9, fixed by `033cc39`). The outside-click ref now wraps toggle+panel together, so re-clicking the open Custom pill closes it. Live-verified in this phase's Task 2 human checkpoint (approved): re-click, Escape, and true outside-click all close the popover correctly.

5. **[RESOLVED] G-07-1 — Rust dead_code warning** (`src-tauri/src/lib.rs`, fixed by `50492d5`). `PrintOrderData.table` field is now read (prints the table number on dine-in receipts). Re-verified this phase: `cargo check --lib` reports zero warnings.

6. **[INFO, deferred — not fixed this phase, by design] 3 pre-existing unrelated test failures** (from v1.0, Phase 6): `build-pipeline.test.js` BILD-04 (updater-artifacts config) + `offline-buttons.test.jsx` ×2 (missing `QueryClientProvider` wrapper). Documented in every phase's `deferred-items.md`; confirmed still the ONLY red in an otherwise-green suite at Phase 12's tip (492/495 passing). Explicitly out of scope per Phase 12's CONTEXT.md — left deferred, not a v1.1 regression.

---

## 5. Nyquist Validation Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|-------|---------------|--------|-------------------|----------------|--------|
| 7 | exists | validated | true | COMPLIANT | — |
| 8 | exists | validated | true | COMPLIANT | — |
| 9 | exists | validated | true | COMPLIANT | — |
| 10 | exists | **validated** | **true** | **COMPLIANT** | RESOLVED — `/gsd-validate-phase 10` (Phase 12-04 Task 3, D-10) |
| 11 | exists | **validated** | **true** | **COMPLIANT** | RESOLVED — `/gsd-validate-phase 11` (Phase 12-04 Task 3, D-10) |

Overall: **compliant.** `/gsd-validate-phase 10` and `/gsd-validate-phase 11` were run in this phase
(D-10) and promoted both from the seeded `status: draft` skeleton to `status: validated`,
`nyquist_compliant: true`. Both audits found **zero gaps** — `10-VERIFICATION.md`/`11-VERIFICATION.md`
already carried complete Per-Task evidence (28/30 and 15/15 truths verified respectively, both
phases' Manual-Only items already closed in their own `UAT.md`), so no `gsd-nyquist-auditor` spawn
or new test generation was needed; the promotion reconstructed each `VALIDATION.md`'s Per-Task
Verification Map, Wave 0 Requirements, and Manual-Only sections directly from that existing evidence
and re-derived the sign-off. All 5 v1.1 phases (7–11) are now Nyquist-compliant.

---

## 6. Verdict

All 13 v1.1 requirements are functionally delivered and independently verified; all 5 phases passed
verification and UAT with zero open issues; cross-phase wiring connects and **all 6 of 6** E2E flows
now complete cleanly (flow 6, the return-from-detail round-trip, closed by Phase 12-03). Nyquist
validation is compliant across all 5 phases (§5). The milestone is **substantively done and now
formally clean.**

**Re-derivation reasoning (D-08), item by item:**

- The lone **CRITICAL** finding (financial-total bugs, tax-omission + percent-discount) is
  **RESOLVED** — both fix commits (`7d9810b`, `30c89d8`) predate this audit's original issuance and
  were independently re-verified this phase (`src/data.jsx` byte-unchanged since Phase 12's base,
  full suite green); the previously-missing regression-test coverage was backfilled (12-01).
- The **return-from-detail state-continuity** WARNING is **RESOLVED** — `historySelection` lift
  (12-03) plus a live human checkpoint this phase (approved) confirm all four selections survive
  Back and reset correctly on other navigation.
- The **HIST-06 traceability gap** WARNING is **RESOLVED** — doc-only fix (12-02), no functional
  change was ever needed since the behavior was already delivered.
- The **WR-01 popover re-click** WARNING is **RESOLVED** — fix commit `033cc39` predates this audit's
  original issuance; re-verified this phase via a live human checkpoint (approved).
- The **G-07-1 Rust warning** WARNING is **RESOLVED** — fix commit `50492d5`; `cargo check --lib`
  reports zero warnings, re-confirmed this phase.
- The **3 pre-existing v1.0 test failures** remain the only red in the suite (INFO severity,
  explicitly out of Phase 12's scope per CONTEXT.md) — a deliberate deferral, not an open defect
  introduced by or blocking v1.1.

With every CRITICAL/WARNING item resolved, all 6 E2E flows complete, and Nyquist validation
compliant across all 5 phases, the milestone re-derives to a clean **`passed`** — no judgment-based
qualified state is warranted; nothing here rests on a PARTIAL Nyquist promotion or an unresolved
architectural question. v1.1 is ready for `/gsd-complete-milestone` (a next step, out of scope for
this phase).
