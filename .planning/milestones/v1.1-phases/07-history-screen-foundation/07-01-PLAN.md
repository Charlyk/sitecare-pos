---
phase: 07-history-screen-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/history-utils.js
  - src/data.jsx
  - src/__tests__/history-utils.test.js
  - src/__tests__/normalize-order.test.js
autonomous: true
requirements: [HIST-03, HIST-05]
must_haves:
  truths:
    # --- Goal-backward observable truths ---
    - "getLast30DaysRange() returns { from, to } ISO instants spanning local midnight 29 days ago through local midnight tomorrow — a 30-calendar-day window inclusive of today (HIST-03, D-04)"
    - "groupOrdersByDay() returns day groups sorted newest-day-first, each with orders sorted newest-first within the day, a count of every visible row, and a revenue sum of completed-only rows (HIST-05, D-10/D-11/D-12)"
    - "deriveDisplayStatus() returns 'refunded' for any order with paymentCaptureStatus === 'refunded' regardless of status, 'canceled' for status CANCELLED, 'completed' for status COMPLETED, and null otherwise (D-02)"
    - "filterFinishedOrders() keeps only orders whose status is COMPLETED or CANCELLED; every in-flight status (NEW/ACCEPTED/PREPARING/READY/OUT_FOR_DELIVERY) is dropped (D-01)"
    - "normalizeOrder() resolves dailyOrderNumber as o.dailyOrderNumber ?? o.dailyNumber ?? o.id — an AdminOrder carrying only dailyNumber yields that number, never the UUID (D-05, RESEARCH Pitfall 2)"
    - "Every existing test that exercises normalizeOrder against the kitchen Order shape still passes — the dailyNumber fallback is additive and changes no existing behavior"
    # --- Edge probe lift: HIST-05 / boundary (covered) ---
    - "An order with createdAt at exactly 00:00:00.000 local time groups under that new day, and 23:59:59.999 local groups under the day that is ending — bucketing uses local Date getters (getFullYear/getMonth/getDate), never an ISO-string slice, which would bucket in UTC"
    # --- Edge probe lift: HIST-05 / adjacency (covered) ---
    - "Two orders one millisecond apart across a local-midnight boundary land in two separate day groups with two separate headers, never merged into one group"
    # --- Edge probe lift: HIST-05 / empty (covered) ---
    - "groupOrdersByDay([]) returns []; a single order returns exactly one group with count 1; an order whose createdAt is absent or unparseable is excluded from grouping rather than producing a NaN-keyed day header, and no call throws"
    # --- Edge probe lift: HIST-05 / ordering (covered) ---
    - "When two orders within the same day share an identical createdAt, their relative order is stable — the comparator returns 0 for ties and input order is preserved"
    # --- Edge probe lift: HIST-05 / precision (BACKSTOP) ---
    - statement: "Day-header revenue and summary-strip revenue equal the sum of that period's completed orders' real RON amounts as shown in the SiteCare admin dashboard — i.e. AdminOrder.total is in cents and normalizeOrder's ÷100 conversion is correct, not off by 100×"
      verification: backstop
    # --- Precision, formatting half (covered) ---
    - "Revenue sums are rendered through formatRON (Intl.NumberFormat ro-RO, exactly 2 fraction digits), so float accumulation across many cents-derived values never surfaces as a long decimal tail"
  artifacts:
    - "src/history-utils.js — named exports getLast30DaysRange, filterFinishedOrders, deriveDisplayStatus, groupOrdersByDay, computeSummary; no React and no SDK imports"
    - "src/__tests__/history-utils.test.js — unit tests for all five exports"
    - "src/__tests__/normalize-order.test.js — unit tests for the dailyOrderNumber fallback chain"
    - "src/data.jsx — normalizeOrder() fallback chain extended with o.dailyNumber"
  key_links:
    - "history-utils.js consumes ALREADY-normalized orders (order.total in RON post-cRON, order.placedAt present) — it must never re-divide by 100 nor re-implement normalization"
    - "data.jsx normalizeOrder is the single normalization chokepoint shared with shipped v1.0 screens (use-orders.js, use-sse.js) — the dailyNumber change must be additive only"
  prohibitions:
    - requirement_id: HIST-05
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present a revenue figure that includes money the restaurant did not keep — a canceled or refunded order must contribute zero to any day-header or summary revenue sum, so a shift total can never read higher than what was actually collected"
---

<objective>
Build `src/history-utils.js` — the pure, React-free, SDK-free derivation layer that every other
Phase 7 plan depends on — and fix the `dailyNumber` field-name mismatch at the single
normalization chokepoint in `src/data.jsx`.

Purpose: D-01/D-02/D-04/D-10/D-11/D-12 are all client-side derivation rules with directly testable
behavior contracts. Isolating them from React makes them the phase's Nyquist Wave-0 target and
keeps `screen-history.jsx` free of business logic.

Output: `src/history-utils.js`, `src/__tests__/history-utils.test.js`,
`src/__tests__/normalize-order.test.js`, and one additive line change in `src/data.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-history-screen-foundation/07-CONTEXT.md
@.planning/phases/07-history-screen-foundation/07-RESEARCH.md
@.planning/phases/07-history-screen-foundation/07-PATTERNS.md
@.planning/phases/07-history-screen-foundation/07-VALIDATION.md
</context>

## Artifacts this phase produces

New symbols created by Plan 01 (excluded from cross-file drift verification — they do not exist yet):

| Symbol | Kind | File |
|---|---|---|
| `getLast30DaysRange(now?)` | function (named export) | `src/history-utils.js` |
| `filterFinishedOrders(orders)` | function (named export) | `src/history-utils.js` |
| `deriveDisplayStatus(order)` | function (named export) | `src/history-utils.js` |
| `groupOrdersByDay(orders)` | function (named export) | `src/history-utils.js` |
| `computeSummary(orders)` | function (named export) | `src/history-utils.js` |
| `localDayKey(iso)` | module-private helper | `src/history-utils.js` |

Phase-wide artifact inventory (all plans) is listed in `07-05-PLAN.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for history-utils (Wave 0 scaffold)</name>
  <files>src/__tests__/history-utils.test.js</files>
  <read_first>
    - `.planning/phases/07-history-screen-foundation/07-RESEARCH.md` — "Code Examples" section is the implementation contract these tests encode
    - `.planning/phases/07-history-screen-foundation/07-CONTEXT.md` — D-01, D-02, D-04, D-10, D-11, D-12
    - `src/__tests__/use-orders.test.js` — `describe`/`test` + `expect` conventions used across this suite (globals are enabled via `vitest.config.js`; no imports of `test`/`expect` are needed)
    - `vitest.config.js` — confirms `globals: true`, `environment: 'jsdom'`, `setupFiles: ['./src/__tests__/setup.js']`
  </read_first>
  <behavior>
    Tests for `src/history-utils.js` (which does not exist yet — this task's tests MUST fail RED):

    `getLast30DaysRange(now)` — pass a fixed `now` so the test is deterministic:
    - Returns an object with exactly two string keys, `from` and `to`, both parseable by `new Date()`.
    - `from` parses back to local midnight 29 days before `now`'s local calendar date (assert via
      `new Date(from).getHours() === 0` and `getDate()`/`getMonth()`/`getFullYear()` equal to
      `new Date(now.getFullYear(), now.getMonth(), now.getDate() - 29)`).
    - `to` parses back to local midnight of the day AFTER `now`'s local date (exclusive upper bound).
    - The window spans 30 distinct local calendar days inclusive of `now`'s day.

    `deriveDisplayStatus(order)` — D-02 precedence:
    - `{ status: 'COMPLETED', paymentCaptureStatus: 'refunded' }` → `'refunded'` (refunded wins).
    - `{ status: 'CANCELLED', paymentCaptureStatus: 'refunded' }` → `'refunded'`.
    - `{ status: 'CANCELLED', paymentCaptureStatus: 'captured' }` → `'canceled'`.
    - `{ status: 'COMPLETED', paymentCaptureStatus: 'captured' }` → `'completed'`.
    - `{ status: 'COMPLETED', paymentCaptureStatus: null }` → `'completed'` (cash orders carry null).
    - `{ status: 'PREPARING', paymentCaptureStatus: null }` → `null`.

    `filterFinishedOrders(orders)` — D-01:
    - Keeps `COMPLETED` and `CANCELLED`; drops `NEW`, `ACCEPTED`, `PREPARING`, `READY`,
      `OUT_FOR_DELIVERY`.
    - `filterFinishedOrders([])` returns `[]`.
    - Does not mutate its input array (assert the input's `length` is unchanged and the returned
      array is not the same reference).

    `groupOrdersByDay(orders)` — build fixtures with `new Date(y, m, d, h, min).toISOString()` so
    they are local-time-anchored and TZ-agnostic:
    - Days sorted newest-first; orders within a day sorted newest-first (D-12).
    - `count` includes every row in the day, including canceled and refunded (D-11).
    - `revenue` sums only rows whose `deriveDisplayStatus` is `'completed'` (D-10); a day of only
      canceled/refunded rows yields a non-zero `count` and `revenue === 0`.
    - Local-boundary edge: an order at `new Date(2026, 6, 2, 0, 0, 0, 0)` and one at
      `new Date(2026, 6, 1, 23, 59, 59, 999)` land in TWO groups with keys `2026-07-02` and
      `2026-07-01`, never one.
    - Tie stability: two orders in the same day with an identical `placedAt` keep their input order.
    - `groupOrdersByDay([])` returns `[]`; a single order returns one group with `count === 1`.
    - An order with `placedAt`/`createdAt` absent or unparseable (`'not-a-date'`) is excluded from
      the output and the call does not throw.
    - Does not mutate the input array's element order.

    `computeSummary(orders)` — D-15:
    - `ordersCount` counts all passed (finished) orders.
    - `revenue` sums completed-only totals; a canceled and a refunded order contribute 0.
    - `avg` is `revenue / completedCount`, and is `null` when there are zero completed orders.
    - `refundsCount` and `canceledCount` are counts derived via `deriveDisplayStatus`.
    - `computeSummary([])` returns `{ ordersCount: 0, revenue: 0, avg: null, refundsCount: 0, canceledCount: 0 }`.
  </behavior>
  <action>
    Create `src/__tests__/history-utils.test.js` importing the five named exports from
    `../history-utils.js`: `getLast30DaysRange`, `filterFinishedOrders`, `deriveDisplayStatus`,
    `groupOrdersByDay`, `computeSummary`.

    Group the assertions under five `describe` blocks, one per export, encoding every case listed in
    `<behavior>`. Use plain object literals shaped like a POST-`normalizeOrder` order — that is, the
    `AdminOrder` fields (`id`, `status`, `paymentCaptureStatus`, `createdAt`, `total`) PLUS the
    normalized additions `placedAt` (string ISO) and `total` already in RON (e.g. `total: 45.5`, NOT
    `4550`). `history-utils.js` operates downstream of `normalizeOrder`, so fixtures must reflect
    that; never write a fixture with a cents-valued `total`.

    Vitest globals are enabled — do not import `describe`/`test`/`expect`. No mocks, no
    `renderHook`, no `QueryClientProvider`: these are plain function calls.

    Anchor every timestamp fixture with the local-Date constructor form
    `new Date(2026, 6, 2, 14, 30).toISOString()` (month is 0-indexed), never a hand-written UTC
    string literal — a `'2026-07-02T14:30:00Z'` literal would make the test's expected day key
    depend on the machine's timezone and produce a flaky suite.

    The test file MUST fail at this point because `src/history-utils.js` does not exist. That RED is
    the expected outcome of this task.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/history-utils.test.js 2>&1 | grep -qiE "Failed to load|Cannot find module|history-utils" &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/history-utils.test.js` exists
    - `grep -c "describe" src/__tests__/history-utils.test.js` returns at least 5
    - The file references all five export names: `getLast30DaysRange`, `filterFinishedOrders`, `deriveDisplayStatus`, `groupOrdersByDay`, `computeSummary`
    - `grep -c "new Date(20" src/__tests__/history-utils.test.js` is greater than 0 (local-constructor fixtures present)
    - `npx vitest run src/__tests__/history-utils.test.js` exits non-zero (RED — module under test does not exist yet)
    - No fixture assigns a `total` in cents: every `total:` literal in the file is a RON-scale value
  </acceptance_criteria>
  <done>The test file encodes every behavior in `<behavior>` and fails because `src/history-utils.js` is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/history-utils.js and turn the suite GREEN</name>
  <files>src/history-utils.js</files>
  <read_first>
    - `src/__tests__/history-utils.test.js` — the contract just written (Task 1)
    - `.planning/phases/07-history-screen-foundation/07-RESEARCH.md` — "Code Examples" supplies pre-verified reference implementations for all five functions
    - `.planning/phases/07-history-screen-foundation/07-PATTERNS.md` — "No Analog Found" section: this module has no structural analog; follow the codebase's named-exports-no-default convention (as in `src/data.jsx`, `src/use-orders.js`)
    - `src/data.jsx` — the `formatRON` / `cRON` conventions this module must NOT duplicate
  </read_first>
  <behavior>
    Implements exactly the contract asserted by `src/__tests__/history-utils.test.js` (Task 1).
    No behavior beyond what those tests assert.
  </behavior>
  <action>
    Create `src/history-utils.js` with five named exports and no default export:

    - `getLast30DaysRange(now = new Date())` — returns `{ from, to }`. `from` is
      `new Date(now.getFullYear(), now.getMonth(), now.getDate() - 29, 0, 0, 0, 0).toISOString()`;
      `to` is `new Date(now.getFullYear(), now.getMonth(), now.getDate() + 1, 0, 0, 0, 0).toISOString()`
      (exclusive upper bound = start of tomorrow, local). The `now` parameter exists so tests can
      inject a fixed clock. Per D-04 these are local-Romanian-day boundaries converted to
      unambiguous ISO instants; do not send bare date strings.
    - `filterFinishedOrders(orders)` — returns a new array keeping only `status === 'COMPLETED' ||
      status === 'CANCELLED'` (D-01). Never mutates the input.
    - `deriveDisplayStatus(order)` — precedence order is load-bearing (D-02): check
      `paymentCaptureStatus === 'refunded'` FIRST and return `'refunded'`; then `'CANCELLED'` →
      `'canceled'`; then `'COMPLETED'` → `'completed'`; else `null`.
    - `groupOrdersByDay(orders)` — returns an array of `{ dayKey, orders, count, revenue }`.
      Bucket with a module-private `localDayKey(iso)` returning
      `` `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}` `` from local Date getters —
      NEVER `iso.slice(0, 10)`, which buckets in UTC and misfiles orders near midnight
      (RESEARCH Pitfall 3). Read each order's timestamp as `order.placedAt ?? order.createdAt`
      (`normalizeOrder` sets `placedAt`; raw `AdminOrder` carries `createdAt`).
      Guard the timestamp: if it is absent, or `new Date(ts)` yields `Number.isNaN(d.getTime())`,
      SKIP that order entirely rather than emitting a `NaN-NaN-NaN` day header. Sort day entries by
      `dayKey` descending (`b.localeCompare(a)` — the zero-padded key sorts lexicographically =
      chronologically). Within each day sort by timestamp descending using a comparator that returns
      0 on ties (`Array.prototype.sort` is stable in V8, so input order survives a tie). `count` is
      the day's row count including canceled and refunded (D-11). `revenue` sums `o.total` for rows
      where `deriveDisplayStatus(o) === 'completed'` only (D-10). Copy arrays before sorting; never
      mutate the caller's array.
    - `computeSummary(orders)` — returns `{ ordersCount, revenue, avg, refundsCount, canceledCount }`
      per D-15. `revenue` is the completed-only sum (identical rule to `groupOrdersByDay`, so tiles
      and day headers agree by construction). `avg` is `revenue / completedCount`, or `null` when
      `completedCount === 0` (the UI renders `—` for null). `refundsCount` counts
      `deriveDisplayStatus === 'refunded'`; `canceledCount` counts `'canceled'`.

    Critical: this module operates on ALREADY-normalized orders. `order.total` arrives in RON
    (`normalizeOrder` already applied `cRON`). Do NOT divide by 100 anywhere in this file, and do NOT
    import anything from `data.jsx`, `react`, or `@charlyk/admin-client` — the file must stay pure so
    it is unit-testable without a DOM or a mock client.

    Add a short head comment naming the decisions each function implements (D-01, D-02, D-04, D-10,
    D-11, D-12, D-15) so a future reader can trace behavior back to CONTEXT.md.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/history-utils.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/history-utils.test.js` exits 0 with all tests passing
    - `grep -c "^export function\|^export const" src/history-utils.js` returns 5
    - `grep -c "export default" src/history-utils.js` returns 0
    - `grep -v '^//' src/history-utils.js | grep -c "from 'react'\|from './data.jsx'\|@charlyk" ` returns 0 (module stays pure)
    - `grep -v '^ *//' src/history-utils.js | grep -c "/ 100\|/100"` returns 0 (no re-conversion of cents)
    - `grep -v '^ *//' src/history-utils.js | grep -c "slice(0, 10)\|slice(0,10)"` returns 0 (no UTC-slicing day keys)
    - `npx vitest run` exits 0 (no existing test regressed)
  </acceptance_criteria>
  <done>All five exports implemented; `history-utils.test.js` is GREEN and the full suite still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend normalizeOrder's dailyOrderNumber fallback for AdminOrder.dailyNumber</name>
  <files>src/data.jsx, src/__tests__/normalize-order.test.js</files>
  <read_first>
    - `src/data.jsx` — the current `normalizeOrder()` at line 195, specifically the `dailyOrderNumber` line at 220 and the `cRON` convention at 196
    - `.planning/phases/07-history-screen-foundation/07-PATTERNS.md` — "`src/data.jsx` (MODIFIED)" section supplies the exact before/after
    - `.planning/phases/07-history-screen-foundation/07-RESEARCH.md` — Pitfall 2, and the SDK verification `node_modules/@charlyk/admin-client/dist/index.d.ts:267` (`dailyNumber: number | null`)
    - `src/use-orders.js` — the shipped v1.0 caller of `normalizeOrder` whose behavior must not change
  </read_first>
  <behavior>
    - An `AdminOrder`-shaped input `{ id: 'uuid-abc', dailyNumber: 12 }` normalizes to
      `dailyOrderNumber === 12`.
    - A kitchen `Order`-shaped input `{ id: 'uuid-abc', dailyOrderNumber: 7 }` still normalizes to
      `dailyOrderNumber === 7` (unchanged shipped behavior).
    - When BOTH are present, `dailyOrderNumber` wins (it is first in the chain).
    - `{ id: 'uuid-abc', dailyNumber: null }` falls through to `dailyOrderNumber === 'uuid-abc'`.
    - `{ id: 'uuid-abc' }` with neither field falls through to the UUID.
    - `dailyNumber: 0` yields `0`, not the UUID — `??` must be used, not `||`.
  </behavior>
  <action>
    Change exactly one line in `src/data.jsx`'s `normalizeOrder()` (currently line 220):
    `dailyOrderNumber: o.dailyOrderNumber ?? o.id,` becomes
    `dailyOrderNumber: o.dailyOrderNumber ?? o.dailyNumber ?? o.id,`.

    This is per D-05 and RESEARCH Pitfall 2: `AdminOrder` names the field `dailyNumber`, while the
    kitchen `Order` shape names it `dailyOrderNumber`. Without this, every History row silently
    renders a UUID instead of the real order number. The change is deliberately additive at the
    single shared normalization chokepoint — do NOT create a parallel history-specific normalizer,
    and do NOT reorder the chain (`dailyOrderNumber` must stay first so the shipped kitchen path is
    byte-identical). Use `??` and not `||` so a legitimate `dailyNumber: 0` is preserved.

    Make no other change to `data.jsx`.

    Then create `src/__tests__/normalize-order.test.js` importing `normalizeOrder` from
    `../data.jsx`, asserting each case in `<behavior>`. Vitest globals are enabled — do not import
    `test`/`expect`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/normalize-order.test.js &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "o.dailyOrderNumber ?? o.dailyNumber ?? o.id" src/data.jsx` returns 1
    - `grep -c "dailyOrderNumber: o.dailyOrderNumber ?? o.id," src/data.jsx` returns 0
    - `git diff --stat src/data.jsx` shows exactly 1 insertion and 1 deletion
    - `src/__tests__/normalize-order.test.js` exists and `npx vitest run src/__tests__/normalize-order.test.js` exits 0
    - `npx vitest run` exits 0 — every pre-existing test still passes (the change is additive)
  </acceptance_criteria>
  <done>`normalizeOrder` resolves `AdminOrder.dailyNumber`; new tests pass and the full shipped suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SiteCare API → client | `AdminOrder` records (attacker-influenceable `customerName`, plus `status`, `total`, `createdAt`) cross into client-side derivation |
| system clock → client | The terminal's OS timezone/clock drives every local-day boundary computed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Denial of Service | `groupOrdersByDay()` on a malformed `createdAt` | low | mitigate | Timestamp guard: absent or `Number.isNaN(new Date(ts).getTime())` → order skipped, never a thrown exception or a `NaN-NaN-NaN` day header that would blank the screen |
| T-07-02 | Tampering | `normalizeOrder()` shared with shipped v1.0 screens | medium | mitigate | Change is a single additive `??` link mid-chain with `dailyOrderNumber` still first; acceptance criteria pin the diff to 1 insertion / 1 deletion and require the full shipped suite (`npx vitest run`) green |
| T-07-03 | Repudiation | Day-revenue sums used by staff to reconcile against the till | medium | mitigate | Revenue is completed-only (D-10) and derived by a single rule shared by `groupOrdersByDay` and `computeSummary`, so tiles and headers cannot disagree; unit tests pin an all-canceled day to `revenue === 0` |
| T-07-04 | Information Disclosure | `history-utils.js` | low | accept | The module handles PII fields (`customerName`) only as opaque pass-through values; it neither logs, serializes, nor persists them |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install in this phase — RESEARCH.md "Package Legitimacy Audit" records the gate as not applicable; every library used is already pinned in `package.json` from prior phases. No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `npx vitest run src/__tests__/history-utils.test.js` — GREEN
- `npx vitest run src/__tests__/normalize-order.test.js` — GREEN
- `npx vitest run` — full suite GREEN (no regression in the 166 shipped tests)
- `src/history-utils.js` imports nothing from `react`, `data.jsx`, or `@charlyk/admin-client`
</verification>

<success_criteria>
- Five pure named exports exist in `src/history-utils.js` with full unit coverage
- `normalizeOrder` resolves `AdminOrder.dailyNumber` without altering the kitchen-order path
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/07-history-screen-foundation/07-01-SUMMARY.md` when done
</output>
