---
phase: 07-history-screen-foundation
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/screen-history.jsx
  - src/__tests__/screen-history.test.jsx
autonomous: true
requirements: [HIST-05, HIST-13]
must_haves:
  truths:
    # --- Goal-backward observable truths ---
    - "HistoryScreen calls useHistoryOrders() itself — no server data is prop-drilled from App() (the codebase's screen-owns-its-hook convention)"
    - "Rows are grouped under day headers, newest day first, each header showing that day's row count and completed-only revenue subtotal (HIST-05)"
    - "Only finished orders render — filterFinishedOrders drops every in-flight order before grouping (D-01)"
    - "Each row's Status column shows exactly one chip: chip-sage Completed, chip-red Canceled, or chip-amber Refunded, derived by deriveDisplayStatus (D-02)"
    - "The table grid has 8 tracks (7 data columns + a chevron track) with no blank cells: 150px minmax(200px, 1.8fr) 118px 78px 116px 132px 116px 34px (D-06)"
    - "The Order column renders #dailyOrderNumber; a null dailyNumber falls back to a short UUID slice (D-05)"
    - "The chevron is a static chevRight icon that never rotates — rows navigate, they do not expand (D-07); HistoryReceiptRow from the design source is not ported"
    - "Clicking a row calls the onOpenOrder prop with that order (D-08)"
    - "Summary tiles are computed client-side by computeSummary from the same fetched list that backs the rows — getAdminDashboard is not called anywhere (D-15)"
    - "The full filter bar renders visible, dimmed, and inert with no onClick/onChange handlers, except the 30-day preset which renders selected at full opacity (D-14)"
    # --- UI-SPEC ## UI Considerations lift: E1 day-grouped order list ---
    - "E1/empty: an empty result renders the h_empty/h_empty_sub two-line block inside the table card in place of all rows and day-groups (HIST-13, D-13)"
    - "E1/loading: 6-8 skeleton rows render at the exact real row height (13px/20px padding, single line) with no day-group headers (D-16)"
    - "E1/error: the table body is replaced by a centered h_error_title + check_connection + h_retry button while the header row still renders (D-16)"
    - "E1/populated: every finished order in the period renders — no cap, no warning, no virtualization (D-03)"
    - "E1/partial: a row whose dailyNumber is null renders a short UUID slice rather than a blank Order cell (D-05)"
    - "E1/overflow: the Customer column applies white-space: nowrap + overflow: hidden + text-overflow: ellipsis; the list scrolls vertically"
    - "E1/zero-one-many: zero rows renders the empty state; a 1-order day header reads '1 comandă' via h_orders_count_one, never '1 comenzi'"
    - "E1/long-text: a long customer name truncates with an ellipsis in the space-constrained row rather than wrapping or widening the track"
    # --- UI-SPEC ## UI Considerations lift: E1/partial customerName (BACKSTOP) ---
    - statement: "The Customer cell renders a readable placeholder, never a visually empty cell, when customerName is absent or an empty string. AdminOrder types customerName as non-nullable, so this is treated as possible rather than assumed away."
      verification: backstop
    # --- UI-SPEC ## UI Considerations lift: E2 day-group header ---
    - "E2/loading: no day-group header renders during loading — skeleton rows are ungrouped (D-16)"
    - "E2/populated: each header is a flex space-between row with the day label left and '{count} {noun} · {revenue}' right (Day-Group Header Contract)"
    - "E2/overflow: the header uses flex space-between with a short localized date left and count·revenue right — neither side overflows the other"
    - "E2/zero-one-many: plural handling is explicit — h_orders_count_one vs h_orders_count_other"
    # --- UI-SPEC ## UI Considerations lift: E2/partial all-canceled day (BACKSTOP) ---
    - statement: "A day containing only canceled and/or refunded rows renders its true non-zero row count alongside 0,00 lei revenue — correct by construction under D-10 + D-11, but it reads as a bug if either half is wrong."
      verification: backstop
    # --- UI-SPEC ## UI Considerations lift: E3 summary strip ---
    - "E3/empty: a successfully loaded but empty list renders computed zeros (0, 0,00 lei, — for avg, 0), NOT dashes — dashes are reserved for the error state"
    - "E3/loading: placeholder shimmer blocks render at identical tile dimensions, so there is no layout jump when data arrives (D-16)"
    - "E3/error: all 4 tiles show an em-dash with neutral grey icon tiles, visually distinct from the empty state's colored zeros (D-16)"
    - "E3/populated: 4 tiles — Orders, Revenue, Avg, Refunds — all derived from the same fetched list; the Refunds tile is count-only with no amount (D-15)"
    - "E3/zero-one-many: tiles render zeros at zero; the count-only Refunds tile carries no amount to pluralize"
    # --- UI-SPEC ## UI Considerations lift: E3/overflow (BACKSTOP) ---
    - statement: "A large revenue or average-order-value figure (wide period, high volume) must not overflow or wrap its fixed-dimension summary tile."
      verification: backstop
    # --- UI-SPEC ## UI Considerations lift: E5 empty state ---
    - "E5/empty: the empty state is the h_empty heading (15px/600) + h_empty_sub body (13px/400) centered with 48-56px padding, matching the shipped OrdersScreen empty-state pattern (D-13)"
    - "E5/long-text: copy is authored in both ro and en; ro is the longer locale and fits the centered block"
  artifacts:
    - "src/screen-history.jsx — named export HistoryScreen"
    - "src/__tests__/screen-history.test.jsx — render tests for the loading, error, empty, and populated states"
  key_links:
    - "HistoryScreen → useHistoryOrders() (Plan 03) → filterFinishedOrders/groupOrdersByDay/computeSummary (Plan 01): the screen must contain NO derivation logic of its own"
    - "typeMeta is imported from ./screen-orders.jsx (already exported at line 310) — do not re-derive an order-type map"
    - "formatRON and orderTimeLabel come from ./data.jsx — do not write a second money or time formatter"
    - "Every user-facing string resolves through useT(lang) against keys added in Plan 02 — the screen adds no key to i18n.jsx"
  prohibitions:
    - requirement_id: HIST-05
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT silently omit, cap, truncate, or virtualize away any finished order in the loaded period — a list staff use to reconcile a shift must render every row it has, and any future omission must be visible to the reader rather than inferred from a short scroll"
---

<objective>
Build `src/screen-history.jsx` — the History screen: a client-computed summary strip, an inert
filter bar, and a day-grouped order table with loading, error, and empty states.

Purpose: HIST-05 and HIST-13 land here, plus the D-14/D-15/D-16 surfaces CONTEXT.md absorbed into
Phase 7. This is the phase's largest visual surface and the one place the pixel-perfect-port rule
binds hardest.

Output: `src/screen-history.jsx` and `src/__tests__/screen-history.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-history-screen-foundation/07-CONTEXT.md
@.planning/phases/07-history-screen-foundation/07-UI-SPEC.md
@.planning/phases/07-history-screen-foundation/07-PATTERNS.md
@.planning/phases/07-history-screen-foundation/07-VALIDATION.md
@.planning/phases/07-history-screen-foundation/07-01-SUMMARY.md
@.planning/phases/07-history-screen-foundation/07-02-SUMMARY.md
@.planning/phases/07-history-screen-foundation/07-03-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Plan 04:

| Symbol | Kind | File |
|---|---|---|
| `HistoryScreen({ lang, onOpenOrder, isOffline })` | React component (named export) | `src/screen-history.jsx` |
| `historyStatusMeta(displayStatus, t)` | module-private lookup helper | `src/screen-history.jsx` |
| `HIST_GRID` | module-private grid-template constant | `src/screen-history.jsx` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing render tests for HistoryScreen's four states (Wave 0 scaffold)</name>
  <files>src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/__tests__/screen-orders.test.jsx` — the exact analog: render + query-by-text conventions for `screen-*.jsx` components
    - `src/__tests__/use-orders.test.js` — the `vi.mock` convention for mocking a hook module
    - `.planning/phases/07-history-screen-foundation/07-UI-SPEC.md` — Loading/Error State Contract, Empty State Contract, Summary Strip Contract, Row & Table Layout Contract, and the E1/E2/E3/E5 UI Considerations tables
    - `src/i18n.jsx` — the exact ro copy for `h_empty`, `h_empty_sub`, `h_error_title`, `h_retry`, `h_col_*`, `h_orders_count_one/_other` (added in Plan 02)
    - `src/history-utils.js` — the shape `groupOrdersByDay`/`computeSummary` return (Plan 01)
  </read_first>
  <behavior>
    Tests for `src/screen-history.jsx` (which does not exist yet — MUST fail RED). Mock
    `../use-history-orders.js` with `vi.mock` so each state is driven by the hook's return value.

    Loading (`{ isLoading: true }`):
    - Skeleton rows render (assert a stable `data-testid="history-skeleton-row"` count between 6 and 8).
    - No day-group header renders (query `data-testid="history-day-header"` → 0 results).
    - The 4 summary tiles are present in the DOM (the strip does not disappear).
    - The column header labels (`h_col_order` ro copy "Comandă", etc.) still render.

    Error (`{ isError: true, error: new Error('boom') }`):
    - `h_error_title` ro copy ("Nu am putut încărca istoricul") renders.
    - The reused `check_connection` copy renders.
    - An `h_retry` ("Reîncearcă") button renders; clicking it calls the hook's `refetch` (assert the
      mocked `refetch` was called once).
    - The raw error text 'boom' does NOT appear anywhere in the rendered output.
    - The 4 summary tiles each show an em-dash `—`.

    Empty (`{ data: [], isSuccess: true }`):
    - `h_empty` and `h_empty_sub` ro copy render.
    - No `history-day-header` and no row renders.
    - The summary tiles show computed ZEROS (assert "0" is present and `—` is NOT present as a tile
      value) — this is the covered distinction between "no data yet" and "failed to load".

    Empty (in-flight-only): `data` containing only orders with `status: 'PREPARING'` → the empty
    state renders (D-01 filters them out before grouping).

    Populated: build normalized fixtures (totals in RON, `placedAt` set via
    `new Date(y, m, d, h, min).toISOString()`) spanning two local days:
    - Two `history-day-header` elements render, newest day first (assert DOM order).
    - A day with 3 rows (2 completed at 10.00 and 20.00, 1 canceled at 99.00) renders count "3" and
      revenue "30,00 lei" — the canceled order contributes nothing (D-10/D-11).
    - A day with exactly 1 order renders the `h_orders_count_one` noun ("comandă"), not "comenzi".
    - A day of only canceled/refunded rows renders its true count with "0,00 lei".
    - A refunded order (`status: 'COMPLETED'`, `paymentCaptureStatus: 'refunded'`) renders the
      `h_status_refunded` chip and NOT a Completed chip (D-02).
    - An order with `dailyNumber: null` (post-normalize: `dailyOrderNumber` equals its UUID) renders
      a short slice, not the full 36-char UUID.
    - An order with `customerName: ''` renders a non-empty Customer cell (the E1 backstop).
    - Clicking a row calls the `onOpenOrder` prop exactly once with that row's order object.
    - The filter bar's controls are inert: query the Export button and the search input and assert
      each is `disabled`.

    All copy assertions use the ro locale (`lang="ro"`), matching the file's canonical locale.
  </behavior>
  <action>
    Create `src/__tests__/screen-history.test.jsx` mirroring `src/__tests__/screen-orders.test.jsx`'s
    render conventions. Mock the data hook:
    `vi.mock('../use-history-orders.js', () => ({ useHistoryOrders: vi.fn() }))`, then per test
    `useHistoryOrders.mockReturnValue({ data, isLoading, isError, error, refetch })`.

    Because the hook is mocked, no `QueryClientProvider` wrapper and no mock SDK client are needed —
    that is the point of the screen-owns-its-hook split.

    Fixtures must be POST-`normalizeOrder` shaped: `total` in RON (`10.00`, not `1000`), `placedAt`
    present, `dailyOrderNumber` resolved, plus the raw fields the derivation functions read
    (`status`, `paymentCaptureStatus`, `id`, `customerName`, `type`, `payment`). Anchor every
    timestamp with `new Date(2026, 6, 2, 14, 30).toISOString()` (month 0-indexed) — never a UTC
    string literal, which would make day-grouping assertions timezone-dependent and flaky.

    Add the `data-testid` hooks the assertions need (`history-skeleton-row`, `history-day-header`,
    `history-row`) to the expectations here; Task 2 implements them.

    Encode every case in `<behavior>`. Vitest globals are enabled — do not import `describe`/`test`/
    `expect`/`vi`.

    The file MUST fail because `src/screen-history.jsx` does not exist. That RED is expected.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx 2>&1 | grep -qiE "Failed to load|Cannot find module|screen-history" &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/screen-history.test.jsx` exists
    - It references all four states: `isLoading`, `isError`, an empty `data: []` case, and a populated case
    - `grep -c "history-day-header\|history-skeleton-row\|history-row" src/__tests__/screen-history.test.jsx` is at least 3
    - `grep -c "new Date(20" src/__tests__/screen-history.test.jsx` is greater than 0 (local-constructor fixtures)
    - No fixture assigns a cents-scale `total`
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits non-zero (RED — screen does not exist yet)
  </acceptance_criteria>
  <done>The test file encodes all four render states and fails because `src/screen-history.jsx` is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the day-grouped table, rows, and the loading/error/empty states</name>
  <files>src/screen-history.jsx</files>
  <read_first>
    - `src/__tests__/screen-history.test.jsx` — the contract just written (Task 1)
    - `src/screen-orders.jsx` — the exact analog (310 lines): imports (1-5), `typeMeta`/`stateMeta` maps (7-33), the `content-pad` wrapper (207), and the empty-state block (290-304). Note `typeMeta` is already exported at line 310 — import it, do not copy it.
    - `sitecare-orders/project/src/screen-history.jsx` — the design source: port its structure and styling verbatim. Its `HIST_GRID` (line 14) is superseded by D-06 and `HistoryReceiptRow` (lines 20-86) is dropped entirely by D-07. Its `window.*` globals and `useStateHist` aliasing are prototype-era — production uses named ES imports.
    - `.planning/phases/07-history-screen-foundation/07-UI-SPEC.md` — Row & Table Layout Contract (the 8-track grid and the per-column table), Day-Group Header Contract, Loading/Error State Contract, Empty State Contract, Typography and Spacing tables
    - `src/styles.css` — the existing `card`, `chip`, `chip-sage`, `chip-red`, `chip-amber`, `chip-slate`, `btn-secondary`, `content-pad` classes (no new CSS class is needed this phase)
    - `src/history-utils.js` — `filterFinishedOrders`, `deriveDisplayStatus`, `groupOrdersByDay` (Plan 01)
    - `src/data.jsx` — `formatRON` (175) and `orderTimeLabel` (183)
  </read_first>
  <behavior>
    Implements the loading, error, empty, and populated table behaviors asserted in
    `src/__tests__/screen-history.test.jsx` (Task 1). The summary strip and filter bar land in Task 3.
  </behavior>
  <action>
    Create `src/screen-history.jsx` exporting `HistoryScreen({ lang, onOpenOrder, isOffline })`.

    Data flow — the screen calls its own hook and does zero derivation inline:
    `const { data, isLoading, isError, refetch } = useHistoryOrders();` then
    `const finished = useMemo(() => filterFinishedOrders(data ?? []), [data]);` and
    `const days = useMemo(() => groupOrdersByDay(finished), [finished]);`. Note `OrdersScreen`
    receives `orders` as a prop from `App()` — that is a legacy exception; follow the stated
    screen-owns-its-hook convention here, per RESEARCH's architecture diagram.

    Module-private constant `HIST_GRID` — the D-06 8-track rebalance replacing the design source's
    9-track definition:
    `'150px minmax(200px, 1.8fr) 118px 78px 116px 132px 116px 34px'`
    (order, customer, type, time, payment, status, total, chevron). No blank cells — the dropped
    items-count track and the dropped source/address sub-lines are absorbed by widening the customer
    track from `1.6fr`/min-150px to `1.8fr`/min-200px.

    Module-private `historyStatusMeta(displayStatus, t)` returning `{ chip, label, icon }`:
    `completed` → `chip-sage` + `t('status_completed')`, `canceled` → `chip-red` +
    `t('status_canceled')`, `refunded` → `chip-amber` + `t('status_refunded')`. Precedence is NOT
    this function's job — `deriveDisplayStatus` (Plan 01) already resolved it; this map only picks
    the chip class. Do not write an order-type map: import `typeMeta` from `./screen-orders.jsx`.

    Outer wrapper: `className="content-pad"` (same as `OrdersScreen` line 207).

    Table card (`className="card"`), containing:
    - A header row: padding `11px 20px`, background `#faf6ec`, labels at `10.5px / 700 / uppercase /
      0.06em` letter-spacing — `h_col_order`, `h_col_customer`, `h_col_type`, `h_col_time`,
      `h_col_payment`, `h_col_status`, `h_col_total`, plus an empty 8th cell for the chevron track.
      This row renders in EVERY state including loading and error.
    - Day-group headers (`data-testid="history-day-header"`): flex `space-between`, padding
      `9px 20px`, background `#fcfaf4`, `border-top`/`border-bottom` `1px solid hsl(120 10% 93%)`.
      Left: the day label at `12.5px / 800 / -0.01em` — `h_today` for today's `dayKey`, `h_yesterday`
      for yesterday's, otherwise the localized weekday+date via `toLocaleDateString(lang === 'ro' ? 'ro-RO' : 'en-GB', { weekday: 'long', day: 'numeric', month: 'long' })`.
      Right: `{count} {noun} · {revenue}` at `11.5px / 600`, with `noun` =
      `count === 1 ? t('h_orders_count_one') : t('h_orders_count_other')` and the revenue figure in a
      nested span at `11.5px / 800` colored `var(--sc-primary)`, formatted with `formatRON(revenue)`.
    - Data rows (`data-testid="history-row"`): `display: grid`, `gridTemplateColumns: HIST_GRID`,
      padding `13px 20px`, `cursor: pointer`, hover background `#faf7ef` with a 120ms transition,
      `onClick={() => onOpenOrder(order)}`. Single-line content throughout so every row is the same
      height. Columns, left to right:
      1. Order — a 30×30 status-tinted icon tile (radius 9px) + `#{orderNumberLabel(order)}` at
         `14px / 900 / -0.01em / tabular-nums`. `orderNumberLabel` renders
         `order.dailyOrderNumber` directly when it is a number; when it equals `order.id` (the D-05
         UUID fallback path), render a short slice — `String(order.id).slice(0, 8)` — never the full
         36-char UUID.
      2. Customer — `order.customer?.name` at `13.5px / 600` with `white-space: nowrap; overflow:
         hidden; text-overflow: ellipsis`. When the name is absent or an empty string, render the
         em-dash `—` rather than an empty cell (the E1 backstop; `AdminOrder` types `customerName`
         as non-nullable but this is treated as possible, not assumed away).
      3. Type — `chip chip-slate` with `typeMeta(order.type, t)`'s icon + label. No table-number
         suffix — `AdminOrder` has no table field.
      4. Time — `orderTimeLabel(order.placedAt)` at `13px / 600 / var(--sc-muted-foreground) /
         tabular-nums`.
      5. Payment — icon + label at `12.5px / 600 / var(--sc-muted-foreground)`, mapping
         `cash→'cash'`, `card→'card'`, `online→'wifi'`. This is a deliberate deviation from the
         design source (which collapses online into the card icon) for consistency with the shipped
         `OrderCard`/`OrderDetailScreen` — do not follow the design source here.
      6. Status — exactly one `chip` via `historyStatusMeta(deriveDisplayStatus(order), t)`.
      7. Total — right-aligned `formatRON(order.total).replace(' lei', '')` at
         `14.5px / 900 / tabular-nums / -0.01em`. Canceled rows: `textDecoration: 'line-through'` +
         `opacity: 0.6`. Refunded rows render at FULL opacity — money was collected.
      8. Chevron — a static `<Icon name="chevRight" size={16} />`, muted-foreground, centered. It
         never rotates: this phase navigates, it does not expand.

    States (mutually exclusive, rendered inside the table card below the header row):
    - Loading: 7 skeleton rows (`data-testid="history-skeleton-row"`) at the EXACT real row height
      (same `13px 20px` padding, same `HIST_GRID`, single line) with grey shimmering blocks per
      column — a 30px circle for the icon tile, bar shapes elsewhere. NO day-group headers render
      during loading; skeleton rows are ungrouped. Matching the height exactly is what prevents a
      layout jump when data arrives.
    - Error: the entire table body is replaced by a centered block — `h_error_title` + the reused
      `check_connection` copy + a `btn-secondary` button with a `refresh` icon and the `h_retry`
      label wired to `onClick={() => refetch()}`. The header row still renders. CRITICAL: render the
      STATIC i18n copy only — never `error.message` or any field off the thrown Error. Raw SDK error
      text must not reach the screen (threat T-07-10).
    - Empty (`days.length === 0` and not loading and not error): the two-line centered block copied
      from `OrdersScreen` lines 290-304 — `h_empty` at `15px / 600` and `h_empty_sub` at `13px / 400
      / var(--sc-muted-foreground)`, `text-align: center`, padding 48-56px. One component, no
      per-filter variant (D-13).

    Render EVERY row in `days` — no cap, no `slice`, no warning banner, no virtualization. D-03
    supersedes STATE.md's ">500 orders" watch-out and defers virtualization until measured against
    real data.

    Do NOT port `HistoryReceiptRow` (design source lines 20-86) or any expand/collapse state — D-07
    drops the inline expandable receipt entirely. Do NOT add a CSS class to `styles.css`: every
    visual primitive needed already exists. Do NOT add an i18n key: Plan 02 added all 34.

    Use `window.*` for nothing — the design source's globals are prototype-era; production is ES
    modules only (CLAUDE.md).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` passes every table/state test from Task 1
    - `grep -c "150px minmax(200px, 1.8fr) 118px 78px 116px 132px 116px 34px" src/screen-history.jsx` returns 1
    - `grep -c "useHistoryOrders()" src/screen-history.jsx` returns 1
    - `grep -c "import { typeMeta" src/screen-history.jsx` returns 1 (imported, not re-derived)
    - `grep -c "HistoryReceiptRow\|chevDown" src/screen-history.jsx` returns 0
    - `grep -v '^ *//' src/screen-history.jsx | grep -c "window\."` returns 0
    - `grep -v '^ *//' src/screen-history.jsx | grep -c "error.message"` returns 0 (no raw error text on screen)
    - `grep -v '^ *//' src/screen-history.jsx | grep -c "getAdminDashboard\|admin.dashboard"` returns 0
    - `git diff src/styles.css src/i18n.jsx` produces no output (no new CSS class, no new i18n key)
    - Behavior: a day with 2 completed + 1 canceled order renders count "3" and revenue "30,00 lei" — asserted in `screen-history.test.jsx`
    - Behavior: clicking a row calls `onOpenOrder` once with that order — asserted in `screen-history.test.jsx`
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>The day-grouped table renders with all four states; every screen-history table test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the client-computed summary strip and the inert filter bar</name>
  <files>src/screen-history.jsx</files>
  <read_first>
    - `src/screen-history.jsx` — the file as Task 2 left it
    - `src/__tests__/screen-history.test.jsx` — the summary-tile and inert-control assertions from Task 1
    - `src/screen-orders.jsx` — the stats-strip pattern (lines 197-221: `repeat(4, 1fr)` card grid, 44×44 icon tile, label/value stack) and the filter-pill + search markup (lines 165-181, 224-283)
    - `.planning/phases/07-history-screen-foundation/07-UI-SPEC.md` — Summary Strip Contract, Filter Bar Contract, the Color section's accent-reserved list, and the Typography table's 20px/900 tile-value row
    - `src/history-utils.js` — `computeSummary`'s return shape (Plan 01)
  </read_first>
  <behavior>
    Implements the summary-strip and filter-bar behaviors asserted in
    `src/__tests__/screen-history.test.jsx` (Task 1).
  </behavior>
  <action>
    Add two blocks above the table card in `src/screen-history.jsx`.

    **Summary strip (D-15).** `const summary = useMemo(() => computeSummary(finished), [finished]);`
    — derived from the SAME filtered list that backs the rows, so tiles and day headers agree by
    construction. Do NOT call `getAdminDashboard`/`client.admin.dashboard.get`: D-15 drops that
    endpoint permanently, and its numbers would contradict a finished-only list anyway.

    Copy the tile structure from `screen-orders.jsx` lines 197-221 verbatim —
    `display: grid; gridTemplateColumns: repeat(4, 1fr); gap: 12; marginBottom: 20`, each tile a
    `.card` with `padding: 16`, a 44×44 icon tile at radius 12, and a label/value stack with the
    label at `11px / 600 / uppercase / 0.06em / var(--sc-muted-foreground)`. Apply two Phase-7
    deltas on top: the value renders at **20px / 900 / -0.02em** (not `screen-orders.jsx`'s 22px —
    see the UI-SPEC Typography table), and a third `sub` line renders below the value.

    The four tiles:
    | Tile | Label | Value | Sub | Icon tint |
    |---|---|---|---|---|
    | Orders | `h_orders` | `summary.ordersCount` | `h_period_30` | sage |
    | Revenue | `h_revenue` | `formatRON(summary.revenue)` | `h_period_30` | sage |
    | Avg | `h_avg` | `summary.avg === null ? '—' : formatRON(summary.avg)` | `h_avg_sub` | slate (neutral) |
    | Refunds | `h_refunds` | `summary.refundsCount` | `` `${summary.canceledCount} ${t('h_canceled_suffix')}` `` | terracotta |

    The Refunds tile is COUNT-ONLY — no amount. No `refundAmount` field exists on `AdminOrder`
    (D-15/D-10). Sage is reserved for the Orders and Revenue tiles only; Avg uses a neutral slate
    tint and Refunds a terracotta tint, per the UI-SPEC accent-reserved list.

    Tile states, all three visually distinct:
    - Loading: the icon tile becomes a neutral grey pulsing block and label/value/sub become grey
      rounded bars at the same widths as real content — identical tile dimensions, so no layout jump.
    - Error: all 4 tiles show an em-dash `—` in place of the value, with icon tiles in neutral grey
      rather than their normal tint.
    - Empty (loaded successfully, list empty): tiles show computed ZEROS (`0`, `0,00 lei`, `—` for
      avg, `0`) — NOT dashes. Dashes are reserved for the error state; this distinction is what keeps
      "no data yet" readable as different from "failed to load".

    **Inert filter bar (D-14).** Copy the pill-group and search-input markup from `screen-orders.jsx`
    lines 224-283 verbatim (including the `3px` pill-container padding and the `search` class), then
    strip every `onClick`/`onChange` handler and every `useState`. Nothing here is data-bound this
    phase.
    - Container: `opacity: 0.5` on the whole bar.
    - EXCEPTION: the `h_period_30` pill renders at full opacity with selected styling — a dark
      `--sc-foreground` background and white text (the period-pill convention; distinct from
      status-pill selection, which uses sage). It reflects real current state, per D-14.
    - Period pills: `h_period_today`, `h_period_7`, `h_period_30` (selected), `h_period_custom`.
    - Status pills: `all`, `h_status_completed`, `h_status_canceled`, `h_status_refunded` — with NO
      counts. Live counts are Phase 10's job (Filters + Search).
    - Search input: `h_search` placeholder visible, `disabled`.
    - Export button: `download` icon + `h_export` label, `disabled`.
    - Every button and input carries the `disabled` attribute; the bar also sets
      `pointer-events: none` and `cursor: not-allowed`. No handler is wired.

    The bar renders at its FINAL visual position and size in every state — that is what reserves the
    page's shape so later phases wire controls up with zero layout shift, and why there is no jump
    between loading, error, and content.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0 with every Task-1 test green, including the summary-tile and inert-control cases
    - `grep -c "computeSummary" src/screen-history.jsx` is at least 1
    - `grep -v '^ *//' src/screen-history.jsx | grep -c "getAdminDashboard\|admin.dashboard"` returns 0
    - `grep -c "repeat(4, 1fr)" src/screen-history.jsx` returns 1
    - `grep -c "disabled" src/screen-history.jsx` is at least 5 (period pills, status pills, search, export)
    - `grep -c "h_period_30" src/screen-history.jsx` is at least 1
    - Behavior: the Export button and the search input both report `disabled` in a populated render — asserted in `screen-history.test.jsx`
    - Behavior: an error render shows `—` in all 4 tiles; an empty render shows `0` and no `—` tile value — asserted in `screen-history.test.jsx`
    - `git diff src/i18n.jsx src/styles.css` produces no output
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>The summary strip computes live from the fetched list, the filter bar renders inert at final size, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API-supplied strings → DOM | `customerName` is customer-supplied and reaches the Customer column and the detail route |
| thrown Error → DOM | The error state is where a server-side message could leak to the screen |
| authenticated user → historical PII | Anyone logged in as cashier sees 30 days of customer names and order totals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | XSS via `customerName` in the Customer column | low | mitigate | Rendered as a JSX text node, which React escapes by default. No `dangerouslySetInnerHTML` is introduced — `grep -c dangerouslySetInnerHTML src/screen-history.jsx` must return 0, consistent with every shipped `screen-*.jsx` |
| T-07-10 | Information Disclosure | Raw SDK error text rendered by the error state | medium | mitigate | The error block renders the static `h_error_title` + `check_connection` i18n copy ONLY; an acceptance criterion pins `grep -c "error.message"` to 0, and a Task-1 test asserts the raw 'boom' message never appears in the output |
| T-07-14 | Information Disclosure | 30 days of customer PII (names) on one screen for any authenticated user | medium | accept | The dataset is what the admin token is already authorized to return; the client renders no field the API withheld. Role-scoping is server-side (T-07-09, transferred). Mitigating client-side would be security theatre — the data is already in the response. History is cashier-visible only at the nav level (T-07-06). GDPR/data-retention is canon — covered by `/gsd-secure-phase`; not minted as a bespoke prohibition here |
| T-07-15 | Denial of Service | Unbounded row rendering on a wide, high-volume period | low | accept | D-03 explicitly supersedes STATE.md's ">500 orders" watch-out: render every row, measure against real data before adding virtualization. ~500 lightweight single-line rows are within React's comfort. VALIDATION.md carries a manual smoke test for a >500-order range |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install in this phase (RESEARCH.md "Package Legitimacy Audit": not applicable). No `[ASSUMED]`/`[SUS]` packages, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `npx vitest run src/__tests__/screen-history.test.jsx` — GREEN across loading, error, empty, populated
- `npx vitest run` — full suite GREEN
- `git diff src/i18n.jsx src/styles.css` — empty (no new keys, no new CSS classes)
- `grep -c "HistoryReceiptRow\|chevDown\|getAdminDashboard" src/screen-history.jsx` — 0
</verification>

<success_criteria>
- Orders render grouped by day, newest first, with per-day count and completed-only revenue
- Empty, loading, and error states render without crashing and without layout jump
- Summary tiles are computed from the same list as the rows; the filter bar is visible and inert
</success_criteria>

<output>
Create `.planning/phases/07-history-screen-foundation/07-04-SUMMARY.md` when done
</output>
