---
phase: 10-filters-search
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/screen-history.jsx
autonomous: true
requirements: [HIST-07, HIST-08, HIST-09]
must_haves:
  truths:
    - "The four status pills render in order All / Completed / Refunded / Canceled (F-03 correction; HIST-07 ordering)"
    - "The type pills render in order All / Delivery / Pickup / Dine-in with icons grid / moped / bag / utensils (HIST-08 ordering)"
    - "Status pill counts are integer row tallies computed via deriveDisplayStatus — no subtraction arithmetic, no rounding (HIST-07 precision)"
    - "The All-status count equals the number of type+search matches (D-02: All = byTypeAndSearch length)"
    - "Selecting one status leaves the sibling status pills showing their true type+search-only counts, never 0-because-of-status (D-01/D-02 exclude-self faceting)"
    - "Day headers, their counts/subtotals, and the four summary tiles all recompute from the filtered visible set, not the period (D-04; SC4)"
    - "Under a Completed-only filter the Refunds tile reads 0 · 0 canceled and the tile sub-label still shows the period label only, not the active filters (D-05, D-06)"
    - "The Avg tile renders formatRON(0) — not the em-dash — whenever completedCount is 0 and isError is false, including canceled-only and refunded-only filtered views with rows on screen (D-15)"
    - "Status, type, and query all survive a period switch — filter state is not keyed to the active range (D-12)"
    - { statement: "A rapid keystroke burst produces exactly ONE filtered recompute 250ms after the last keystroke, and clearing/emptying the box applies immediately with no delay (HIST-09 boundary; D-10)", verification: backstop }
    - { statement: "Selecting a status never zeroes the sibling pills' counts — each sibling reflects type+search only (D-02 populated)", verification: backstop }
    - { statement: "The three pill groups sit on row 1 and search + Export wrap together to a right-aligned row 2 at 1440x900 (D-07 two-row bar)", verification: backstop }
    - { statement: "The type+search count pass and the status+type+search row pass both traverse the full in-memory (up to 366-day) array on every debounced keystroke without visibly freezing the UI (E4 overflow; virtualization deferred)", verification: backstop }
  prohibitions:
    - "Filter selections must not touch the TanStack Query key or trigger a refetch — filters are UI state applied to the already-fetched array, not a fetch input"
    - "Filter useState must not be keyed to, derived from, or reset by the active period/range (D-12 requires survival across a period switch)"
    - "Status counts must not use the design's period-wide `orders - cancelCount - refundCount` subtraction — tally via deriveDisplayStatus over byTypeAndSearch"
  artifacts:
    - "src/screen-history.jsx: status/type/query filter useState + debouncedQuery, the byTypeAndSearch and visible useMemo chain, statusCounts tally, and the restructured FilterBar (activated status pills + counts, net-new type group, D-07 search+export nesting)"
    - "src/screen-history.jsx SummaryStrip Avg-tile gate changed from isEmptyState to isError (D-15)"
  key_links:
    - "byTypeAndSearch → statusCounts (D-02 counts) AND → visible (status-filtered) → groupOrdersByDay / computeSummary (rows, day headers, tiles — D-04)"
    - "query → useEffect+setTimeout(250) → debouncedQuery → matchesSearch (D-10 debounce; clear bypasses the timer)"
    - "FilterBar filter state + setters + statusCounts passed as props from HistoryScreen"
---

<objective>
Wire the History screen's live filtering: filter state, the debounce, the two-derived-set faceted
count chain (D-01/D-02), the filtered summary/day-header recompute (D-04), the D-15 Avg-tile gate
fix, and the two-row FilterBar restructure that activates the status pills (with counts + F-03
reorder), adds the net-new type group, and nests search + Export per D-07.

Purpose: This is the bulk of HIST-07/08/09 — where the pure predicates from 10-01 and the corrected
type value from 10-02 become a working, faceted, debounced filter over the fetched list. Everything
downstream (empty-state variants, integration tests) lands in 10-04.
Output: `src/screen-history.jsx` (filter state + memo chain + FilterBar restructure + Avg gate fix).

⚠ Cross-phase note: Phase 9 (Period Control) wires the period pills into this same FilterBar before
Phase 10 executes. At execution time FilterBar already carries period props/state from Phase 9 — this
plan EXTENDS that component (adds status/type/search), it does not rebuild the period group. Read the
current FilterBar at execution time; if the period wiring differs from the inert bar described below,
preserve it and graft the new groups alongside.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-filters-search/10-CONTEXT.md
@.planning/phases/10-filters-search/10-RESEARCH.md
@.planning/phases/10-filters-search/10-PATTERNS.md
@.planning/phases/10-filters-search/10-UI-SPEC.md
</context>

<assumption_delta_decision>
Primary noun: order type vocabulary + the filter axis.
- Order type vocabulary — decision: **promote**. D-08 unifies the SDK's `'local'` to the app-wide
  `'dinein'` at the normalizeOrder boundary (landed in 10-02), so a single vocabulary flows both
  inbound and outbound — no history-only second vocabulary.
- Filter selection vs. period — decision: **add-alongside**. Status/type/search are a distinct axis
  from the period axis (D-12): filters survive a period switch, and clearing filters must never
  retarget the fetch. Filter state lives in its own useState, not keyed to the range.
</assumption_delta_decision>

<artifacts_this_phase_produces>
New/changed symbols from THIS plan:
- `HistoryScreen` gains: status/type/query/debouncedQuery useState, the debounce useEffect,
  `byTypeAndSearch` + `visible` useMemos, `statusCounts` tally, and a filter-reset handler passed down
- `FilterBar` signature changes from `({ t })` to accept filter state, setters, and statusCounts as props
- `SummaryStrip` Avg-tile expression changed (gate on isError, not isEmptyState)
- `days` and `summary` memos now derive from `visible` (was `finished`)
Reused unchanged: filterFinishedOrders, groupOrdersByDay, computeSummary, deriveDisplayStatus,
historyStatusMeta, typeMeta, orderNumberLabel, formatRON, DayGroup, HistoryRow, TableHeaderRow.
The predicates matchesStatus/matchesType/matchesSearch and i18n keys come from 10-01; the corrected
`type` value comes from 10-02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter state, debounce, two-derived-set faceted chain, D-04 recompute, D-15 Avg fix</name>
  <files>src/screen-history.jsx</files>
  <read_first>
    - src/screen-history.jsx:214-240 (HistoryScreen — the finished→days/summary chain and isEmpty at :222 to be re-sourced from `visible`) and :244-251 (SummaryStrip — the Avg-tile expression at :251 gating on isEmptyState)
    - src/history-utils.js (matchesStatus/matchesType/matchesSearch/deriveDisplayStatus — the predicates 10-01 added)
    - .planning/phases/10-filters-search/10-RESEARCH.md § Pattern 1 (two-derived-set), § Pattern 2 (debounce), § Pitfall 1 (count from wrong list), § Pitfall 5 (Avg gate)
    - .planning/phases/10-filters-search/10-PATTERNS.md (the memo-chain, debounce, and Avg-fix excerpts)
    - .planning/phases/10-filters-search/10-CONTEXT.md D-01, D-02, D-04, D-05, D-06, D-10, D-12, D-15
    - .planning/phases/10-filters-search/10-UI-SPEC.md § Faceted Status-Count Contract + § Summary Strip / Avg-Tile Contract
  </read_first>
  <behavior>
    - byTypeAndSearch = finished filtered by matchesType(typeFilter) AND matchesSearch(debouncedQuery); visible = byTypeAndSearch further filtered by matchesStatus(statusFilter)
    - statusCounts.all === byTypeAndSearch.length; statusCounts.completed/refunded/canceled are deriveDisplayStatus tallies over byTypeAndSearch (exclude-self: not filtered by statusFilter)
    - days and summary derive from visible (not finished): a canceled-only filter recomputes day headers and tiles to the canceled subset (D-04)
    - Avg tile: summary.avg === null renders formatRON(0) when isError is false (canceled-only view with rows present), and '—' only when isError is true (D-15)
    - Typing 'abc' rapidly then pausing 250ms sets debouncedQuery once to 'abc'; clearing to '' sets debouncedQuery to '' on the same tick (no 250ms wait)
    - Switching period does not reset statusFilter/typeFilter/query (state not keyed to range)
  </behavior>
  <action>
    In HistoryScreen add filter useState: statusFilter ('all'), typeFilter ('all'), query (''),
    debouncedQuery (''). Add the debounce useEffect: when query === '' set debouncedQuery '' and
    return (immediate clear, D-10); else setTimeout 250ms to set debouncedQuery, returning
    clearTimeout as cleanup (Pitfall 3). Add two useMemos: byTypeAndSearch = finished.filter(o =>
    matchesType(o, typeFilter) && matchesSearch(o, debouncedQuery)); visible = byTypeAndSearch.filter(o
    => matchesStatus(o, statusFilter)). Add a statusCounts useMemo over byTypeAndSearch: all =
    length, and tally completed/refunded/canceled via deriveDisplayStatus (NOT subtraction — Pitfall 1).
    Re-source days = groupOrdersByDay(visible) and summary = computeSummary(visible), and isEmpty from
    visible/days (was finished). In SummaryStrip change the Avg-tile expression at :251 to gate on
    isError instead of isEmptyState: null → (isError ? '—' : formatRON(0)) (D-15). Keep tile
    sub-labels as the existing period label (D-06 — no change). Pass statusFilter/typeFilter/query and
    their setters plus statusCounts down to FilterBar (Task 2 consumes them). Do NOT add filter params
    to groupOrdersByDay/computeSummary; do NOT let any filter value enter the useHistoryOrders query key.
  </action>
  <acceptance_criteria>
    - screen-history.jsx defines byTypeAndSearch and visible useMemos, and days/summary derive from `visible` (grep: `groupOrdersByDay(visible)` and `computeSummary(visible)` present; `groupOrdersByDay(finished)` absent)
    - statusCounts is tallied via deriveDisplayStatus over byTypeAndSearch — no `orders -`/`cancelCount`/`refundCount` subtraction in screen-history.jsx
    - the Avg-tile expression gates on isError, not isEmptyState (`isError ? '—' : formatRON(0)` present; the old `isEmptyState ? formatRON(0) : '—'` gone)
    - the debounce useEffect returns a clearTimeout cleanup and applies query==='' immediately (no setTimeout on the empty branch)
    - no filter value appears in the useHistoryOrders query key / call
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/history-utils.test.js src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <done>
    byTypeAndSearch/visible/statusCounts wired; days+summary derive from visible; Avg tile gates on isError;
    debounce is 250ms with immediate clear; filter state independent of the range. Existing screen-history tests
    still pass (integration coverage for the new behavior lands in 10-04).
  </done>
</task>

<task type="auto">
  <name>Task 2: FilterBar restructure — activate status pills + counts + F-03 reorder, net-new type group, D-07 search/export nesting</name>
  <files>src/screen-history.jsx</files>
  <read_first>
    - src/screen-history.jsx:298-341 (the current inert FilterBar — statusFilters at :305 with the F-03-swapped order, the disabled pills, the standalone search at :335, the lone Export auto-margin div at :338)
    - .planning/phases/10-filters-search/10-PATTERNS.md (the design's target nesting, status count-badge markup, type-group markup excerpts) and sitecare-orders/project/src/screen-history.jsx:227-269 (design source — authoritative layout/spacing/color)
    - src/screen-orders.jsx:236-240 (the shipped type-pill cream/sage selected style — the exact analog to port)
    - src/icons.jsx (confirm grid/moped/bag/utensils/search/download icons exist)
    - .planning/phases/10-filters-search/10-UI-SPEC.md § Type Filter Contract, § Filter Bar Structure Contract, § Faceted Status-Count Contract, § Color, § Spacing Exceptions
    - .planning/phases/10-filters-search/10-CONTEXT.md D-03, D-07, F-03
  </read_first>
  <action>
    Change FilterBar's signature to receive the filter state, setters, and statusCounts as props
    (from Task 1). Status group: remove `disabled`/`pointerEvents:none`, wire onClick to setStatusFilter,
    render the selected pill with solid var(--sc-primary) fill + white text and unselected with
    transparent + #555; reorder statusFilters to All / Completed / Refunded / Canceled (F-03 — production
    currently has Canceled before Refunded); append a count badge span to each pill (fontSize 11,
    fontWeight 700, padding '1px 6px', borderRadius 999, background selected rgba(255,255,255,0.25)
    else hsl(120 10% 90%), text currentColor) reading statusCounts[f.id]. A 0 count renders the
    identical badge and the pill stays clickable — no disable, no dim (D-03). Add a net-new type pill
    group (structurally identical container to status: #fff bg, 1px solid hsl(120 10% 90%), radius 10,
    padding 3) with options all/delivery/pickup/dinein in that order, each with its icon
    (grid/moped/bag/utensils), NO count badge; selected state cream #f7f1e1 fill + var(--sc-primary)
    text, unselected transparent + #777, padding '7px 11px' (ported verbatim from the design). Wire
    onClick to setTypeFilter. D-07 nesting: put BOTH the search input and the (still-inert, disabled)
    Export button inside ONE `marginLeft:auto` flex container (gap 8) so they wrap together to a
    right-aligned row 2; remove search's standalone flex-child position. Activate search: remove
    `disabled`, wire value={query} onChange to setQuery, keep the existing h_search placeholder and
    .search chrome. Export stays disabled (Phase 11). Preserve the existing flexWrap:'wrap' on the
    outer bar. Do not alter the period group beyond leaving Phase 9's wiring intact.
  </action>
  <acceptance_criteria>
    - the four status pills are clickable (no `disabled`/`pointerEvents:'none'` on them) and render in order All / Completed / Refunded / Canceled
    - each status pill renders a count badge reading statusCounts[f.id]; a 0 count keeps the pill clickable (no disable, no dim)
    - a type pill group with 4 options (all/delivery/pickup/dinein) and icons grid/moped/bag/utensils exists, selected style cream #f7f1e1 fill + var(--sc-primary) text, no count badge
    - search input and the (disabled) Export button are inside ONE `marginLeft:'auto'` container; search has value={query} onChange wired; Export stays disabled
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <done>
    Status pills are clickable, ordered All/Completed/Refunded/Canceled, each showing its statusCounts badge
    (0 stays clickable); the type group renders 4 pills with icons and the cream/sage selected style; search is
    active and nested with Export in one marginLeft:auto container (two-row wrap); Export stays inert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user filter/keystroke → client filter state | selections and query text drive client-side derived arrays only (no network) |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Information Disclosure | byTypeAndSearch / visible filter chain | low | accept | The chain only NARROWS `finished` (already auth-scoped, finished-only); no predicate can add rows, so no filter combination reveals unauthorized data |
| T-10-03 | Denial of Service (client, self-inflicted) | count+row passes over the full in-memory array on every keystroke | low | accept | The 250ms debounce (D-10) collapses a keystroke burst into one traversal; list virtualization is deliberately deferred (10-CONTEXT.md). No server call exists to amplify |
| T-10-01 | Tampering | filter state must not enter the useHistoryOrders query key | low | mitigate | Prohibition-checked: filters are UI state; the query key stays keyed to the range only, so a filter change cannot silently retarget or replay the fetch |
</threat_model>

<verification>
- `npx vitest run src/__tests__/screen-history.test.jsx` green (existing suite still passes; new behavior tested in 10-04)
- `grep -n "orders - " src/screen-history.jsx` — no period-wide subtraction arithmetic for counts (Pitfall 1)
- Manual (UI-checker / human, see 10-VALIDATION Manual-Only): two-row bar wrap at 1440x900; 250ms debounce feels responsive, clear is immediate
</verification>

<success_criteria>
Live faceted filtering works: status counts are exclude-self (D-02), rows/day-headers/tiles follow the
filtered set (D-04), the Avg tile shows computed 0 not em-dash under completedCount 0 (D-15), search is
250ms-debounced with immediate clear (D-10), filters survive a period switch (D-12), and the two-row
FilterBar shows activated status pills (F-03 order + counts), the net-new type group, and the D-07 nesting.
</success_criteria>

<output>
Create `.planning/phases/10-filters-search/10-03-SUMMARY.md` when done
</output>
