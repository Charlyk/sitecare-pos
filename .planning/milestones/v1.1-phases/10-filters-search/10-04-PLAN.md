---
phase: 10-filters-search
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - src/screen-history.jsx
  - src/__tests__/screen-history.test.jsx
autonomous: true
requirements: [HIST-07, HIST-08, HIST-09]
must_haves:
  truths:
    - "When filters are active (status ≠ all OR type ≠ all OR query non-empty) and nothing matches, the filtered empty-state Variant B renders h_empty_filtered_title on the MAIN line — not only the sub-line (D-13)"
    - "When no filters are active and nothing matches, the period-copy empty state (Variant A) renders unchanged (D-13)"
    - "A zero-count status pill is clickable and lands on the filtered empty state (D-03; HIST-07 boundary)"
    - "The Clear Filters button appears only in Variant B and resets status → all, type → all, query → '' (D-14)"
    - "Clear Filters does NOT change the active period/date-range — filters and period are separate axes (D-14; D-12)"
    - "Integration coverage exists for exclude-self faceting (D-02), the 250ms/immediate-clear debounce via fake timers (D-10), D-04 filtered day-headers/tiles, the D-15 Avg-tile fix, and both empty-state variants"
  prohibitions:
    - "The Clear Filters handler must not call any period/range setter — it resets the three filter axes only"
    - "The filtered-empty copy must not interpolate active filter labels into a Romanian sentence (ro/en composition trap — D-13 rejected alternative)"
  artifacts:
    - "src/screen-history.jsx: EmptyBlock gains a filtersActive variant (Variant B main line + Clear Filters button) wired from HistoryScreen with the filtersActive flag and reset handler"
    - "src/__tests__/screen-history.test.jsx: integration tests for faceting, debounce (fake timers), D-04, D-15, and both empty-state variants"
  key_links:
    - "HistoryScreen filtersActive flag (status≠all || type≠all || query!=='') → EmptyBlock variant selection"
    - "Clear Filters onClick → setStatusFilter('all') + setTypeFilter('all') + setQuery('') — never the period setter"
---

<objective>
Add the filtered empty-state (D-13's second variant + D-14's Clear Filters remedy) and the
integration test suite that proves the whole phase's behavior end-to-end.

Purpose: Closes the last two locked decisions (D-13/D-14) and converts the plan-level behaviors of
10-01…10-03 into RTL integration assertions — faceting, debounce timing (fake timers), filtered
day-headers/tiles, the Avg-tile fix, and both empty-state variants. Sequenced after 10-03 because it
touches the same file (screen-history.jsx) and the tests need the fully-wired screen.
Output: `src/screen-history.jsx` (EmptyBlock variant + reset handler), `src/__tests__/screen-history.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-filters-search/10-CONTEXT.md
@.planning/phases/10-filters-search/10-RESEARCH.md
@.planning/phases/10-filters-search/10-PATTERNS.md
@.planning/phases/10-filters-search/10-UI-SPEC.md
@.planning/phases/10-filters-search/10-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New/changed symbols from THIS plan:
- `EmptyBlock` gains a `filtersActive` prop (+ a clear-filters callback) selecting Variant A vs. B
- `HistoryScreen` computes `filtersActive` and passes it + a reset handler to EmptyBlock
- new `describe` blocks in `src/__tests__/screen-history.test.jsx`
Reused unchanged: the h_empty/h_empty_sub keys (Variant A) and the h_empty_filtered_title/h_clear_filters
keys added in 10-01 (Variant B); ErrorBlock's retry-button shape (the analog for the Clear Filters button).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EmptyBlock two variants + Clear Filters remedy (D-13/D-14)</name>
  <files>src/screen-history.jsx</files>
  <read_first>
    - src/screen-history.jsx:104-111 (current single-variant EmptyBlock) and :92-102 (ErrorBlock — the btn-secondary icon+label retry button whose shape the Clear Filters button mirrors) and :214-240 (HistoryScreen — where filtersActive is computed and EmptyBlock is rendered at :233)
    - .planning/phases/10-filters-search/10-UI-SPEC.md § Filtered-Empty-State Contract + § Copywriting Contract (exact copy, btn-secondary, marginTop 16, no sub-line in Variant B)
    - .planning/phases/10-filters-search/10-CONTEXT.md D-13, D-14, D-12
    - src/i18n.jsx (h_empty_filtered_title, h_clear_filters from 10-01; h_empty, h_empty_sub existing)
  </read_first>
  <action>
    Extend EmptyBlock to take a filtersActive boolean and an onClearFilters callback. When
    filtersActive is false, render the existing Variant A unchanged (h_empty main line + h_empty_sub).
    When true, render Variant B: h_empty_filtered_title on the SAME main-line role (fontSize 15,
    fontWeight 600), NO sub-line, and below it a Clear Filters button using the ErrorBlock retry-button
    shape — className btn-secondary, marginTop 16, an Icon (x) + t('h_clear_filters') — whose onClick
    calls onClearFilters. In HistoryScreen compute filtersActive = statusFilter !== 'all' || typeFilter
    !== 'all' || query !== '' and pass it plus a reset handler that calls setStatusFilter('all'),
    setTypeFilter('all'), setQuery('') — and NOTHING that touches the period/range (D-14/D-12). Wire
    filtersActive/onClearFilters into the EmptyBlock render at :233. Do not interpolate filter labels
    into the copy (D-13 rejected).
  </action>
  <acceptance_criteria>
    - EmptyBlock accepts a filtersActive prop; when true it renders t('h_empty_filtered_title') on the main line and no sub-line
    - Variant B renders a btn-secondary Clear Filters button (icon + t('h_clear_filters')) whose onClick is the reset handler
    - HistoryScreen computes filtersActive = statusFilter!=='all' || typeFilter!=='all' || query!=='' and the reset handler calls setStatusFilter('all')/setTypeFilter('all')/setQuery('') and NO period/range setter
    - filtersActive false renders the unchanged Variant A (h_empty + h_empty_sub)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <done>
    Variant B renders h_empty_filtered_title + a working Clear Filters button when filters are active and no
    rows match; Variant A unchanged when no filters active; the reset handler clears the three filter axes only,
    leaving the period untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test suite — faceting, debounce, D-04, D-15, empty variants</name>
  <files>src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - src/__tests__/screen-history.test.jsx (existing HistoryScreen RTL suite + populated-state describe block to extend; existing fixture/mock helpers)
    - .planning/phases/10-filters-search/10-RESEARCH.md § Validation Architecture → Phase Requirements → Test Map (the exact behaviors to assert) and § Wave 0 Gaps (vi.useFakeTimers is available; globals:true)
    - .planning/phases/10-filters-search/10-VALIDATION.md § Per-Task Verification Map + Manual-Only Verifications
    - .planning/phases/10-filters-search/10-CONTEXT.md D-01, D-02, D-03, D-04, D-10, D-12, D-15
  </read_first>
  <action>
    Extend src/__tests__/screen-history.test.jsx with integration tests seeding a fixture set that
    spans completed/refunded/canceled and delivery/pickup/dinein rows. Assert: (D-02) selecting
    Completed leaves the Canceled and Refunded pills showing their true non-zero type+search-only
    counts, not 0; (F-03) the four status pills render in order All/Completed/Refunded/Canceled;
    (D-03) a zero-count pill is clickable and lands on the filtered empty state (Variant B); (D-10)
    using vi.useFakeTimers, a rapid keystroke burst yields exactly one filtered recompute after
    vi.advanceTimersByTime(250), and clearing the box applies immediately (no timer advance needed);
    (D-04) applying a canceled-only filter recomputes day-header counts/subtotals and the summary
    tiles to the canceled subset; (D-15) a canceled-only filter with canceled rows present renders the
    Avg tile as a RON zero, not '—'; (D-13/D-14) Variant B shows h_empty_filtered_title and its Clear
    Filters button resets status/type/query. If a D-12 compose-with-period test is added, gate it on
    Phase 9's period-switch plumbing being present (write it only if the period pills are wired at
    execution time; otherwise record it as a deferred manual check per 10-VALIDATION). Restore real
    timers after fake-timer tests.
  </action>
  <acceptance_criteria>
    - a test asserts selecting Completed leaves the Canceled and Refunded pills with true non-zero counts (D-02 exclude-self)
    - a fake-timer test asserts one recompute after advanceTimersByTime(250) for a keystroke burst, and immediate application on clear (D-10)
    - a test asserts a canceled-only filter recomputes day-header counts/subtotals + tiles (D-04) and the Avg tile shows a RON zero, not '—' (D-15)
    - a test asserts a zero-count pill click lands on Variant B, and Clear Filters resets the three filter axes
    - `npx vitest run` full suite is green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <done>
    Integration tests cover exclude-self faceting, F-03 order, D-03 zero-pill→empty, the 250ms/immediate-clear
    debounce (fake timers), D-04 filtered recompute, the D-15 Avg fix, and both empty-state variants; suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Clear Filters reset | resets client-only UI filter state (no mutation, no network) |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | Clear Filters reset handler | low | accept | Resets only the three client-side filter useState values; it performs no mutation and issues no network call, and by prohibition it never touches the period/range setter — nothing to tamper with server-side |
| T-10-04 | Tampering (XSS) | filtered-empty-state copy | low | accept | Copy comes from static i18n keys rendered as JSX text nodes (React-escaped); no user text is interpolated into the empty-state string (D-13) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/screen-history.test.jsx` green (all new integration blocks)
- `npx vitest run` full suite green before `/gsd-verify-work` (phase gate per 10-VALIDATION)
- Manual (10-VALIDATION Manual-Only): filtered empty-state copy + Clear Filters reset at 1440x900
</verification>

<success_criteria>
The filtered empty-state renders D-13's main-line copy with D-14's Clear Filters remedy (resetting filters,
not the period); integration tests prove exclude-self faceting, F-03 order, D-03 zero-pill behavior, the
250ms/immediate-clear debounce, D-04 filtered recompute, and the D-15 Avg-tile fix; full suite green.
</success_criteria>

<output>
Create `.planning/phases/10-filters-search/10-04-SUMMARY.md` when done
</output>
