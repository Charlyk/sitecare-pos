---
phase: 11-reprint-csv-export
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/app.jsx
  - src/screen-detail.jsx
  - src/__tests__/app-history-route.test.jsx
  - src/__tests__/screen-detail.test.jsx
autonomous: true
requirements: [HIST-11]
user_setup: []

must_haves:
  truths:
    - "The history-detail route in app.jsx passes onPrint={handlePrint} so a reprint-button click never throws onPrint-is-not-a-function (Pitfall 1)"
    - "The read-only order detail shows Print kitchen and Print customer buttons that call onPrint(order,'kitchen'|'customer'), mirroring the live !readOnly row (D-01)"
    - "The reprint buttons reuse the existing print_kitchen/print_customer i18n labels verbatim — no new label keys (D-02)"
    - "When no printer is configured the reprint buttons render disabled + greyed (opacity 0.5, pointerEvents none, cursor not-allowed) with the print_configure_hint tooltip (D-05, D-06)"
    - "printerConfigured is derived from a mount-time read of preferences.json -> printer.port, gated on readOnly, held in local component state (D-04)"
    - "The reprint row is an ADDED readOnly && block — the live !readOnly Advance/Cancel controls stay hidden (D-03)"
    - "The two-button reprint row is display flex gap 8 with each button flex 1; short labels never overflow at either locale (overflow coverage)"
    - "Reprint reuses handlePrint unchanged — success fires toast_printed, failure fires print_failed (no new copy)"
  artifacts:
    - src/app.jsx
    - src/screen-detail.jsx
    - src/__tests__/app-history-route.test.jsx
    - src/__tests__/screen-detail.test.jsx
  key_links:
    - "app.jsx history-detail route (app.jsx:254-267) onPrint prop -> handlePrint (unchanged, app.jsx:127-176)"
    - "screen-detail printerConfigured mount effect reads load('preferences.json') -> store.get('printer') -> !!config?.port"
---

<objective>
Surface reprint in the read-only order detail (HIST-11): add `onPrint={handlePrint}` to the `history-detail` route in `app.jsx` (the Pitfall-1 landmine — the read-only route does not currently pass it), and add a `readOnly &&` Print kitchen / Print customer button row to `screen-detail.jsx` gated by a mount-time printer-configured check.

Purpose: Give staff the same two print controls on archived orders that live orders already have, greyed-out when no printer is configured — reusing the exact `handlePrint`/`print_receipt` path with zero changes to the handler or the Rust command.
Output: Reprint buttons live in read-only mode; the `history-detail` route wires the handler; regression + behavior tests guard both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reprint-csv-export/11-RESEARCH.md
@.planning/phases/11-reprint-csv-export/11-PATTERNS.md
@.planning/phases/11-reprint-csv-export/11-UI-SPEC.md
</context>

## Artifacts this plan produces

- New prop line `onPrint={handlePrint}` on the `history-detail` `<OrderDetailScreen>` block in `src/app.jsx`
- New `readOnly &&` print-button row + new `printerConfigured` local state + mount effect in `src/screen-detail.jsx`
- New assertion in `src/__tests__/app-history-route.test.jsx` that `onPrint` reaches `OrderDetailScreen` on the `history-detail` route
- Extended `describe('readOnly mode', ...)` reprint assertions in `src/__tests__/screen-detail.test.jsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire onPrint={handlePrint} onto the history-detail route (Pitfall 1)</name>
  <files>src/app.jsx, src/__tests__/app-history-route.test.jsx</files>
  <read_first>
    - src/app.jsx (live `detail` route with onPrint at line 252 — analog; the `history-detail` block at lines 254-267 that is MISSING onPrint; handlePrint at lines 127-176 — unchanged)
    - src/__tests__/app-history-route.test.jsx (existing history-detail route render test — analog to extend)
    - .planning/phases/11-reprint-csv-export/11-RESEARCH.md (## Common Pitfalls Pitfall 1)
  </read_first>
  <behavior>
    - Rendering the `history-detail` route passes a defined `onPrint` function to `OrderDetailScreen` (equal to `handlePrint`), not undefined.
    - This is the regression guard: without the prop, a read-only reprint click throws `onPrint is not a function`.
  </behavior>
  <action>
    Add `onPrint={handlePrint}` as a new prop line to the `history-detail` `<OrderDetailScreen>` block in `src/app.jsx` (lines 254-267), exactly as the live `detail` route at line 252 already has it. `handlePrint` itself needs ZERO changes — it already reads printer config from the store, invokes `print_receipt`, and fires `toast_printed`/`print_failed`. Do this as its own task/commit and its own assertion; do not assume the `screen-detail.jsx` edit covers it. Extend `src/__tests__/app-history-route.test.jsx` with an assertion that `onPrint` is passed (a defined function) to `OrderDetailScreen` on the `history-detail` route — spy on / capture the rendered props of the mocked `OrderDetailScreen`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-history-route.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "history-detail" src/app.jsx` region contains an `onPrint={handlePrint}` prop.
    - `src/__tests__/app-history-route.test.jsx` asserts `onPrint` is a defined function on the `history-detail` render (fails if the prop is removed).
    - `npx vitest run src/__tests__/app-history-route.test.jsx` passes.
  </acceptance_criteria>
  <done>The history-detail route passes onPrint={handlePrint}; a regression test fails without it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add readOnly reprint-button row + printer-configured gate to screen-detail</name>
  <files>src/screen-detail.jsx, src/__tests__/screen-detail.test.jsx</files>
  <read_first>
    - src/screen-detail.jsx (the `!readOnly` print row at lines 261-270 — analog to mirror; `<ThermalTicket>` at line 259 for placement; the `!readOnly` Advance button at line 273 that must stay hidden)
    - src/screen-printer.jsx (the load('preferences.json') -> store.get('printer') mount effect at lines 42-67 — analog for the printer-configured read)
    - src/screen-history.jsx (the inert-Export dimming style at line 843 — the exact opacity 0.5 / pointerEvents none / cursor not-allowed convention to copy, D-05)
    - src/__tests__/screen-detail.test.jsx (existing `describe('readOnly mode', ...)` block — analog to extend; store mock shape from print-receipt.test.jsx)
    - .planning/phases/11-reprint-csv-export/11-UI-SPEC.md (## Additional Implementation Notes — placement, exact disabled values, do NOT reuse .btn-disabled-offline)
  </read_first>
  <behavior>
    - When readOnly and printerConfigured: two buttons render — Print kitchen (.btn-secondary, flex 1) then Print customer (.btn-primary, flex 1), each with the printer icon and the existing print_kitchen/print_customer label; clicking each calls onPrint(order,'kitchen') / onPrint(order,'customer').
    - When readOnly and NOT printerConfigured: both buttons are `disabled`, styled with opacity 0.5 / pointerEvents none / cursor not-allowed, and carry a native `title` of print_configure_hint; clicking does nothing.
    - When NOT readOnly: the reprint row does not render (the existing live `!readOnly` row is unchanged), and the live Advance/Cancel controls remain gated behind `!readOnly`.
    - printerConfigured starts false and is set from a mount-time read of preferences.json -> printer.port, only when readOnly; a store read failure leaves it false.
  </behavior>
  <action>
    Add a `readOnly &&` print-button row to `src/screen-detail.jsx` immediately after `<ThermalTicket ...>` (line 259) and before the `!readOnly` Advance button (line 273), so the new row is gated the opposite direction from the existing live row and the Advance/Cancel controls stay hidden in read-only (D-03 — this is an ADDED block, not a widened guard). Mirror the existing `!readOnly` row's structure exactly: a `display: flex; gap: 8` container with Print kitchen (`.btn-secondary`, `flex: 1`, `justifyContent: center`) then Print customer (`.btn-primary`, `flex: 1`), each rendering `<Icon name="printer" size={14} />` + the existing `t('print_kitchen')`/`t('print_customer')` label (D-01/D-02 — reuse those keys, no new label keys). Add a `printerConfigured` local `useState(false)` and a mount `useEffect` gated on `readOnly` that calls `load('preferences.json', { autoSave: false })` -> `store.get('printer')` -> `setPrinterConfigured(!!config?.port)` with a `.catch` that sets it false (D-04, mirroring screen-printer.jsx). When `!printerConfigured`, set both buttons `disabled`, spread the inline `opacity: 0.5; pointerEvents: 'none'; cursor: 'not-allowed'` style (D-05 — the exact screen-history.jsx:843 values, NOT `.btn-disabled-offline`), and set `title={t('print_configure_hint')}` (D-06). Extend the existing `describe('readOnly mode', ...)` block in `src/__tests__/screen-detail.test.jsx` with the enabled-click, disabled+greyed+tooltip, and Advance-stays-hidden assertions, mocking the same store shape as print-receipt.test.jsx (printer present vs absent).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-detail.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/screen-detail.jsx` contains a `readOnly &&` block rendering two buttons calling `onPrint(order, 'kitchen')` and `onPrint(order, 'customer')`.
    - A test asserts both reprint buttons render and fire `onPrint` with the correct kind when a printer is configured.
    - A test asserts both buttons are `disabled` with the `print_configure_hint` title when no printer is configured.
    - A test asserts the live Advance control is still absent in read-only mode (the `!readOnly` guard was not widened).
    - `grep -c "btn-disabled-offline" src/screen-detail.jsx` did not increase (the offline class is NOT used for this state).
    - `npx vitest run src/__tests__/screen-detail.test.jsx` passes.
  </acceptance_criteria>
  <done>The read-only detail shows greyed-when-unconfigured reprint buttons wired to onPrint; Advance/Cancel stay hidden; behavior tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React click handler → Rust print_receipt IPC | A reprint click crosses into a native serial-port write (unchanged path) |
| plugin-store read → UI enabled-state | printer config gates whether the buttons are clickable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-R1 | Denial of Service (runtime crash) | missing `onPrint` on `history-detail` route (Pitfall 1) | high | mitigate | Task 1 adds `onPrint={handlePrint}` with a regression test that fails if the prop is dropped — a reprint click can never throw `onPrint is not a function` |
| T-11-R2 | Elevation of Privilege (unintended print) | reprint buttons when no printer configured | low | mitigate | Buttons are `disabled` + `pointerEvents: none` when `printerConfigured` is false — inert, not merely visually dimmed |
| T-11-R3 | Tampering | reprint path reuses existing `handlePrint`/`print_receipt` | low | accept | No new IPC surface introduced; the existing click-time printer-config guard and `print_receipt` port validation are unchanged |
</threat_model>

<verification>
- `npx vitest run src/__tests__/app-history-route.test.jsx src/__tests__/screen-detail.test.jsx` green.
- Manual (VALIDATION Manual-Only): with a printer configured, open a historical order → Print kitchen / Print customer produce correct receipts (hardware-only).
</verification>

<success_criteria>
Staff can reprint a historical receipt from the read-only detail with two working buttons that grey out when no printer is configured; the history-detail route wires the handler so no click throws; behavior and regression tests pass.
</success_criteria>

<output>
Create `.planning/phases/11-reprint-csv-export/11-03-SUMMARY.md` when done.
</output>
