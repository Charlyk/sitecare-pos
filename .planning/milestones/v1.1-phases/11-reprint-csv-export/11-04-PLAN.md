---
phase: 11-reprint-csv-export
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/screen-history.jsx
  - src/__tests__/screen-history.test.jsx
autonomous: true
requirements: [HIST-12]
user_setup: []

must_haves:
  truths:
    - "Clicking Export CSV builds the CSV from the visible filtered memo (D-07 — no getOrder hydration, no new fetch), opens a native Save dialog via save(), and writes with writeTextFile()"
    - "The default filename is orders_<from>_<to>.csv derived from the active period range (D-14)"
    - "When visible.length === 0 the Export button is disabled + greyed (opacity 0.5, pointerEvents none, cursor not-allowed) with the h_export_empty_tooltip tooltip (empty coverage)"
    - "A cancelled Save dialog (save() resolves null/undefined) is a silent no-op — no toast and no writeTextFile call (error-vs-cancel coverage)"
    - "A save() or writeTextFile() throw shows the h_export_error_title error toast with String(err) detail (error coverage)"
    - "Export success shows the toast_saved toast with a pluralized count detail using h_orders_count_one/h_orders_count_other (populated coverage)"
    - "No in-app spinner or disabled-while-pending state — the CSV builds synchronously; only the native save() dialog is async (loading coverage)"
    - "The Export CSV label reuses the existing h_export key verbatim and stays single-line at both locales (long-text coverage)"
  artifacts:
    - src/screen-history.jsx
    - src/__tests__/screen-history.test.jsx
  key_links:
    - "Export onClick -> handleExportCsv -> buildCsv(visible) -> save() -> writeTextFile()"
    - "range.from / range.to (screen-history.jsx:309) -> orders_<from>_<to>.csv default filename"
    - "pushToast added via useAppStore((s) => s.pushToast) selector (mirrors app.jsx)"
---

<objective>
Activate the currently-inert Export CSV button in `src/screen-history.jsx` (HIST-12): wire an `handleExportCsv` handler that serializes the `visible` filtered order set via `buildCsv` (from 11-02), opens a native Save dialog (`save()` from plugin-dialog installed in 11-01), and writes the file (`writeTextFile()` from plugin-fs), with locked cancel/error/empty/success behavior.

Purpose: Let staff export exactly what they are looking at — the filtered/searched/period-scoped rows — for downstream accounting, entirely client-side from in-hand summaries (D-07), via the narrow native Save flow.
Output: A working Export button (disabled+tooltipped when nothing is visible), correct default filename, and integration tests for the happy/cancel/error/empty paths mocking both plugins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reprint-csv-export/11-RESEARCH.md
@.planning/phases/11-reprint-csv-export/11-PATTERNS.md
@.planning/phases/11-reprint-csv-export/11-UI-SPEC.md
</context>

## Artifacts this plan produces

- New `handleExportCsv` async handler + `save`/`writeTextFile` imports + `buildCsv` import + a `pushToast` store selector in `src/screen-history.jsx`
- Activated `h_export` Export button: `disabled={visible.length === 0}`, conditional dimming style, `title` = `h_export_empty_tooltip` when empty, `onClick={handleExportCsv}`
- Extended `src/__tests__/screen-history.test.jsx` with Export click / cancel / error / empty-state tests mocking `@tauri-apps/plugin-dialog` and `@tauri-apps/plugin-fs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire handleExportCsv (build → save → write) with cancel/error handling</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - src/screen-history.jsx (inert Export button at lines 843-845; `visible` memo at lines 380-382; `range` memo with from/to at lines 309-312; the toolbar container at line 838; existing imports at lines 5-23 — note the store is NOT yet imported here)
    - src/app.jsx (useAppStore import at line 13 and pushToast usage in handlePrint ~lines 132/172 — analog for adding the pushToast selector)
    - src/__tests__/print-receipt.test.jsx (lines 1-26 — the vi.mock plugin-mocking convention to extend for plugin-dialog/plugin-fs)
    - src/__tests__/screen-history.test.jsx (existing suite — analog to extend)
    - .planning/phases/11-reprint-csv-export/11-RESEARCH.md (## Architecture Patterns Pattern 2; ## Common Pitfalls Pitfalls 2 and 3)
    - .planning/phases/11-reprint-csv-export/11-UI-SPEC.md (## Copywriting Contract — success/error toast copy; ## Additional Implementation Notes — filename format)
  </read_first>
  <behavior>
    - Happy path: click Export → buildCsv(visible) → save({ defaultPath: `orders_<from>_<to>.csv`, filters: [{ name: 'CSV', extensions: ['csv'] }] }) resolves a path → writeTextFile(path, csv) → toast_saved success toast whose detail is the pluralized visible count (h_orders_count_one/h_orders_count_other).
    - Cancel: save() resolves null/undefined → early return; writeTextFile is NOT called and NO toast is pushed.
    - Error: save() or writeTextFile() throws → catch pushes an error toast titled h_export_error_title with detail String(err); writeTextFile is not called after a save() throw.
    - Filename: defaultPath is exactly `orders_${range.from}_${range.to}.csv` using the YYYY-MM-DD values already driving the query (D-14).
    - No spinner: the handler shows no in-app pending/disabled-while-running state (loading coverage).
  </behavior>
  <action>
    In `src/screen-history.jsx`, add `import { save } from '@tauri-apps/plugin-dialog'`, `import { writeTextFile } from '@tauri-apps/plugin-fs'`, and `import { buildCsv } from './history-utils.js'` (or extend the existing `./history-utils.js` import). The store is not yet imported in this file — add `import { useAppStore } from './store.js'` and a `const pushToast = useAppStore((s) => s.pushToast)` selector (mirroring app.jsx). Write an async `handleExportCsv` following Research Pattern 2: build the CSV from `visible`, call `save()` with the D-14 `orders_${range.from}_${range.to}.csv` default path and the CSV filter, then GUARD with an explicit `if (!path) return;` BEFORE `writeTextFile` (Pitfall 3 — a cancelled dialog is a silent no-op, never routed to the error toast), then `writeTextFile(path, csv)`, then push a `toast_saved` success toast with the pluralized-count detail (`h_orders_count_one`/`h_orders_count_other`, reusing the shipped keys). Wrap in try/catch; the catch pushes a `h_export_error_title` error toast with `String(err)` detail. Do not add a broad fs scope or construct the path independently of `save()`'s return (Pitfall 2). Extend `src/__tests__/screen-history.test.jsx` with vi.mock for `@tauri-apps/plugin-dialog` (`save`) and `@tauri-apps/plugin-fs` (`writeTextFile`) matching the print-receipt.test.jsx convention, covering happy/cancel/error paths and asserting the default filename shape.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `handleExportCsv` calls `buildCsv(visible)` then `save(...)` then `writeTextFile(path, csv)` in that order, with an `if (!path) return;` guard between save and write.
    - A test asserts the happy path calls `writeTextFile` and pushes a `toast_saved` success toast.
    - A test asserts a `null` `save()` resolution pushes NO toast and does NOT call `writeTextFile`.
    - A test asserts a thrown `writeTextFile` pushes an `h_export_error_title` error toast.
    - A test asserts the `save()` defaultPath matches `orders_<from>_<to>.csv` for the active range.
    - `npx vitest run src/__tests__/screen-history.test.jsx` passes.
  </acceptance_criteria>
  <done>The Export handler builds/saves/writes with correct cancel-vs-error semantics, D-14 filename, and success toast; integration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate the Export button — disabled + tooltip when nothing is visible</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - src/screen-history.jsx (the inert Export button at lines 843-845 — currently hardcoded `disabled` with the dimming style; the toolbar wrapper at line 838)
    - .planning/phases/11-reprint-csv-export/11-UI-SPEC.md (## UI Considerations E1 empty row; ## Copywriting Contract — h_export_empty_tooltip)
  </read_first>
  <behavior>
    - When visible.length > 0: the Export button is enabled, full-opacity, clickable, and calls handleExportCsv.
    - When visible.length === 0: the button is disabled with opacity 0.5 / pointerEvents none / cursor not-allowed and a native title of h_export_empty_tooltip; clicking does nothing.
    - The label always reads h_export ("Export CSV" / "Exportă CSV"), single-line.
  </behavior>
  <action>
    In `src/screen-history.jsx`, replace the Export button's hardcoded `disabled` with `disabled={visible.length === 0}`, condition the inline `opacity: 0.5; pointerEvents: 'none'; cursor: 'not-allowed'` dimming style on that same boolean (full-opacity/clickable when non-empty), add `title={visible.length === 0 ? t('h_export_empty_tooltip') : undefined}`, and add `onClick={handleExportCsv}`. Keep the `.btn-secondary` class and the existing `<Icon name="download" size={14} /> {t('h_export')}` composition (D-10/UI-SPEC — Export is a toolbar utility action, label unchanged). This resolves the empty-list discretion item as disable-with-tooltip (the project greyed-out-not-hidden convention), NOT a header-only-file fallback. Extend `src/__tests__/screen-history.test.jsx` with an empty-`visible` render asserting the button is disabled and carries the `h_export_empty_tooltip` title, and a non-empty render asserting it is enabled.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The Export button uses `disabled={visible.length === 0}` (no hardcoded `disabled`).
    - A test asserts the button is disabled with the `h_export_empty_tooltip` title when `visible` is empty.
    - A test asserts the button is enabled and clickable (invokes `handleExportCsv`) when `visible` is non-empty.
    - `npx vitest run src/__tests__/screen-history.test.jsx` passes.
  </acceptance_criteria>
  <done>The Export button is enabled only when there is something to export, greyed + tooltipped when empty, and wired to handleExportCsv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS export handler → OS filesystem (via save+writeTextFile) | A user-picked path receives an app-generated file |
| in-hand AdminOrder summaries → CSV artifact | Already-fetched data (no new fetch) becomes an exported file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-E1 | Tampering (arbitrary file overwrite) | `save()` defaultPath / write path | low | mitigate | Filename is derived from the app's own period `from`/`to` (D-14), never user-typed; the actual write path comes only from `save()`'s return (never reconstructed), and the native OS Save dialog enforces overwrite confirmation |
| T-11-E2 | Information Disclosure | CSV contents = data the user already sees | low | accept | Export operates only on `visible` — data already on screen for an already-authorized staff user; no new data surfaced |
| T-11-E3 | Denial of Service (misrouted error) | cancel-vs-error handling | medium | mitigate | Explicit `if (!path) return;` before `writeTextFile` so a cancelled dialog is a silent no-op, not an error toast (Pitfall 3), asserted by a test |
</threat_model>

<verification>
- `npx vitest run src/__tests__/screen-history.test.jsx` green (happy/cancel/error/empty).
- Wave merge: `npx vitest run` full suite green.
- Manual (VALIDATION Manual-Only): filter History → Export CSV → save → open in Excel; confirm rows/headers/escaping and Romanian diacritics render (BOM); early `npm run tauri dev` smoke test confirms the capability grants permit save()+writeTextFile() at runtime (Research A1).
</verification>

<success_criteria>
Staff can export the currently-filtered History results to a CSV via a native Save dialog with a self-describing filename; cancel is a silent no-op, errors toast, and the button greys out when nothing is visible; integration tests pass.
</success_criteria>

<output>
Create `.planning/phases/11-reprint-csv-export/11-04-SUMMARY.md` when done.
</output>
