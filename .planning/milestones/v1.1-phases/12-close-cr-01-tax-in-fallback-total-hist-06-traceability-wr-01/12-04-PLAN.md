---
phase: 12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/phases/10-filters-search/10-VALIDATION.md
  - .planning/phases/11-reprint-csv-export/11-VALIDATION.md
autonomous: false
requirements: [D-05, D-07, D-08, D-10]
must_haves:
  truths:
    - "The full vitest suite is green except the 3 documented pre-existing v1.0 failures (build-pipeline BILD-04, offline-buttons ×2), which are noted as the only remaining red"
    - "cargo check --lib reports ZERO warnings (G-07-1 confirmed closed — src-tauri/src/lib.rs table field read)"
    - "The already-fixed CR-01/WR-01/G-07-1 code (src/data.jsx, src/screen-history.jsx CustomRangePopover, src-tauri/src/lib.rs) is NOT re-touched by this phase (verify-only per D-05)"
    - "v1.1-MILESTONE-AUDIT.md marks CR-01 (tax-omission + percent-discount), WR-01, and G-07-1 as RESOLVED, citing commit SHAs 7d9810b/30c89d8, 033cc39, 50492d5"
    - "The audit correction reconciles the CR-01/CR-02 numbering discrepancy: it cites both financial bugs by SHA + one-line description (tax-omission 7d9810b, percent-discount 30c89d8) rather than silently relabeling, noting the audit's numbering diverges from 10-REVIEW.md's original CR-01(discount)/CR-02(tax)"
    - "Phases 10 and 11 VALIDATION.md are promoted from status: draft to a real verdict via /gsd-validate-phase 10 and 11 (D-10); whatever COMPLIANT/PARTIAL result is accepted and reflected in the audit's Nyquist section"
    - "The audit verdict is re-derived from the phase's own green results (CR-01/WR-01/G-07-1 resolved, HIST-06 documented, state continuity fixed, Nyquist 10/11 reconciled) toward a clean passed or a judgment-based qualified state"
  artifacts:
    - ".planning/v1.1-MILESTONE-AUDIT.md — corrected tech_debt entries, updated Nyquist section, re-derived verdict + frontmatter status"
    - ".planning/phases/10-filters-search/10-VALIDATION.md — promoted verdict (via /gsd-validate-phase 10)"
    - ".planning/phases/11-reprint-csv-export/11-VALIDATION.md — promoted verdict (via /gsd-validate-phase 11)"
  key_links:
    - "12-01 D-06 tests + 12-02 HIST-06 docs + 12-03 state-continuity fix ↔ audit re-derivation — the audit verdict depends on all three Wave-1 plans landing green"
  verification: backstop
---

<objective>
The final, last-wave plan: VERIFY that CR-01/WR-01/G-07-1 are genuinely closed (D-05), run the two
human/live checks (D-07 WR-01 popover + D-02 return-from-detail end-to-end), promote the Phase 10/11
Nyquist VALIDATION.md files (D-10), and correct `v1.1-MILESTONE-AUDIT.md` in place (D-08) — citing
commit SHAs, reconciling the CR-01/CR-02 numbering discrepancy, and re-deriving the verdict from this
phase's own green results.

This plan MUST run AFTER 12-01/12-02/12-03 land, because the audit re-derivation reflects their
results (D-06 tests green, HIST-06 documented, state continuity fixed). Do NOT re-touch the
already-fixed CR-01/WR-01/G-07-1 CODE — verify and document only (D-05).

Purpose: re-derive the v1.1 milestone from `tech_debt` toward a clean `passed`, unblocking
`/gsd-complete-milestone` as a next step (out of scope here).
Output: promoted Nyquist verdicts + a corrected, re-derived audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-CONTEXT.md
@.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-RESEARCH.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<artifacts_this_phase_produces>
This plan EDITS existing documentation and runs validation commands — it creates no new source files.

Changes:
- `v1.1-MILESTONE-AUDIT.md`: tech_debt entries for CR-01/WR-01/G-07-1 marked resolved (with SHAs),
  the CR-01/CR-02 numbering reconciled, the Nyquist section (§5) updated for phases 10/11, and the
  §6 verdict + frontmatter `status` re-derived.
- `10-VALIDATION.md` / `11-VALIDATION.md`: `status` and `nyquist_compliant` promoted by
  `/gsd-validate-phase` (these files are written by that command, not hand-edited here).

No production code changes. CR-01/WR-01/G-07-1 source files are verified read-only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify-only pass — full suite green + cargo check clean + CR-01/WR-01/G-07-1 code untouched (D-05)</name>
  <files>(verify-only — reads src/data.jsx, src/screen-history.jsx, src-tauri/src/lib.rs; edits none)</files>
  <read_first>
    - .planning/v1.1-MILESTONE-AUDIT.md §112-124 — the ranked tech-debt list this pass confirms
      resolved (CR-01 critical, WR-01, G-07-1) and the 3 [INFO] pre-existing failures to leave red.
    - 12-RESEARCH.md §Sources — the exact expected baseline: `npx vitest run` → `Tests 3 failed |
      481 passed (484)` before this phase's new tests; `cargo check --lib` → zero warnings.
    - src/data.jsx §200-221, src/screen-history.jsx §585-815 (CustomRangePopover + customWrapperRef),
      src-tauri/src/lib.rs §53-72 + §240-270 — the already-fixed code to confirm is UNCHANGED by
      this phase (read to confirm, do not edit).
    - The four fix commits to cite downstream: `7d9810b` (CR-02 tax), `30c89d8` (CR-01 percent
      discount), `033cc39` (WR-01 popover boundary), `50492d5` (G-07-1 table field).
  </read_first>
  <action>
    Run the verification pass (no code edits):

    1. `npx vitest run` — full suite. Confirm the ONLY red is the 3 documented pre-existing v1.0
       failures (`build-pipeline.test.js` BILD-04, `offline-buttons.test.jsx` ×2). The new D-06
       (12-01) and store/history (12-03) tests must be green. Record the exact pass/fail counts.

    2. `cd src-tauri && cargo check --lib` — confirm ZERO warnings (G-07-1: `table` field read on the
       thermal ticket). Record the result.

    3. Confirm the already-fixed CR-01/WR-01/G-07-1 code is UNCHANGED by this phase:
       `git diff <phase-base>..HEAD -- src/data.jsx src-tauri/src/lib.rs` must show NO change to the
       CR-01/G-07-1 fix regions, and the WR-01 CustomRangePopover/customWrapperRef region of
       `src/screen-history.jsx` must be unchanged EXCEPT for 12-03's state-wiring edits (which touch
       the period/filter state block, not the popover boundary). Record the confirmation.

    Capture all three results in this plan's SUMMARY as the evidence trail feeding Task 3's audit
    re-derivation. Do NOT modify any source or test file in this task.
  </action>
  <verify>
    <automated>npx vitest run && cd src-tauri && cargo check --lib</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` shows only the 3 pre-existing v1.0 failures red; all Phase 12 new tests green.
    - `cargo check --lib` completes with zero warnings.
    - `git diff` confirms CR-01 (`src/data.jsx`) and G-07-1 (`src-tauri/src/lib.rs`) fix regions are byte-unchanged this phase; the WR-01 popover boundary in `screen-history.jsx` is unchanged apart from 12-03's state wiring.
    - No source or test file modified by THIS task (verify-only).
  </acceptance_criteria>
  <done>
    Full suite green (except the 3 documented reds), cargo check clean, and the CR-01/WR-01/G-07-1
    code confirmed untouched — the evidence trail for the audit re-derivation is captured.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — WR-01 popover live check + return-from-detail continuity + reset-on-leave (D-07/D-01/D-02/D-03)</name>
  <action>Blocking human checkpoint. Present the verification script below, wait for the resume signal, and record the outcome of all three steps in the SUMMARY. Do NOT edit any code in this task.</action>
  <what-built>
    The return-from-detail state-continuity fix (12-03 historySelection slice) and the already-fixed
    WR-01 Custom-pill popover boundary. Both need a live human check against the running app — the
    WR-01 audit claimed "independently reproduced" (D-07), and the D-02 round-trip is a
    human-observable UX assertion beyond the component tests.
  </what-built>
  <how-to-verify>
    Run the app (`npm run tauri dev` or the project's standard dev launch), sign in, open History, then:

    1. WR-01 popover (D-07): click the **Custom** period pill to OPEN its date popover, then click the
       **Custom** pill AGAIN. Expected: the popover CLOSES (no reopen-on-re-click race). Also confirm
       Escape and a true outside-click still close it.

    2. Return-from-detail continuity (D-01/D-02): set a NON-default period (e.g. 7 days or a Custom
       range) AND a status filter (e.g. Completed) AND a type filter (e.g. Delivery) AND type a search
       term. Click a row to open the read-only detail, then click **Back**. Expected: the list returns
       with ALL FOUR selections intact (period + status + type + search), and the search results are
       correctly filtered on the very first frame (no flash of unfiltered rows).

    3. Reset-on-leave (D-03): from History (still holding non-default selections), navigate to
       **Orders** (or KDS/POS) via the sidebar, then return to **History**. Expected: selections are
       RESET to defaults (30 days / All / All / empty search).
  </how-to-verify>
  <resume-signal>Type "approved" if all three behave as expected, or describe what differed (which step, observed vs expected).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promote Nyquist 10/11 (D-10) and correct v1.1-MILESTONE-AUDIT.md in place (D-08)</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md, .planning/phases/10-filters-search/10-VALIDATION.md, .planning/phases/11-reprint-csv-export/11-VALIDATION.md</files>
  <read_first>
    - .planning/v1.1-MILESTONE-AUDIT.md (whole file) — the frontmatter (`status: tech_debt`, scores,
      tech_debt list, nyquist block), §4 ranked tech debt, §5 Nyquist coverage table, §6 verdict.
    - .planning/phases/10-filters-search/10-VALIDATION.md and
      .planning/phases/11-reprint-csv-export/11-VALIDATION.md — currently `status: draft`,
      `nyquist_compliant: false`; the D-10 promotion targets.
    - .planning/phases/10-filters-search/10-REVIEW.md §45,57,67 — the ORIGINAL CR-01(percent-discount
      100×)/CR-02(tax-omission) numbering that the audit inverted; source for the reconciliation note.
    - 12-RESEARCH.md §Pitfall 2 and §Open Questions #1 — the recommended reconciliation approach
      (cite by SHA + description, add a footnote on the numbering divergence, do NOT silently relabel).
    - Task 1's captured evidence (suite counts, cargo result, code-untouched confirmation) and Task 2's
      human approval — the inputs to the re-derived verdict.
  </read_first>
  <action>
    Two coupled steps — Nyquist promotion first (its output feeds the audit's §5), then the audit
    correction:

    1. D-10 Nyquist promotion: run `/gsd-validate-phase 10` then `/gsd-validate-phase 11` to promote
       each draft VALIDATION.md to a real verdict. These are coverage TODOs, not compliance failures
       (both phases are otherwise fully verified). ACCEPT whatever COMPLIANT/PARTIAL verdict each
       produces — do not force a result. Record the two verdicts.

    2. D-08 audit correction — edit `.planning/v1.1-MILESTONE-AUDIT.md` in place (scoped Edits):
       - Mark CR-01 RESOLVED: cite the tax-omission fix `7d9810b` AND the percent-discount fix
         `30c89d8`, each by SHA + a one-line description, plus that 12-01 added the regression tests.
         Add a footnote reconciling the numbering: the audit/CONTEXT label the tax bug "CR-01" while
         `10-REVIEW.md` defines CR-01 as the percent-discount 100× bug and CR-02 as tax-omission, and
         the fix commits follow `10-REVIEW.md`'s numbering. Do NOT silently relabel — cite by SHA so a
         future reader diffing against `10-REVIEW.md` is not misled (Pitfall 2 / Open Question #1).
       - Mark WR-01 RESOLVED (commit `033cc39`; 3 regression tests present + the D-07 live check just
         passed).
       - Mark G-07-1 RESOLVED (commit `50492d5`; `cargo check --lib` clean per Task 1).
       - Mark the HIST-06 traceability gap RESOLVED (12-02) and the return-from-detail state-continuity
         limitation RESOLVED (12-03).
       - Update the Nyquist section (§5 + the frontmatter `nyquist` block) with the promoted Phase
         10/11 verdicts from step 1.
       - Note the 3 pre-existing v1.0 test failures ([INFO]) remain the ONLY red in an otherwise-green
         suite — left deferred, NOT fixed this phase.
       - Re-derive the §6 verdict and the frontmatter `status` from these facts. With the lone CRITICAL
         (CR-01) and WR-01/G-07-1 resolved, HIST-06 documented, state continuity fixed, and Nyquist
         10/11 reconciled, the milestone should re-derive toward a clean `passed` (or a
         judgment-based qualified state if a Nyquist promotion returns PARTIAL — use judgment against
         the re-derived facts per Claude's Discretion, and state the reasoning).

    Cite D-08 and D-10 in the edited prose. Do NOT edit CR-01/WR-01/G-07-1 source code.
  </action>
  <verify>
    <automated>grep -Eq '7d9810b|30c89d8' .planning/v1.1-MILESTONE-AUDIT.md && grep -q '033cc39' .planning/v1.1-MILESTONE-AUDIT.md && grep -q '50492d5' .planning/v1.1-MILESTONE-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `10-VALIDATION.md` and `11-VALIDATION.md` no longer have `status: draft` (promoted by /gsd-validate-phase; whatever verdict, recorded).
    - `v1.1-MILESTONE-AUDIT.md` cites all four SHAs (7d9810b, 30c89d8, 033cc39, 50492d5) and marks CR-01/WR-01/G-07-1 resolved.
    - The audit contains an explicit reconciliation note on the CR-01/CR-02 numbering divergence vs 10-REVIEW.md (no silent relabel).
    - The audit's §5 Nyquist table reflects the promoted Phase 10/11 verdicts.
    - The §6 verdict and frontmatter `status` are re-derived (with stated reasoning) from this phase's green results; the 3 pre-existing failures are noted as the only remaining red.
    - No CR-01/WR-01/G-07-1 source file edited.
  </acceptance_criteria>
  <done>
    Phase 10/11 Nyquist verdicts are promoted; the milestone audit is corrected in place with SHA
    citations, the CR-01/CR-02 numbering reconciled, the Nyquist section updated, and the verdict
    re-derived toward a clean milestone state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verify-and-document plan. Runs tests + validation commands and edits documentation. No code change, no external input, no API/SDK/Tauri/CSP change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-01 | Repudiation | Milestone audit verdict (`v1.1-MILESTONE-AUDIT.md`) | low | mitigate | The re-derivation is backed by an explicit evidence trail (Task 1 suite/cargo results, Task 2 human approval, cited commit SHAs) and reconciles the CR-01/CR-02 numbering rather than silently relabeling — so the audit remains diff-verifiable against 10-REVIEW.md. |
| T-12-04-02 | Tampering | Financial-display fallback path (verify-only) | low | accept | Confirmed already-mitigated: 12-01's regression tests + this plan's verify pass guard the fallback total / percent-discount path. No new external threat; correctness guardrail already in place. Accepted. |

Attack surface is minimal: verification and documentation only. No new external inputs, no new
API/SDK calls, no new Tauri capability grants, no new CSP entries. No package installs.
</threat_model>

<verification>
- `npx vitest run` green except the 3 documented pre-existing failures.
- `cargo check --lib` zero warnings.
- Human checkpoint (Task 2) approved: WR-01 popover closes on re-click; return-from-detail preserves all four selections; leaving History resets to defaults.
- Audit cites all four SHAs and reconciles the CR-01/CR-02 numbering.
- Phase 10/11 VALIDATION.md promoted out of `draft`.
</verification>

<success_criteria>
CR-01/WR-01/G-07-1 verified closed (code + tests + live check); Nyquist 10/11 promoted; the milestone
audit corrected and re-derived toward a clean state — v1.1 ready for `/gsd-complete-milestone` (a
next step, out of scope here).
</success_criteria>

<output>
Create `.planning/phases/12-close-cr-01-tax-in-fallback-total-hist-06-traceability-wr-01/12-04-SUMMARY.md` when done
</output>
