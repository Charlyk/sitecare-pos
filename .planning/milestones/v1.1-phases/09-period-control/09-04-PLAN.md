---
phase: 09-period-control
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - src/screen-history.jsx
  - src/__tests__/screen-history.test.jsx
autonomous: true
requirements: [HIST-04]
must_haves:
  truths:
    # --- Goal-backward observable truths (SC1, SC3) ---
    - "Clicking the Today, 7 days, or 30 days pill refetches the list for that range and the day-grouped rows reload for it — the pills are no longer disabled and no longer hardcode their styling (HIST-04, SC1)"
    - "The summary strip retargets with the period automatically because it is computed by computeSummary from the same fetched list the day groups are built from — there is no second fetch, no second loading state, and no second error state (SC3, P7 D-15)"
    - "The active range is resolved exactly once per state transition inside a useMemo keyed on the selected period, never by calling a range builder inline in the render body — a fresh clock reading every render would change the query key every render and refetch in a loop (RESEARCH Pitfall 1)"
    - "The default period on mount is 30 days, matching the range Phase 7 shipped — opening History shows the same data it showed before this phase"
    - "The screen calls getPresetRange for preset periods and passes the resolved { from, to } into useHistoryOrders — the screen performs no date arithmetic of its own (D-14/09-01)"
    # --- D-05: dimmed loading, distinct from inert ---
    - "During a period switch the previous rows and day headers stay visible at opacity 0.6 with pointer-events unchanged and cursor still pointer — rows remain clickable and the treatment reads as loading, never as disabled (D-05, UI-SPEC Period Switch Loading Contract)"
    - "The dimming gates on isFetching, not isLoading — isLoading is false for every switch after the first successful load, so gating on it would silently render no feedback at all (RESEARCH Pitfall 2)"
    - "The 0.6 dimming value is distinct from the inert bar's 0.5, and the dimmed region never sets pointer-events none or cursor not-allowed — the two conventions render on this screen simultaneously and must not collide (P7 D-14, PATTERNS Shared Patterns)"
    - "A 16px refresh icon carrying the .spin class renders beside the period pill group while a switch is in flight, and is absent otherwise (UI-SPEC Period Switch Loading Contract)"
    - "The first-load skeleton still gates on isLoading and is unchanged — a period switch never renders skeleton rows, because keepPreviousData means there is always a previous value to dim (UI-SPEC)"
    # --- D-06: label follows data ---
    - "The tile sub-label and the empty-state period phrase derive from the range that PRODUCED the visible rows, not from the clicked pill — during an in-flight switch from 30 days to 7 days, the sub-label still reads '30 zile' while the 30-day figures are still on screen, and label and number flip together when the new data lands (D-06)"
    - "The selected pill DOES update immediately on click — it is the intent signal, and the dimming is what visually ties the lit pill to the lagging tiles so the mismatch never reads as a bug (D-06)"
    # --- D-07 / D-08: failed switch ---
    - "A failed period switch renders the same full-area ErrorBlock as a failed first load — exactly one error treatment exists on this screen, and the previously visible dimmed rows are discarded rather than kept (D-07, deliberately diverging from P8 D-07)"
    - "The pill the staff member clicked stays selected through the error, and Retry refetches THAT range — the selection never snaps back to the last successful period (D-08)"
    - "Switching from a failed pill to a previously-successful pill restores that list instantly from cache with no error flash and no refetch (D-08)"
    # --- D-12 / D-13: period copy ---
    - "The Orders and Revenue tile sub-labels read the settled period's label from the same h_period_* keys the pills use — the hardcoded t('h_period_30') at screen-history.jsx:246-247, which claims '30 zile' on every tile regardless of period, is gone (D-12)"
    - "The empty state renders h_empty_prefix followed by the settled period's prepositional phrase and a full stop — 'Nicio comandă azi.' / 'Nicio comandă în ultimele 7 zile.' — never the pill label interpolated directly, which is ungrammatical in Romanian (D-13)"
    - "The Today prepositional phrase reuses h_period_today lowercased rather than a ninth i18n key — 'azi' is a bare adverb that fits mid-sentence without a preposition, unlike the other three periods (D-13, UI-SPEC table)"
    - "The Avg and Refunds tiles' sub-labels are unchanged — h_avg_sub and the canceled-count suffix are not period-dependent and D-12 does not touch them"
    - "The tile sub-label's font weight stays 500 and the pill padding stays 7px 12px — this plan changes computed content and interactivity only, never a shipped visual value (UI-SPEC Typography, Spacing rows 1-2)"
    # --- Inert-control preservation ---
    - "The status pills, the search input, and the Export button remain disabled and dimmed — only the four period pills become interactive; this plan must not accidentally enable a Phase 10/11 control while editing the shared FilterBar (CLAUDE.md rule 5, UI-SPEC scope note)"
    # --- Edge probe lift: HIST-04 / empty (covered) ---
    - "Switching to a period with zero finished orders renders the empty-state component with that period's phrase, not an error and not a stale row set; the summary tiles show computed zeroes with the Avg tile showing a formatted zero rather than an em-dash (P7 Avg convention preserved)"
    # --- Edge probe lift: HIST-04 / ordering (covered) ---
    - "After a period switch, day groups remain newest-day-first and rows within a day newest-first, and two orders sharing an identical timestamp keep a stable relative order — retargeting the fetch changes which orders groupOrdersByDay receives, never how it orders them"
    # --- UI-SPEC lift: E3 / empty (BACKSTOP) ---
    - statement: "Switching TO a period that turns out to have zero orders passes through the dimmed-loading state first and then lands on the empty-state component — it does not skip straight to empty, and does not flash the previous period's rows as though they were the new period's empty result"
      verification: backstop
    # --- UI-SPEC lift: E3 / overflow (BACKSTOP) ---
    - statement: "Switching to the 366-day cap's worst case does not visibly freeze the UI during the dimmed window — the spinner keeps animating even while the main thread is briefly busy parsing a large response. This is D-10's knowingly-unmeasured 12x case and is the trigger 09-CONTEXT.md flags for revisiting virtualization"
      verification: backstop
    # --- UI-SPEC lift: E1 covered rows ---
    - "E1/loading: the selected pill's own state updates immediately on click while the data lags (D-06)"
    - "E1/error: the clicked pill stays selected through a failed switch and does not snap back (D-08)"
    - "E1/populated: pill selected/unselected styling is state-driven — selected renders var(--sc-foreground) with #fff text, unselected transparent with #555 — ported from the design source's live ternary rather than hardcoded per pill"
    # --- UI-SPEC lift: E3 covered rows ---
    - "E3/loading: rows and tiles dim to 0.6 with the spinner present and the sub-label pinned to the settled period"
    - "E3/populated: rows and tiles return to full opacity, the spinner disappears, and the sub-label flips to the new period — all in the same render, never staggered"
    # --- UI-SPEC lift: E4 covered rows ---
    - "E4/loading: Retry re-triggers the fetch for the same attempted range via the existing refetch() and returns to the dimmed treatment"
    - "E4/error: the error state reuses ErrorBlock and SummaryStrip's existing isError branch verbatim — no new error component and no new error copy is introduced"
    - "E4/populated: a successful retry transitions directly to normal populated rows with no intermediate recovered visual"
    - "E4/partial: the previous period's cache entry survives the failure — switching away from the failed pill to an already-successful one restores it with no error residue"
    # --- UI-SPEC lift: E5 covered rows ---
    - "E5/empty: the empty-state prepositional phrase renders correctly for all four periods per the Period-Dependent Copy Contract table"
    - "E5/loading: the sub-label text is pinned to the settled data's period throughout the loading window"
    - "E5/error: on error the tile shows an em-dash via the existing isError branch and no stale sub-label persists under it"
    - "E5/populated: the tile sub-label is the exact period label that produced the visible rows, from the same key the pill reads (D-12)"
    - "E5/long-text: both Romanian prepositional phrases render in their sites without clipping — ro is the longer locale and was authored to fit"
  prohibitions:
    - statement: "The screen must never display a period label describing data it is not currently showing. A tile that reads '7 zile' above a figure computed from 30 days of orders is a false claim about money to the staff who reconcile against it — it is not a cosmetic lag. The rendered period must be derived from the range that produced the visible rows, never from the pill's selected state, for the entire duration of every switch."
    - statement: "A failed period switch must never leave the previous period's rows on screen in a state a staff member could read as the newly requested period's result. Either the rows belong to the period the screen claims, or the screen shows an error instead — a dimmed row set under a lit pill naming a different period, with no error, is the one outcome that is worse than losing the data."
  artifacts:
    - "src/screen-history.jsx — selectedPeriod state, useMemo range resolution, settledPeriod tracking, live preset pills, D-05 dimming + spinner, periodPhrase helper, D-12 tile sub-labels, D-13 empty state"
    - "src/__tests__/screen-history.test.jsx — extended with preset-switch, dimming, settled-label, error/retry, and empty-state-copy assertions"
  key_links:
    - "selectedPeriod -> useMemo(getPresetRange) -> useHistoryOrders({from,to}) is the one path from a click to a fetch; the useMemo is the query-key-stability guard and must be keyed on the period, not recomputed per render"
    - "isPlaceholderData transitioning to false is the only thing that advances settledPeriod — this is D-06's whole mechanism, and reading selectedPeriod instead anywhere in the render is the prohibition above"
    - "isFetching drives the dimming; isLoading still drives the skeleton — two different booleans for two different states"
    - "EmptyBlock and SummaryStrip both consume the settled period through the same periodPhrase / label lookup, so pill, tiles, and empty state cannot drift (D-12)"
---

<objective>
Make the period pills live. Wire the selected period through a memoized range into the parameterized
hook, add D-05's dimmed-in-place loading treatment, pin every period-dependent label to the range
that actually produced the visible rows (D-06), and fix the hardcoded "30 zile" sub-label that
currently claims one period on every tile regardless of state.

Purpose: this is SC1 and SC3. After this plan a staff member can retarget the whole screen with the
three presets, and the summary strip follows for free because it was never a second data source.
Output: `src/screen-history.jsx` with three of the four periods live; the Custom pill's popover is
09-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-period-control/09-CONTEXT.md
@.planning/phases/09-period-control/09-RESEARCH.md
@.planning/phases/09-period-control/09-PATTERNS.md
@.planning/phases/09-period-control/09-UI-SPEC.md
@.planning/phases/09-period-control/09-01-SUMMARY.md
@.planning/phases/09-period-control/09-02-SUMMARY.md
@.planning/phases/09-period-control/09-03-SUMMARY.md
@src/screen-history.jsx
@src/history-utils.js
@src/use-history-orders.js
</context>

## Artifacts this phase produces

See `09-01-PLAN.md` § "Artifacts this phase produces" for the full Phase 9 symbol list. This plan
contributes, in `src/screen-history.jsx`:

- `periodPhrase(period, t, lang)` — module-private prepositional-copy helper (D-13)
- `data-testid` values: `history-period-pill`, `history-switch-spinner`
- A changed `FilterBar` signature (now receives period state and handlers)
- A changed `SummaryStrip` signature (now receives the settled period and `isFetching`)
- A changed `EmptyBlock` signature (now receives the settled period)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Period state, memoized range resolution, live preset pills</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/screen-history.jsx` — the whole file (345 lines). Specifically: `HistoryScreen` (214-240) for the hook call and branch structure; `FilterBar` (294-345) for the inert pills, the `inertBtn` constant at 311, and the status/search/Export controls at 325-342 that must stay inert.
    - `src/use-history-orders.js` — as rewritten by 09-03. The hook now takes `{ from, to }` and returns `isPlaceholderData` / `isFetching` alongside the existing fields.
    - `src/history-utils.js` — `getPresetRange` as added by 09-01, and its `null` return for unrecognized ids.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Period Pills Contract — the state/background/text-color table, and the explicit note that padding, container chrome, and group layout do not change.
    - `.planning/phases/09-period-control/09-RESEARCH.md` § Common Pitfalls 1 — why the range must be memoized rather than recomputed per render.
    - `sitecare-orders/project/src/screen-history.jsx` lines 149-165 and 231-238 — the design source's `periods` array and live pill ternary. Port the ternary's logic; do NOT port `useStateHist` or any `window.*` global, which are prototype-era module scaffolding forbidden by CLAUDE.md rule 4.
  </read_first>
  <action>
    In `HistoryScreen`, introduce the period state and resolve it to a range exactly once per
    transition:

    - `const [selectedPeriod, setSelectedPeriod] = useState({ id: '30' })` — an object, not a bare
      string. 09-05 adds a `customRange` field to it; shaping it as an object now means 09-05 extends
      this state rather than replacing it. `'30'` is the default so opening History shows exactly the
      data Phase 7 shipped.
    - `const range = useMemo(() => getPresetRange(selectedPeriod.id), [selectedPeriod])` — the
      `useMemo` is load-bearing, not an optimization. `getPresetRange` reads the clock; calling it
      inline in the render body would produce fresh ISO strings on every render, changing the query
      key every render and refetching in a loop. This is the same discipline the hook's old lazy
      initializer enforced, moved one level up, and it is why 09-03 was able to delete that
      initializer. 09-05 extends this memo with the custom branch.
    - `const { data, isLoading, isError, isFetching, isPlaceholderData, refetch } = useHistoryOrders(range ?? {})`
      — `getPresetRange` returns `null` for an unrecognized id, and the hook's `enabled` guard
      (`!!from && !!to`) turns that into "no request" rather than a request with undefined params.

    Track the settled period per D-06:
    - `const [settledPeriod, setSettledPeriod] = useState(selectedPeriod)`
    - An effect that advances it only when the data is real: when the query has succeeded AND
      `isPlaceholderData` is false, set `settledPeriod` to the current `selectedPeriod`. Depend on
      the success flag, `isPlaceholderData`, and `selectedPeriod`. `selectedPeriod` is a stable
      object reference between transitions (it only changes when `setSelectedPeriod` is called), so
      this effect does not loop.
    - Every period-dependent render — the tile sub-labels and the empty state (Task 3) — reads
      `settledPeriod`. The pill styling, and ONLY the pill styling, reads `selectedPeriod`. This split
      is the whole of D-06: the pill is the intent signal, the labels are the truth signal, and the
      dimming (Task 2) is what ties them together visually so their disagreement reads as "loading",
      not as a bug.

    Rewrite `FilterBar`'s period pill group:
    - `FilterBar` now takes `{ t, selectedPeriod, onSelectPeriod, isFetching }`.
    - Replace the four unrolled `disabled` buttons (lines 318-321) with a `.map()` over the existing
      `periods` array. Per UI-SPEC's Period Pills Contract, styling is state-driven: selected →
      `background: 'var(--sc-foreground)', color: '#fff'`; unselected → `background: 'transparent',
      color: '#555'`. No `opacity` override on these four buttons at all — the 0.5 dimming was the
      inert marker and these are no longer inert.
    - These four buttons need their own style constant rather than the shared `inertBtn`: keep the
      identical `border: 0, padding: '7px 12px', borderRadius: 8, fontWeight: 600, fontSize: 13,
      fontFamily: 'inherit'` values, but with `cursor: 'pointer'` and no `pointerEvents: 'none'`.
      Do not change the padding — UI-SPEC's Spacing table row 2 formally grandfathers `7px 12px` and
      re-spacing shipped pixel-perfect UI would itself be the unsanctioned change Design Fidelity
      forbids. Leave `inertBtn` in place and unchanged for the status pills, which still use it.
    - Give each pill `data-testid="history-period-pill"` and `onClick={() => onSelectPeriod(p.id)}`.
    - The `'custom'` pill maps like the others in this task. Clicking it calls `onSelectPeriod('custom')`,
      which resolves to a `null` range and therefore no fetch — the list simply keeps showing the
      previous period. 09-05 replaces that no-op with the popover. Wire `onSelectPeriod` in
      `HistoryScreen` to ignore `'custom'` for now (early-return before `setSelectedPeriod`), so this
      wave never renders a lit pill above data from a different period — which is exactly the D-06
      prohibition. Record this as a deliberate one-wave placeholder in the SUMMARY.
    - The container `div` at line 317 — its `#fff` background, `1px solid hsl(120 10% 90%)` border,
      `10px` radius, and `3px` padding — is untouched. UI-SPEC's Spacing table row 1 formally
      grandfathers the `3px`.

    Do NOT touch the status pill group (325-331), the search input (333-336), or the Export button
    (338-342). They stay `disabled`, at opacity 0.5, with `pointerEvents: 'none'`. CLAUDE.md rule 5
    and UI-SPEC's scope note both require it, and this plan edits the file they live in.

    Extend `src/__tests__/screen-history.test.jsx`. The file mocks `useHistoryOrders` wholesale, so
    the hook's return value is fully controllable — assert the *call argument* to verify the resolved
    range reaches it.
  </action>
  <behavior>
    - On mount, `useHistoryOrders` is called with a `{ from, to }` deep-equal to `getPresetRange('30')` for the same day.
    - The 30-days pill renders selected (`var(--sc-foreground)` background) on mount; the other three render unselected (`transparent`).
    - None of the four period pills has the `disabled` attribute; all four are clickable.
    - Clicking the "7 zile" pill calls `useHistoryOrders` on the next render with a range deep-equal to `getPresetRange('7')`, and the 7-day pill becomes selected while the 30-day pill becomes unselected.
    - Clicking "Azi" resolves to `getPresetRange('today')`.
    - Re-rendering the screen without a click passes an unchanged `{ from, to }` — asserted by the memo not producing a new value; a naive inline call would be caught by the range differing across renders when the clock advances.
    - The status filter pills, the search input, and the Export button all still carry `disabled`.
    - Clicking a status pill or the Export button changes nothing about the fetched range.
  </behavior>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0.
    - `grep -c "getPresetRange" src/screen-history.jsx` returns 2 — the import and exactly one call site, inside the memo.
    - `grep -c "useMemo(() => getPresetRange" src/screen-history.jsx` returns 1 — the resolution is memoized.
    - `grep -c "data-testid=\"history-period-pill\"" src/screen-history.jsx` returns 1 — the pills are mapped, not unrolled, so there is one JSX site.
    - `grep -c "history-period-pill" src/__tests__/screen-history.test.jsx` returns at least 1.
    - A render test asserts exactly 4 elements with `data-testid="history-period-pill"` exist and none has the `disabled` attribute.
    - A render test asserts the status pills, search input, and Export button all still have `disabled` — `screen.getByPlaceholderText` for the search input and role-based queries for the buttons.
    - `grep -c "padding: '7px 12px'" src/screen-history.jsx` returns at least 1 — the grandfathered pill padding survives.
    - `grep -c "borderRadius: 10, padding: 3" src/screen-history.jsx` returns 1 — the grandfathered container padding survives.
    - `git diff src/screen-history.jsx | grep -cE "^[-+].*(statusFilters|h_search|h_export)"` returns 0 — no line touching a Phase 10/11 control was added or removed.
  </acceptance_criteria>
  <done>Three preset pills switch the fetched range through a memoized resolution; pill styling is state-driven; settledPeriod tracks the range that produced the data; every inert control is provably still inert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-05 dimmed-loading treatment + spinner; D-07/D-08 switch failure</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/screen-history.jsx` — `HistoryScreen`'s branch structure (216-237), and `SummaryStrip`'s `dimmed` boolean at line 262 (`const dimmed = isLoading || isError`), which already implements tile dimming for two states and is the thing to extend.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Period Switch — Loading Contract and § Period Switch — Error Contract. The 0.6-vs-0.5 distinction, the no-pointer-events-none rule, and the spinner spec are all there.
    - `.planning/phases/09-period-control/09-RESEARCH.md` § Common Pitfalls 2 and § Anti-Patterns — why `isLoading` is the wrong boolean for this and `isFetching` is the right one.
    - `.planning/phases/09-period-control/09-PATTERNS.md` § Shared Patterns → "Greyed-out/inert vs. dimmed-loading" and "isLoading vs isFetching".
    - `src/styles.css` — the `.spin` class as added by 09-02.
    - `.planning/phases/08-read-only-order-detail-view/08-CONTEXT.md` § D-07 — the keep-what-you-have instinct this phase's D-07 deliberately diverges from. Read it before touching the error branch, so the divergence is understood as user-directed rather than mistaken for an oversight.
  </read_first>
  <action>
    Add the dimmed-loading treatment. The gating boolean is `isFetching && !isLoading`: `isFetching`
    is true during any fetch including every switch after the first, whereas `isLoading` is
    `isPending && isFetching` and is therefore false for the entire remaining life of the component
    after the first successful load. Reusing `isLoading` here compiles fine and silently renders no
    feedback on any switch — that is RESEARCH Pitfall 2, and it is the most likely way this task
    fails.

    - Rows and day headers: wrap the `days.map(...)` output region in a container carrying
      `opacity: isSwitching ? 0.6 : 1` and a short opacity transition. Do NOT set
      `pointerEvents: 'none'` and do NOT change `cursor` — rows stay clickable and the treatment must
      read as data settling in place, not as a disabled feature. The `0.6` is deliberately distinct
      from the inert bar's `0.5`; both conventions render on this screen at the same time (the status
      pills are inert while the list is switching), and a shared value would make them
      indistinguishable.
    - Summary tiles: extend `SummaryStrip`'s existing `dimmed` boolean from `isLoading || isError` to
      also cover the switch, and pass `isFetching` in. The tile-dimming visual already built for
      first-load and error then covers D-05 for free. Do not add the shimmer skeleton to the switch
      path — the skeleton is first-load-only per UI-SPEC, and `keepPreviousData` guarantees a
      previous value exists to dim.
    - Spinner: render a 16px `refresh` `Icon` with `className="spin"` and
      `color: 'var(--sc-muted-foreground)'`, placed immediately after the period pill group's
      container inside `FilterBar`, and only while `isFetching && !isLoading`. UI-SPEC leaves the
      placement to the planner between "inline next to the selected pill" and "the table card header's
      right edge"; beside the pill group is chosen because it puts the loading signal adjacent to the
      control that caused it, which is what makes the lit-pill/lagging-tile disagreement read as
      loading. Give it `data-testid="history-switch-spinner"`. Sage is explicitly NOT used —
      UI-SPEC reserves the accent for the Apply button and lists the spinner as muted-foreground.
    - The skeleton branch keeps gating on `isLoading` and is otherwise unchanged.

    Error path (D-07/D-08) — mostly a no-op by design, and that is the point:
    - `ErrorBlock` is reused verbatim. Do not add a second error component, a period-switch-specific
      error strip, or any new error copy. D-07 is explicit that exactly one error treatment exists on
      this screen, matching first-load failure. The user chose this over keeping the old list,
      against the recommendation and against `P8 D-07`'s instinct — it is deliberate and user-directed.
    - The existing branch order already produces the correct behavior: `isError` replaces the table
      body and the dimmed rows are discarded. Verify this holds when `isError` coexists with
      `data` being a placeholder from the previous range — the branch must key on `isError` before it
      renders any rows, or a failed switch would show the previous period's rows under a pill naming
      the new one, which is the second prohibition in this plan's `must_haves`.
    - D-08 falls out for free and must be asserted rather than built: `selectedPeriod` is not reset on
      error, so the clicked pill stays lit; `refetch()` re-runs the current query key, which is the
      attempted range. Do not add any error-triggered `setSelectedPeriod`.

    Extend `src/__tests__/screen-history.test.jsx` for each state.
  </action>
  <behavior>
    - With the hook mocked as `{ data: [order], isLoading: false, isError: false, isFetching: true, isPlaceholderData: true }`: the rows region renders at opacity 0.6, the rows are still present, and the spinner testid is in the document.
    - The same state renders NO skeleton rows — a switch never shows the skeleton.
    - The dimmed rows region does not carry `pointer-events: none`, and a row's click handler still fires when clicked while dimmed.
    - With `isLoading: true`: the skeleton renders, the spinner does not, and the previous behavior is unchanged from Phase 7.
    - With `isFetching: false`: the rows render at opacity 1 and the spinner is absent.
    - With `isFetching: true`, the summary tiles render dimmed (the existing dimmed styling), not as shimmer skeletons.
    - With `isError: true` AND `data: [order]` (a placeholder from the previous range): `ErrorBlock` renders, and zero `history-row` elements are in the document — the previous rows are discarded, not kept.
    - With `isError: true`, the summary tiles show an em-dash via the existing isError branch.
    - Clicking the 7-day pill, then rendering with `isError: true`: the 7-day pill is still selected.
    - With `isError: true`, clicking Retry calls the mocked `refetch` exactly once, and does not change the selected pill.
    - The inert status pills at opacity 0.5 and the dimmed rows at 0.6 are simultaneously present in the isFetching state and carry different opacity values.
  </behavior>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0.
    - `grep -c "isFetching" src/screen-history.jsx` returns at least 3 — the hook destructure, the rows gate, and the `SummaryStrip` prop.
    - `grep -c "opacity: isSwitching ? 0.6 : 1" src/screen-history.jsx` returns 1, or an equivalent single site computing the 0.6 switch opacity.
    - `grep -c "data-testid=\"history-switch-spinner\"" src/screen-history.jsx` returns 1.
    - `grep -c "className=\"spin\"" src/screen-history.jsx` returns 1.
    - `grep -c "var(--sc-muted-foreground)" src/screen-history.jsx` returns at least 1 and the spinner's own color is that token — asserted by a test reading the rendered spinner's inline style, not by grep alone.
    - `grep -c "ErrorBlock" src/screen-history.jsx` returns 3 — the definition, the JSX use, and nothing more; no second error component was added.
    - `git diff src/screen-history.jsx | grep -cE "^\+.*(h_error_title|check_connection|h_retry)"` returns 0 — no new error copy was introduced.
    - A test asserts that with `isError: true` and non-empty placeholder `data`, `screen.queryAllByTestId('history-row')` has length 0.
    - A test asserts the spinner is absent when `isFetching` is false and present when it is true with `isLoading` false.
    - `npx vitest run` exits 0 — the full suite, including 09-01/09-02/09-03's, is green at this wave.
  </acceptance_criteria>
  <done>A switch dims rows and tiles to 0.6 without disabling them and shows a spinning refresh icon; the skeleton stays first-load-only; a failed switch discards the placeholder rows and shows the one existing ErrorBlock with the attempted pill still lit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-12 tile sub-labels + D-13 period-aware empty state</name>
  <files>src/screen-history.jsx, src/__tests__/screen-history.test.jsx</files>
  <read_first>
    - `src/screen-history.jsx` — `SummaryStrip`'s `tiles` array (245-257), specifically the hardcoded `sub: t('h_period_30')` on the Orders and Revenue tiles at 246-247; and `EmptyBlock` (104-111).
    - `src/i18n.jsx` — as changed by 09-02: the eight new keys, and the fact that the old period-baked empty-state key no longer exists.
    - `.planning/phases/09-period-control/09-UI-SPEC.md` § Period-Dependent Copy Contract — the four-row table mapping each period to its pill label and its prepositional form, and the exact rendered examples.
    - `.planning/phases/09-period-control/09-CONTEXT.md` D-12 and D-13 — including D-13's note that `h_period_today` lowercased serves the Today case, so no ninth key is added.
    - `src/history-utils.js` — `formatDateRange` as added by 09-01, for the custom branch.
  </read_first>
  <action>
    Add a module-private `periodPhrase(period, t, lang)` helper near `dayGroupLabel`, returning the
    prepositional phrase for the empty-state sentence per UI-SPEC's Period-Dependent Copy Contract
    table:
    - `'today'` → `t('h_period_today').toLowerCase()` — 'azi' / 'today'. A bare adverb that fits
      mid-sentence without a preposition; this is why D-13 adds no ninth key for it.
    - `'7'` → `t('h_period_in_7')`
    - `'30'` → `t('h_period_in_30')`
    - `'custom'` → `` `${t('h_period_in_range_prefix')} ${formatDateRange(from, to, locale)}` `` using
      the settled period's own `from`/`to` and the `lang === 'ro' ? 'ro-RO' : 'en-GB'` mapping the
      file already uses at `dayGroupLabel` line 64. Write this branch now even though `'custom'` is
      not reachable until 09-05 — it is one branch of one helper, and leaving it out would mean 09-05
      has to reopen this function.

    Add a module-private `periodLabel(period, t, lang)` (or a single helper returning both forms —
    the executor's call, as long as there is ONE lookup site per form) returning the pill/tile label:
    `h_period_today` / `h_period_7` / `h_period_30` for presets, and `formatDateRange(...)` for
    custom. D-12's requirement is that the pill and the tile sub-label read from the same source and
    therefore cannot drift; satisfy it by having the pill's `.map()` and the tile's `sub` both go
    through this one function rather than each doing its own `t()` lookup.

    Fix `SummaryStrip`:
    - It now receives the SETTLED period, not the selected one. Replace `sub: t('h_period_30')` on
      both the Orders and Revenue tiles with `sub: periodLabel(settledPeriod, t, lang)`. The
      hardcoded key at 246-247 currently makes every tile claim 30 days regardless of the period —
      harmless today because 30 days is the only period, a live defect the moment Task 1 lands.
    - The Avg tile's `sub: t('h_avg_sub')` and the Refunds tile's canceled-count suffix are NOT
      period-dependent. Do not touch them.
    - Do not change the sub-label's `fontWeight: 500` at line 281-284. UI-SPEC's Typography table
      explicitly grandfathers it: this plan changes the sub-label's computed content, never its font
      weight, and restyling shipped UI to satisfy a lint would be the unsanctioned change Design
      Fidelity forbids.
    - `SummaryStrip` needs `lang` passed through for the custom branch's locale mapping.

    Fix `EmptyBlock`:
    - It now receives the settled period, `t`, and `lang`. Its main line renders
      `` `${t('h_empty_prefix')} ${periodPhrase(settledPeriod, t, lang)}.` `` — note the trailing full
      stop is appended here, which is why 09-02's `h_empty_prefix` deliberately carries none.
    - `h_empty_sub` is unchanged and still renders on its own line below. `P7 D-13` reserves the
      sub-line for Phase 10's filter copy; the period rides the main line only.
    - Do not interpolate the pill label into the sentence. 'Nicio comandă în Azi' is broken Romanian,
      and avoiding it is the entire reason the second label family exists.

    Everything period-dependent reads `settledPeriod` from Task 1 — never `selectedPeriod`. During an
    in-flight switch the sub-label must still read the OUTGOING period's text while the outgoing
    figures are on screen. A tile that reads '7 zile' above a 30-day number is a false claim about
    money to the staff reconciling against it, which is what D-06 exists to prevent and what
    `P7 D-15` dropped an entire endpoint to avoid.
  </action>
  <behavior>
    - With the settled period `{ id: '30' }` and populated data, the Orders and Revenue tile sub-labels read '30 zile' (ro) / '30 days' (en).
    - With the settled period `{ id: '7' }`, they read '7 zile' / '7 days'; with `{ id: 'today' }`, 'Azi' / 'Today'.
    - The Avg tile's sub-label reads `h_avg_sub` in every period; the Refunds tile's reads the canceled-count suffix. Neither changes with the period.
    - D-06, the load-bearing test: selected `{ id: '7' }`, settled `{ id: '30' }`, `isFetching: true`, `isPlaceholderData: true`, `data` = the 30-day fixture → the 7-day pill is lit AND the tile sub-labels read '30 zile'. The lit pill and the sub-label deliberately disagree during the switch.
    - Following that, with `isPlaceholderData: false` and the 7-day fixture, the sub-label reads '7 zile' and the figures are the 7-day ones — they flip in the same render.
    - Empty state, settled `{ id: 'today' }`, ro → main line reads exactly 'Nicio comandă azi.' — lowercased adverb, single space, trailing full stop.
    - Empty state, settled `{ id: '7' }`, ro → 'Nicio comandă în ultimele 7 zile.'; en → 'No orders in the last 7 days.'
    - Empty state, settled `{ id: '30' }`, en → 'No orders in the last 30 days.'
    - Empty state, settled `{ id: 'custom', from, to }`, ro → 'Nicio comandă în intervalul 3 mar. – 17 mar.'
    - `h_empty_sub` still renders on its own line in every empty case.
    - Empty period, summary tiles: Orders and Revenue show computed zeroes, and the Avg tile shows a formatted zero rather than an em-dash — the em-dash is reserved for the error state (Phase 07 convention, preserved).
    - Ordering after a switch: with a two-day fixture, day headers render newest-day-first and rows within a day newest-first, unchanged by which range produced them.
  </behavior>
  <verify>
    <automated>npx vitest run src/__tests__/screen-history.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/screen-history.test.jsx` exits 0.
    - `grep -c "t('h_period_30')" src/screen-history.jsx` returns 0 — the hardcoded sub-label is gone.
    - `grep -c "h_empty_prefix" src/screen-history.jsx` returns 1.
    - `grep -c "h_empty_sub" src/screen-history.jsx` returns 1 — the sub-line is untouched.
    - `grep -c "h_avg_sub" src/screen-history.jsx` returns 1 — the Avg tile's sub-label is untouched.
    - `grep -c "function periodPhrase" src/screen-history.jsx` returns 1.
    - `grep -c "fontWeight: 500" src/screen-history.jsx` returns 1 — the grandfathered sub-label weight survives.
    - `grep -c "selectedPeriod" src/screen-history.jsx` — every occurrence is in `HistoryScreen`'s state/handler or the `FilterBar` pill styling. Verify by inspection that no occurrence appears inside `SummaryStrip` or `EmptyBlock`: `sed -n '/function SummaryStrip/,/^}/p' src/screen-history.jsx | grep -c "selectedPeriod"` returns 0, and the same for `EmptyBlock`.
    - A test asserts the exact string 'Nicio comandă azi.' renders for settled `{id:'today'}` in ro.
    - A test asserts the exact string 'No orders in the last 7 days.' renders for settled `{id:'7'}` in en.
    - The D-06 test exists and passes: `npx vitest run src/__tests__/screen-history.test.jsx -t "settled"` exits 0.
    - `npx vitest run` exits 0 — full suite green.
  </acceptance_criteria>
  <done>Tile sub-labels and the empty-state sentence both derive from the settled period through one label source per form; the hardcoded 30-day sub-label is gone; the D-06 disagreement-during-switch behavior is pinned by a test; no grandfathered visual value moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pill click → query key | A user gesture now determines the parameters of an unpaginated network fetch. The memoized resolution is the only thing between a click and the key. |
| settled range → rendered period label | The label tells staff which accounting period the figures on screen describe. A label sourced from the wrong state is a false financial claim, not a cosmetic lag. |
| shared FilterBar → Phase 10/11 inert controls | This plan edits the component that also hosts the status pills, search, and Export. Those controls' inertness is the only thing keeping unbuilt features unreachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Spoofing (false assertion about a financial record) | A tile sub-label sourced from `selectedPeriod` rather than `settledPeriod`, claiming one period above another period's revenue figure | high | mitigate | The two states are structurally separated in Task 1, and Task 3 forbids `selectedPeriod` from appearing inside `SummaryStrip` or `EmptyBlock` at all — asserted by a scoped `sed`+`grep` acceptance criterion, so the wrong source cannot be reached from those components even by a later edit. The D-06 test pins the disagreement-during-switch behavior positively. Blocking threshold is `high`, so this must close before the phase seals. |
| T-09-13 | Denial of Service (client-side, self-inflicted) | An unmemoized `getPresetRange()` call in the render body producing a fresh key every render and refetching in a loop against the admin endpoint | high | mitigate | The `useMemo` is mandated by Task 1 and asserted by a grep criterion (`useMemo(() => getPresetRange`), and `getPresetRange` is required to have exactly one call site in the file. 09-03's stable-key test covers the hook side of the same guarantee. This is RESEARCH Pitfall 1 and the file's own former header comment's warning, relocated. |
| T-09-14 | Elevation of Privilege | An inert Phase 10/11 control (status filter, search, Export) accidentally enabled while editing the shared `FilterBar` | medium | mitigate | The plan forbids touching lines 325-342, a render test asserts all three still carry `disabled`, and a `git diff` acceptance criterion fails if any line mentioning `statusFilters`, `h_search`, or `h_export` was added or removed. Three independent checks because this is a whole-feature exposure, not a styling slip. |
| T-09-15 | Spoofing (stale data presented as current) | A failed switch rendering the previous range's `keepPreviousData` rows beneath a pill naming the newly requested period, with no error shown | high | mitigate | Task 2 requires the `isError` branch to be evaluated before any row rendering, and a test asserts that with `isError: true` and non-empty placeholder `data`, zero `history-row` elements are in the document. This is the second prohibition in `must_haves` made executable. |
| T-09-16 | Repudiation (of a loading state that never fires) | Gating the dimming on `isLoading`, which is false for every switch after the first — the D-05 contract silently unimplemented despite compiling | medium | mitigate | RESEARCH Pitfall 2 is called out in Task 2's action and `read_first`; the gate is required to be `isFetching && !isLoading`, and tests assert both directions (spinner present when `isFetching` is true with `isLoading` false; absent when `isFetching` is false). |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan or phase — zero new dependencies per `09-RESEARCH.md` § Package Legitimacy Audit. |
</threat_model>

<verification>
- `npx vitest run` — full suite green, including `screen-history.test.jsx`, which was expected red at the end of 09-03 and must be green here.
- The D-06 test (`-t "settled"`) passes — a lit pill above a lagging sub-label is asserted behavior, not an accident.
- `sed -n '/function SummaryStrip/,/^}/p' src/screen-history.jsx | grep -c "selectedPeriod"` returns 0.
- `git diff src/screen-history.jsx | grep -cE "^[-+].*(statusFilters|h_search|h_export)"` returns 0.
</verification>

<success_criteria>
- SC1: Today / 7 days / 30 days switch the period and the day-grouped list reloads for the new range.
- SC3: the summary strip retargets with the period from the same fetched list — no second fetch, no second loading state, no second error state exists in the file.
- D-05: a switch dims to 0.6 with a spinner and stays interactive; the skeleton remains first-load-only.
- D-06: every period-dependent label derives from the settled range; the prohibition is pinned by a test.
- D-07/D-08: one error treatment; the attempted pill stays lit; Retry targets that range.
- D-12/D-13: one label source per form; the hardcoded 30-day sub-label is gone; the empty state reads grammatically in both locales for all four periods.
- Every Phase 10/11 control is provably still inert.
</success_criteria>

<output>
Create `.planning/phases/09-period-control/09-04-SUMMARY.md` when done. Record the one-wave
`'custom'` pill no-op as a deliberate placeholder that 09-05 replaces.
</output>
