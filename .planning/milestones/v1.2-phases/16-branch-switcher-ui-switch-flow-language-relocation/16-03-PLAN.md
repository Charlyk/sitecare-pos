---
phase: 16-branch-switcher-ui-switch-flow-language-relocation
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/screen-pos.jsx
  - src/app.jsx
  - src/__tests__/app-branch-switch.test.jsx
autonomous: true
requirements: [SCOPE-03, SCOPE-04]

must_haves:
  truths:
    - "Switching from the POS screen with a non-empty cart shows a confirm dialog before switch() fires; confirming discards the order and proceeds, cancelling stays on the current branch (SCOPE-03, D-13)"
    - "Switching with an empty cart (or from any non-POS screen) proceeds immediately with no confirm step (D-13)"
    - "A successful switch exits an open order-detail or history-detail view back to the Orders screen via setScreen('orders') (SCOPE-03, D-14)"
    - "The POS screen remounts via key={currentBranch?.id} on a successful switch, resetting its cart to empty; other screens (menu, settings, printer) stay put and re-scope via Phase-14 caches (D-14)"
    - "PosScreen reports cart emptiness to app.jsx via an onCartEmptyChange(isEmpty) callback fired from a useEffect on cart (not on every render); posCartEmpty defaults to true (RESEARCH Pattern 3)"
    - "Order mutations stay blocked for the full pending window including the D-09 bounded-timeout fallback, because the overlay covers all screens the entire time (SCOPE-04)"
    - "The cart-discard confirm dialog has header/body/footer with a destructive primary (Switch and discard) and neutral secondary (Stay here); shown only when screen==='pos' AND cart non-empty (E7 populated, E7 empty)"
    - "A rejected switch after confirm surfaces via the failure toast; the confirm dialog itself has no failure mode of its own (E7 error)"
  artifacts:
    - src/screen-pos.jsx
    - src/app.jsx
    - src/__tests__/app-branch-switch.test.jsx
  key_links:
    - "PosScreen onCartEmptyChange callback → app.jsx posCartEmpty → D-13 confirm gate in handleSelectBranch"
    - "key={currentBranch?.id} on <PosScreen> → remount on switch → cart resets to [] (SCOPE-03)"
    - "release effect switchPhase==='done' → setScreen('orders') when on detail/history-detail (D-14)"
---

<objective>
Complete the switch flow's state-safety behaviors layered on the tracer: the cart-discard confirm gate (D-13), neutral-landing routing out of detail views (D-14), the POS cart reset via remount key (D-14/SCOPE-03), the PosScreen cart-emptiness callback channel (RESEARCH Pattern 3), and the D-09 bounded-timeout completeness so mutations stay blocked the entire pending window.

Purpose: guarantee no prior-branch working state (a half-built POS cart, an open detail view) carries into the new branch, and that a mis-tap on a non-empty cart cannot silently discard an order.

Output: `onCartEmptyChange` on `PosScreen`, the cart gate + remount key + neutral-landing in `app.jsx`, the cart-discard confirm dialog, and expansion tests in `app-branch-switch.test.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-CONTEXT.md
@.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-RESEARCH.md
@.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-PATTERNS.md
@.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-UI-SPEC.md
@.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-01-SUMMARY.md
</context>

<artifacts_produced>
- `onCartEmptyChange` prop on `PosScreen` (`src/screen-pos.jsx`) + a useEffect reporting `cart.length === 0`
- `posCartEmpty` state + `key={currentBranch?.id}` remount + cart-discard confirm state/dialog + D-14 neutral-landing in `src/app.jsx` (consumes i18n cart-discard keys added in Plan 01)
- Expansion describe blocks in `src/__tests__/app-branch-switch.test.jsx` (cart gate, neutral landing, remount, bounded timeout)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Cart-emptiness callback + cart gate + neutral landing + POS remount</name>
  <files>src/screen-pos.jsx, src/app.jsx</files>
  <read_first>
    - src/screen-pos.jsx (cart local state at :57 `const [cart, setCart] = useState([])`; current prop signature `function PosScreen({ lang, isOffline })` at :14; import line :1)
    - src/app.jsx (post-tracer state — `fireSwitch`, `switchPhase`, `handleSelectBranch`, the release effect from Plan 01; PosScreen render at :257; detail/history-detail render at :258-274; CancelDialog block :316-344 for the confirm-dialog-as-sibling pattern)
    - src/cancel-dialog.jsx:22-33 (backdrop + card chrome to mirror for the cart-discard confirm)
    - 16-RESEARCH.md Pattern 3 (the onCartEmptyChange callback + posCartEmpty default true + remount-resyncs-emptiness note) and Pattern 2 (the release effect where the D-14 setScreen belongs), Pitfall 4 (clearTimeout on every new fireSwitch)
    - 16-UI-SPEC.md Copywriting Contract (cart-discard title/body/confirm/cancel copy) + E7 rows
  </read_first>
  <action>
    In screen-pos.jsx: add `onCartEmptyChange` to the props (`function PosScreen({ lang, isOffline, onCartEmptyChange })`), import `useEffect` alongside the existing `useState`/`useMemo`, and add `useEffect(() => { onCartEmptyChange?.(cart.length === 0); }, [cart, onCartEmptyChange]);` — report on cart change, not every render (RESEARCH Pattern 3). Touch nothing else in this file.

    In app.jsx: add `const [posCartEmpty, setPosCartEmpty] = useState(true);` (default true — PosScreen only mounts on `screen==='pos'` and reports on mount). Update the PosScreen render (:257) to `<PosScreen key={currentBranch?.id} lang={lang} isOffline={isOffline} onCartEmptyChange={setPosCartEmpty} />` — the `key` remount resets the cart on a successful switch (D-14/SCOPE-03); read `currentBranch` via a `useAppStore((s) => s.currentBranch)` selector if not already present. Change `handleSelectBranch(branch)` (from Plan 01) so it gates: `if (screen === 'pos' && !posCartEmpty) { setCartDiscardConfirm(branch); return; }` else `fireSwitch(branch)` (D-13). Add `const [cartDiscardConfirm, setCartDiscardConfirm] = useState(null)`. Render the cart-discard confirm dialog as a SIBLING of `<Shell>` (mirroring the CancelDialog block): shown when `cartDiscardConfirm` is set, header `branch_cart_discard_title`, body `branch_cart_discard_body`, destructive primary `branch_cart_discard_confirm` → `setCartDiscardConfirm(null)` then `fireSwitch(cartDiscardConfirm)`, neutral secondary `branch_cart_discard_cancel` → `setCartDiscardConfirm(null)` (stay on current branch, nothing changes). In the Plan-01 release effect (`switchPhase==='done'`), add the D-14 neutral landing: `if (screen === 'detail' || screen === 'history-detail') setScreen('orders');` (mirrors CancelDialog's existing `setScreen('orders')` at :333). Ensure `clearTimeout(bridgeTimeoutRef.current)` runs at the top of `fireSwitch` (Pitfall 4) — if Plan 01 already added it, leave it.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-branch-switch.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/screen-pos.jsx` destructures `onCartEmptyChange` and calls it inside a `useEffect` whose dependency array includes `cart`
    - `src/app.jsx` renders `<PosScreen` with a `key={currentBranch?.id}` attribute and an `onCartEmptyChange={setPosCartEmpty}` prop
    - `src/app.jsx` `handleSelectBranch` gates on `screen === 'pos'` and the cart-empty flag before calling `fireSwitch`
    - `src/app.jsx` release effect calls `setScreen('orders')` for the `detail`/`history-detail` screens
    - `npx vitest run src/__tests__/app-branch-switch.test.jsx` exits 0
  </acceptance_criteria>
  <done>A switch from POS with items prompts a confirm; empty-cart and non-POS switches are immediate; a successful switch resets the POS cart and exits detail views to Orders — no prior-branch state carries forward.</done>
  <reversibility rating="reversible">The cart gate, remount key, and neutral-landing are additive orchestration in app.jsx; removing them leaves the tracer's happy path intact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch-flow test coverage — cart gate, remount, neutral landing, bounded timeout (SCOPE-03/SCOPE-04)</name>
  <files>src/__tests__/app-branch-switch.test.jsx</files>
  <read_first>
    - src/__tests__/app-branch-switch.test.jsx (the Plan-01 happy-path file — ADD new describe blocks; reuse its App render + fake-timers + mock header)
    - src/__tests__/use-sse.test.js:167-207 (fake-timers + isConnected transition mocking to drive the bridge/timeout)
    - src/app.jsx (behaviors under test from Task 1)
  </read_first>
  <action>
    Extend app-branch-switch.test.jsx. SCOPE-03 cart gate (D-13): with `screen==='pos'` and the mocked PosScreen reporting a non-empty cart, assert selecting a branch opens the cart-discard confirm and does NOT call the switch mutation until the destructive-confirm button is clicked; assert cancelling leaves currentBranch unchanged and fires no mutation. SCOPE-03 neutral landing (D-14): starting on `detail`/`history-detail`, assert a successful switch calls `setScreen('orders')` (assert via the store or rendered screen). SCOPE-03 remount: assert the PosScreen render carries `key={currentBranch?.id}` semantics — driving a successful switch changes the key so the cart resets (assert PosScreen remounts / posCartEmpty resyncs to true). SCOPE-04 bounded timeout (D-09): drive a switch success WITHOUT an isConnected recovery, advance fake timers past 6000ms, and assert the overlay releases, the success toast fires exactly once, and no second toast fires when `isConnected` later recovers. Reuse the Plan-01 mock header and `vi.useFakeTimers()`; no watch-mode flags.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-branch-switch.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/app-branch-switch.test.jsx` contains describe/test blocks covering SCOPE-03 (cart gate + neutral landing) and SCOPE-04 (bounded timeout)
    - The cart-gate test asserts the switch mutation is not called until the destructive confirm is clicked
    - The bounded-timeout test advances fake timers past 6000ms and asserts exactly one success toast fires
    - `npx vitest run src/__tests__/app-branch-switch.test.jsx` exits 0
  </acceptance_criteria>
  <done>SCOPE-03 (cart reset, detail exit, confirm gate) and SCOPE-04 (mutations blocked for the full pending window incl. bounded timeout) are proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pending window → order mutations | the overlay must cover all screens for the full pending+bridging+timeout window so no POS submit / accept / advance / cancel / reprint lands mid-switch |
| PosScreen local cart → app orchestration | cart emptiness crosses the component boundary via a callback; the reset crosses back via a remount key — no prior-branch cart may survive a switch |
| detail view → new branch | an open order-detail bound to a prior-branch order must not remain visible after the branch changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Elevation of Privilege (of timing) | pending window incl. D-09 timeout | high | mitigate | The overlay covers all of Shell's children for pending + bridging + until the bounded-timeout release (SCOPE-04); order mutations cannot land against the wrong branch at any point in the window. Proven by the bounded-timeout test. |
| T-16-08 | Tampering (stale working state) | POS cart / open detail view | medium | mitigate | Cart reset via `key={currentBranch?.id}` remount and detail exit via `setScreen('orders')` on success (D-14/SCOPE-03) — no prior-branch working state carries into the new branch. |
| T-16-09 | Repudiation (silent data loss) | cart-discard on mis-tap | medium | mitigate | D-13 confirm dialog gates the discard when the cart is non-empty, so an in-progress order is never silently nuked by a mis-tap. |
| T-16-SC | Tampering (supply chain) | package installs | low | accept | Zero new packages this phase. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/app-branch-switch.test.jsx` exits 0
- Full suite `npx vitest run` green before the phase gate
- Manual (per 16-VALIDATION.md): live multi-branch switch bridges the real SSE reconnect with no false OfflineBanner flash; D-09 bounded-timeout fallback releases the overlay with the honest OfflineBanner
</verification>

<success_criteria>
- POS cart reset + open detail exited on switch (SCOPE-03)
- Cart-discard confirm gates a non-empty cart; empty-cart switch is immediate (D-13)
- Order mutations blocked for the full pending window incl. bounded timeout (SCOPE-04)
</success_criteria>

<output>
Create `.planning/phases/16-branch-switcher-ui-switch-flow-language-relocation/16-03-SUMMARY.md` when done.
</output>
