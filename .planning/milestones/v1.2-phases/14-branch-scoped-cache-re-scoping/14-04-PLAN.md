---
phase: 14-branch-scoped-cache-re-scoping
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/use-order-actions.js
  - src/screen-pos.jsx
  - src/screen-menu.jsx
  - src/__tests__/use-order-actions.test.js
autonomous: true
requirements: [SCOPE-01]
must_haves:
  truths:
    - "use-order-actions.js updateStatus AND updateEstimatedTime each invalidate `['orders', branchId]`, `['order', branchId]`, `['stats', branchId]` — branchId read once at the hook-body top via `useAppStore((s) => s.currentBranch?.id) ?? null` and closed over in onSuccess. (D-03, D-04)"
    - "screen-pos.jsx POS-submit onSuccess invalidates `['orders', branchId]`. (D-04)"
    - "screen-menu.jsx stock-toggle onSuccess invalidates `['menu', branchId]`. (D-04)"
    - "Exact branch-scoped invalidateQueries leaves a sibling branch's cached entry untouched — mutating for branch-a does not clear qc.getQueryData(['orders','branch-b']). (SC2)"
    - "Phase-gate grep audit returns zero unscoped-key queryKey matches across src/ outside use-sse.js. (SC1/SC2 completeness)"
  artifacts:
    - src/use-order-actions.js
    - src/screen-pos.jsx
    - src/screen-menu.jsx
    - src/__tests__/use-order-actions.test.js
  key_links:
    - "TanStack Query v5 prefix-match: invalidating ['orders', branchId] reaches every status-filtered variant of the current branch while never matching ['orders', otherBranchId] — this is the mechanism SC2 relies on (D-03)"
  prohibitions:
    - statement: "MUST NOT call the `useAppStore((s) => ...)` selector hook inside a `useMutation` onSuccess callback. In use-order-actions.js read branchId ONCE at the hook-body top and close over it (Pitfall 4)."
      status: enforced
      verification: "use-order-actions.js reads branchId at the top of useOrderActions()'s body, not inside onSuccess; no useAppStore call appears inside any onSuccess block"
    - statement: "MUST NOT use the broad unscoped prefix `['orders']` for any invalidation — it partial-matches every branch and fails SC2."
      status: enforced
      verification: "SC2 sibling-untouched test passes; grep finds no bare `queryKey: ['orders']`/`['order']`/`['stats']`/`['menu']` invalidation in these files"
    - statement: "MUST NOT use `queryClient.resetQueries()` (D-01 uniform mechanism)."
      status: enforced
      verification: "grep -rn 'resetQueries' src/ returns zero matches"
    - statement: "MUST NOT leave any of the 6 invalidation sites (2 mutations x 3 keys + POS + menu + refresh-in-14-01) unscoped — missing one reproduces the exact stale-branch bug (D-04)."
      status: enforced
      verification: "full-suite green + phase-gate grep audit zero unscoped matches after all Wave 2 plans land"
---

<objective>
Move the mutation-side invalidation sites onto branch-scoped keys in lockstep with the query-key change: `use-order-actions.js` (updateStatus + updateEstimatedTime → `['orders'|'order'|'stats', branchId]`), `screen-pos.jsx` POS-submit (→ `['orders', branchId]`), and `screen-menu.jsx` stock-toggle (→ `['menu', branchId]`). Establish the SC2 sibling-branch-untouched assertion in use-order-actions.test.js — the single most important assertable behavior in this phase.

Purpose: SCOPE-01 SC2 — mutations must invalidate only the active branch's cache entries, never a different branch's. Missing any one lockstep site reproduces the stale-branch bug this phase exists to prevent (D-04).
Output: Three retrofitted mutation call sites + the SC2 sibling-untouched test.

Note: the sixth invalidation site (screen-orders.jsx:281 manual refresh) was moved in the tracer Plan 14-01. This plan closes the remaining three mutation sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-CONTEXT.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch-scope the three mutation invalidation call sites</name>
  <read_first>
    - src/use-orders.js (proven tracer reference — the branchId selector form)
    - src/use-order-actions.js (current: 2 mutations, each onSuccess invalidates `['orders']`,`['order']`,`['stats']` — lines ~26-28, ~40-42)
    - src/screen-pos.jsx (line ~172, createOrder onSuccess invalidating `['orders']`)
    - src/screen-menu.jsx (line ~40, toggleStock onSuccess invalidating `['menu']`)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (use-order-actions/screen-pos/screen-menu retrofits; Pitfall-4 hook-body-vs-callback rule)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-CONTEXT.md (D-03, D-04)
  </read_first>
  <files>src/use-order-actions.js, src/screen-pos.jsx, src/screen-menu.jsx</files>
  <behavior>
    - After updateStatus succeeds for branch-a, invalidateQueries is called with ['orders','branch-a'], ['order','branch-a'], ['stats','branch-a'].
    - After a POS submit succeeds, invalidateQueries is called with ['orders','branch-a'].
    - After a stock toggle succeeds, invalidateQueries is called with ['menu','branch-a'].
  </behavior>
  <action>
    In src/use-order-actions.js: import `useAppStore` from './store.js'; read `const branchId = useAppStore((s) => s.currentBranch?.id) ?? null;` ONCE at the top of useOrderActions()'s function body (this is the hook's own render — valid); in BOTH updateStatus and updateEstimatedTime onSuccess blocks close over branchId and invalidate `{ queryKey: ['orders', branchId] }`, `{ queryKey: ['order', branchId] }`, `{ queryKey: ['stats', branchId] }`. Do NOT call the useAppStore selector hook inside onSuccess (Pitfall 4).
    In src/screen-pos.jsx: add `const branchId = useAppStore((s) => s.currentBranch?.id) ?? null;` near the top of the component (import useAppStore if needed); change the createOrder onSuccess invalidation (line ~172) to `{ queryKey: ['orders', branchId] }`.
    In src/screen-menu.jsx: add the same branchId selector near the top of the component; change the toggleStock onSuccess (line ~40) to `queryClient.invalidateQueries({ queryKey: ['menu', branchId] })`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-order-actions.test.js</automated>
  </verify>
  <acceptance_criteria>
    - use-order-actions.js reads branchId at the hook-body top; both onSuccess blocks invalidate the three branch-scoped keys; no useAppStore call inside onSuccess.
    - screen-pos.jsx POS-submit invalidates `['orders', branchId]`.
    - screen-menu.jsx stock-toggle invalidates `['menu', branchId]`.
    - `grep -rn "resetQueries" src/` returns zero matches.
  </acceptance_criteria>
  <done>All three mutation invalidation sites are branch-scoped, read via the hook/component-body branchId selector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC2 sibling-branch-untouched test + branch-scoped-key assertion update</name>
  <read_first>
    - src/__tests__/use-order-actions.test.js (existing test — the `qc.setQueryData(['orders'], ...)` + invalidateSpy pattern to extend; current assertion `toHaveBeenCalledWith({ queryKey: ['orders'] })`)
    - src/__tests__/auth-token.test.jsx (line ~154 — useAppStore.setState seeding precedent)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (Test Patterns — use-order-actions sibling-branch-untouched example)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-VALIDATION.md (SC2 map + test convention)
  </read_first>
  <files>src/__tests__/use-order-actions.test.js</files>
  <behavior>
    - Existing assertion updated: after updateStatus for branch-a, invalidateSpy was called with { queryKey: ['orders', 'branch-a'] } (was ['orders']).
    - SC2: with qc pre-populated for both ['orders','branch-a'] and ['orders','branch-b'], mutating for branch-a leaves qc.getQueryData(['orders','branch-b']) exactly equal to its seeded value.
  </behavior>
  <action>
    Extend src/__tests__/use-order-actions.test.js: import the REAL useAppStore from '../store.js' (do NOT mock the store); add `beforeEach(() => useAppStore.setState({ currentBranch: null }))`. Update the existing invalidation assertion to expect the branch-scoped key `{ queryKey: ['orders', 'branch-a'] }` after seeding `useAppStore.setState({ currentBranch: { id: 'branch-a', name: 'A', slug: 'a', isDefault: true, isActive: true } })`. Add a new SC2 test: seed currentBranch branch-a, `qc.setQueryData(['orders', 'branch-a'], [{ id: 'ord-001' }])` and `qc.setQueryData(['orders', 'branch-b'], [{ id: 'ord-002' }])`, render useOrderActions, `await act(async () => await result.current.updateStatus.mutateAsync({ id: 'ord-001', currentStatus: 'NEW', toStatus: 'IN_PROGRESS' }))`, then assert `qc.getQueryData(['orders', 'branch-b'])` still equals `[{ id: 'ord-002' }]` (sibling untouched).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-order-actions.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/use-order-actions.test.js` exits 0.
    - The existing invalidation assertion now expects `{ queryKey: ['orders', 'branch-a'] }`.
    - A new SC2 test asserts the sibling branch-b cache entry is unchanged after a branch-a mutation.
  </acceptance_criteria>
  <done>SC2 sibling-branch-untouched behavior is proven by an automated test; the mutation invalidation is branch-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | branchId scopes client-side invalidation only; never sent as a request param (server resolves branch server-side, D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | client-persisted branchId as authorization boundary | medium | mitigate | Mutation sites only READ currentBranch?.id from session-only Zustand state (Phase 13 D-10). No persistence added; store.js not edited. |
| T-14-03 | Information Disclosure / cache leakage | over-broad invalidation reaching another branch's cache | medium | mitigate | Exact branch-scoped keys (never the bare `['orders']` prefix) ensure invalidation matches only the current branch; SC2 sibling-untouched test enforces this by construction (D-03). |

**Out of scope:** V4 Access Control is server-enforced. No package installs → no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run src/__tests__/use-order-actions.test.js` exits 0 (includes the SC2 sibling-untouched assertion).
- Full suite `npx vitest run` green after this and all Wave 2 plans land.
- Phase-gate grep audit (VALIDATION.md) returns zero unscoped-key matches across src/ outside use-sse.js.
- `grep -rn "resetQueries" src/` returns zero matches.
</verification>

<success_criteria>
All three mutation invalidation sites are branch-scoped in lockstep with the query keys; SC2 (sibling branch untouched) is proven by an automated test.
</success_criteria>

<output>
Create `.planning/phases/14-branch-scoped-cache-re-scoping/14-04-SUMMARY.md` when done.
</output>
