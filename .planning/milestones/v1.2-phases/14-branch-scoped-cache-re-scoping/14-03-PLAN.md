---
phase: 14-branch-scoped-cache-re-scoping
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/use-restaurant-settings.js
  - src/use-delivery-areas.js
  - src/use-history-orders.js
  - src/__tests__/use-restaurant-settings.test.js
  - src/__tests__/use-delivery-areas.test.js
  - src/__tests__/use-history-orders.test.js
autonomous: true
requirements: [SCOPE-01]
must_haves:
  truths:
    - "use-restaurant-settings.js queryKey is `['restaurant-settings', branchId]`; error path routes through unwrapSdkResult('Failed to fetch restaurant settings'). (SC1)"
    - "use-delivery-areas.js queryKey is `['delivery-areas', branchId]`; error path routes through unwrapSdkResult('Failed to fetch delivery areas'); the cents→units .fee mapping is preserved unchanged. (SC1)"
    - "use-history-orders.js queryKey is `['history-orders', branchId, from, to]` — KEY-ONLY change; its existing `.diagnostic` error enrichment is preserved untouched. (SC1, RESEARCH Pitfall 5)"
    - "In all three hooks branchId = `useAppStore((s) => s.currentBranch?.id) ?? null` and is the first variable segment immediately after the resource name. (D-07)"
  artifacts:
    - src/use-restaurant-settings.js
    - src/use-delivery-areas.js
    - src/use-history-orders.js
    - src/__tests__/use-restaurant-settings.test.js
    - src/__tests__/use-delivery-areas.test.js
    - src/__tests__/use-history-orders.test.js
  key_links:
    - "use-restaurant-settings and use-delivery-areas import unwrapSdkResult from src/data.jsx (Plan 14-01) — read-only"
    - "use-history-orders keeps its own error path; only its queryKey gains the branchId slot"
  prohibitions:
    - statement: "MUST NOT route use-history-orders.js's error path through unwrapSdkResult — its `.diagnostic` enrichment is load-bearing for the OPEN debug session `windows-history-network-error` (status: investigating). Change ONLY its queryKey."
      status: enforced
      verification: "use-history-orders.js diff touches only the queryKey line + the branchId selector import/read; the entire `if (result.error) {...}` diagnostic block is byte-unchanged"
    - statement: "MUST NOT add `!!branchId` to any hook's enabled — stays `enabled: !!client`."
      status: enforced
      verification: "grep confirms no `branchId` term inside any `enabled:` expression in the three hooks"
    - statement: "MUST NOT leave any of the three hooks on an unscoped key."
      status: enforced
      verification: "phase-gate grep audit returns zero unscoped `['restaurant-settings']`/`['delivery-areas']`/`['history-orders']` queryKey matches in these files"
---

<objective>
Expand the proven tracer pattern to the remaining three fetch hooks: `use-restaurant-settings.js`, `use-delivery-areas.js` (both key + unwrapSdkResult), and `use-history-orders.js` (KEY-ONLY — preserve its `.diagnostic` error enrichment). Add a branch-key SC1 test for each.

Purpose: Complete the 7-hook branch-scoping so every branch-scoped fetch re-scopes on a branch change (SCOPE-01 SC1), while protecting the active Windows-history debug diagnostic.
Output: Three retrofitted hooks + three new test files.

**Pitfall 5 / A2 resolution (verified at plan time):** the debug session `.planning/debug/windows-history-network-error.md` is `status: investigating` (OPEN). Therefore use-history-orders.js is a KEY-ONLY change — its `.diagnostic` enrichment stays. Do NOT fold it into unwrapSdkResult.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-CONTEXT.md
@.planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch-scope use-restaurant-settings.js and use-delivery-areas.js</name>
  <read_first>
    - src/use-orders.js (proven tracer reference from 14-01 — import + selector + key + unwrapSdkResult shape)
    - src/use-restaurant-settings.js (current key `['restaurant-settings']`, error line)
    - src/use-delivery-areas.js (current key `['delivery-areas']`, error line, the `.fee`/100 cents mapping)
    - src/data.jsx (confirm unwrapSdkResult signature)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (per-file retrofit for these two hooks)
  </read_first>
  <files>src/use-restaurant-settings.js, src/use-delivery-areas.js</files>
  <behavior>
    - useRestaurantSettings() with currentBranch { id: 'branch-a' } produces key ['restaurant-settings', 'branch-a'].
    - useDeliveryAreas() with currentBranch { id: 'branch-a' } produces key ['delivery-areas', 'branch-a'] and still divides raw fee by 100.
  </behavior>
  <action>
    In src/use-restaurant-settings.js: import `useAppStore` from './store.js' and `unwrapSdkResult` from './data.jsx'; read `const branchId = useAppStore((s) => s.currentBranch?.id) ?? null;`; change queryKey to `['restaurant-settings', branchId]`; change the queryFn to `return unwrapSdkResult(result, 'Failed to fetch restaurant settings')`. Keep `enabled: !!client` and staleTime unchanged.
    In src/use-delivery-areas.js: same imports and branchId selector; change queryKey to `['delivery-areas', branchId]`; replace the error line so the queryFn does `const data = unwrapSdkResult(result, 'Failed to fetch delivery areas');` and keep the existing `(data?.deliveryAreas ?? []).map(...)` block (id/name/fee-divided-by-100) EXACTLY as-is. Keep `enabled: !!client` and staleTime unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-restaurant-settings.test.js src/__tests__/use-delivery-areas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - use-restaurant-settings.js queryKey literal is exactly `['restaurant-settings', branchId]`.
    - use-delivery-areas.js queryKey literal is exactly `['delivery-areas', branchId]`; the `.fee` cents→units mapping is unchanged.
    - Both import unwrapSdkResult from './data.jsx' and route errors through it; `enabled: !!client` unchanged with no branchId term.
  </acceptance_criteria>
  <done>Both settings/delivery hooks are branch-scoped and error-routed through the shared helper, mapping preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch-scope use-history-orders.js key only + SC1 tests for all three hooks</name>
  <read_first>
    - src/use-history-orders.js (current key line 41 `['history-orders', from, to]`; the `if (result.error) {...}` `.diagnostic` block lines 46-63 — MUST stay untouched; top-of-file comment referencing windows-history-network-error)
    - src/__tests__/use-history-orders.test.js (existing test file to extend)
    - src/__tests__/use-orders.test.js (new-hook test template)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-PATTERNS.md (useHistoryOrders key-only retrofit; Test Patterns)
    - .planning/phases/14-branch-scoped-cache-re-scoping/14-VALIDATION.md (test convention; SC1 map)
  </read_first>
  <files>src/use-history-orders.js, src/__tests__/use-history-orders.test.js, src/__tests__/use-restaurant-settings.test.js, src/__tests__/use-delivery-areas.test.js</files>
  <behavior>
    - useHistoryOrders({ from, to }) with currentBranch { id: 'branch-a' } produces key ['history-orders', 'branch-a', from, to].
    - SC1 (each of the three hooks): the seeded currentBranch id appears as the segment immediately after the resource name.
  </behavior>
  <action>
    In src/use-history-orders.js: import `useAppStore` from './store.js' (if not already), read `const branchId = useAppStore((s) => s.currentBranch?.id) ?? null;` in the hook body, and change ONLY the queryKey line to `['history-orders', branchId, from, to]` — branchId placed before from/to for consistency with the other six hooks (Discretion, recommended). Do NOT modify the `if (result.error) {...}` diagnostic block or route this hook through unwrapSdkResult (Pitfall 5 — debug session is OPEN).
    Extend src/__tests__/use-history-orders.test.js with an SC1 assertion: seed `useAppStore.setState({ currentBranch: { id: 'branch-a', ... } })` and assert the cache key's segment after 'history-orders' is 'branch-a'. Add `beforeEach(() => useAppStore.setState({ currentBranch: null }))` if not present.
    Create src/__tests__/use-restaurant-settings.test.js and src/__tests__/use-delivery-areas.test.js mirroring the use-orders.test.js scaffolding (mock @tauri-apps/plugin-store, @charlyk/admin-client, @tauri-apps/plugin-opener, @tauri-apps/api/core, ../auth.jsx; real useAppStore, no store mock), each seeding a currentBranch, rendering the hook, awaiting isSuccess, and asserting 'branch-a' is the segment after the resource name.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-history-orders.test.js src/__tests__/use-restaurant-settings.test.js src/__tests__/use-delivery-areas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - use-history-orders.js queryKey literal is exactly `['history-orders', branchId, from, to]`; its `.diagnostic` error block is byte-unchanged (only queryKey line + branchId selector import/read differ in the diff).
    - `npx vitest run src/__tests__/use-history-orders.test.js src/__tests__/use-restaurant-settings.test.js src/__tests__/use-delivery-areas.test.js` exits 0.
    - Each of the three test files asserts 'branch-a' is the segment after the resource name in the cache key.
  </acceptance_criteria>
  <done>All 7 fetch hooks are now branch-scoped; use-history-orders's diagnostic is preserved; three SC1 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | branchId folded into client-side cache keys only; never sent as a request param (server resolves branch server-side, D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | client-persisted branchId as authorization boundary | medium | mitigate | Hooks only READ currentBranch?.id from session-only Zustand state (Phase 13 D-10). No persistence added; store.js not edited. |
| T-14-02 | Information Disclosure | raw SDK error strings; use-history-orders `.diagnostic` block | low | accept | No new UI surfacing. use-history-orders' `.diagnostic` is unchanged and already feeds internal ErrorBlock diagnostics only (not customer-facing copy). |

**Out of scope:** V4 Access Control is server-enforced. No package installs → no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run src/__tests__/use-history-orders.test.js src/__tests__/use-restaurant-settings.test.js src/__tests__/use-delivery-areas.test.js` exits 0.
- Phase-gate grep audit finds zero unscoped `['restaurant-settings']`/`['delivery-areas']`/`['history-orders']` queryKeys in these files.
- use-history-orders.js `.diagnostic` error block unchanged.
</verification>

<success_criteria>
All 7 fetch hooks are branch-scoped; use-history-orders is key-only with its diagnostic preserved; each of the three hooks has a passing SC1 branch-key test.
</success_criteria>

<output>
Create `.planning/phases/14-branch-scoped-cache-re-scoping/14-03-SUMMARY.md` when done.
</output>
