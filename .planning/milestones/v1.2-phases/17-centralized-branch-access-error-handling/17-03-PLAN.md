---
phase: 17-centralized-branch-access-error-handling
plan: 03
type: tdd
wave: 3
depends_on: ["17-02"]
files_modified:
  - src/store.js
  - src/use-branches.js
  - src/app.jsx
  - src/i18n.jsx
  - src/__tests__/store.test.js
  - src/__tests__/use-branches.test.js
  - src/__tests__/app-branch-switch.test.jsx
autonomous: true
requirements: [BERR-01, BERR-02]
must_haves:
  truths:
    - "handleBranchError handles all three codes: BRANCH_INACTIVE and BRANCH_ACCESS_REVOKED share the same recovery behavior (toast + setBranchSwitcherForceOpen(true) + invalidate ['branches']) but render distinct per-code copy (branch_err_inactive_* vs branch_err_revoked_*); NO_BRANCH_ACCESS instead calls setNoBranchAccess(true) and fires no toast/reopen (BERR-01, D-03)."
    - "A branch-code switch-call 403 produces EXACTLY ONE toast, not two: fireSwitch's own onError guards its generic toast behind `!BRANCH_CODES.includes(err?.code)` so the generic branch_switch_error toast fires only for non-branch failures, while the per-code toast comes solely from handleBranchError via MutationCache.onError (D-05, Pitfall 2)."
    - "fireSwitch's switchPhase/pendingBranch cleanup (return to idle, clear pendingBranch, clear bridge timeout) runs UNCONDITIONALLY regardless of err.code, so a rejected switch always visibly returns the app to a stable state on the previously-selected branch (BERR-02)."
    - "A rejected switch (403) never mutates currentBranch — setCurrentBranch is called only in useBranchSwitch's onSuccess, so the app stays on the previous branch with no change beyond the error notice (BERR-02)."
    - "noBranchAccess is a session-only store flag (default false) with a setNoBranchAccess setter, and is NOT present in store.js partialize's allowlist (never persisted)."
    - "A single-branch tenant with valid access never trips handleBranchError under normal (non-403) operation — the handler only activates on an actual BRANCH_* 403, which such a tenant does not produce (Pitfall 11)."
    - statement: "A burst of simultaneously-inflight branch-scoped queries that each 403 with a BRANCH_* code converges to a single visible recovery (one toast, one reopen), not a stack of N — D-05's 'exactly one recovery per failure' at the concurrent-error edge (UI-SPEC E2-toast zero-one-many)."
      verification: backstop
  artifacts:
    - "store.js: noBranchAccess: false field + setNoBranchAccess setter, excluded from partialize"
    - "use-branches.js: handleBranchError extended to all three BRANCH_CODES branches"
    - "app.jsx: fireSwitch onError trimmed with the BRANCH_CODES guard (imports BRANCH_CODES)"
    - "i18n.jsx: branch_err_inactive_title/detail, branch_no_access_title/body/retry/retry_busy in ro + en"
  key_links:
    - "MutationCache.onError (handleBranchError) + fireSwitch onError -> must compose to exactly one toast"
    - "handleBranchError NO_BRANCH_ACCESS -> setNoBranchAccess(true) -> (block UI consumed in 17-04)"
  prohibitions:
    - "MUST NOT persist noBranchAccess to storage — it must never appear in store.js partialize."
    - "MUST NOT fire the generic branch_switch_error toast for a BRANCH_* code (that is the double-toast D-05 forbids)."
    - "MUST NOT make the switchPhase/pendingBranch cleanup conditional on err.code — only the toast is conditional (BERR-02)."
---

<objective>
Complete the central handler now that the matcher is locked (17-02): add the BRANCH_INACTIVE
distinct-copy branch and the NO_BRANCH_ACCESS branch (setting a new session-only store flag), and
close the D-05 double-toast gap by trimming `fireSwitch`'s local onError so a branch-code switch
failure produces exactly one toast. This makes BERR-01 whole for all three codes and satisfies
BERR-02 (rejected switch stays on the previous branch with no extra change).

Purpose: with the spine proven and the matcher verified, expand horizontally to the remaining codes
and eliminate the one composition hazard (cache-level + mutation-level onError both firing).
Output: a complete handleBranchError, the noBranchAccess flag, and a no-double-toast switch path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-centralized-branch-access-error-handling/17-CONTEXT.md
@.planning/phases/17-centralized-branch-access-error-handling/17-RESEARCH.md
@.planning/phases/17-centralized-branch-access-error-handling/17-PATTERNS.md
@.planning/phases/17-centralized-branch-access-error-handling/17-UI-SPEC.md
@src/use-branches.js
@src/store.js
@src/app.jsx
@src/i18n.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add noBranchAccess store flag and complete handleBranchError for all three codes</name>
  <files>src/store.js, src/use-branches.js, src/i18n.jsx, src/__tests__/store.test.js, src/__tests__/use-branches.test.js</files>
  <read_first>
    - src/store.js:65-72 (session-only auth block with branchSwitcherForceOpen), :121 (setBranchSwitcherForceOpen), :127-134 (partialize allowlist — the exclusion mechanism is omission).
    - src/use-branches.js — handleBranchError + BRANCH_CODES from 17-01/17-02 (REVOKED wired, INACTIVE/NO_BRANCH_ACCESS still guarded-but-empty).
    - 17-UI-SPEC.md ## Copywriting Contract — exact ro/en for branch_err_inactive_title/detail and branch_no_access_title/body/retry/retry_busy.
    - 17-PATTERNS.md § src/store.js (lines 189-218) — the noBranchAccess mirror of branchSwitcherForceOpen and the partialize allowlist.
    - src/__tests__/store.test.js — existing store test conventions.
  </read_first>
  <behavior>
    - Test: the store exposes noBranchAccess defaulting to false and setNoBranchAccess(true) sets it; noBranchAccess is absent from the persisted (partialize) subset.
    - Test: handleBranchError({ code: 'BRANCH_INACTIVE' }, qc) pushes a toast whose title is branch_err_inactive_title (distinct from the revoked title), sets branchSwitcherForceOpen true, invalidates ['branches'].
    - Test: handleBranchError({ code: 'NO_BRANCH_ACCESS' }, qc) calls setNoBranchAccess(true) and pushes NO toast and does NOT set branchSwitcherForceOpen.
    - Test: BRANCH_INACTIVE and BRANCH_ACCESS_REVOKED both reach the same reopen+refetch behavior but with different copy.
  </behavior>
  <action>
    In store.js, add noBranchAccess: false to the session-only auth block beside branchSwitcherForceOpen, add
    setNoBranchAccess: (v) => set({ noBranchAccess: v }) beside setBranchSwitcherForceOpen, and leave partialize's
    six-key allowlist untouched (omission is the exclusion). In use-branches.js, extend handleBranchError: for
    BRANCH_INACTIVE, push the same recovery (reopen + invalidate ['branches']) as REVOKED but with title
    t('branch_err_inactive_title') and detail t('branch_err_inactive_detail') (branch-name interpolated by the same
    err.branchName ?? currentBranch?.name ?? t('branch_generic_fallback') chain); for NO_BRANCH_ACCESS, call
    setNoBranchAccess(true) and return without a toast or reopen. Factor the two recoverable codes through a
    per-code copy map (revoked/inactive titleKey+detailKey) so the shared behavior is written once. In i18n.jsx,
    add branch_err_inactive_title/detail and branch_no_access_title/body/retry/retry_busy to BOTH ro and en blocks
    per the UI-SPEC copy, preserving 1:1 parity (the block copy keys are added here so 17-04's block UI can consume them).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/store.test.js src/__tests__/use-branches.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: store test asserts noBranchAccess default false, setNoBranchAccess flips it, and it is excluded from the persisted subset.
    - Behavior: use-branches test asserts all three codes dispatch correctly (INACTIVE distinct copy + reopen + refetch; NO_BRANCH_ACCESS sets the flag with no toast/reopen).
    - Source: `grep -c "branch_err_inactive_title\|branch_no_access_title" src/i18n.jsx` confirms the new keys exist in both language blocks.
    - Source: noBranchAccess does not appear inside the partialize object (`grep -A8 "partialize:" src/store.js` shows no noBranchAccess line).
  </acceptance_criteria>
  <done>handleBranchError covers all three codes with distinct recoverable-code copy and a NO_BRANCH_ACCESS flag path; the flag is session-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trim fireSwitch's onError to prevent the D-05 double toast</name>
  <files>src/app.jsx, src/__tests__/app-branch-switch.test.jsx</files>
  <read_first>
    - src/app.jsx:201-229 — fireSwitch and its current unconditional generic-toast onError (:217-227).
    - src/use-branches.js — the exported BRANCH_CODES to import.
    - 17-RESEARCH.md Pitfall 2 and Code Examples (fireSwitch before/after, lines 358-385) — cache-level and mutation-level onError compose, they do not override.
    - src/__tests__/app-branch-switch.test.jsx — existing switch-flow test harness.
  </read_first>
  <behavior>
    - Test: a switch mutation rejecting with err.code 'BRANCH_ACCESS_REVOKED' produces exactly one toast in the toast stack (the per-code one from handleBranchError), not two — fireSwitch's generic toast is suppressed.
    - Test: a switch mutation rejecting with a non-branch error (e.g. err.code undefined or a validation string) still fires the generic branch_switch_error toast.
    - Test: in both cases switchPhase returns to 'idle' and pendingBranch is cleared (unconditional cleanup).
  </behavior>
  <action>
    Import BRANCH_CODES from ./use-branches.js in app.jsx. In fireSwitch's onError, keep the clearTimeout +
    setSwitchPhase('idle') + setPendingBranch(null) cleanup unconditional, then wrap ONLY the generic pushToast
    (branch_switch_error_title/detail) in an `if (!BRANCH_CODES.includes(err?.code))` guard so branch codes are
    left entirely to handleBranchError. Change the onError signature to receive err. Do not otherwise alter the
    switch flow or the bridging/timeout logic.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-branch-switch.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the test proves exactly one toast for a branch-code switch failure and a generic toast for a non-branch failure.
    - Behavior: switchPhase/pendingBranch cleanup asserted to run for both branch and non-branch failures (BERR-02).
    - Source: `grep -n "BRANCH_CODES.includes" src/app.jsx` shows the guard around the generic toast only.
  </acceptance_criteria>
  <reversibility rating="costly">D-05 linchpin: the single-recovery-per-failure invariant. Splitting handling back per-call-site reintroduces Anti-Pattern 3. Flagged, not gated.</reversibility>
  <done>A branch-code switch 403 yields one toast via the central handler; non-branch failures keep the generic toast; the app always returns to a stable state on the previous branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK 403 -> dual onError composition | cache-level + mutation-level callbacks both fire; must not double-surface |
| client noBranchAccess flag -> UI gating | a client flag that gates which screen renders (never data access) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Information Disclosure | recoverable-code toasts | low | mitigate | Distinct codes map to localized i18n copy; raw SDK strings never reach the DOM |
| T-17-01 | Tampering / Elevation of Privilege | noBranchAccess flag | medium | mitigate | The flag only gates UI rendering; every data op is still server-revalidated per request — a tampered flag can at most show the wrong screen, never grant data access (block render/enforcement in 17-04) |
| T-17-04 | Denial of Service | concurrent branch 403 burst | low | accept | D-05's single-recovery convergence keeps a burst from stacking N toasts/reopens (backstop-verified) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/store.test.js src/__tests__/use-branches.test.js src/__tests__/app-branch-switch.test.jsx` green.
- Full suite `npx vitest run` shows no regression in the existing switch-flow tests.
- Single-branch fixture regression (standing item): login -> orders -> KDS -> POS with a one-branch fixture never trips handleBranchError or sets noBranchAccess.
</verification>

<success_criteria>
All three BRANCH_* codes recover correctly (distinct recoverable-code copy, NO_BRANCH_ACCESS sets
the flag), a branch-code switch failure produces exactly one toast, and a rejected switch leaves
the app stable on the previous branch.
</success_criteria>

<output>
Create `.planning/phases/17-centralized-branch-access-error-handling/17-03-SUMMARY.md` when done.
</output>
