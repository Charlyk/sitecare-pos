---
phase: 17-centralized-branch-access-error-handling
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/use-branches.js
  - src/__tests__/use-branches.test.js
autonomous: false
requirements: [BERR-01]
user_setup:
  - service: sitecare-orders-api (live)
    why: "The literal runtime string inside the 403 { error } envelope (BRANCH_INACTIVE / BRANCH_ACCESS_REVOKED / NO_BRANCH_ACCESS vs a human sentence) is unverifiable from SDK types or docs — only the live API reveals it. A test account whose branch can be deactivated/revoked is needed to capture a real 403 body."
must_haves:
  truths:
    - "The real 403 response body shape is captured for BOTH an ordinary branch-scoped request AND the SSE onopen path (via the existing use-sse.js console.warn('[SSE] non-2xx onopen') scaffold), and the actual err.code extraction in handleBranchError matches the captured runtime value (mirroring Phase 14's unwrapSdkResult correction precedent)."
    - "If the live body differs from the assumed { error: '<LITERAL_CODE>' } shape, the BRANCH_CODES matcher / err.code extraction is corrected so recovery fires against the real value; if it matches, the provisional matcher is confirmed and recorded as verified."
    - statement: "The captured SSE 403 body shape is recorded distinctly from the ordinary-request body shape (they may differ — the SSE route is hand-rolled server-side) so plan 17-05's extractBranchCodeFromSseBody parses the real shape, not an assumed one."
      verification: backstop
  artifacts:
    - "A recorded finding (in the SUMMARY and as a project decision) of the real 403 body shapes for REST and SSE, with the matcher's final locked form"
  key_links:
    - "live 403 body -> BRANCH_CODES / err.code extraction (the load-bearing matcher lock all downstream recovery depends on)"
  prohibitions:
    - "MUST NOT lock the matcher against the literal-string assumption on a passing synthetic test alone — the real body shape must be observed or the risk explicitly flagged in verification notes if live access is infeasible."
---

<objective>
Verify the load-bearing unknown that gates the entire phase: whether the 403 `{ error: string }`
envelope's runtime value is the literal enum code (BRANCH_INACTIVE / BRANCH_ACCESS_REVOKED /
NO_BRANCH_ACCESS) or a human-readable sentence — for BOTH ordinary requests and the SSE onopen
body — and lock (or correct) the `err.code` matcher accordingly before the expansion plans build.

Purpose: A1 is HIGH-risk — if the literal-string assumption is wrong, every comparison silently
no-ops and all of BERR-01/02/03 fail invisibly. This mirrors Phase 14's build-time correction of
`unwrapSdkResult` (`result.error.code` -> `result.error.error`).
Output: a confirmed-or-corrected matcher and a recorded finding of the real REST + SSE 403 body shapes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-centralized-branch-access-error-handling/17-RESEARCH.md
@.planning/phases/17-centralized-branch-access-error-handling/17-CONTEXT.md
@src/use-branches.js
@src/use-sse.js
@src/data.jsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Capture the real 403 body shape (REST + SSE) against the live API</name>
  <what-built>The provisional BRANCH_CODES matcher (three literal strings) shipped in plan 17-01, plus the Phase 15 SSE console.warn capture scaffold already logging { status, body } on a non-2xx onopen.</what-built>
  <how-to-verify>
    Using a test tenant/account whose branch can be deactivated or whose access can be revoked:
    1. Trigger an ordinary branch-scoped request (e.g. an orders/menu refetch) that returns a 403 for a
       deactivated/revoked branch. Capture the exact response body — is `result.error` the bare string
       "BRANCH_INACTIVE"/"BRANCH_ACCESS_REVOKED"/"NO_BRANCH_ACCESS", or a nested object, or a human sentence?
    2. Trigger the SSE stream (`/v1/sse/orders`) against the same inaccessible branch and read the browser
       console for `[SSE] non-2xx onopen` — record the { status, body } logged. Confirm whether that body is
       JSON-parseable and shaped `{ error: '<CODE>' }`, plain text, or something else.
    3. Confirm getMe() behaviour for a zero-branch account: does `selectedBranch` come back null, or does
       getMe() itself throw a 403 with a NO_BRANCH_ACCESS code? (A3 — affects plan 17-06's focus path.)
    Report the three captured shapes verbatim.

    If a live 403-capable test account is genuinely unavailable during execution: ship the three literal
    strings as-is (they are the values named in REQUIREMENTS.md/CONTEXT.md), record the risk explicitly in
    the SUMMARY verification notes, and leave a follow-up correction task — do NOT silently treat the
    synthetic test as confirmation.
  </how-to-verify>
  <resume-signal>Paste the captured REST 403 body, SSE 403 body, and zero-branch getMe() behaviour — or state "live capture infeasible, shipping provisional literal-string matcher with flagged risk".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Lock or correct the err.code matcher against the captured 403 shape</name>
  <files>src/use-branches.js, src/__tests__/use-branches.test.js</files>
  <read_first>
    - src/use-branches.js — BRANCH_CODES + handleBranchError from plan 17-01; useBranchSwitch's err.code attach at :33-38.
    - src/data.jsx:200-209 — unwrapSdkResult's extraction (message = typeof raw==='string' ? raw : raw?.error); Phase 14 corrected this to read result.error.error.
    - The captured 403 bodies from the checkpoint above.
    - RESEARCH.md Assumptions A1/A2/A3 and Pitfall 1/3.
  </read_first>
  <action>
    Compare the captured runtime 403 value(s) to the provisional BRANCH_CODES literal strings. If they match,
    add a regression test asserting the extraction yields the exact captured value and record the matcher as
    verified. If they differ (e.g. a human sentence, or a nested `.code`, or a differently-keyed field),
    correct the err.code extraction — in useBranchSwitch's mutationFn and, if needed, a small normalization in
    handleBranchError — so the recovery fires against the real value, and update BRANCH_CODES accordingly. Record
    the confirmed REST body shape AND the confirmed SSE body shape as a project decision (both, distinctly) so
    plan 17-05 parses the real SSE shape rather than an assumed one. Do not touch use-sse.js here — only record
    the finding; the SSE parse implementation lands in 17-05.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-branches.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: a regression test asserts err.code extraction yields the captured runtime value for at least one real/representative code, and handleBranchError dispatches on it.
    - Source: the SUMMARY records the REST 403 body shape and the SSE 403 body shape verbatim (or the flagged-risk note if live capture was infeasible).
    - Behavior: `npx vitest run src/__tests__/use-branches.test.js` passes with the locked matcher form.
  </acceptance_criteria>
  <done>The matcher is confirmed-or-corrected against real API behaviour; both 403 body shapes are recorded for downstream plans; no downstream plan builds on an unverified literal-string assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live API 403 body -> err.code matcher | The runtime string that all recovery keys on; a wrong assumption silently disables every recovery path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Tampering / Input Validation | err.code extraction | medium | mitigate | Verify the real body shape against the live API; correct extraction so a malformed/unexpected body fails safe (no wrongful grant, no wrongful block) rather than being assumed |
| T-17-06 | Repudiation / correctness | silent matcher no-op | high | mitigate | A blocking-human checkpoint captures the real 403 body before the matcher is locked; the literal-string assumption is never treated as confirmed by a synthetic test alone (A1, HIGH risk) |
</threat_model>

<verification>
- The blocking-human checkpoint is resolved with the captured shapes (or an explicit flagged-risk resolution).
- `npx vitest run src/__tests__/use-branches.test.js` green with the locked matcher.
- The SUMMARY records both the REST and SSE 403 body shapes for 17-05's consumption.
</verification>

<success_criteria>
The load-bearing 403-envelope unknown is resolved: the matcher is verified or corrected against
real API behaviour, and the SSE body shape is recorded so downstream SSE handling parses reality.
</success_criteria>

<output>
Create `.planning/phases/17-centralized-branch-access-error-handling/17-02-SUMMARY.md` when done.
</output>
