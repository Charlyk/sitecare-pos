---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - _prototype/src/app.jsx
  - _prototype/src/i18n.jsx
  - _prototype/src/icons.jsx
  - _prototype/src/data.jsx
  - _prototype/src/shell.jsx
  - _prototype/src/screen-orders.jsx
  - _prototype/src/screen-kitchen.jsx
  - _prototype/src/screen-pos.jsx
  - _prototype/src/screen-detail.jsx
  - _prototype/src/screen-menu.jsx
  - _prototype/src/screen-printer.jsx
  - _prototype/src/screen-settings.jsx
  - _prototype/assets/colors_and_type.css
  - _prototype/index.html
  - src-tauri/tauri.conf.json
  - src-tauri/src/lib.rs
  - src/main.jsx
  - src/App.jsx
  - package.json
  - vite.config.js
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Rust toolchain is installed: cargo --version exits 0"
    - "Tauri scaffold exists at repo root: package.json has tauri dev and tauri build scripts"
    - "Vite 6 (not 7 or 8) is the bundler: package.json vite field is ^6.x.x"
    - "React 18 (not 19) is installed: package.json react field is ^18.x.x"
    - "Prototype files are archived: sitecare-orders/project/src/ contents are in _prototype/src/"
    - "src-tauri/ directory exists at repo root with tauri.conf.json and src/lib.rs"
  artifacts:
    - path: "package.json"
      provides: "npm scripts: dev, build, tauri dev, tauri build"
      contains: "tauri"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri v2 window configuration"
      contains: "ro.sitecare.pos"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri Rust entry point"
      contains: "tauri::Builder::default()"
    - path: "vite.config.js"
      provides: "Vite configuration with React plugin"
      contains: "@vitejs/plugin-react"
    - path: "_prototype/src/app.jsx"
      provides: "Archived prototype source"
    - path: "_prototype/index.html"
      provides: "Archived prototype entry"
  key_links:
    - from: "package.json scripts.tauri dev"
      to: "src-tauri/tauri.conf.json"
      via: "npm run tauri dev invokes Tauri CLI"
      pattern: "\"tauri\": \"tauri\""
    - from: "vite.config.js"
      to: "src/main.jsx"
      via: "Vite entry point for React frontend"
      pattern: "@vitejs/plugin-react"
---

<objective>
Install Rust, scaffold the Tauri + Vite + React project at the repo root, archive the prototype to `_prototype/`, and verify the scaffold compiles and opens a native window.

Purpose: This is the hard prerequisite for all subsequent plans. Nothing in Phase 1 (or any later phase) can proceed until the Tauri + Vite foundation exists and compiles.

Output: Rust toolchain installed; Tauri + Vite + React scaffold at repo root (package.json, vite.config.js, src/, src-tauri/); prototype archived to _prototype/; `npm run tauri dev` opens a native window.
</objective>

<execution_context>
@/Users/eduardalbu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardalbu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eduardalbu/Developer/sitecare-pos/.planning/PROJECT.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/ROADMAP.md
@/Users/eduardalbu/Developer/sitecare-pos/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Rust toolchain and archive prototype</name>
  <files>_prototype/ (new directory)</files>
  <read_first>
    - /Users/eduardalbu/Developer/sitecare-pos/sitecare-orders/project/ — prototype files to archive (see what exists before moving)
    - /Users/eduardalbu/Developer/sitecare-pos/CLAUDE.md — project rules
  </read_first>
  <action>
Step 1 — Install Rust via rustup:
```
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh -s -- -y
source "$HOME/.cargo/env"
```
Wait for completion. Verify: `cargo --version` must print a version string (e.g., `cargo 1.x.x`).

Step 2 — Archive the prototype. The prototype lives in `sitecare-orders/project/`. The Tauri scaffold will be placed at the repo root (`/Users/eduardalbu/Developer/sitecare-pos/`). Before scaffolding, move the prototype to `_prototype/` so the scaffold does not overwrite it:
```
mkdir -p _prototype
cp -r sitecare-orders/project/src _prototype/src
cp -r sitecare-orders/project/assets _prototype/assets
cp -r sitecare-orders/project/lib _prototype/lib
cp sitecare-orders/project/index.html _prototype/index.html
```
Verify: `_prototype/src/app.jsx` exists and `_prototype/assets/colors_and_type.css` exists.

The `sitecare-orders/` directory remains untouched — it is the original prototype repo and is NOT deleted.
  </action>
  <verify>
    <automated>cargo --version 2>&1 | grep -E "cargo [0-9]+" && ls /Users/eduardalbu/Developer/sitecare-pos/_prototype/src/app.jsx && ls /Users/eduardalbu/Developer/sitecare-pos/_prototype/assets/colors_and_type.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo --version` prints a version string (cargo X.Y.Z)
    - `/Users/eduardalbu/Developer/sitecare-pos/_prototype/src/app.jsx` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/_prototype/assets/colors_and_type.css` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/_prototype/index.html` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/_prototype/lib/` exists (contains macos-window.jsx)
  </acceptance_criteria>
  <done>Rust is installed (cargo --version exits 0) and prototype is fully archived to _prototype/.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Tauri + Vite + React at repo root and pin correct versions</name>
  <files>
    package.json
    vite.config.js
    index.html
    src/main.jsx
    src/App.jsx
    src-tauri/tauri.conf.json
    src-tauri/src/lib.rs
    src-tauri/Cargo.toml
    src-tauri/build.rs
    src-tauri/capabilities/default.json
    src-tauri/icons/
  </files>
  <read_first>
    - /Users/eduardalbu/Developer/sitecare-pos/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: scaffold command and interactive prompts)
  </read_first>
  <action>
Run the Tauri scaffold at the repo root. The working directory is `/Users/eduardalbu/Developer/sitecare-pos/`.

Use the non-interactive form to avoid prompts:
```
npm create tauri-app@latest . -- --template react --manager npm --identifier ro.sitecare.pos
```

If the CLI requires interactive mode (some versions do not support non-interactive with `.`), use interactive mode and answer as follows:
```
Project name: . (dot — current directory)
Identifier: ro.sitecare.pos
Frontend language: JavaScript
Package manager: npm
UI template: React
UI flavor: JavaScript
```

After scaffolding, install npm dependencies:
```
npm install
```

Then check and fix versions. Open `package.json` and apply these corrections:

1. If `"vite"` is `"^7.x.x"` or `"^8.x.x"` → downgrade: `npm install --save-dev vite@6`
2. If `"react"` is `"^19.x.x"` → downgrade: `npm install react@18 react-dom@18`

Delete scaffold boilerplate files (not needed):
```
rm -f src/App.css src/assets/react.svg
```

Note: The scaffold generates `src/App.jsx` (capital A). This placeholder file is kept for now — it will be replaced entirely in Plan 05.
  </action>
  <verify>
    <automated>
ls /Users/eduardalbu/Developer/sitecare-pos/src-tauri/tauri.conf.json && \
ls /Users/eduardalbu/Developer/sitecare-pos/src-tauri/src/lib.rs && \
grep -q "ro.sitecare.pos" /Users/eduardalbu/Developer/sitecare-pos/src-tauri/tauri.conf.json && \
node -e "const p=require('./package.json'); const v=p.dependencies?.vite||p.devDependencies?.vite||''; const r=p.dependencies?.react||''; console.log('vite:', v, 'react:', r); if(v.includes('8') || v.includes('7')) process.exit(1); if(r.includes('19')) process.exit(1); console.log('VERSIONS OK');" 2>/dev/null || \
(grep '"vite"' /Users/eduardalbu/Developer/sitecare-pos/package.json && grep '"react"' /Users/eduardalbu/Developer/sitecare-pos/package.json)
    </automated>
  </verify>
  <acceptance_criteria>
    - `/Users/eduardalbu/Developer/sitecare-pos/src-tauri/tauri.conf.json` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/src-tauri/src/lib.rs` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/src-tauri/capabilities/default.json` exists
    - `/Users/eduardalbu/Developer/sitecare-pos/package.json` contains `"ro.sitecare.pos"` OR `src-tauri/tauri.conf.json` contains `"ro.sitecare.pos"`
    - `package.json` vite version is `^6.x.x` (NOT ^7 or ^8)
    - `package.json` react version is `^18.x.x` (NOT ^19)
    - `node_modules/` directory exists (npm install completed)
    - `src-tauri/` directory exists with `Cargo.toml` and `build.rs`
  </acceptance_criteria>
  <done>Tauri + Vite scaffold exists at repo root with correct Vite 6 and React 18 versions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Tauri dev window opens (first Rust compile)</name>
  <what-built>
    Rust toolchain installed, prototype archived to _prototype/, Tauri + Vite + React scaffold created at repo root with Vite 6 and React 18.
  </what-built>
  <how-to-verify>
    Run in the project root:
    ```
    npm run tauri dev
    ```
    IMPORTANT: The first run compiles all Tauri + plugin Rust crates. This takes 5–10 minutes. Wait for it to complete.

    Expected outcome:
    1. Terminal shows Vite dev server starting at http://localhost:5173
    2. Terminal shows Rust compilation progress (many "Compiling..." lines)
    3. A native macOS window opens showing the Tauri + React scaffold placeholder (a white page with "Welcome to Tauri" or similar content)
    4. No error: "command not found: cargo" (Rust is installed)
    5. No error: "error[E0000]" in Rust compile output

    If you see a blank window or the scaffold placeholder — that is correct. The placeholder content will be replaced by the prototype screens in Plans 03–05.

    After verifying: close the Tauri window with Cmd+Q.
  </how-to-verify>
  <resume-signal>Type "approved" if the native window opened without Rust errors. Type "failed: [describe issue]" if there was a problem.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell → npm registry | npm install fetches packages; supply chain risk on scaffold templates |
| Shell → rustup.rs | Rust installer downloaded via curl; MITM risk mitigated by TLS pinning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | CSP configuration (tauri.conf.json) | mitigate | Plan 02 adds explicit CSP object with all 6 directives; IPC channels included in connect-src |
| T-1-02 | Information Disclosure | window.* globals in src/ | mitigate | Plans 03+04 convert all window.* reads/writes to ES module imports/exports; grep verification in acceptance criteria |
| T-1-03 | Credential exposure | .npmrc file | mitigate | .npmrc must use `${NODE_AUTH_TOKEN}` variable syntax only — never commit a literal PAT; Task 1 in Plan 02 verifies this with grep |
| T-1-04 | Tampering | rustup installer fetched via curl | accept | TLS with `--proto '=https' --tlsv1.2` flag enforced; official rustup.rs domain; low risk for internal dev toolchain |
</threat_model>

<verification>
After all three tasks complete:

1. `cargo --version` prints a version string
2. `ls /Users/eduardalbu/Developer/sitecare-pos/src-tauri/` shows tauri.conf.json, Cargo.toml, src/, capabilities/, icons/
3. `ls /Users/eduardalbu/Developer/sitecare-pos/_prototype/src/` shows all 12 prototype .jsx files
4. `grep "vite" /Users/eduardalbu/Developer/sitecare-pos/package.json` shows ^6.x (not ^7 or ^8)
5. `grep '"react"' /Users/eduardalbu/Developer/sitecare-pos/package.json` shows ^18.x (not ^19)
6. Human-verified: native Tauri window opened on first `npm run tauri dev`
</verification>

<success_criteria>
- Rust toolchain is installed and `cargo --version` succeeds
- Prototype files are archived to `_prototype/` (all 12 .jsx files + CSS + index.html + lib/)
- Tauri + Vite scaffold exists at repo root with identifier `ro.sitecare.pos`
- `package.json` contains `tauri dev` and `tauri build` scripts
- Vite version is 6.x.x; React version is 18.x.x
- A native macOS window opened on `npm run tauri dev` (human verified)
</success_criteria>

<output>
After completion, create `/Users/eduardalbu/Developer/sitecare-pos/.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
