---
phase: 03-shell-data-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 03-05
files_modified: []
autonomous: false
requirements:
  - KDS-01
  - OFF-01
  - OFF-02
  - OFF-03

must_haves:
  truths:
    - "KDS screen receives a new order without page reload — SSE delivers it in real-time"
    - "Network disruption shows the offline banner within ~35 seconds"
    - "Reconnecting the network hides the banner automatically"
    - "Accept and Advance buttons are visually greyed out while offline and re-enable on reconnect"
    - "All automated tests pass (full suite green)"
  artifacts:
    - path: "src/use-sse.js"
      provides: "Real-time SSE connection verified end-to-end"
    - path: "src/offline-banner.jsx"
      provides: "Offline banner visually confirmed"
  key_links:
    - from: "SSE server"
      to: "TanStack Query ['orders'] cache"
      via: "useSSE onmessage → setQueryData"
      pattern: "live order appears in KDS without reload"
---

<objective>
Human verification of all four Phase 3 success criteria against the running Tauri app and live API.
Automated tests cover unit behavior. This checkpoint covers the integration behaviors that
require a real SSE connection, real network disruption, and visual inspection.

Purpose: The four Phase 3 success criteria from ROADMAP.md cannot be verified by unit tests alone.
This checkpoint gates the phase as complete.
Output: Human confirmation that all four phase success criteria are met.
</objective>

<execution_context>
@/Users/eduardalbu/Developer/sitecare-pos/.planning/ROADMAP.md
</execution_context>

<context>
@/Users/eduardalbu/Developer/sitecare-pos/.planning/phases/03-shell-data-foundation/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <read_first>
    - src/__tests__/ directory listing — confirm all test files exist
  </read_first>
  <files></files>
  <action>
Run the full test suite and confirm all tests pass:

```bash
cd /Users/eduardalbu/Developer/sitecare-pos && npx vitest run
```

If any test fails, do NOT proceed to the checkpoint. Fix the failing test first by re-running the relevant implementation plan.
  </action>
  <verify>
    <automated>cd /Users/eduardalbu/Developer/sitecare-pos && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - Output contains "0 failed" or equivalent
    - Tests covering U9 (useSSE), U10 (OfflineBanner), U11 (hooks), U12 (offline buttons), U8/U13 (i18n) all show PASS
  </acceptance_criteria>
  <done>Full test suite green. Zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 full integration:
- useSSE persistent connection at App level (Bearer token in header)
- order_new events update TanStack Query cache in real-time
- isConnected → isOffline → OfflineBanner (amber, 40px, slideDown animation)
- 4 mutating screens disable buttons when offline (opacity 0.45 + pointer-events: none)
- orderCount in sidebar badge derived from live data
  </what-built>
  <how-to-verify>
Run the app:
```bash
cd /Users/eduardalbu/Developer/sitecare-pos && npm run tauri dev
```

Log in with your SiteCare credentials.

**Test 1 — Real-time order delivery (KDS-01):**
1. Navigate to the Kitchen screen (chef hat nav item)
2. Using another browser tab or Postman, create a new order via the SiteCare API
3. Verify: the new order appears on the Kitchen screen within 2 seconds WITHOUT refreshing the page

**Test 2 — Offline banner appears (OFF-01):**
4. While the app is running and connected, disable your WiFi or unplug ethernet
5. Wait up to 35 seconds (SSE reconnect timeout)
6. Verify: a yellow/amber banner appears at the top of the content area (NOT spanning the sidebar)
7. Verify: banner shows "Connection lost" / "Conexiune întreruptă" (depending on language)
8. Verify: banner has a left amber border and subtle amber background

**Test 3 — Cached data while offline (OFF-02):**
9. While offline (from Test 2), navigate between screens
10. Verify: orders and other data loaded before the disconnect are still visible (served from TanStack Query cache)

**Test 4 — Buttons disabled while offline (OFF-03):**
11. Navigate to the Orders screen while offline
12. Verify: Accept and Advance buttons appear visually greyed out (opacity ~0.45)
13. Verify: clicking the greyed-out buttons has no effect (pointer-events: none)

**Test 5 — Auto-reconnect (OFF-01, D-09):**
14. Re-enable WiFi/ethernet
15. Wait 10-15 seconds
16. Verify: the offline banner disappears automatically WITHOUT refreshing the page
17. Verify: buttons become clickable again
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass. Describe any issues if they fail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live API → Tauri renderer | SSE events and API responses cross the network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-11 | Tampering | Live API response during verification | accept | Verification uses the authenticated real API. No security test is needed here — this is a functional integration check only. Security mitigations T-3-01 through T-3-10 were applied in implementation plans. |
</threat_model>

<verification>
Manual verification as specified in Task 2 checkpoint.

Automated pre-gate:
```bash
cd /Users/eduardalbu/Developer/sitecare-pos && npx vitest run
```
</verification>

<success_criteria>
- Full test suite green (all tests pass)
- All 4 Phase 3 ROADMAP success criteria verified by human in the running app:
  1. KDS receives new order without reload
  2. Offline banner appears within ~35s of network loss
  3. Cached data visible while offline
  4. Mutating buttons visually disabled while offline, re-enable on reconnect
</success_criteria>

<output>
After verification is approved, update `.planning/STATE.md`:
- current_plan: none (Phase 3 complete)
- status: ready (for Phase 4)
- current_phase: 4 — Core Screens
- progress.completed_phases: 3
- progress.completed_plans: 16

Then create `.planning/phases/03-shell-data-foundation/03-06-SUMMARY.md` following the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
