---
quick_id: 260507-q1
slug: hide-settings-tabs
description: Hide all settings tabs except Afisaj (display) — others not yet implemented
date: 2026-05-07
---

# Quick Task 260507-q1: Hide Settings Tabs

## Goal

In `src/screen-settings.jsx`, hide the `users`, `tax`, `store`, and `integrations` tabs from the tab bar. Only the `display` (Afisaj) tab should remain visible. Change the default tab from `'users'` to `'display'`.

## Tasks

### Task 1: Filter tabs array and update default tab

**File:** `src/screen-settings.jsx`

**Changes:**
1. Change `useState('users')` → `useState('display')` (line 17)
2. Remove `users`, `tax`, `store`, `integrations` entries from the `tabs` array — keep only `display`

**Done when:** Tab bar renders only the Afisaj/Display tab; clicking settings shows the display tab content immediately.
