---
milestone: v1.2
milestone_name: Branch Switching
audited: 2026-07-24
status: tech_debt
scores:
  requirements: 15/15
  phases: 5/5
  integration: 4/4 flows wired
  flows: 4/4
gaps:
  requirements: []   # No unsatisfied or orphaned requirements — FAIL gate not tripped
  integration: []    # Integration checker: 0 broken flows, 0 orphaned exports
  flows: []
tech_debt:
  - phase: 17-centralized-branch-access-error-handling
    items:
      - "WINDOWS #1 (open, deviation): BRANCH_* 403 matcher (BRANCH_INACTIVE/BRANCH_ACCESS_REVOKED/NO_BRANCH_ACCESS) is UNVERIFIED against the live API. The entire BERR-01/02/03 recovery path parses an ASSUMED { error: '<CODE>' } envelope (REST + SSE), locked only by a synthetic test. If the real body differs, code===undefined → recovery silently degrades to a generic-error toast (no reopen, no full-screen block). Single largest residual risk of the milestone. Follow-up: re-capture the real 403 body against a live tenant with a deactivable/revocable branch and correct the matcher/extractor."
      - "WINDOWS #2 (open, unrun-verify): handleBranchError has no de-dup/in-flight guard — a burst of simultaneously-403ing branch-scoped queries pushes one toast per query (a stack of N) instead of one converged recovery. UI-SPEC E2-toast zero-one-many row is backstop-only. Follow-up: add a dedup guard or accept via manual concurrent-query testing."
      - "Human-verification backstops (4): toast branch-name fallback naturalness; NO_BRANCH_ACCESS Retry spinner/label visual; Retry network-failure no-extra-toast; SSE branch-403 recovery no cross-branch cache bleed. State logic unit-tested; visual/live confirmation pending."
  - phase: 16-branch-switcher-ui-switch-flow-language-relocation
    items:
      - "Human-verification backstops (6): popover/overlay/collapsed-chip pixel fidelity vs 16-UI-SPEC.md; many-branch popover scroll; CartDiscardConfirm visual; LIVE multi-branch switch bridging real SSE reconnect with no false OfflineBanner flash; single-branch first-paint regression on a real one-branch fixture; D-09 bounded-timeout against a genuinely-dead new-branch stream. All require a live multi-branch SiteCare account, which was unavailable."
      - "16-VALIDATION.md scaffold left status: draft with TBD/pending rows (documentation-hygiene gap; note: Nyquist frontmatter reads validated/compliant)."
  - phase: 15-sse-branch-aware-reconnect
    items:
      - "Nyquist PARTIAL: 15-VALIDATION.md is status: validated but nyquist_compliant: false — SC2 (two concurrent live sessions confirm KDS + order list receive the NEW branch's events after a switch) is genuinely non-automatable and correctly deferred to a live UAT checkpoint once the switcher shipped (Phase 16). Mechanism is fully code-verified."
  - phase: cross-cutting
    items:
      - "Pre-existing unrelated test failure carried through the whole milestone: src/__tests__/build-pipeline.test.js BILD-04 (bundle.createUpdaterArtifacts is 'v1Compatible' not true, from Phase 6 macOS packaging). Confirmed on the pre-v1.2 baseline; not a v1.2 regression. Full suite otherwise 620/621."
      - "STATE.md Phase Summary table and ROADMAP.md progress table are stale (show Phases 15/16/17 'Not started'/'In Progress' and Phase 17 5/6) though all 16 plans executed and all 5 VERIFICATION.md files exist. Cosmetic bookkeeping drift; the frontmatter and gsd-tools init both report all_phases_complete: true."
---

# Milestone v1.2 "Branch Switching" — Audit Report

**Audited:** 2026-07-24
**Status:** `tech_debt` — all 15 requirements satisfied and all cross-phase flows wired, but open ledger items and pending live/visual verification remain.
**Scope:** Phases 13–17

---

## 1. Requirements Coverage (3-Source Cross-Reference)

Every requirement was cross-checked against (a) each phase's VERIFICATION.md, (b) each SUMMARY.md `requirements-completed` frontmatter, and (c) the REQUIREMENTS.md traceability table `[x]` state. **All 15 satisfied. No orphans, no unsatisfied — FAIL gate not tripped.**

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|-------------|---------------------|-----------------|-------|
| BSTATE-01 | 13 | passed | 13-01 | `[x]` | ✅ satisfied |
| BSTATE-02 | 13 | passed | 13-02 | `[x]` | ✅ satisfied |
| SCOPE-01 | 14 | passed | 14-01/02/03/04 | `[x]` | ✅ satisfied |
| SCOPE-02 | 15 | human_needed* | 15-01 | `[x]` | ✅ satisfied (mechanism; live SC2 deferred) |
| SCOPE-03 | 16 | human_needed* | 16-03 | `[x]` | ✅ satisfied |
| SCOPE-04 | 16 | human_needed* | 16-01/03 | `[x]` | ✅ satisfied |
| SWCH-01 | 16 | human_needed* | 16-01/02 | `[x]` | ✅ satisfied |
| SWCH-02 | 16 | human_needed* | 16-02 | `[x]` | ✅ satisfied |
| SWCH-03 | 16 | human_needed* | 16-01 | `[x]` | ✅ satisfied |
| SWCH-04 | 16 | human_needed* | 16-01 | `[x]` | ✅ satisfied |
| BERR-01 | 17 | human_needed* | 17-01/02/03/05 | `[x]` | ✅ satisfied (⚠ unverified 403 shape) |
| BERR-02 | 17 | human_needed* | 17-03 | `[x]` | ✅ satisfied |
| BERR-03 | 17 | human_needed* | 17-04 | `[x]` | ✅ satisfied (⚠ unverified 403 shape) |
| BERR-04 | 17 | human_needed* | 17-06 | `[x]` | ✅ satisfied |
| LANG-01 | 16 | human_needed* | 16-01/02 | `[x]` | ✅ satisfied |

\* `human_needed` on Phases 15/16/17 reflects pending **live/visual backstop checks**, not code gaps — every code-verifiable success criterion is VERIFIED with passing tests. Phases 13 and 14 are `passed` outright.

**Coverage: 15/15 requirements satisfied. 0 unsatisfied. 0 orphaned.**

---

## 2. Phase Verification Summary

| Phase | VERIFICATION status | Code score | Notes |
|-------|--------------------|-----------|-------|
| 13 Branch State & Launch Seeding | passed | 10/10 truths | Clean. Session-only currentBranch, cold-start + signIn + focus seeding. |
| 14 Branch-Scoped Cache Re-Scoping | passed | 4/4 SC | SC3 closed by human-directed fast-follow `ae08341` (err.code on use-history-orders). |
| 15 SSE Branch-Aware Reconnect | human_needed | 13/13 code truths | SC2 (live two-session) non-automatable, deferred to UAT. Nyquist PARTIAL. |
| 16 Branch Switcher UI / Switch Flow / Language | human_needed | 5/5 SC, 12/12 plan truths | 6 pixel/live backstops pending. 44/44 phase tests pass. |
| 17 Centralized Branch-Access Error Handling | human_needed | 30/30 code truths (4/4 SC) | 4 visual/live backstops + 2 open WINDOWS caveats. 155/155 target tests pass. |

---

## 3. Cross-Phase Integration

Integration checker (gsd-integration-checker) result: **all 4 core E2E flows WIRED, 0 broken, 0 orphaned exports.** `store.js`'s `currentBranch` is the single shared coupling point read/written consistently by all five phases.

1. **Branch switch → cache re-scope → SSE reconnect → UI settle** — WIRED. `shell.jsx` popover → `app.jsx` fireSwitch → `useBranchSwitch` onSuccess `setCurrentBranch` (sole write site) → all 7 hooks' query keys + `use-sse.js` effect dependency + bridging watcher + PosScreen remount.
2. **403 branch-access error → central handler → recovery UI** — WIRED. REST via `unwrapSdkResult`→`main.jsx` QueryCache/MutationCache onError; SSE via `use-sse.js` onopen short-circuit; both → `handleBranchError`. ⚠ Contingent on the unverified 403 envelope (see §4); degrades **silently** (generic toast, no reopen/block) if the real shape differs — a soft fallback, not a hard break.
3. **Cold start / sign-in branch seeding** — WIRED. Shared `seedFromMe` helper; currentBranch excluded from partialize.
4. **Language switching post RO/EN removal** — WIRED. `screen-settings.jsx` Afișaj → same `setLang` store field every screen's `useT(lang)` consumes. LANG-01 not broken by the pill removal.

---

## 4. Tech Debt & Deferred Items

### Blocking `/gsd-ship` (open ledger items — `open_count: 2`)

- **WINDOWS #1 — Unverified branch-access 403 envelope (REST + SSE).** The whole BERR recovery (BERR-01/02/03) parses an ASSUMED `{ error: '<CODE>' }` shape, locked only by synthetic tests — live capture was infeasible (no test tenant with a revocable branch). **This is the milestone's single largest residual risk.** If the real body differs, recovery silently downgrades to a generic-error toast. Follow-up: re-capture against a live tenant, correct matcher/extractor.
- **WINDOWS #2 — No concurrent-error de-dup.** A burst of simultaneous branch-403s stacks N toasts. Follow-up: add a dedup guard or accept via manual testing.

### Non-blocking deferred verification

- **Phase 15 Nyquist PARTIAL** — SC2 live two-session confirmation, deferred to UAT (correct, non-automatable).
- **Phase 16 (6 backstops)** — pixel fidelity + live multi-branch switch bridging real SSE + single-branch first-paint + bounded-timeout against a dead stream. All need a live multi-branch account.
- **Phase 17 (4 backstops)** — toast fallback naturalness, Retry spinner visual, Retry network-failure, SSE no-cross-branch-bleed.

### Cosmetic

- Pre-existing unrelated `build-pipeline.test.js` BILD-04 failure (Phase 6 macOS packaging; not a v1.2 regression; suite otherwise 620/621).
- STATE.md Phase Summary table and ROADMAP.md progress table are stale vs the completed reality (all 16 plans done, all 5 VERIFICATIONs exist); frontmatter and `init` report `all_phases_complete: true`.
- 16-VALIDATION.md scaffold left `status: draft` with TBD rows (its Nyquist frontmatter, however, reads `validated`/`compliant`).

---

## 5. Verdict

**`tech_debt`** — v1.2 is functionally code-complete: 15/15 requirements satisfied across all three sources, all cross-phase flows wired, ~620 passing tests. It is **not `passed`** because the branch-access 403 recovery — the milestone's central error-handling promise — has never been exercised against the live API (WINDOWS #1), two open ledger items block `/gsd-ship`, and the live/visual behavior of switching (Phases 15/16/17) awaits a multi-branch UAT that no test tenant enabled.

The safest close path is a short live-verification pass (one multi-branch account: capture the real 403 shape, confirm the switch/reconnect/recovery flow end-to-end) that would retire WINDOWS #1, the Phase 15 SC2 checkpoint, and most Phase 16/17 backstops together — or an explicit accept-debt closeout that carries these forward with eyes open.

---

*Audited: 2026-07-24 · Auditor: Claude (gsd-audit-milestone)*
